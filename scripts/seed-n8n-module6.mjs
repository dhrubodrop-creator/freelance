import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "be1db7d2-c690-424d-a059-d272450b6114"; // Reliability, Governance & Monetization

const sections = [
  {
    section_type: "the_field",
    title: "What turning agentic automation skills into a sellable service actually looks like",
    order_index: 0,
    content: "Everything built in Modules 1-5 demonstrates technical capability. This module is about the different, equally real skill of turning that capability into something a client will actually pay for, trust, and keep paying for. That means three distinct things: making the systems you build genuinely reliable and governed (not just working in your own testing), pricing and packaging what you offer in a way that makes sense to a buyer, and being able to propose and explain your work to a client who doesn't care about your n8n node configuration — they care about the business outcome and whether they can trust it.\n\nReliability and governance aren't separate from the client conversation — they're a huge part of what you're actually selling. \"This system has version-controlled prompts, guardrails against unintended actions, logging for every decision, and access controls\" is a genuinely different, more valuable pitch than \"I built you an AI thing that mostly works.\" Clients (especially business clients, not hobbyists) are buying reliability and accountability as much as they're buying the automation itself.\n\nPricing agentic automation services is its own skill, distinct from pricing traditional software development. A workflow that saves a client 10 hours a week of manual work has a value that can be quantified and priced against, differently from an hourly billing model — and packaging (a fixed-scope project, a retainer for ongoing maintenance, a productized service applied across multiple similar clients) is a real strategic choice, not an afterthought once the technical work is done.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about reliability, governance, and monetization",
    order_index: 1,
    content: "**1. Prompt version control exists because prompts are production code, and they need to be treated that way.** An unversioned prompt that gets casually edited in production, with no way to see what changed or roll back, is a real operational risk once real business processes depend on it.\n\n**2. Guardrails are what you build in, not what you hope the model does.** Relying on the model to simply behave well is not a governance strategy — explicit constraints (what actions are technically possible, what requires approval, what's hard-blocked) are.\n\n**3. Logging and access controls aren't compliance theater — they're what makes a system trustworthy enough for a real business to depend on.** A client asking \"who can change this, and how would we know if something went wrong\" deserves a real, specific answer, not a shrug.\n\n**4. Pricing based on value delivered, not hours spent, is usually the better model for this kind of work — where it's honestly assessable.** A workflow that saves 10 hours a week has a concrete, calculable value to the client that's usually much higher than the hours it took to build — pricing purely on build-time hours leaves real money on the table and undersells the actual outcome.\n\n**5. A retainer relationship reflects the real, ongoing nature of agentic systems — they need maintenance, not just delivery.** Unlike a piece of software that can genuinely be \"finished,\" an agentic automation depending on external APIs, evolving business processes, and model behavior needs ongoing attention — pricing and packaging should reflect that reality honestly, not pretend a one-time delivery is the whole relationship.\n\n**6. A client proposal that leads with governance and reliability, not just capability, signals real professionalism.** Anyone can demo an impressive-looking agent. A proposal that also addresses what happens when it's wrong, who's accountable, and how the client stays in control is what distinguishes a genuinely professional service offering from an enthusiastic hobbyist's demo.",
  },
  {
    section_type: "decision_framework",
    title: "How should I price and package this work, and what governance does it actually need?",
    order_index: 2,
    content: "IF the automation you're building has a clearly quantifiable business value (hours saved, error rate reduced, revenue captured) THEN price primarily against that value, not just your build-time hours. BECAUSE value-based pricing better reflects what the client is actually paying for, and typically better compensates the real leverage of a well-built automation over a purely hourly rate. WATCH OUT FOR: overstating a speculative value estimate to justify a higher price is dishonest and will damage trust once the client can actually measure the real result — use a defensible, conservative estimate.\n\nIF the system you're delivering will need ongoing operation, monitoring, or adaptation to changing business needs THEN propose a retainer or maintenance arrangement alongside (or instead of) a one-time project fee. BECAUSE agentic systems are not \"finish once, done forever\" — external APIs change, business processes evolve, and something that isn't maintained tends to degrade or break silently, exactly the failure mode covered in earlier modules. WATCH OUT FOR: proposing a retainer without being specific about what it actually covers (monitoring, prompt updates, incident response) makes it feel like an unjustified recurring charge rather than a real, valuable ongoing service.\n\nIF the automation you're proposing touches sensitive data, makes consequential autonomous decisions, or operates in a regulated context THEN the proposal needs to explicitly address governance: access controls, logging, guardrails, and escalation — not as an afterthought, but as a core part of what's being delivered. BECAUSE this is genuinely what separates a trustworthy business system from an impressive demo, and a sophisticated client will specifically ask about it. WATCH OUT FOR: underselling governance as \"extra\" work that could be cut to reduce price — for a real business client, this is often the actual thing making the automation deployable at all, not optional polish.\n\nIF you're deciding how to package a solution you've built for one client, for future clients THEN consider whether the underlying pattern is genuinely reusable (a productized service) versus genuinely specific to that one client's environment. BECAUSE this connects directly back to the reusable-workflow judgment from earlier in this course track — packaging something too early, before confirming it generalizes, risks selling a pattern that doesn't actually transfer cleanly.",
  },
  {
    section_type: "workflow",
    title: "The real process for delivering a reliable, governed, sellable agentic service",
    order_index: 3,
    content: "1. Version-control every prompt and instruction set used in production, with a clear way to see what changed and roll back if a change causes a regression.\n\n2. Build explicit guardrails: constrain what actions the system can actually take (not just what it's instructed not to do), and define hard-blocked versus approval-required versus fully autonomous actions clearly.\n\n3. Implement logging and access controls sufficient to answer \"who can change this, what did it do, and why\" for any real incident or client question.\n\n4. Estimate the actual business value of what you're delivering (hours saved, error reduction, revenue impact) as honestly and conservatively as you can, and use that as the basis for pricing, not just build-time hours.\n\n5. Decide, explicitly, whether this engagement needs an ongoing retainer given the system's real maintenance needs, and be specific about what that retainer actually covers.\n\n6. Assess honestly whether the pattern you've built is genuinely reusable across future clients (a productized service) or specific to this one client's environment, before packaging it as a repeatable offering.\n\n7. Write a client proposal that leads with the business outcome and explicitly addresses reliability and governance, not just technical capability — written for someone who cares about trust and results, not implementation detail.\n\n8. Present the full portfolio (single agents, an intelligent automation workflow, an enterprise Copilot agent, a multi-agent architecture) as this course's capstone, each fully documented with its design reasoning, not just its technical configuration.",
  },
  {
    section_type: "failure_modes",
    title: "How reliability, governance, and monetization efforts actually break",
    order_index: 4,
    content: "Failure 1 — Prompts and instructions edited casually in production with no version control.\nWhat: a prompt gets tweaked directly in a live system to fix an immediate issue, with no record of what changed, making it impossible to know what caused a subsequent regression or to cleanly roll back.\nHow it happens: editing a prompt feels lightweight and low-stakes compared to changing application code, so it doesn't get the same version-control discipline by default.\nWhy beginners miss it: prompts don't look like \"real code,\" so the instinct to version-control them the same way doesn't naturally kick in, even though they function as production logic.\nDetect: check whether there's any way to see the history of changes to a production prompt, or whether the current version is the only one that exists anywhere.\nPrevent: treat prompts and instructions as production code from the start — version-controlled, with change history, the same discipline applied to any other logic the business depends on.\nInterview question: \"Why should a production prompt be version-controlled the same way application code is?\"\nReal-world consequence: an untraceable regression after a prompt edit, with no way to identify what changed or quickly roll back to the previously-working version.\n\nFailure 2 — Pricing purely on hours, undervaluing genuinely high-leverage automation.\nWhat: a workflow that saves a client substantial ongoing time or cost is priced only against the hours spent building it, significantly undervaluing the actual business impact delivered.\nHow it happens: hourly pricing is the familiar default from traditional freelance/consulting work, and shifting to value-based pricing requires a real, sometimes uncomfortable conversation about the client's actual numbers.\nWhy beginners miss it: hourly pricing feels safer and easier to justify, even though it systematically leaves money on the table for genuinely high-value automation work.\nDetect: for any completed project, estimate the actual ongoing value delivered (time saved, errors reduced) and compare it honestly against what was actually charged.\nPrevent: have an explicit conversation with the client about the business value of the problem before pricing, and price against that value using a defensible, conservative estimate.\nInterview question: \"Why might value-based pricing be more appropriate than hourly pricing for a workflow automation engagement?\"\nReal-world consequence: consistently undercharging for genuinely high-leverage work, which is also a competitive disadvantage against others in the space who do price on value.\n\nFailure 3 — No governance story in the client proposal, treating it as extra work to cut for price.\nWhat: a proposal focuses entirely on the automation's capability, with no explicit answer to \"who can change this,\" \"how would we know if it went wrong,\" or \"what happens if it makes a mistake\" — and a sophisticated client either asks these questions and finds no good answer, or doesn't ask and gets burned by an ungoverned system later.\nHow it happens: governance work (logging, access controls, guardrails) can feel like unbillable overhead rather than part of the actual deliverable, especially under pressure to keep the price competitive.\nWhy beginners miss it: an impressive capability demo is what gets attention in a sales conversation — the governance questions often only surface later, once something has already gone wrong or a more sophisticated buyer specifically probes for them.\nDetect: review a proposal and check whether it explicitly addresses accountability, logging, and control, or only describes what the system can do.\nPrevent: build governance into the core proposal and pricing from the start, framed as part of what makes the system genuinely deployable for a real business, not optional add-on work.\nInterview question: \"A prospective client asks 'what happens if your AI system makes a mistake with our customer data?' How would your proposal have already answered this?\"\nReal-world consequence: losing sophisticated clients who correctly identify the absence of governance as a real risk, or delivering a system that later causes a real incident with no accountability trail.\n\nFailure 4 — Packaging a single client's specific solution as a reusable productized service before confirming it actually generalizes.\nWhat: a solution built for one client's particular environment and constraints gets marketed and sold as a general offering, but turns out to require significant, unbudgeted rework for the next client whose environment differs.\nHow it happens: the excitement of having built something that works creates a pull toward packaging and selling it more broadly, without first validating that the underlying pattern actually transfers.\nWhy beginners miss it: the first successful delivery feels like proof of a generalizable offering — the gap only becomes visible when a second, differently-configured client reveals hidden client-specific assumptions.\nDetect: before marketing something as a productized offering, explicitly identify which parts are genuinely general versus specific to the first client's environment.\nPrevent: apply the same discipline from this course's earlier reusable-workflow guidance — validate against at least one genuinely different real (or realistic) instance before confidently packaging and pricing it as a repeatable service.\nInterview question: \"You built a great automation for one client. What would you check before offering the same thing, at the same price, to a different client?\"\nReal-world consequence: underestimating the real effort for a second delivery, damaging both the profitability of that engagement and the client relationship when the 'productized' offering needs substantially more custom work than promised.",
  },
  {
    section_type: "debugging_playbook",
    title: "The client relationship or delivery isn't going well — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"A production prompt change caused a regression and we can't figure out what changed or roll it back cleanly.\"\nDiagnostic: check whether the prompt was version-controlled at all before this change.\nFix: immediately set up version control for all production prompts going forward, and for this incident, manually reconstruct the previous working version as best you can from any available records or memory.\n\nSymptom: \"A client is pushing back hard on price, and it's making the engagement feel unsustainable.\"\nDiagnostic: check whether the original proposal was priced against a clearly articulated business value, or just hours — a value-anchored proposal is generally easier to defend against pushback than a bare hourly quote.\nFix: for future proposals, invest in explicitly quantifying the business value with the client before pricing, so the number has a clear, defensible justification tied to their own numbers.\n\nSymptom: \"A sophisticated prospective client asked detailed governance questions our proposal didn't address, and we lost confidence in the room.\"\nDiagnostic: review the proposal specifically for whether it addressed accountability, logging, and control, or only capability.\nFix: build a governance section into your standard proposal template going forward, so this is addressed proactively rather than reactively under pressure in a live conversation.\n\nSymptom: \"A second client wants the 'same' solution we built for the first client, but it's requiring much more custom work than expected.\"\nDiagnostic: identify specifically which parts of the original solution were genuinely general versus specific to the first client's particular environment.\nFix: reprice this engagement honestly based on the actual required work, and for future offerings, validate genuine reusability against a second real client before marketing something as a fixed-scope productized service.",
  },
  {
    section_type: "checklist",
    title: "Reliability, Governance & Client-Readiness Checklist",
    order_index: 6,
    content: "- [ ] All production prompts and instructions are version-controlled with visible change history\n- [ ] Explicit guardrails constrain what actions the system can actually take, not just what it's instructed not to do\n- [ ] Logging and access controls can answer \"who changed this, what did it do, and why\" for any real question\n- [ ] Pricing is anchored to a defensible, conservative estimate of actual business value, not just build-time hours\n- [ ] A retainer or maintenance arrangement is proposed when the system genuinely needs ongoing attention, with specific coverage stated\n- [ ] The client proposal explicitly addresses governance and accountability, not just capability\n- [ ] Before packaging a solution as a reusable offering, its genuine generalizability has been validated against more than one real instance\n- [ ] The full capstone portfolio (single agents, intelligent automation, enterprise Copilot agent, multi-agent architecture) is documented with design reasoning, not just technical configuration",
  },
  {
    section_type: "template",
    title: "Client Proposal Framework",
    order_index: 7,
    content: "Client / engagement: ___\n\nBusiness problem being solved (in the client's own terms): ___\n\nEstimated business value (hours saved / errors reduced / revenue impact) and how it was calculated: ___\n\nWhat's being delivered: ___ (agents, workflows, integrations)\n\nGovernance: version control approach ___, guardrails ___, logging/access controls ___, escalation path ___\n\nPricing model and reasoning: fixed project / retainer / hybrid — why: ___\n\nOngoing maintenance needs and what a retainer (if proposed) actually covers: ___\n\nWhat happens if the system makes a mistake: ___ (the honest, specific answer)\n\nSuccess criteria the client can independently verify: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [n8n: Environments and version control](https://docs.n8n.io/source-control-environments/) — the official reference for treating n8n workflows (and by extension, the prompts within them) as version-controlled production assets.\n- [OpenAI: Safety best practices](https://platform.openai.com/docs/guides/safety-best-practices) — covers guardrail design principles directly relevant to this module's governance topic.\n\nRecommended\n- Search for \"value-based pricing consulting\" resources (a well-established discipline in consulting more broadly, not AI-specific) — the underlying pricing philosophy this module draws on predates AI automation and is worth understanding from its original context.\n\nReference\n- [n8n: User management and permissions](https://docs.n8n.io/user-management/) — the practical reference for implementing the access-control governance this module calls for, within n8n specifically.\n\nAdvanced\n- Search for published case studies or frameworks from automation/AI agencies on productizing client work (several publish their approach publicly) — useful for seeing how the packaging and generalization-validation discipline in this module plays out at real agency scale.",
  },
];

const exercises = [
  {
    level: "capstone",
    order_index: 0,
    title: "Capstone: build a client-ready AI agency portfolio",
    problem_statement: "Assemble and fully document a portfolio covering: 2-3 single agents (reuse or extend designs from earlier modules), one intelligent automation workflow (Trigger → Agent → Action, per Module 4), one enterprise-style Copilot/knowledge-grounded agent (per Module 5), and one multi-agent architecture (per Module 5).\n\nFor each item, write a short client-facing description (what it does, the business value) AND a governance summary (version control approach, guardrails, logging, what happens if it's wrong). Then write one complete client proposal, using this module's Client Proposal Framework, for one of the items in your portfolio, including a defensible value-based price.",
    starter_context: "You can reuse and extend work from earlier modules' exercises rather than building everything from scratch — the point of this capstone is the documentation, governance framing, and proposal, not necessarily net-new technical builds.",
    hints: [
      "For the value-based pricing in your proposal, work through an actual (even if hypothetical) calculation — hours saved per week times a reasonable hourly value, for example — rather than picking a number that feels right.",
      "Make sure your governance summaries are genuinely specific to each portfolio item, not a copy-pasted generic paragraph repeated across all of them.",
    ],
    solution_notes: "A strong capstone submission has a portfolio with genuine variety across the required categories (not, for example, four single agents relabeled as different categories), governance summaries specific enough to each item that they couldn't be swapped between portfolio items unchanged, and a proposal with a value-based price backed by an actual, shown calculation rather than an unjustified number. The proposal should explicitly address what happens if the system is wrong — this is the detail most likely to be skipped, and its presence (with a genuinely specific, honest answer) is a strong signal of real understanding of this module's core lesson.",
  },
];

const interviewQuestions = [
  { category: "project_defence", order_index: 0, question: "Walk me through how you calculated the value-based price in your capstone proposal.", what_is_tested: "Whether the pricing was genuinely reasoned from real business value, or an arbitrary number presented as if it were calculated.", strong_answer_structure: "Walk through the specific calculation (hours saved, frequency, a reasonable value per hour or per outcome) and explain why the estimate is conservative/defensible rather than optimistic.", weak_answer_example: "\"I just picked a price that felt fair\" — no actual value calculation behind the number.", follow_up_question: "How would you validate this estimate with a real client before finalizing the price?" },
  { category: "fundamentals", order_index: 1, question: "Why should prompts used in a production system be version-controlled the same way application code is?", what_is_tested: "Understanding that prompts function as production logic, not casual configuration.", strong_answer_structure: "Explain that prompts directly determine system behavior in production, and an untracked change can cause an unexplainable regression with no way to identify what changed or roll back — exactly the same risk profile as unversioned application code.", weak_answer_example: "\"Prompts are just text, they don't need the same rigor as code\" — misunderstands that prompts function as real production logic.", follow_up_question: "What would a good prompt version-control workflow actually look like in practice?" },
  { category: "scenario", order_index: 2, question: "A prospective client asks: 'What happens if your AI system makes a mistake with our customer data?' Your proposal doesn't currently address this. How do you handle the conversation, and what do you change going forward?", what_is_tested: "Whether the candidate treats this as a legitimate, important question requiring a real answer, or something to deflect.", strong_answer_structure: "In the moment: give an honest answer about the actual safeguards in place (or acknowledge a real gap and how it would be addressed). Going forward: build a governance section into the standard proposal template so this question is answered proactively rather than reactively.", weak_answer_example: "Deflecting the question with reassurance ('it's very reliable') without addressing the actual accountability and recovery mechanism.", follow_up_question: "What specific governance elements would you want in place before you'd feel confident answering this question well?" },
  { category: "applied", order_index: 3, question: "How do you decide whether a solution built for one client is ready to be packaged and sold as a reusable productized service to others?", what_is_tested: "Whether the candidate applies genuine validation before generalizing a one-off solution.", strong_answer_structure: "Explain identifying which parts of the solution are genuinely general versus specific to the first client's environment, and validating against at least one other genuinely different real or realistic case before confidently packaging and pricing it as a fixed-scope repeatable offering.", weak_answer_example: "\"If it worked once, I'd start selling it as a standard package\" — no validation step before generalizing.", follow_up_question: "What would you do if the second client's needs revealed the original solution doesn't actually generalize as well as hoped?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to have a real conversation with a client (or a hypothetical one you can reason through) about pricing that felt uncomfortable. How did you handle it?", what_is_tested: "Real or realistic comfort navigating a genuine business conversation, not just technical delivery.", strong_answer_structure: "A strong answer describes the specific tension (undercharging discovered later, client pushback, a scope/price mismatch), how it was actually addressed, and what changed in the approach to pricing or scoping afterward.", weak_answer_example: "A vague answer avoiding any real specifics about the actual pricing conversation or its outcome.", follow_up_question: "How do you now set expectations about pricing and scope earlier in an engagement to avoid this kind of uncomfortable conversation later?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill1 } = await supabase.from("skills").select("id").eq("name", "Service Pricing & Packaging").single();
const { data: skill2 } = await supabase.from("skills").select("id").eq("name", "Proposal Writing").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert([
  { module_id: MODULE_ID, skill_id: skill1.id },
  { module_id: MODULE_ID, skill_id: skill2.id },
]);
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("n8n Module 6 (Reliability, Governance & Monetization) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 2 skill mappings.");
