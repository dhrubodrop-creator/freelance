import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "518dbb41-04b7-4205-b4af-94f1c8eeffc8"; // Skills & Reusable Workflows

const sections = [
  {
    section_type: "the_field",
    title: "What turning one-off prompting into a team asset actually looks like",
    order_index: 0,
    content: "Every team using an AI coding tool eventually discovers the same pattern independently: someone figures out a good way to prompt for a recurring task — writing a migration, doing a security review pass, scaffolding a new API route in the project's specific style — and that knowledge lives entirely in their head, or scattered across their personal prompt history. The next person on the team has to rediscover it from scratch.\n\nThe fix is the same one engineering teams have always used for repeated knowledge: package it as a reusable, versioned asset instead of tribal knowledge. A packaged Skill or workflow captures not just \"what to ask,\" but the specific context, constraints, and steps that made the good result good in the first place — so a teammate who's never done this task before gets the benefit of everyone who has.\n\nThis matters more than it sounds like it should, because the actual leverage of a tool like this compounds at the team level, not just the individual level. A team where five people have each independently found slightly different, undocumented ways to do the same recurring task is leaving most of that leverage on the table.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about reusable workflows",
    order_index: 1,
    content: "**1. If you've explained the same context twice, it should become a reusable asset, not a repeated conversation.** The second time you find yourself typing a similar explanation, that's the signal to package it, not just repeat it more efficiently.\n\n**2. A good reusable workflow captures the reasoning, not just the steps.** \"Do X then Y\" is less durable than \"do X then Y because Z constraint always applies here\" — the reasoning is what lets someone adapt the workflow correctly when the situation isn't quite identical.\n\n**3. Standardization trades a small amount of individual flexibility for a large amount of team consistency.** A shared workflow means results are more predictable and reviewable across the team, even if it's occasionally not the exact approach any one person would have chosen alone.\n\n**4. A reusable workflow needs an owner and a maintenance habit, or it rots.** Codebases and conventions change; a workflow encoded once and never revisited becomes actively misleading, not just stale.\n\n**5. Connecting to a larger codebase or remote environment changes the trust calculus.** A workflow that works well in a small, fully-understood repo may behave differently against a much larger, less-familiar one — the same workflow can need a tighter review loop in a different context.\n\n**6. The goal is leverage, not automation for its own sake.** A packaged Skill is worth building when it saves real, recurring effort across multiple people or sessions — not every one-off task needs to become reusable infrastructure.",
  },
  {
    section_type: "decision_framework",
    title: "Should this become a reusable Skill or standardized workflow?",
    order_index: 2,
    content: "IF you've done a task, or a close variant of it, more than a couple of times, and it required non-obvious context to get right THEN package it as a reusable Skill or documented workflow. BECAUSE the effort of packaging pays back every time it's reused, by you or a teammate. WATCH OUT FOR: packaging something too early, before you actually understand what made it work well — a workflow written after only one attempt often encodes an accident, not a real pattern.\n\nIF the task is genuinely one-off, specific to a particular piece of work that won't recur THEN don't invest in packaging it — a good one-time prompt is enough. BECAUSE the cost of maintaining a reusable asset that never gets reused is pure waste. WATCH OUT FOR: underestimating how often a task actually recurs — many \"one-off\" tasks turn out to happen every few weeks once you're paying attention.\n\nIF multiple team members need to perform the same kind of task consistently (a specific review checklist, a scaffolding pattern, a migration process) THEN standardize it as a shared, version-controlled workflow rather than letting each person develop their own version. BECAUSE inconsistent individual approaches to the same task produce inconsistent, harder-to-review results across the team. WATCH OUT FOR: standardizing before there's real agreement on what the right approach actually is — a premature standard can lock in a worse pattern than what individuals would have converged on organically.\n\nIF the workflow involves connecting to a remote environment or a much larger codebase than where it was originally built THEN treat the first several uses in that new context as a trust-building phase with closer review, not an automatic extension of the original trust level. BECAUSE the workflow's assumptions may not transfer cleanly to a different scale or environment.",
  },
  {
    section_type: "workflow",
    title: "The real process for building a reusable Skill",
    order_index: 3,
    content: "1. Notice the repetition. Track when you (or a teammate) do a similar task, requiring similar context, for the second or third time.\n\n2. Capture what actually made the good result good — the specific context, constraints, and reasoning, not just the sequence of actions. This is usually harder than it sounds, because a lot of what worked was implicit knowledge in your head.\n\n3. Package it as a Skill (or a documented, version-controlled workflow) with enough specificity that someone unfamiliar with the original task could use it correctly.\n\n4. Test it on a genuinely different instance of the same task category, not the original one it was built from — this reveals whether the packaging actually generalized, or just re-described one specific case.\n\n5. Share it with the team and treat it as a real artifact: reviewed, version-controlled, discoverable by anyone who needs it.\n\n6. Assign it an owner and revisit it when the codebase or conventions it depends on change materially — a Skill referencing an outdated pattern actively misleads whoever uses it next.\n\n7. When connecting a workflow to a larger or remote codebase for the first time, run it with closer review initially, and only extend full trust once you've confirmed it holds up in that new context.",
  },
  {
    section_type: "failure_modes",
    title: "How reusable workflows actually break",
    order_index: 4,
    content: "Failure 1 — Packaging a one-off success as if it were a general pattern.\nWhat: a Skill or workflow is built from a single good result, but it turns out to encode something specific to that one situation rather than the general task.\nHow it happens: one successful run feels like proof the approach works, when it may have succeeded partly due to something incidental to that specific case.\nWhy beginners miss it: the packaged workflow reads as complete and confident, with no obvious signal that it was only validated once.\nDetect: before trusting a new Skill broadly, test it against a genuinely different instance of the task, not a close variant of the original.\nPrevent: wait for at least a couple of real, somewhat different instances of the task before packaging, or explicitly mark a newly-built Skill as unverified until it's been tested on a second case.\nInterview question: \"You built a reusable workflow after one successful use. What's your biggest risk?\"\nReal-world consequence: a Skill that works for the exact original case but fails or misleads on the next slightly different one, undermining trust in reusable workflows generally.\n\nFailure 2 — Letting a shared workflow go stale as the codebase evolves.\nWhat: a team's Skill or documented workflow references a pattern, file structure, or convention that has since changed, but nobody updated it.\nHow it happens: workflows are usually written once, during a moment of active attention, and then forgotten once they're \"done.\"\nWhy beginners miss it: the workflow still runs and produces output — there's no error that signals staleness, only a subtly wrong or outdated result.\nDetect: periodically re-run key shared workflows against current code and check whether their assumptions still hold.\nPrevent: assign explicit ownership and a review cadence to important shared workflows, the same way you'd own a piece of shared infrastructure.\nInterview question: \"How do you keep a team's shared AI workflows from becoming outdated as the codebase changes?\"\nReal-world consequence: new team members get onboarded onto outdated conventions via a stale but confidently-presented workflow.\n\nFailure 3 — Standardizing too early, before the team actually agrees on the right approach.\nWhat: one person's individual approach gets promoted to \"the team standard\" before there's real consensus that it's actually the best pattern.\nHow it happens: the first person to package something often has disproportionate influence over what becomes the default, regardless of whether it's genuinely the best approach.\nWhy beginners miss it: having any shared standard feels like progress over no standard, so the question of whether it's the right standard gets skipped.\nDetect: check whether the standardized workflow was actually reviewed and agreed on by the team, or just adopted because it was first and convenient.\nPrevent: treat a newly proposed shared workflow as a draft open for real review before it becomes the default everyone is expected to follow.\nInterview question: \"How would you handle disagreement about which approach should become the team's standard workflow for a recurring task?\"\nReal-world consequence: a suboptimal pattern becomes entrenched simply because it was first, making it harder to change later once more people depend on it.\n\nFailure 4 — Applying a workflow built for a small, familiar codebase directly to a much larger or unfamiliar one without adjustment.\nWhat: a workflow that reliably works in one context produces unreliable or unreviewed results when applied somewhere its underlying assumptions don't hold.\nHow it happens: past success in one context creates confidence that gets extended automatically to a different context, without re-verifying the assumptions transfer.\nWhy beginners miss it: the workflow's steps still execute without obvious errors — the mismatch shows up in quality, not in a crash.\nDetect: explicitly ask whether the new context (codebase size, remote environment, team's familiarity) matches the one the workflow was originally built and tested in.\nPrevent: treat the first uses in a meaningfully different context as a trust-building phase with closer review, per the decision framework above.\nInterview question: \"Would you trust a workflow you built for a small repo to work the same way against a much larger, less familiar codebase? Why or why not?\"\nReal-world consequence: a workflow trusted based on past performance in a different context produces a subtly wrong result in the new one, without the usual safety margin of closer review that a genuinely new task would have gotten.",
  },
  {
    section_type: "debugging_playbook",
    title: "A shared workflow isn't working as expected — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"A Skill that used to work well is now producing worse results.\"\nDiagnostic: check whether the codebase, conventions, or file structure it depends on have changed since it was built or last reviewed.\nFix: update the Skill's assumptions to match the current codebase, and assign it an owner if it doesn't already have one.\n\nSymptom: \"Different team members get inconsistent results from what's supposed to be the same shared workflow.\"\nDiagnostic: check whether everyone is actually using the same, current version of the workflow, or whether people have quietly diverged with their own local variants.\nFix: consolidate on one version-controlled source of truth for the workflow, and retire or reconcile the individual variants.\n\nSymptom: \"A workflow that worked great in our main repo produced a much worse result in a different, larger codebase.\"\nDiagnostic: this is very likely the workflow's assumptions not transferring to a different scale or structure, not a general reliability problem.\nFix: treat the new context as a fresh trust-building phase — review closely, adjust the workflow's assumptions for this specific context, and only extend full trust once verified here too.\n\nSymptom: \"We built a Skill for a recurring task, but nobody on the team is actually using it.\"\nDiagnostic: check whether the workflow is genuinely discoverable and whether it was actually validated as better than what people were already doing individually.\nFix: make sure it's documented somewhere the team actually looks, and get direct feedback on whether it's genuinely saving effort compared to the ad-hoc approach it was meant to replace.",
  },
  {
    section_type: "checklist",
    title: "Reusable Workflow / Skill Checklist",
    order_index: 6,
    content: "- [ ] The task has genuinely recurred more than once, or is expected to recur across the team, before being packaged\n- [ ] The packaged workflow captures the reasoning behind key decisions, not just a sequence of steps\n- [ ] It's been tested against a genuinely different instance of the task, not just the original case it was built from\n- [ ] It's version-controlled and discoverable by anyone on the team who needs it, not kept in one person's private notes\n- [ ] It has an assigned owner and a plan for revisiting it when the underlying codebase or conventions change\n- [ ] When applied to a meaningfully different context (a larger codebase, a remote environment) it goes through a fresh trust-building phase with closer review\n- [ ] The team actually agreed the standardized approach is the right one, not just the first one someone happened to write down",
  },
  {
    section_type: "template",
    title: "Reusable Skill / Workflow Documentation",
    order_index: 7,
    content: "Task this workflow handles: ___\n\nWhy this approach (the reasoning, not just the steps): ___\n\nWhen NOT to use this workflow: ___ (situations where the assumptions don't hold)\n\nContext/constraints it assumes about the codebase: ___\n\nOwner: ___\nLast verified against current codebase: ___\nTested on (list the distinct real instances it's been validated against): ___\n\nKnown limitations: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Claude Code: Skills](https://docs.claude.com/en/docs/claude-code/skills) — the official reference for building and installing reusable Skills, directly relevant to this module's core topic.\n- [Claude Code: Slash commands](https://docs.claude.com/en/docs/claude-code/slash-commands) — a lighter-weight mechanism for packaging a repeated prompt pattern for team reuse.\n\nRecommended\n- [Claude Code: MCP](https://docs.claude.com/en/docs/claude-code/mcp) — for standardizing connections to external tools and remote environments across a team, relevant to the 'connecting to remote environments' topic in this module.\n\nReference\n- [Claude Code: Settings](https://docs.claude.com/en/docs/claude-code/settings) — for understanding how team-wide configuration (as opposed to individual preference) can be shared and version-controlled.\n\nAdvanced\n- [Claude Code: Enterprise deployment](https://docs.claude.com/en/docs/claude-code/third-party-integrations) — relevant once a team's shared workflows need to scale across a larger organization with more formal governance.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Package a task you've done at least twice",
    problem_statement: "Identify a real task you've done more than once with Claude Code (or would plausibly do more than once) — for example, scaffolding a new API route in a specific style, or running a specific kind of code review pass. Write it up using this module's Reusable Skill / Workflow Documentation template, being explicit about the reasoning behind each step, not just the steps themselves.\n\nThen test it on a genuinely different instance of the task than the one you originally had in mind, and note whether the documentation held up or needed adjustment.",
    starter_context: "If you don't have a real recurring task on hand, a reasonable substitute is: writing a consistent pull-request description, or scaffolding a new component matching your project's existing patterns.",
    hints: [
      "Force yourself to write down WHY each step matters, not just what it is — this is the part that's usually missing from a first draft.",
      "The test-on-a-different-instance step is where most packaging gaps actually surface — don't skip it even if you're confident the documentation is complete.",
    ],
    solution_notes: "A strong submission's documentation explicitly states the reasoning behind its steps (not just a step list), and the test against a genuinely different instance either confirms it generalizes or surfaces a specific, concrete gap that gets noted and fixed. Submissions that skip the second-instance test, or that only restate the original task in different words, haven't actually validated that the packaging generalized.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Decide whether three candidate tasks are worth packaging",
    problem_statement: "Consider three candidate tasks: (1) a task you've only done once, but suspect might recur; (2) a task multiple people on a hypothetical team do slightly differently today; (3) a task that's genuinely specific to one piece of work and won't recur.\n\nFor each, apply this module's decision framework and state explicitly whether you'd package it as a reusable Skill, and why or why not.",
    starter_context: "You can use real tasks from your own experience or reasonably realistic hypothetical ones — the point is applying the decision framework correctly, not the specific tasks chosen.",
    hints: [
      "For task 2, consider whether there's already real team agreement on the right approach, or whether packaging now would prematurely lock in one person's version.",
      "For task 1, consider the risk of packaging from a single instance versus the cost of waiting for a second occurrence.",
    ],
    solution_notes: "A strong submission reaches different, well-justified conclusions for each of the three tasks rather than applying the same answer to all three — task 3 should clearly not be packaged, task 1 involves a real judgment call about packaging-too-early risk versus recurrence likelihood, and task 2 should flag the premature-standardization risk from this module's failure modes before recommending outright packaging.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Audit a stale shared workflow",
    problem_statement: "You're told: \"our team's Skill for scaffolding new API routes was built six months ago, and the codebase has changed a fair amount since then — nobody's checked whether it still matches our current patterns.\" You're not given the actual Skill or codebase.\n\nDescribe, concretely, how you'd audit whether this workflow is still trustworthy, what specific things you'd check, and what you'd do with what you find.",
    starter_context: "This is a process-design exercise — the deliverable is your audit plan and reasoning, not a specific fixed workflow.",
    hints: [
      "Consider comparing the workflow's stated assumptions against the actual current codebase conventions, not just re-running it and eyeballing the output.",
      "Consider what you'd do differently going forward to prevent this kind of staleness from recurring, not just how you'd fix this one instance.",
    ],
    solution_notes: "A strong answer proposes a concrete audit: diffing the workflow's documented assumptions against current codebase conventions, re-testing it against a fresh real task, and checking whether an owner was ever assigned. It should also propose a forward-looking fix — an ownership and review-cadence policy — rather than treating this as a one-time cleanup, directly applying the ownership principle from this module's mental models and checklist.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why does a reusable Skill or workflow need to capture reasoning, not just steps?", what_is_tested: "Understanding of what makes packaged knowledge actually generalize versus just describe one instance.", strong_answer_structure: "Explain that a step list without the underlying reasoning can't be correctly adapted when the next instance of the task differs slightly from the original — the reasoning is what lets someone judge how to apply the workflow to a new but related situation.", weak_answer_example: "\"Steps are enough if they're detailed\" — misses that detail alone doesn't convey judgment or adaptability.", follow_up_question: "Give an example of a workflow step that would need to change if the underlying reasoning behind it were properly understood, versus blindly copied." },
  { category: "applied", order_index: 1, question: "How do you decide whether a recurring task is worth packaging as a reusable Skill?", what_is_tested: "A real decision framework, not a blanket 'always package repeated tasks' rule.", strong_answer_structure: "Reference actual recurrence frequency, how much non-obvious context the task requires, and whether multiple people would benefit — not packaging every repeated task regardless of these factors.", weak_answer_example: "\"If I've done it before, I package it\" — no consideration of whether the packaging effort is actually worth it.", follow_up_question: "Describe a task you've done multiple times that you'd still choose not to package. Why?" },
  { category: "scenario", order_index: 2, question: "You discover a shared team workflow was built from a single successful run and never validated again. How do you handle it?", what_is_tested: "Recognition of the 'packaging a one-off as a general pattern' failure mode and a concrete remediation approach.", strong_answer_structure: "Propose testing it against a genuinely different real instance of the task before continuing to trust it broadly, and flag it to the team as unverified in the meantime rather than assuming it's fine because it's already in use.", weak_answer_example: "\"If it's already being used, it's probably fine\" — treats existing adoption as proof of correctness.", follow_up_question: "How would you communicate this concern to a team that's been relying on the workflow without issue so far?" },
  { category: "debugging", order_index: 3, question: "A workflow that reliably worked well in a small repo produces worse results in a much larger codebase. What's your first hypothesis?", what_is_tested: "Understanding that trust built in one context doesn't automatically transfer to a different scale or environment.", strong_answer_structure: "Hypothesize that the workflow's underlying assumptions (about codebase size, structure, or familiarity) don't hold in the new context, and propose treating this as a fresh trust-building phase with closer review rather than assuming general reliability.", weak_answer_example: "\"The tool must just be worse at large codebases\" — a vague capability claim rather than an assumption-mismatch diagnosis.", follow_up_question: "What specifically would you check to confirm or rule out this hypothesis?" },
  { category: "system_design", order_index: 4, question: "How would you design an ownership and maintenance process for a team's library of shared AI workflows, so they don't go stale?", what_is_tested: "Systems thinking about maintaining a growing collection of reusable assets over time.", strong_answer_structure: "Propose assigning explicit owners to each workflow, a review cadence tied to major codebase changes, and a lightweight process for retiring or updating workflows that no longer match current conventions.", weak_answer_example: "\"We'd just fix them when someone notices a problem\" — a reactive-only approach with no proactive maintenance mechanism.", follow_up_question: "How would you decide when a workflow should be retired entirely rather than updated?" },
  { category: "behavioural", order_index: 5, question: "Tell me about a time you turned a one-off task into a reusable process or tool that others ended up using. What made it actually get adopted?", what_is_tested: "Real experience with the adoption challenge, not just the technical packaging.", strong_answer_structure: "A strong answer describes a specific task, how it was packaged with enough context for others to use correctly, and what specifically drove real adoption (discoverability, demonstrated time savings, direct sharing) rather than assuming good documentation alone guarantees use.", weak_answer_example: "A vague answer about building something without addressing whether or how it was actually adopted by others.", follow_up_question: "What would you have done differently if adoption had been slower than you expected?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "Codebase Context Engineering").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("Claude Code Module 3 (Skills & Reusable Workflows) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
