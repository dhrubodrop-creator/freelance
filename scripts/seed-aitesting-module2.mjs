import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "6e499c4b-ba4f-41a3-b75c-3ab8eee0dfae"; // Evaluation Metrics & Golden Datasets

const sections = [
  {
    section_type: "the_field",
    title: "What proving an AI feature works, with numbers, actually looks like",
    order_index: 0,
    content: "\"It seems to work well\" is not a QA deliverable — it's an impression. The QA professional's actual job on an AI feature is turning that impression into a defensible, numeric claim: a golden dataset (a curated, representative test set with known-correct expected characteristics) run through a defined evaluation harness, producing real metrics a stakeholder can trust and a future regression can be measured against.\n\nFaithfulness and relevancy are the two metrics most specific to LLM output (especially for RAG-backed features) that a traditional QA background won't automatically include: faithfulness measures whether the output is actually grounded in the source material it's supposed to be based on (not fabricated), and relevancy measures whether the output actually addresses what was asked, independent of whether it's factually faithful. A response can be perfectly faithful to its source material while being irrelevant to the actual question, or perfectly relevant while fabricating unsupported claims — these are genuinely separate failure modes requiring separate measurement.\n\nLLM-as-judge validation deserves particular QA scrutiny: using one model to evaluate another's output is itself a system that needs to be tested and trusted, not assumed reliable because it's automated. A QA professional's instinct to validate the validator — checking the judge's verdicts against real human judgment before trusting it — is exactly the rigor this technique needs and often doesn't get from engineers moving fast.",
  },
  {
    section_type: "mental_models",
    title: "How QA professionals think about evaluation metrics and golden datasets",
    order_index: 1,
    content: "**1. A golden dataset is a QA deliverable with the same rigor requirements as any other test artifact — versioned, reviewed, and representative, not an afterthought.** Treating it as a quick, informal collection of a few examples undermines everything built on top of it.\n\n**2. Faithfulness and relevancy are separate failure modes, and a single blended \"quality\" score hides which one actually failed.** A QA report that can distinguish \"the answer was unfaithful to the source\" from \"the answer was faithful but didn't address the question\" gives engineering something actually actionable; a blended score doesn't.\n\n**3. An LLM-as-judge is a system under test, not a trusted oracle.** The QA instinct to validate any automated judgment mechanism against ground truth applies directly here — the judge's verdicts need to be checked against real human evaluation before being trusted for reporting or gating decisions.\n\n**4. \"Representative\" is a real, checkable property of a golden dataset, not just a claim.** A golden dataset built entirely from easy, convenient examples isn't actually representative of the real input variety a feature will face, and this gap is directly checkable by comparing the dataset's composition against real production traffic patterns.\n\n**5. Metrics need to translate into a decision, not just a number.** A QA report's real value is in what it lets a stakeholder decide (ship, don't ship, fix this specific gap first) — a metric reported with no connection to a decision threshold is incomplete.\n\n**6. Golden datasets and evaluation criteria are living QA artifacts, updated as real production issues are discovered — the same principle established in the LLMOps course track, now owned specifically as a QA responsibility, not just an engineering one.**",
  },
  {
    section_type: "decision_framework",
    title: "How should I design metrics and build a golden dataset for QA reporting?",
    order_index: 2,
    content: "IF the feature is RAG-backed or otherwise grounded in specific source material THEN measure faithfulness and relevancy as separate, distinct metrics, not one blended quality score. BECAUSE these are genuinely separate failure modes requiring separate diagnosis and separate fixes, and a blended score obscures which one actually needs attention. WATCH OUT FOR: a feature can score well on one and poorly on the other — report both explicitly, even when one looks fine, so a stakeholder sees the complete picture.\n\nIF you're building a golden dataset for QA reporting THEN construct it to genuinely represent the real input variety the feature faces, validated by comparing its composition against real production traffic patterns where available, not just a curated set of convenient examples. BECAUSE an unrepresentative golden dataset produces metrics that look reassuring but don't actually reflect real-world reliability. WATCH OUT FOR: a golden dataset that's representative at launch can become stale as real usage patterns evolve — periodically re-validate representativeness, the same living-artifact principle established in the LLMOps course track.\n\nIF you're using an LLM-as-judge for any metric THEN validate its verdicts against real human evaluation on a meaningful sample before trusting it for QA reporting or gating decisions. BECAUSE an unvalidated automated judge is itself an untested system, and QA rigor requires validating any evaluation mechanism before relying on its output. WATCH OUT FOR: validate periodically, not just once — a judge's reliability can shift if the underlying model changes, or if the feature's typical output style shifts enough to fall outside what the judge was originally validated against.\n\nIF a metric is being reported to a stakeholder THEN connect it explicitly to a decision (does this meet the threshold for shipping, is this a blocking gap, is this acceptable residual risk), not just present a number in isolation. BECAUSE a metric without a decision connection leaves the stakeholder to guess at its significance, undermining the actual point of QA reporting.",
  },
  {
    section_type: "workflow",
    title: "The real process for building an evaluation harness with defensible metrics",
    order_index: 3,
    content: "1. Identify which metrics genuinely matter for this specific feature — faithfulness and relevancy for RAG-backed features, plus any feature-specific correctness criteria (format compliance, required-field presence).\n\n2. Build the golden dataset deliberately for representativeness: real input variety, genuine edge cases, and validated (where real production data is available) against actual traffic patterns.\n\n3. For any metric using LLM-as-judge, validate the judge's verdicts against real human evaluation on a meaningful sample before trusting it.\n\n4. Run the evaluation harness against the golden dataset, reporting faithfulness, relevancy, and any other relevant metrics separately, not blended into one score.\n\n5. Connect each reported metric explicitly to a decision threshold — what score is acceptable to ship, what constitutes a blocking gap.\n\n6. Present the evaluation report to stakeholders with clear, decision-relevant framing, not just raw numbers.\n\n7. When a production issue is found that the golden dataset didn't catch, add a representative case to the dataset, treating this as a required QA follow-up.\n\n8. Periodically re-validate both the golden dataset's representativeness and the LLM-as-judge's calibration, since both can drift from their original validated state over time.",
  },
  {
    section_type: "failure_modes",
    title: "How QA evaluation metrics and golden datasets actually go wrong",
    order_index: 4,
    content: "Failure 1 — A golden dataset built from convenient, easy examples that doesn't actually represent real input variety.\nWhat: the golden dataset consistently produces high scores because it's composed of examples the feature handles easily, while real production input includes genuinely harder cases the dataset never captures.\nHow it happens: easy examples are the fastest and most obvious to write when first building a golden dataset, and it's easy to stop there without deliberately validating representativeness.\nWhy beginners miss it: high evaluation scores look like a genuine QA success — the gap is only visible when comparing the dataset's actual composition against real production traffic, which requires deliberate additional effort.\nDetect: compare the golden dataset's input characteristics against real production traffic patterns, checking for genuine coverage of the harder, messier real-world cases.\nPrevent: deliberately curate hard cases and edge cases into the golden dataset from the start, per this module's workflow, and validate representativeness against real traffic where available.\nInterview question: \"Your golden dataset shows a 95% pass rate, but users are reporting quality issues. What's your first hypothesis about the dataset itself?\"\nReal-world consequence: a QA report that looks reassuring but doesn't actually reflect real-world reliability, undermining trust in QA once the gap between reported and real-world quality becomes apparent.\n\nFailure 2 — Trusting an LLM-as-judge's verdicts without validating them against human evaluation.\nWhat: an automated judge's scores are reported as QA findings without ever confirming they match what a human evaluator would conclude, risking a systematically miscalibrated evaluation being trusted for real decisions.\nHow it happens: an LLM-as-judge feels objective and automated in a way that invites unquestioning trust, especially once it's set up and producing consistent-looking scores.\nWhy beginners miss it: the judge produces confident, plausible-looking scores every time — there's no obvious signal that it might be systematically wrong on some dimension without deliberately checking against human judgment.\nDetect: sample a set of the judge's verdicts and have a human independently evaluate the same outputs, checking for meaningful disagreement.\nPrevent: validate the judge against human evaluation before trusting it for QA reporting, per this module's decision framework, treating this as a required, not optional, step.\nInterview question: \"You're using an LLM-as-judge to score faithfulness for your QA reports. How do you know the judge itself is reliable?\"\nReal-world consequence: a QA report based on an uncalibrated judge presents a misleading picture of quality, either overstating or understating real reliability, undermining confidence in QA once the discrepancy is discovered.\n\nFailure 3 — Reporting a single blended quality score instead of distinct faithfulness and relevancy metrics.\nWhat: a QA report presents one overall \"quality\" number, obscuring whether a low score reflects a faithfulness problem (fabricated content), a relevancy problem (off-topic responses), or something else entirely.\nHow it happens: a single blended score is simpler to compute and present, and it's easy to default to it without deliberately separating the distinct failure modes.\nWhy beginners miss it: a blended score does provide a real, if incomplete, signal — the loss of diagnostic specificity isn't obvious until someone actually needs to know what specifically to fix.\nDetect: check whether the QA report distinguishes faithfulness from relevancy (and other relevant metrics) or presents one combined number.\nPrevent: report each relevant metric separately, per this module's workflow, giving engineering an actionable, specific finding rather than a vague overall impression.\nInterview question: \"A feature's overall quality score dropped. Engineering asks what specifically to fix. What does your report need to tell them?\"\nReal-world consequence: engineering wastes time investigating the wrong potential cause because the QA report didn't specify which distinct quality dimension actually degraded.\n\nFailure 4 — A golden dataset and evaluation criteria that never get updated after real production issues.\nWhat: a real issue is found in production that the golden dataset's original composition didn't cover, but no representative case is added afterward, leaving the same gap in place for future testing.\nHow it happens: once an immediate production issue is resolved, the pressure of the moment passes, and updating the golden dataset can feel like a lower-priority follow-up.\nWhy beginners miss it: the immediate issue does get fixed, which feels like resolution — the gap is specifically in whether the same category of issue would be caught by future testing, which isn't addressed by fixing just the one instance.\nDetect: for a past production issue, check whether a representative case was ever added to the golden dataset afterward.\nPrevent: make golden dataset updates a required, standard part of the QA follow-up process for any production issue the existing dataset didn't catch, per this module's workflow.\nInterview question: \"A production quality issue occurred that your golden dataset didn't catch. What's the QA team's responsibility here, beyond just noting the incident?\"\nReal-world consequence: the golden dataset gradually falls behind real-world quality issues, providing less and less genuine protection over time as known gaps accumulate without being closed.",
  },
  {
    section_type: "debugging_playbook",
    title: "Your evaluation metrics or golden dataset aren't providing real confidence — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"Our golden dataset shows high scores, but real users report quality problems.\"\nDiagnostic: compare the golden dataset's actual composition against real production traffic patterns, checking for coverage gaps in harder, messier real-world cases.\nFix: add representative hard cases to the golden dataset based on the actual reported issues, and re-run evaluation to get an honest, updated score.\n\nSymptom: \"We're not confident our LLM-as-judge is actually giving us reliable scores.\"\nDiagnostic: sample a set of judge verdicts and compare against independent human evaluation of the same outputs.\nFix: if meaningful disagreement is found, adjust the judge's prompt/criteria based on the specific disagreement pattern, and re-validate before trusting it again for reporting.\n\nSymptom: \"A quality score dropped and engineering doesn't know what to fix.\"\nDiagnostic: check whether the report distinguishes faithfulness from relevancy (and other relevant metrics) or presents one blended number.\nFix: re-run evaluation reporting each metric separately, giving engineering a specific, actionable finding.\n\nSymptom: \"A production issue keeps recurring even though it was supposedly fixed before.\"\nDiagnostic: check whether a representative case was ever added to the golden dataset after the original occurrence.\nFix: add the missing case now, and establish golden dataset updates as a required, standard part of the QA follow-up process for future issues.",
  },
  {
    section_type: "checklist",
    title: "AI Evaluation Metrics & Golden Dataset QA Checklist",
    order_index: 6,
    content: "- [ ] Faithfulness and relevancy (or other relevant metrics) are reported separately, not blended into one score\n- [ ] The golden dataset genuinely represents real input variety, validated against real production traffic where available\n- [ ] Any LLM-as-judge metric has been validated against real human evaluation before being trusted for reporting\n- [ ] Every reported metric is connected to an explicit decision threshold, not presented as a bare number\n- [ ] Production issues the golden dataset didn't catch result in a representative case being added afterward\n- [ ] The golden dataset and judge calibration are periodically re-validated, not treated as permanently accurate",
  },
  {
    section_type: "template",
    title: "QA Evaluation Report",
    order_index: 7,
    content: "Feature: ___\nGolden dataset size and composition: ___\nRepresentativeness validated against: ___\n\nFaithfulness score: ___ — decision threshold: ___ — meets threshold? ___\nRelevancy score: ___ — decision threshold: ___ — meets threshold? ___\nOther relevant metrics: ___\n\nLLM-as-judge calibration: validated against human sample of ___ — agreement rate: ___\n\nRecommendation: ship / do not ship / ship with caveats — reasoning: ___\n\nGolden dataset updates from recent production issues: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [RAGAS documentation](https://docs.ragas.io/) (referenced in the LLMOps course track) — the direct reference for faithfulness and relevancy metric definitions and implementations this module centers on.\n- Zheng et al., [\"Judging LLM-as-a-Judge\"](https://arxiv.org/abs/2306.05685) (referenced in the LLMOps course track) — foundational research on LLM-as-judge reliability and known biases, directly relevant to this module's judge-validation discipline.\n\nRecommended\n- The LLMOps course track's Module 2 (Evaluation at Scale) — the engineering-side companion to this module's QA-side treatment of the same core evaluation concepts.\n\nReference\n- [ISTQB: AI Testing (CT-AI) syllabus](https://www.istqb.org/certifications/artificial-intelligence-tester) (referenced in Module 1) — the formal QA profession's own framework covering evaluation methodology for AI systems.\n\nAdvanced\n- Search for published QA/testing case studies on \"AI evaluation metrics in production\" from companies with mature AI QA practices — useful for seeing this module's principles applied at real organizational scale.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Build a golden dataset and measure faithfulness and relevancy separately",
    problem_statement: "Using a RAG-backed feature (reuse your work from the LangChain course track if available, or build a simple one), construct a golden dataset of at least 10 question/expected-answer pairs, including at least 3 deliberately hard cases. Measure faithfulness (is the answer grounded in the retrieved source material) and relevancy (does it address the actual question) as two separate scores, not one blended metric.\n\nReport both scores and identify any case where they diverge (high faithfulness but low relevancy, or vice versa).",
    starter_context: "The divergence check is the actual point of this exercise — demonstrating that faithfulness and relevancy are genuinely separate failure modes.",
    hints: [
      "Construct at least one test case deliberately designed to produce a faithful-but-irrelevant answer (e.g., a question with an ambiguous or tangential match to available source material) to actually observe the divergence.",
      "Score each case manually if you don't have RAGAS or similar tooling set up — the point is understanding the distinction, not necessarily automating it for this exercise.",
    ],
    solution_notes: "A strong submission has a genuinely varied golden dataset, separately reported faithfulness and relevancy scores (not blended), and successfully identifies at least one case where the two metrics diverge — directly demonstrating the core lesson that these are distinct failure modes requiring separate measurement and diagnosis.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Validate an LLM-as-judge against your own human evaluation",
    problem_statement: "Using your golden dataset from the guided exercise, score all cases yourself as a human evaluator (faithfulness and relevancy, 1-5 scale each). Then design an LLM-as-judge prompt to score the same cases, and compare the judge's scores against your own.\n\nReport the agreement rate and analyze any specific pattern of disagreement.",
    starter_context: "Score your own human evaluation before looking at the judge's output, to avoid anchoring your judgment to the automated result.",
    hints: [
      "Write clear scoring criteria for yourself before starting, the same way you'd want your LLM-as-judge prompt to have clear, explicit criteria.",
      "For any case where you disagree with the judge, look closely at what specifically drove the difference — this is the most useful part of the exercise for understanding real calibration gaps.",
    ],
    solution_notes: "A strong submission produces independent human scores with clear criteria applied consistently, an honest agreement-rate comparison against the LLM-as-judge, and specific analysis of any disagreement pattern found — directly demonstrating the judge-validation discipline this module's decision framework requires before trusting an automated judge for QA reporting.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Audit a golden dataset's representativeness against real usage data",
    problem_statement: "You're given this description: \"Our golden dataset has 20 test cases, all written by the engineering team based on the features they built. We've never compared it against actual user query logs.\" You're given a hypothetical sample of 10 real user queries that seem noticeably different in style and complexity from what you'd expect the golden dataset to contain (you construct these 10 yourself, based on realistic user behavior for any feature of your choice).\n\nAssess the likely representativeness gap and propose specific additions to the golden dataset.",
    starter_context: "Construct your own realistic 'real user query' examples for a feature of your choice, then reason about how they'd likely differ from an engineer-written golden dataset.",
    hints: [
      "Consider how real users often phrase questions less precisely, combine multiple intents in one query, or use domain terminology differently than an engineer familiar with the system's internal design would.",
      "Propose specific, concrete additions to the golden dataset based on the actual gaps you identify, not a generic 'add more variety' recommendation.",
    ],
    solution_notes: "A strong submission constructs genuinely realistic user queries that plausibly differ from engineer-written test cases (less precise phrasing, mixed intents, unexpected terminology), correctly diagnoses the representativeness gap this reveals, and proposes specific, concrete golden dataset additions addressing the actual identified gaps — directly applying this module's representativeness-validation principle to a realistic scenario.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why are faithfulness and relevancy measured as separate metrics rather than one blended quality score?", what_is_tested: "Understanding that these are genuinely distinct failure modes requiring separate diagnosis.", strong_answer_structure: "Explain that a response can be faithful to its source material while being irrelevant to the actual question, or relevant while fabricating unsupported claims — a blended score can't distinguish which specific failure occurred, while separate metrics give engineering an actionable, specific finding.", weak_answer_example: "\"They're basically measuring the same thing\" — misses the genuine independence of these two failure modes.", follow_up_question: "Give an example of a response that would be faithful but not relevant." },
  { category: "applied", order_index: 1, question: "How would you validate that an LLM-as-judge is reliable enough to trust for QA reporting?", what_is_tested: "Practical process for judge calibration, applying general QA validation instincts to this specific technique.", strong_answer_structure: "Describe having the judge score a sample independently evaluated by a human, comparing agreement, and investigating any specific disagreement pattern before trusting the judge's scores for real QA decisions.", weak_answer_example: "\"If it's automated, it should be consistent and reliable\" — conflates consistency with actual correctness.", follow_up_question: "How often would you re-validate the judge's calibration, and what might prompt an earlier re-check?" },
  { category: "scenario", order_index: 2, question: "Your golden dataset shows a 95% pass rate, but users keep reporting quality issues. What's your investigation?", what_is_tested: "Recognition that a high golden-dataset score with real reported issues points to a representativeness gap.", strong_answer_structure: "Investigate whether the golden dataset's composition actually reflects the real variety and difficulty of production queries, comparing it against real user query logs if available, rather than assuming the 95% score is accurate.", weak_answer_example: "\"The dataset score is probably right and users are just being unusually picky\" — dismisses the real reported issue without questioning the dataset's own validity.", follow_up_question: "How would you go about acquiring real user query data to compare against, if it's not already being logged?" },
  { category: "debugging", order_index: 3, question: "A quality metric dropped and engineering asks what specifically to fix. Your report only shows one blended quality number. What's the gap?", what_is_tested: "Recognition that a blended metric doesn't give engineering an actionable specific finding.", strong_answer_structure: "Explain that the blended score can't tell engineering whether the issue is faithfulness, relevancy, or something else, and propose re-running evaluation with separately reported metrics to give a genuinely actionable finding.", weak_answer_example: "Suggesting engineering just 'investigate the drop' without recognizing the report itself needs to be more specific.", follow_up_question: "How would you restructure your standard QA report template to prevent this gap going forward?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to build or validate an evaluation methodology (not necessarily AI-related) that others would rely on for a real decision.", what_is_tested: "Real or realistic experience with the rigor of building a trustworthy evaluation mechanism.", strong_answer_structure: "A strong answer describes the specific evaluation methodology built, how it was validated for reliability before being trusted, and how it was actually used to inform a real decision.", weak_answer_example: "A vague answer with no specific evaluation methodology or validation process described.", follow_up_question: "How did you know your evaluation methodology was actually trustworthy, versus just looking reasonable?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLM Evaluation & Testing").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AI LLM Testing Module 2 (Evaluation Metrics & Golden Datasets) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
