import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "8b559fd5-4757-41db-8db3-6a0237178d5c"; // LLMOps, Observability & Incident Response

const sections = [
  {
    section_type: "the_field",
    title: "What real observability and incident response for AI systems looks like",
    order_index: 0,
    content: "A deployed agent system that runs without visibility into what it's actually doing is a black box you're hoping stays healthy. Real observability means you can answer, at 2am during an incident, specific questions: which requests are failing, why, how much they're costing, and whether a change you just made improved or worsened things — without guessing.\n\nAI systems add dimensions generic observability material doesn't fully cover: token usage and cost per request (which can spike unpredictably if a prompt or retrieval step balloons), latency that includes both your infrastructure and an external LLM provider's response time (which you don't control and can't optimize directly), and failure modes that don't look like a crash — a model that responds fluently but incorrectly doesn't throw an error, it just quietly produces a wrong answer that looks fine to a health check.\n\nIncident response for these systems is a real, learnable discipline: recognizing you're in an incident, triaging severity, finding the actual root cause under pressure without guessing wildly, communicating status honestly, and — critically — writing an honest postmortem afterward that changes something, rather than a ritual document nobody reads. The FDE who can walk into a genuine production fire, stay calm, and methodically narrow down the cause is doing the single most valuable and most differentiating part of the role.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about observability and incidents",
    order_index: 1,
    content: "**1. You can't debug what you can't see, and 'it's working' is not the same as 'I can see that it's working.'** Logs, metrics, and traces aren't optional polish added after the system works — they're what lets you actually know whether it's working, and why, when something goes wrong.\n\n**2. Metrics tell you something is wrong; traces tell you where.** An aggregate metric (error rate spiked) tells you to look; distributed tracing across the actual request path (which service, which call, which dependency) tells you where to look — you generally need both, not one or the other.\n\n**3. Token usage and cost are a real operational metric, not just a finance concern.** A prompt or retrieval bug that silently doubles token usage per request is a production incident even if nothing crashes — it's burning real money and often signals a real correctness problem too (unnecessarily large context, retries, a runaway loop).\n\n**4. A fluent, confident-sounding wrong answer is a failure mode with no crash to alert on.** Traditional monitoring catches errors and downtime; catching \"the model answered, but the answer is wrong\" requires deliberate evaluation-style monitoring (sampling outputs, tracking specific quality signals), not just uptime checks.\n\n**5. Incident response under pressure benefits from a process, not improvisation.** Trying to figure out both what to investigate and how to communicate status, live, during a stressful incident is much harder than following a pre-agreed process that tells you what to check and who to update.\n\n**6. A postmortem's value is entirely in what it changes.** A postmortem that documents what happened but doesn't lead to a concrete process, monitoring, or code change is a formality, not a real learning tool — and blameless framing (focused on systems and process, not individual fault) is what makes people willing to be honest in it.",
  },
  {
    section_type: "decision_framework",
    title: "What should I actually monitor, and how severe is this incident?",
    order_index: 2,
    content: "IF you're setting up observability for an LLM-calling service THEN track, at minimum: request latency (broken down by your infrastructure vs. the external LLM call), error rate, token usage and cost per request, and some form of output-quality sampling. BECAUSE these cover both the traditional failure modes (crashes, timeouts) and the AI-specific ones (cost blowups, fluent-but-wrong answers) that generic infrastructure monitoring misses entirely. WATCH OUT FOR: monitoring only uptime/error rate and assuming that's sufficient — a service can have 100% uptime and 0% errors while still producing systematically wrong answers.\n\nIF an alert fires or a problem is reported THEN triage severity based on actual user impact (how many users, how severe, is there a workaround) before deciding how urgently to respond. BECAUSE not every anomaly is an emergency, and treating everything as equally urgent burns out the team and delays response to things that actually matter more. WATCH OUT FOR: a low-volume but high-severity issue (a small number of users getting completely wrong, potentially harmful answers) can matter more than a high-volume but low-severity one (slightly slower responses for everyone) — severity isn't just about volume.\n\nIF you're investigating a live incident THEN start from the metrics and traces, form a specific hypothesis, and check it directly — rather than making speculative changes and seeing if they help. BECAUSE unstructured trial-and-error during a live incident risks making things worse and rarely converges as fast as a hypothesis-driven investigation. WATCH OUT FOR: the pressure of a live incident pushes toward action for its own sake — resist making a change you can't clearly justify with evidence, even under pressure to \"do something.\"\n\nIF the incident is resolved THEN write a real, honest postmortem within a short window while details are fresh, focused on what changes as a result — not just documenting the timeline. BECAUSE the value of a postmortem is entirely in what it changes; a postmortem that doesn't lead to a concrete action is a wasted opportunity to actually prevent a recurrence.",
  },
  {
    section_type: "workflow",
    title: "The real observability-to-incident-resolution workflow",
    order_index: 3,
    content: "1. Instrument the service with structured logging, metrics (latency, error rate, token usage/cost, output-quality sampling), and distributed tracing across the real request path, including the external LLM call itself.\n\n2. Set up dashboards that make the key metrics visible at a glance, and alerts tied to thresholds that actually indicate a real problem (not so sensitive they cause alert fatigue, not so loose they miss real issues).\n\n3. When an alert fires, triage severity based on real user impact before deciding response urgency.\n\n4. Investigate using metrics and traces to form a specific, evidence-based hypothesis about the root cause, rather than guessing or making speculative changes.\n\n5. Communicate status honestly and at a reasonable cadence to stakeholders during the incident, even when the root cause isn't yet known — \"we're investigating, here's what we know so far\" is better than silence.\n\n6. Apply a fix once the hypothesis is confirmed, and verify the fix actually resolves the issue using the same metrics that first indicated the problem, not just an assumption that it worked.\n\n7. Write a blameless postmortem shortly after resolution: what happened, the actual root cause (not just the surface symptom), the timeline, and — most importantly — specific, concrete follow-up actions with owners.\n\n8. Track whether postmortem action items actually get completed — an incident review process with no follow-through on its own action items isn't actually preventing recurrence.",
  },
  {
    section_type: "failure_modes",
    title: "How observability and incident response actually breaks",
    order_index: 4,
    content: "Failure 1 — Monitoring uptime and errors, but not output quality.\nWhat: the service reports healthy (no crashes, low error rate, normal latency) while quietly producing systematically wrong or degraded answers.\nHow it happens: uptime and error-rate monitoring is the default, well-understood pattern from traditional infrastructure monitoring, while output-quality monitoring requires deliberate, AI-specific instrumentation that's easy to skip.\nWhy beginners miss it: everything looks fine on every dashboard that exists — the gap is in what isn't being measured at all, which is much harder to notice than a metric that's visibly bad.\nDetect: periodically sample real production outputs and evaluate their quality directly, rather than relying solely on infrastructure-level health signals.\nPrevent: build output-quality sampling and evaluation into the standard monitoring setup from the start, treating it as a first-class signal alongside uptime and error rate.\nInterview question: \"Your dashboards show 100% uptime and 0% errors, but users are complaining the answers are wrong. How is that possible, and what would you build to catch this earlier?\"\nReal-world consequence: a real quality regression goes undetected for an extended period because nothing in the monitoring setup was actually looking for it.\n\nFailure 2 — A token usage or cost spike that goes unnoticed until the bill arrives.\nWhat: a bug (an unbounded retry loop, an oversized context window, a runaway agent loop) causes token usage — and cost — to increase substantially, but nothing alerts on it in real time.\nHow it happens: cost is often treated as a finance/billing concern reviewed periodically, rather than a real-time operational metric with its own alerting.\nWhy beginners miss it: the service may still be functioning correctly from a pure uptime/error-rate perspective, so nothing about the incident looks urgent from a traditional monitoring lens.\nDetect: track token usage and cost per request as a first-class metric with its own alerting thresholds, not just a monthly billing review.\nPrevent: set real-time alerts on cost/token anomalies, and treat a sudden spike as a real incident requiring investigation, not just a budget conversation after the fact.\nInterview question: \"How would you detect a token-usage spike in real time, before it shows up as an unexpected bill at the end of the month?\"\nReal-world consequence: significant, avoidable cost accumulates over days or weeks before anyone notices, often traced back to a bug that could have been caught immediately with proper alerting.\n\nFailure 3 — Speculative, unstructured changes during a live incident instead of hypothesis-driven investigation.\nWhat: under pressure to resolve an incident quickly, changes get made based on guesses rather than evidence from metrics and traces, sometimes making the situation worse or obscuring the actual root cause.\nHow it happens: the pressure to visibly \"do something\" during a live incident is strong, and speculative action feels more productive in the moment than methodical investigation.\nWhy beginners miss it: sometimes a speculative change accidentally helps, reinforcing the habit even though it wasn't a reliable process.\nDetect: after an incident, check whether the actual root cause was identified through evidence, or whether the incident just happened to resolve after a series of untested changes.\nPrevent: establish and practice a hypothesis-driven investigation habit — form a specific, falsifiable hypothesis from the available metrics/traces, check it directly, and only then act — even under real time pressure.\nInterview question: \"You're mid-incident and someone suggests trying a random configuration change to see if it helps. How do you respond?\"\nReal-world consequence: incidents that take longer to resolve than necessary, or that resolve without anyone actually understanding the true root cause — leaving the underlying issue able to recur.\n\nFailure 4 — A postmortem that documents the incident but doesn't change anything.\nWhat: a postmortem is written, reviewed, and filed away, but no concrete action items are actually completed, so the same category of incident recurs later.\nHow it happens: postmortems can become a compliance ritual (\"we're required to write one\") rather than a genuine learning tool, especially once the immediate pressure of the incident has passed.\nWhy beginners miss it: writing the postmortem itself feels like the completion of the process, when the actual value is entirely in what happens after it's written.\nDetect: check whether previous postmortems' action items were actually completed, and whether their described root causes have genuinely stopped recurring.\nPrevent: treat postmortem action items with the same tracking rigor as any other real engineering work — assigned owners, and follow-up to confirm completion, not just a document that gets filed away.\nInterview question: \"How do you make sure a postmortem actually prevents a repeat of the incident, rather than just documenting what happened?\"\nReal-world consequence: the same category of incident recurs, sometimes more than once, because the organizational learning from the first postmortem never actually got implemented.",
  },
  {
    section_type: "debugging_playbook",
    title: "You're mid-incident — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"Error rate and latency are normal, but users report the AI is giving wrong or nonsensical answers.\"\nDiagnostic: this is a quality regression that traditional infrastructure monitoring won't catch — check recent changes to the prompt, retrieval logic, or model/provider configuration, and sample real recent outputs directly.\nFix: identify the specific recent change correlated with the quality drop (a prompt edit, a retrieval pipeline change, a model version change) and roll it back or correct it, then verify via direct output sampling, not just metrics returning to normal.\n\nSymptom: \"Costs have spiked significantly with no corresponding change in traffic volume.\"\nDiagnostic: check token usage per request, not just total request count — a bug causing larger contexts, more retries, or a runaway loop can spike cost without spiking request volume.\nFix: trace the specific request pattern responsible for the increased per-request token usage, fix the underlying bug (context bloat, retry loop, etc.), and add real-time cost/token alerting if it wasn't already in place.\n\nSymptom: \"Latency has increased, but it's unclear whether it's our infrastructure or the external LLM provider.\"\nDiagnostic: check distributed traces that separate time spent in your own services from time spent waiting on the external API call specifically.\nFix: if the delay is on the provider side, check their status page and consider whether a fallback/retry strategy is warranted; if it's your own infrastructure, trace further into which specific internal step is slow.\n\nSymptom: \"We resolved the incident but aren't sure we actually understand the root cause.\"\nDiagnostic: this is a sign the investigation was resolved by a change that happened to work, rather than a confirmed hypothesis.\nFix: before closing the incident, go back through the metrics/traces and confirm a specific, evidence-backed explanation for why the fix worked — an unexplained resolution risks the same issue recurring in a different form.",
  },
  {
    section_type: "checklist",
    title: "Observability & Incident Response Checklist",
    order_index: 6,
    content: "- [ ] Latency, error rate, token usage/cost, and output-quality sampling are all tracked as first-class metrics\n- [ ] Distributed tracing covers the full request path, including time spent waiting on external LLM calls specifically\n- [ ] Alert thresholds are tuned to catch real problems without causing alert fatigue\n- [ ] Incident severity triage is based on real user impact, not just alert volume\n- [ ] Investigation during incidents is hypothesis-driven, using metrics/traces as evidence, not speculative trial-and-error\n- [ ] Status is communicated honestly and at a reasonable cadence during incidents, even before the root cause is known\n- [ ] Fixes are verified using the same metrics that first indicated the problem, not just assumed to have worked\n- [ ] A blameless postmortem is written shortly after resolution, with specific action items and owners\n- [ ] Postmortem action items are actually tracked to completion, not just filed away",
  },
  {
    section_type: "template",
    title: "Incident Postmortem",
    order_index: 7,
    content: "Incident title: ___\nDate/time (start, detected, resolved): ___\nSeverity: ___\n\nUser impact: ___ (who was affected, how, for how long)\n\nTimeline: ___ (key events, in order, with timestamps)\n\nRoot cause: ___ (the actual underlying cause, not just the surface symptom)\n\nHow it was detected: ___ (which alert/metric/report first surfaced it)\n\nHow it was resolved: ___\n\nWhat went well in the response: ___\nWhat could have gone better: ___\n\nAction items (specific, owned, with deadlines):\n1. ___\n2. ___\n3. ___\n\nFollow-up: were these action items completed? ___ (revisit this section after the fact)",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Prometheus documentation](https://prometheus.io/docs/introduction/overview/) and [Grafana documentation](https://grafana.com/docs/) — the standard open-source stack for metrics collection and dashboards referenced throughout this module.\n- [OpenTelemetry documentation](https://opentelemetry.io/docs/) — the standard framework for distributed tracing, directly relevant to tracing requests across your service and the external LLM call.\n\nRecommended\n- [Google SRE Book: Monitoring Distributed Systems](https://sre.google/sre-book/monitoring-distributed-systems/) — foundational reading on what to monitor and why, applicable well beyond AI-specific systems.\n- [Google SRE Book: Postmortem Culture](https://sre.google/sre-book/postmortem-culture/) — the canonical reference on blameless postmortems and what makes them actually effective, directly relevant to this module's incident-response workflow.\n\nReference\n- LLM provider status pages and incident-history pages (e.g. major model providers publish these) — worth bookmarking directly, since provider-side incidents are a real and recurring category of latency/availability issue for any LLM-calling service.\n\nAdvanced\n- Search for \"LLM observability\" tooling (several specialized platforms exist specifically for token usage, cost, and output-quality tracking beyond generic APM tools) — worth evaluating once a team's needs outgrow what Prometheus/Grafana alone comfortably cover for AI-specific signals.",
  },
];

const exercises = [
  {
    level: "capstone",
    order_index: 0,
    title: "Capstone: build an observability stack and run a simulated incident postmortem",
    problem_statement: "Using the containerized agent stack from Module 2 (or a comparable stack you set up for this exercise), add a real observability layer: metrics for latency, error rate, and token usage/cost, plus basic tracing across the request path including the external LLM call.\n\nThen simulate a production incident: deliberately introduce a bug that causes one of the following (your choice): a token-usage spike (e.g. an unbounded retry or oversized context), a quality regression (e.g. a broken prompt that produces plausible-but-wrong answers without erroring), or a latency issue (e.g. a slow or failing dependency). Detect it using your own observability setup — not by knowing in advance where you put the bug — investigate using metrics/traces to form and confirm a real hypothesis, fix it, and verify the fix using the same metrics.\n\nWrite a full postmortem using this module's template, including specific, concrete action items.",
    starter_context: "The point of 'detect it using your own observability setup' is important: don't just fix the bug you know you introduced — actually use your dashboards/traces to find it the way you would a real unknown incident, to test whether your monitoring setup would have actually caught this in production.",
    hints: [
      "If you choose the quality-regression path, make sure your observability setup actually includes some form of output sampling — this is the one failure mode traditional uptime/error monitoring won't catch at all.",
      "Time-box your investigation and write down your hypotheses as you form them, before confirming which one was correct — this makes the postmortem's 'how it was resolved' section honest and specific rather than reconstructed after the fact.",
    ],
    solution_notes: "A strong capstone submission has real, working observability (not just a description of what it would monitor), successfully detects the deliberately-introduced issue using that observability rather than prior knowledge of where the bug was placed, and produces a postmortem with a genuinely specific root cause (not a vague summary) and concrete, actionable follow-up items. Submissions that choose the quality-regression scenario and correctly note that traditional monitoring wouldn't have caught it without deliberate output-quality sampling are demonstrating the core insight this module is built around.",
  },
];

const interviewQuestions = [
  { category: "project_defence", order_index: 0, question: "Walk me through the incident you simulated in your capstone. How did you detect it, and how confident are you in the root cause you identified?", what_is_tested: "Whether the investigation was actually evidence-driven and specific, or a reconstructed narrative after already knowing the answer.", strong_answer_structure: "Describe the specific metric or trace that first indicated something was wrong, the hypothesis formed from that evidence, how it was confirmed (not just assumed), and how the fix was verified using the same signal that first flagged the problem.", weak_answer_example: "\"I knew what bug I introduced, so I just fixed it\" — skips the actual detection-and-investigation process the exercise was designed to test.", follow_up_question: "If your dashboards hadn't caught this, what would you add to make sure a similar issue is detectable in the future?" },
  { category: "fundamentals", order_index: 1, question: "Why can a service have 100% uptime and 0% error rate while still having a serious production problem?", what_is_tested: "Understanding that quality regressions in AI systems don't necessarily manifest as traditional infrastructure failures.", strong_answer_structure: "Explain that a model can respond fluently and without error while still producing an incorrect or degraded answer — this requires output-quality monitoring specifically, which traditional uptime/error-rate metrics don't capture at all.", weak_answer_example: "\"That shouldn't be possible if monitoring is set up correctly\" — misses that this requires a specific, deliberate category of monitoring beyond standard infrastructure metrics.", follow_up_question: "What would you actually build to catch this category of issue?" },
  { category: "scenario", order_index: 2, question: "You're mid-incident, users are affected, and a teammate suggests trying a config change 'to see if it helps' without a clear reason to believe it will. How do you respond?", what_is_tested: "Commitment to hypothesis-driven investigation even under real pressure to act.", strong_answer_structure: "Push back constructively — propose checking the available metrics/traces first to form an actual hypothesis, explaining that unverified speculative changes risk making things worse or obscuring the real root cause, even though the instinct to 'do something' is understandable under pressure.", weak_answer_example: "\"Sure, let's just try it, we need to do something\" — prioritizes visible action over evidence-based investigation.", follow_up_question: "How do you balance the real pressure to resolve an incident quickly against the value of a more careful, evidence-based approach?" },
  { category: "debugging", order_index: 3, question: "Your token usage costs have tripled over the past week with no corresponding increase in request volume. Walk me through your investigation.", what_is_tested: "Practical diagnostic process for a cost-based AI-specific incident.", strong_answer_structure: "Check token usage per request (not just total requests), look for a specific recent change (prompt, retrieval logic, retry configuration) correlated with the increase, and trace to the specific request pattern responsible before proposing a fix.", weak_answer_example: "\"I'd just look at the billing dashboard\" — doesn't propose an actual investigation path to find the root cause.", follow_up_question: "How would you prevent this from taking a full week to notice next time?" },
  { category: "behavioural", order_index: 4, question: "Tell me about a postmortem you wrote (or would write) where the action items actually got implemented and prevented a repeat incident. What made that happen?", what_is_tested: "Understanding that postmortem value depends entirely on follow-through, not just documentation quality.", strong_answer_structure: "Describe specific, owned, tracked action items (not vague intentions), and a concrete mechanism (a tracking process, a follow-up review) that ensured they were actually completed rather than filed away.", weak_answer_example: "A vague answer focused only on the quality of the postmortem document itself, with no mention of whether or how action items were tracked to completion.", follow_up_question: "What would you do if you noticed a pattern of postmortem action items consistently not getting completed across your team?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill1 } = await supabase.from("skills").select("id").eq("name", "Model Observability").single();
const { data: skill2 } = await supabase.from("skills").select("id").eq("name", "AI Incident Response").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert([
  { module_id: MODULE_ID, skill_id: skill1.id },
  { module_id: MODULE_ID, skill_id: skill2.id },
]);
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("FDE Module 4 (LLMOps, Observability & Incident Response) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 2 skill mappings.");
