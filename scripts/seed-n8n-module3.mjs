import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "132187b0-7cb8-4264-9c9b-56fe0cc86879"; // Intelligence Layer (OpenAI Agents)

const sections = [
  {
    section_type: "the_field",
    title: "What designing the reasoning layer of an agent actually looks like",
    order_index: 0,
    content: "There's a real difference between a prompt and an instruction set for a reusable agent. A prompt is something you type once for a single answer. Instructions are what define a reasoning component's job every time it runs — its scope, its output format, what it should and shouldn't do, and how it hands off to whatever comes next in the pipeline. Treating these the same way is one of the most common reasons agent-based systems feel unreliable: the 'instructions' were actually written like a one-off prompt, with no real thought given to consistency across many different inputs.\n\nThe practical skill this module builds is designing agents that produce structured, predictable output — not prose that has to be re-parsed with fragile string matching, but genuine structured data (JSON with a defined schema) that the next step in a pipeline can consume reliably. This 'JSON mindset' — thinking about an agent's output as data with a contract, not just a helpful-sounding response — is what makes it possible to chain agents together into a real pipeline (Research → Analyze → Summarize, for example) without the whole thing becoming fragile.\n\nGood agent design at this layer also means being deliberate about memory and context: what does this specific reasoning step actually need to know to do its job well, versus what's irrelevant noise that dilutes its focus or invites it to go off-task? And when one agent hands off to another, what's the reviewer/approval pattern for catching a mistake before it propagates downstream?",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about designing the intelligence layer",
    order_index: 1,
    content: "**1. Instructions define a role, a prompt asks a question.** A well-designed agent's instructions should read like a job description — scope, responsibilities, output format, boundaries — not like a single clever question you'd ask once.\n\n**2. Structured output is a contract, not a preference.** When one step's output feeds into another step, that output needs a defined, consistent shape (a JSON schema) the downstream step can rely on — free-form prose forces the next step to guess at meaning, which is where pipelines quietly break.\n\n**3. Context should be curated, not maximized.** Giving an agent every piece of information you have access to isn't more helpful — irrelevant context can genuinely degrade reasoning quality and increase the chance the agent focuses on the wrong thing. Deciding what this specific step actually needs is real design work.\n\n**4. Memory has a scope, and that scope is a design decision.** Does this agent need to remember the full conversation history, just the last exchange, or a specific persisted fact across sessions? Each is a different, deliberate choice with different cost and reliability implications, not a default to leave unexamined.\n\n**5. A reviewer/approval pattern exists to catch errors before they compound across a pipeline.** In a multi-step Research → Analyze → Summarize chain, an error introduced early (a hallucinated fact, a misread source) propagates and often gets amplified by later steps — a review checkpoint between steps is what catches this before it reaches the final output.\n\n**6. An agent that hands off cleanly is one whose output the next agent (or system) never has to guess about.** Clean handoff isn't a nicety — it's the actual measure of whether the pipeline will be reliable at scale, across the messy variety of real inputs it will eventually see.",
  },
  {
    section_type: "decision_framework",
    title: "How should I design instructions, output format, and memory for this agent?",
    order_index: 2,
    content: "IF an agent's output will be consumed by another system or agent step, not read directly by a human THEN design it to return structured output (JSON with an explicit schema), not free-form prose. BECAUSE structured output can be reliably parsed and validated, while prose forces the next step to guess at meaning and breaks unpredictably when phrasing varies. WATCH OUT FOR: defining a schema that's technically valid JSON but still ambiguous in meaning (e.g., a field that could contain either a summary or an error message with no way to distinguish) — the schema needs semantic clarity, not just syntactic validity.\n\nIF you're deciding what context to give an agent for a specific reasoning step THEN include only what's actually relevant to that step's specific job, not everything available. BECAUSE unnecessary context can dilute focus and increase the chance of the agent reasoning about the wrong thing, and it also increases cost and latency for no benefit. WATCH OUT FOR: cutting context so aggressively that the agent lacks information it genuinely needs — this is a real trade-off requiring actual judgment about the specific task, not a blanket minimize-everything rule.\n\nIF an agent needs to remember information across multiple turns or sessions THEN explicitly decide the scope of that memory (this single exchange, this session, or persisted long-term) rather than defaulting to \"remember everything.\" BECAUSE unscoped memory grows context size unpredictably over time and can surface stale or irrelevant information into later reasoning. WATCH OUT FOR: long-term persisted memory raises real questions about data freshness and correctness — a fact that was true when stored may not be true anymore.\n\nIF a reasoning step's output feeds into a consequential downstream action (not just informs another reasoning step) THEN add a reviewer/approval checkpoint between them. BECAUSE errors introduced early in a multi-step pipeline tend to compound, and catching them before they reach a consequential action is far cheaper than catching them after. WATCH OUT FOR: reviewing every intermediate step regardless of actual consequence adds unnecessary friction — scope review checkpoints to where an error would actually matter, consistent with the human-in-the-loop principle from Module 1.",
  },
  {
    section_type: "workflow",
    title: "The real process for designing an agent's reasoning layer",
    order_index: 3,
    content: "1. Write the agent's instructions as a role definition: its specific responsibility, scope, output format, and explicit boundaries — not as a single example prompt.\n\n2. Define the exact structured output schema the agent should return, thinking through every field a downstream step will need to consume reliably.\n\n3. Decide, deliberately, what context this specific step actually needs — curate rather than maximize.\n\n4. Decide the memory scope for this agent: single-turn, session-scoped, or persisted — and design accordingly, not as a default.\n\n5. If this step's output feeds a consequential downstream action, design an explicit reviewer/approval checkpoint before it, per the decision framework.\n\n6. Test the agent against a genuinely varied set of realistic inputs, not just the one example that motivated the design — check whether the structured output schema holds up consistently across that variety.\n\n7. When chaining multiple agents (Research → Analyze → Summarize), explicitly verify each handoff: does the receiving agent's instructions correctly anticipate the exact shape of what the previous agent actually produces?\n\n8. Iterate the instructions based on where the agent's output was inconsistent or incorrect across your test inputs — treating instruction refinement as an expected, ongoing part of the design process, not a one-time draft.",
  },
  {
    section_type: "failure_modes",
    title: "How the intelligence layer actually breaks",
    order_index: 4,
    content: "Failure 1 — Writing instructions like a one-off prompt instead of a reusable role definition.\nWhat: the agent behaves inconsistently across different real inputs because its instructions were really written and tuned around one specific example, not a general role.\nHow it happens: it's natural to write and test instructions against the first example that comes to mind, and stop once that one example works well.\nWhy beginners miss it: the single test case looks great, creating false confidence that the instructions are complete.\nDetect: test the same instructions against several genuinely different real inputs, not just the original motivating example, and check for consistency.\nPrevent: explicitly write instructions as a role definition (scope, responsibilities, boundaries, format) and validate against input variety before considering the design finished.\nInterview question: \"How is writing instructions for a reusable agent different from writing a one-off prompt?\"\nReal-world consequence: an agent that works impressively in a demo but degrades noticeably once it encounters the real variety of production input.\n\nFailure 2 — Returning free-form prose instead of structured output for a pipeline step.\nWhat: a downstream step has to parse the previous agent's prose output with fragile string matching or ad-hoc heuristics, which breaks whenever the phrasing varies even slightly.\nHow it happens: prose feels more natural to both write instructions for and read during development, and structured output can feel like unnecessary rigidity early on.\nWhy beginners miss it: it works fine when a human is reading the intermediate output directly during development — the fragility only shows up once another system has to consume it programmatically and reliably.\nDetect: check whether any downstream step relies on parsing free text from an upstream agent rather than consuming a defined, validated schema.\nPrevent: define structured output schemas for any step whose output feeds another system, from the start, not as a refactor after the pipeline proves unreliable.\nInterview question: \"Why would you have an agent return JSON instead of a well-written paragraph, if a human could read either one just fine?\"\nReal-world consequence: an intermittently broken pipeline where a downstream step misinterprets or fails to parse an upstream agent's output, often only on certain phrasing variations that are hard to reproduce and debug.\n\nFailure 3 — Giving an agent too much irrelevant context, degrading its focus.\nWhat: an agent is given a large amount of available context \"just in case,\" and its reasoning quality suffers because it's not clear which parts are actually relevant to its specific task.\nHow it happens: it feels safer to include more information than less, on the assumption that more context can only help.\nWhy beginners miss it: the failure often looks like general unreliability or occasional off-topic responses, without an obvious link back to the amount or relevance of the context provided.\nDetect: for a case where the agent's reasoning seems unfocused or off-task, check whether the context given includes information genuinely irrelevant to the specific step's job.\nPrevent: deliberately curate context to what this specific step actually needs, per the decision framework, rather than defaulting to \"include everything available.\"\nInterview question: \"Why might giving an agent more context sometimes make its output worse, not better?\"\nReal-world consequence: inconsistent, unfocused agent output that's hard to debug because the actual cause (context dilution) isn't the first thing anyone thinks to check.\n\nFailure 4 — No reviewer checkpoint in a multi-step pipeline, letting an early error compound.\nWhat: an error introduced in an early step (Research) propagates uncorrected through later steps (Analyze, Summarize), and the final output is confidently wrong in a way that's hard to trace back to its actual source.\nHow it happens: each individual step may look reasonable in isolation, and adding a review checkpoint between steps takes real additional design and implementation effort.\nWhy beginners miss it: the final output often reads fluently and confidently, giving no obvious signal that an earlier step's error is baked into it.\nDetect: when a multi-step pipeline's final output is wrong, trace back through each intermediate step's actual output, not just the final result, to find where the error was introduced.\nPrevent: add a reviewer/approval checkpoint at the specific points where an early error would be both likely and costly if it propagates, per this module's decision framework.\nInterview question: \"A three-step agent pipeline produces a confidently wrong final answer. How do you figure out which step actually introduced the error?\"\nReal-world consequence: wrong conclusions delivered with high apparent confidence, and a debugging process that takes much longer than necessary because there's no visibility into where the pipeline actually went wrong.",
  },
  {
    section_type: "debugging_playbook",
    title: "The agent's reasoning output isn't reliable — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"The agent works great on my test example but behaves inconsistently on other real inputs.\"\nDiagnostic: check whether the instructions were really tuned around that one specific example rather than written as a general role definition.\nFix: test against a genuinely varied set of real inputs, and rewrite the instructions to explicitly cover the scope and boundaries needed for that variety, not just the original case.\n\nSymptom: \"A downstream step keeps failing to correctly interpret the previous agent's output.\"\nDiagnostic: check whether the upstream agent is returning structured, schema-validated output, or free-form prose being parsed with fragile heuristics.\nFix: define an explicit output schema for the upstream agent and validate its output against that schema before passing it downstream.\n\nSymptom: \"The agent's responses feel unfocused or occasionally go off-task.\"\nDiagnostic: review exactly what context is being provided for this step, and check for information that's irrelevant to its specific job.\nFix: curate the context down to what's actually needed for this specific reasoning step, and re-test.\n\nSymptom: \"A multi-step pipeline's final output is confidently wrong, and it's unclear where the error came from.\"\nDiagnostic: inspect the intermediate output of each step individually, not just the final result, to locate exactly where the error was introduced.\nFix: add a reviewer/approval checkpoint at that specific point in the pipeline going forward, so a similar early error is caught before it propagates next time.",
  },
  {
    section_type: "checklist",
    title: "Agent Reasoning Layer Design Checklist",
    order_index: 6,
    content: "- [ ] Instructions are written as a role definition (scope, responsibilities, format, boundaries), not a single example prompt\n- [ ] Any output consumed by another system or agent is structured (JSON with a defined schema), not free-form prose\n- [ ] The output schema is semantically clear, not just syntactically valid\n- [ ] Context provided to the agent is deliberately curated to what this specific step needs, not maximized\n- [ ] Memory scope (single-turn, session, persisted) is an explicit design decision, not a default\n- [ ] A reviewer/approval checkpoint exists wherever an early error in a multi-step pipeline would be costly if it propagates\n- [ ] The design has been tested against a genuinely varied set of real inputs, not just the original motivating example\n- [ ] For chained agents, each handoff has been explicitly verified against what the previous agent actually produces",
  },
  {
    section_type: "template",
    title: "Agent Instruction & Output Schema Spec",
    order_index: 7,
    content: "Agent name / role: ___\n\nScope and responsibilities: ___\nExplicit boundaries (what this agent should NOT do): ___\n\nContext provided (and why each piece is needed): ___\nMemory scope: single-turn / session / persisted — reasoning: ___\n\nOutput schema:\n{ field: type — meaning, ... }\n\nDownstream consumer of this output: ___\nReviewer/approval checkpoint needed before downstream action? ___\n\nTest inputs used to validate consistency (list at least 3 genuinely varied examples): ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [OpenAI: Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs) — the official reference for defining and enforcing a JSON schema on model output, directly relevant to this module's core recommendation.\n- [OpenAI: Prompt engineering guide](https://platform.openai.com/docs/guides/prompt-engineering) — covers the instructions-as-role-definition approach this module builds on.\n\nRecommended\n- [Anthropic: Building effective agents](https://www.anthropic.com/research/building-effective-agents) — its discussion of context curation and multi-step pipeline patterns (prompt chaining, evaluator-optimizer) maps closely to this module's memory-scoping and reviewer-checkpoint topics.\n\nReference\n- [JSON Schema documentation](https://json-schema.org/learn/getting-started-step-by-step) — for teams wanting a more formal grounding in schema design beyond what a specific LLM provider's structured-output feature requires.\n\nAdvanced\n- [OpenAI: Agents SDK / handoffs documentation](https://platform.openai.com/docs/guides/agents) — for teams moving beyond manually-chained agent calls toward a framework with built-in handoff and orchestration primitives.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Design instructions and an output schema for a single agent",
    problem_statement: "Design a 'Lead Qualifier' agent: given a raw lead submission (name, company, message text), it should assess whether the lead looks like a good fit for a hypothetical B2B software product and output a structured qualification result.\n\nWrite the full instructions (as a role definition, not a single prompt example) and the exact JSON output schema. Test your design (by hand, reasoning through it, or by actually running it if you have API access) against at least three genuinely different example leads — a clearly good fit, a clearly poor fit, and an ambiguous one.",
    starter_context: "You define what 'good fit' means for this hypothetical product — the point is the design process and consistency across varied inputs, not a specific correct answer.",
    hints: [
      "Your output schema should include a field for the qualification decision, but also a field for the reasoning/evidence behind it — a bare yes/no with no rationale is hard for a human reviewer to trust or check.",
      "The ambiguous test case is the most important one — this is where inconsistent instructions tend to reveal themselves.",
    ],
    solution_notes: "A strong submission's instructions read as a genuine role definition with explicit scope and boundaries, the output schema includes both a decision and supporting reasoning (not just a bare classification), and the three test cases — especially the ambiguous one — produce outputs consistent with the stated instructions rather than surprising or unexplainable results.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Design a Research → Analyze → Summarize pipeline with a reviewer checkpoint",
    problem_statement: "Design a three-agent pipeline: a Research agent that gathers information on a given topic, an Analyze agent that identifies key insights from that research, and a Summarize agent that produces a final client-ready summary. Define the output schema for each agent's handoff to the next, and decide where (if anywhere) a reviewer/approval checkpoint belongs in this pipeline.\n\nJustify your reviewer-checkpoint placement decision explicitly using this module's decision framework.",
    starter_context: "You choose the topic domain — the design reasoning matters more than the specific subject matter.",
    hints: [
      "Consider what kind of error is most likely and most costly at each stage — a research agent citing an inaccurate fact is a strong candidate for review before it gets amplified by the Analyze step.",
      "Not every handoff necessarily needs a review checkpoint — justify explicitly why you placed it where you did (or didn't place one at all), rather than adding review everywhere by default.",
    ],
    solution_notes: "A strong submission defines clear, distinct output schemas for each of the three handoffs, and places (or deliberately omits) a reviewer checkpoint with explicit reasoning tied to where an error would be most likely and costly to compound — most commonly between Research and Analyze, since an inaccurate research finding is the kind of error most likely to get amplified rather than caught by later steps.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose an inconsistent agent from its instructions alone",
    problem_statement: "You're given this agent's instructions verbatim: \"You are a helpful assistant. When given a customer message, decide if it's urgent and respond appropriately.\" You're told this agent behaves inconsistently — sometimes flagging clearly non-urgent messages as urgent, and its responses vary wildly in format.\n\nWithout more information, diagnose every design flaw in these instructions that could plausibly explain the reported inconsistency, and rewrite the instructions to fix them.",
    starter_context: "This is a diagnostic and redesign exercise based purely on the given instruction text.",
    hints: [
      "Notice how little this instruction actually defines: what makes something 'urgent'? What does 'respond appropriately' mean in terms of format or content? These gaps are the likely source of inconsistency.",
      "Consider whether this task even needs free-form 'appropriate' responses, or whether a structured output (an urgency flag plus a suggested response) would be more reliable.",
    ],
    solution_notes: "A strong submission identifies that the instructions define neither a clear urgency criterion nor a concrete output format, both of which directly explain the reported inconsistency — this is a textbook case of instructions written like a vague one-off prompt rather than a role definition. The rewrite should define explicit urgency criteria (or at least clear guidance), specify a structured output (an urgency classification plus a response draft, as separate fields), and state clear scope/boundaries — directly applying this module's checklist.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "What's the difference between writing a prompt and writing instructions for a reusable agent?", what_is_tested: "Whether the candidate understands instructions need to generalize across varied real input, not just work for one example.", strong_answer_structure: "Explain that a prompt is typically written and evaluated against a single case, while instructions for a reusable agent need to define a consistent role, scope, and output format that holds up across the full variety of real inputs the agent will actually encounter.", weak_answer_example: "\"They're basically the same thing, just longer\" — misses the generalization requirement that's the actual point.", follow_up_question: "How would you test whether a set of instructions actually generalizes, versus just working for the example you wrote them against?" },
  { category: "applied", order_index: 1, question: "Why would you have an agent return structured JSON instead of a well-written natural-language response, if a human is eventually going to read the result anyway?", what_is_tested: "Understanding that structured output matters for pipeline reliability even when a human is the eventual consumer, if intermediate steps aren't.", strong_answer_structure: "Explain that even if a human eventually reads a final summary, any intermediate step consumed by another system benefits from structured output, since prose requires fragile parsing that breaks on phrasing variation — and that a structured intermediate representation can still be rendered into natural language at the final step if needed.", weak_answer_example: "\"JSON is just better practice\" without explaining the actual reliability mechanism.", follow_up_question: "In a Research → Analyze → Summarize pipeline, which steps' outputs would you make structured, and which (if any) would you leave as prose?" },
  { category: "scenario", order_index: 2, question: "A three-step agent pipeline produces a confidently wrong final answer. How do you figure out where the error was introduced?", what_is_tested: "Diagnostic process for tracing an error through a multi-step reasoning pipeline.", strong_answer_structure: "Inspect each intermediate step's actual output individually, not just the final result, to locate where the error first appears — and check whether a reviewer checkpoint existed (or should have existed) at that point.", weak_answer_example: "Only examining the final output and trying to reason backward about what might be wrong, without actually inspecting intermediate steps.", follow_up_question: "Once you find where the error was introduced, how would you decide whether the fix is better instructions, a schema change, or a new reviewer checkpoint?" },
  { category: "debugging", order_index: 3, question: "An agent's responses seem unfocused and occasionally go off-topic. You suspect it's a context problem, not an instructions problem. How would you confirm that?", what_is_tested: "Ability to distinguish a context-curation problem from an instructions-quality problem diagnostically.", strong_answer_structure: "Review exactly what context is currently being provided, identify anything irrelevant to the agent's specific task, and test whether removing that irrelevant context improves focus and consistency — isolating the variable rather than changing both instructions and context at once.", weak_answer_example: "Rewriting the instructions first without first checking whether context volume/relevance was the actual issue.", follow_up_question: "How would you decide how much context is 'enough' for a given reasoning step, rather than either too little or too much?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you designed an agent or automated reasoning step, tested it against one example, and then discovered it didn't generalize well to other real inputs. What did you learn?", what_is_tested: "Real or realistic experience with the 'tuned for one example' failure mode and what changed afterward.", strong_answer_structure: "A strong answer describes the specific gap discovered, what the original instructions were missing, and a concrete change in process (testing against varied inputs earlier, writing instructions as a role definition from the start) made as a result.", weak_answer_example: "A vague answer with no specific example or no lasting change in how instructions get designed and tested going forward.", follow_up_question: "How many varied test inputs do you now consider 'enough' before trusting a new agent design?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "Structured Prompting for Agents").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("n8n Module 3 (Intelligence Layer) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
