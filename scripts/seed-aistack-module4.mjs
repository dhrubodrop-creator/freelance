import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "1fc7d860-5fe4-4f8f-b309-5c87743da54b"; // Full-Stack Integration

const sections = [
  {
    section_type: "the_field",
    title: "What owning the entire AI stack end to end actually looks like",
    order_index: 0,
    content: "Someone who's deeply expert in exactly one layer — a model researcher, a pure infrastructure engineer, a prompt specialist — is valuable, but a different, genuinely distinct kind of valuable than someone who can own an entire feature from model choice through to a deployed, monitored, secured endpoint. This is the specific role this course and this capstone are built around: not maximum depth in any single layer, but the ability to move across all of them, understand how they connect, and ship a complete thing.\n\nThis final module connects everything: the model/prompting layer's provider-agnostic design (Module 1), the orchestration-data boundary (Module 2), and the serving/infrastructure layer (Module 3), plus two things that only become real concerns once you're looking at the whole stack together — cost monitoring across the entire pipeline (not just the model call, but retrieval infrastructure and serving costs too), and security basics that specifically span layers (a vulnerability at the orchestration-data boundary, per the AI Security course track, is just as real a concern as one at the model layer).\n\nThe deployment checklist that closes this module isn't a formality — it's the concrete artifact that proves you can actually verify, systematically, that a full-stack feature is ready to ship, rather than confidently believing it's ready because each individual piece seemed to work when you built it.",
  },
  {
    section_type: "mental_models",
    title: "How full-stack AI engineers think about complete ownership",
    order_index: 1,
    content: "**1. Full-stack ownership means being able to trace a problem to its actual layer, and fix it there, not just recognize that 'something's wrong.'** This is the practical payoff of the cross-layer understanding built across this course's earlier modules — a full-stack engineer isn't necessarily the deepest expert in any one layer, but can navigate all of them well enough to actually diagnose and resolve a real issue.\n\n**2. Cost accumulates across every layer, not just the model API call.** Vector store hosting, compute for serving, and the model calls themselves all contribute to real operating cost — monitoring only the model API bill misses a meaningful part of the actual cost picture for a full-stack feature.\n\n**3. Security is a cross-layer concern, and a vulnerability anywhere in the stack matters.** The threat modeling principles from the AI Security course track apply not just to the model/prompting layer (prompt injection) but to the data layer (RAG poisoning) and the serving layer (standard API security) — full-stack ownership means considering all of these, not just the AI-specific parts.\n\n**4. A deployment checklist is a verification tool, not a formality to rush through.** Its actual value is in systematically confirming readiness across every layer before shipping, catching gaps a confident but non-systematic \"I think it's ready\" assessment would miss.\n\n**5. \"Owns the entire stack\" is proven by actually shipping something complete, not by understanding each layer's concepts in isolation.** The capstone's requirement — model to deployed endpoint — is deliberately an end-to-end deliverable, because that's the actual skill this course is building, distinct from separately understanding each layer.\n\n**6. Full-stack breadth and layer-specific depth are complementary, not competing.** This course's breadth doesn't replace the deep-dive courses on any individual layer — it's what lets someone navigate and integrate the depth those other courses provide into one coherent, shipped feature.",
  },
  {
    section_type: "decision_framework",
    title: "How should I approach cost monitoring, security, and final readiness across the full stack?",
    order_index: 2,
    content: "IF you're setting up cost monitoring for a full-stack AI feature THEN track cost across every layer that contributes — model API calls, vector store hosting/queries, and compute for serving — not just the model bill. BECAUSE a full picture of real operating cost requires visibility into every layer, and focusing only on the most obviously \"AI-specific\" cost (the model call) can miss significant infrastructure costs elsewhere in the stack. WATCH OUT FOR: vector store and compute costs are sometimes billed and monitored through entirely separate systems from the model API — make sure your monitoring actually aggregates across all of them into one coherent view, not several disconnected dashboards nobody looks at together.\n\nIF you're assessing security for a full-stack feature THEN apply threat modeling across every layer — the model/prompting layer (prompt injection), the data layer (RAG poisoning, per the AI Security course track), and the serving layer (standard API security practices: authentication, rate limiting, input validation) — rather than only considering the most obviously AI-specific risk. BECAUSE a full-stack feature's actual attack surface spans every layer, and a security review that only considers the model layer misses real risk elsewhere. WATCH OUT FOR: standard API security practices (authentication, rate limiting) are sometimes assumed to be \"someone else's job\" or already handled, when for a genuinely full-stack feature, verifying they're actually in place is part of full ownership.\n\nIF you're deciding whether a full-stack feature is ready to ship THEN work through a systematic checklist covering every layer explicitly, rather than relying on an overall confident impression. BECAUSE a systematic checklist catches gaps that individually-confident-but-not-cross-checked assessments of each layer would miss — this is the actual value of a deployment checklist as a real verification tool, not a formality. WATCH OUT FOR: a checklist that's followed mechanically without genuine verification (checking a box without actually confirming the underlying condition) provides false confidence, no better than skipping it entirely.\n\nIF a problem is found in a full-stack feature after shipping THEN diagnose it by identifying which specific layer is actually responsible, applying the layer-isolation debugging discipline established throughout this course, rather than a vague \"something's wrong with the AI feature\" investigation. BECAUSE precise layer attribution is what lets you actually fix the right thing efficiently, versus broad, unfocused troubleshooting across the whole stack.",
  },
  {
    section_type: "workflow",
    title: "The real process for full-stack integration and shipping readiness",
    order_index: 3,
    content: "1. Confirm each individual layer (model/prompting, orchestration/data, serving/infrastructure) is independently sound, per Modules 1-3's respective checklists.\n\n2. Set up cost monitoring that aggregates across every contributing layer — model API, vector store, and compute — into one coherent view, not disconnected per-layer dashboards.\n\n3. Conduct a security pass across every layer: prompt injection risk at the model layer, RAG poisoning risk at the data layer, standard API security (auth, rate limiting, input validation) at the serving layer.\n\n4. Verify the full end-to-end feature works correctly under realistic conditions — real data, real concurrent load, real edge cases across each layer's boundary.\n\n5. Work through a complete deployment checklist covering every layer explicitly, verifying each item genuinely (not just checking a box), before considering the feature ready to ship.\n\n6. Ship the feature, with monitoring and observability from Module 3 actively watched, not just present but unattended.\n\n7. If a post-ship issue arises, diagnose it by identifying the specific responsible layer, applying this course's layer-isolation debugging discipline.\n\n8. Document the complete feature — architecture across all layers, cost profile, security considerations, and deployment configuration — so it can genuinely be owned and maintained by someone beyond its original builder.",
  },
  {
    section_type: "failure_modes",
    title: "How full-stack integration and readiness assessment actually breaks",
    order_index: 4,
    content: "Failure 1 — Cost monitoring that only tracks the model API bill, missing infrastructure costs elsewhere in the stack.\nWhat: real operating cost from vector store hosting or serving compute goes unmonitored and unaccounted for, because cost visibility efforts focused only on the most obviously \"AI\" cost — the model API.\nHow it happens: the model API cost is the most directly visible and easily understood cost, while infrastructure costs (vector store, compute) are billed through separate systems that don't naturally get the same attention.\nWhy beginners miss it: the model API bill genuinely does represent real, trackable cost — the gap is in what's NOT being tracked, which is easy to overlook without deliberately checking.\nDetect: audit every layer of the stack for a real, billed cost, and confirm each is actually being monitored, not just the model API.\nPrevent: build cost monitoring that explicitly aggregates across every contributing layer from the start, per this module's workflow.\nInterview question: \"Your team tracks LLM API costs closely but hasn't looked at vector database hosting costs in months. What's your concern?\"\nReal-world consequence: an unmonitored cost source (vector store scaling, compute over-provisioning) accumulates significant, avoidable expense that goes unnoticed because monitoring effort was concentrated elsewhere.\n\nFailure 2 — A security review that only considers the model/prompting layer, missing data-layer and serving-layer risk.\nWhat: the security assessment focuses on prompt injection (the most AI-specific, most discussed risk) while overlooking RAG poisoning at the data layer or standard API vulnerabilities (missing authentication, no rate limiting) at the serving layer.\nHow it happens: prompt injection is the most prominently discussed AI security topic, creating a natural focus that can crowd out consideration of the other layers' real risks.\nWhy beginners miss it: a thorough-feeling review of the model layer creates a sense of completeness, even though it only covers part of the feature's actual full-stack attack surface.\nDetect: check whether the security review explicitly addresses each layer separately (model, data, serving), or is concentrated entirely on the model/prompting layer.\nPrevent: apply the AI Security course track's threat-modeling discipline across every layer explicitly, per this module's decision framework, not just the model layer.\nInterview question: \"Your security review covers prompt injection thoroughly but doesn't mention API authentication or rate limiting. What's missing?\"\nReal-world consequence: a real vulnerability at the data or serving layer (an unauthenticated endpoint, an exploitable RAG poisoning vector) goes undetected because the review's attention was concentrated entirely on the more AI-specific model-layer risk.\n\nFailure 3 — A deployment checklist followed mechanically without genuine verification.\nWhat: checklist items get checked off based on assumption or a quick glance, rather than actually confirming the underlying condition is true, providing false confidence.\nHow it happens: under time pressure to ship, it's tempting to move through a checklist quickly, checking items that \"probably\" are fine without deliberately verifying each one.\nWhy beginners miss it: a completed checklist looks the same whether each item was genuinely verified or just assumed — the difference is invisible from the artifact alone, and only shows up when an unverified item turns out to actually be a real gap.\nDetect: for any completed checklist, spot-check a few items by actually re-verifying them independently, not just trusting the checkmark.\nPrevent: require genuine evidence (a test result, a confirmed configuration check) for each checklist item, not just a checked box, per this module's workflow.\nInterview question: \"Your deployment checklist is fully checked off, but a problem shipped anyway that one of the items should have caught. What does that tell you?\"\nReal-world consequence: a checklist that provides false confidence rather than real verification, letting a genuine gap slip through specifically because it was assumed rather than actually checked.\n\nFailure 4 — Diagnosing a post-ship issue with a vague, unfocused investigation instead of layer isolation.\nWhat: when something goes wrong after shipping, the investigation treats \"the AI feature\" as one undifferentiated thing, rather than applying the layer-isolation discipline established throughout this course to localize the actual responsible layer.\nHow it happens: under the pressure of a real, live issue, it can feel faster to try broad, unfocused troubleshooting than to methodically isolate the responsible layer first.\nWhy beginners miss it: broad troubleshooting sometimes stumbles onto the right fix eventually, reinforcing the habit even though it's a much less efficient process than deliberate layer isolation.\nDetect: for a past post-ship issue, check whether the actual root cause was identified through deliberate layer isolation, or found through unfocused trial and error.\nPrevent: apply this course's established layer-isolation debugging discipline (checking retrieval separately from generation, checking the API layer separately from the underlying pipeline) as the default first step for any post-ship investigation.\nInterview question: \"A full-stack AI feature is misbehaving in production. Walk me through your investigation process.\"\nReal-world consequence: a longer, less efficient incident resolution than a methodical, layer-isolated investigation would have achieved, and sometimes a fix applied to the wrong layer entirely.",
  },
  {
    section_type: "debugging_playbook",
    title: "Something's wrong with the full-stack feature — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"Our overall AI feature costs are higher than expected, but our model API bill looks normal.\"\nDiagnostic: check vector store hosting and serving compute costs separately — the unexpected cost is likely coming from a layer that wasn't being closely monitored.\nFix: build unified cost monitoring across all layers, and identify and address the specific unmonitored cost source.\n\nSymptom: \"We passed a prompt-injection-focused security review, but a vulnerability was found elsewhere in the stack.\"\nDiagnostic: check whether the original review actually assessed the data and serving layers, or was concentrated entirely on the model/prompting layer.\nFix: conduct a full cross-layer security review now, applying the AI Security course track's threat modeling to every layer, not just the model layer.\n\nSymptom: \"A deployment checklist was fully completed, but a real problem still shipped that the checklist should have caught.\"\nDiagnostic: investigate whether the relevant checklist item was genuinely verified at the time, or just checked off without real confirmation.\nFix: re-verify all checklist items with real evidence going forward, treating a checkbox without actual verification as equivalent to not having done the check at all.\n\nSymptom: \"Something's wrong with our AI feature in production and we're not sure where to start investigating.\"\nDiagnostic: apply layer isolation — check the serving/API layer, the orchestration/data layer, and the model/prompting layer independently, rather than treating the feature as one undifferentiated thing.\nFix: once the responsible layer is identified, apply that layer's specific debugging playbook from this course's earlier modules.",
  },
  {
    section_type: "checklist",
    title: "Full-Stack Deployment Checklist",
    order_index: 6,
    content: "- [ ] Model/prompting layer: provider-agnostic, evaluated model choice, per Module 1's checklist\n- [ ] Orchestration/data layer: explicit boundary design, truncation strategy, empty-retrieval handling, per Module 2's checklist\n- [ ] Serving/infrastructure layer: containerized, health-checked, load-tested, per Module 3's checklist\n- [ ] Cost monitoring aggregates across model, data, and serving layers, not just the model API bill\n- [ ] Security review covers model layer (prompt injection), data layer (RAG poisoning), and serving layer (auth, rate limiting) explicitly\n- [ ] Every checklist item was genuinely verified with real evidence, not just checked off on assumption\n- [ ] The full end-to-end feature was tested under realistic conditions across all layers together\n- [ ] The complete feature (architecture, cost, security, deployment) is documented for handoff",
  },
  {
    section_type: "template",
    title: "Full-Stack Feature Shipping Record",
    order_index: 7,
    content: "Feature name: ___\n\nModel/prompting layer: provider ___, evaluated? ___\nOrchestration/data layer: boundary design confirmed? ___\nServing/infrastructure layer: containerized, health-checked, load-tested? ___\n\nCost monitoring: model ___, vector store ___, compute ___ — aggregated view location: ___\n\nSecurity review: model layer ___, data layer ___, serving layer ___\n\nDeployment checklist: completed with genuine verification (not just checked off)? ___\n\nEnd-to-end test results across all layers: ___\n\nDocumentation location: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- The AI Security course track (all 4 modules) — the direct reference for the cross-layer threat modeling this module requires, extending prompt injection awareness to data and serving layer risks.\n- The LLMOps course track's Module 3 (Deployment & Infrastructure) — direct reference for the cost/latency monitoring discipline this module applies across all layers.\n\nRecommended\n- The AI Engineering course track's Module 4 (LLMOps, Observability & Incident Response) — for extending this module's checklist into ongoing production operations.\n\nReference\n- The MLOps course track's Module 4 (Monitoring & Production Operations) — its drift-detection and retraining-trigger discipline is relevant if your full-stack feature includes a trained model component beyond pure LLM calls.\n\nAdvanced\n- Search for published \"AI platform engineering\" or \"full-stack AI architecture\" case studies — useful for seeing how real organizations structure teams and ownership across the model, data, and serving layers this course covers.",
  },
];

const exercises = [
  {
    level: "capstone",
    order_index: 0,
    title: "Capstone: one full-stack AI feature, model to deployed endpoint",
    problem_statement: "Bring together your work from Modules 1-3 (or build a comparable feature if you didn't complete all of them) into one complete, deployed full-stack AI feature: a provider-agnostic model layer, a RAG-backed orchestration/data layer with explicit boundary handling, and a containerized, observable, load-tested serving layer.\n\nComplete a full Full-Stack Feature Shipping Record, including unified cost monitoring across all layers and a security review covering all three layers explicitly (not just the model layer). Complete the full deployment checklist with genuine verification (real evidence) for every item, not assumptions.",
    starter_context: "This capstone is a synthesis of the entire course — reuse your work from Modules 1-3 directly rather than building from scratch.",
    hints: [
      "For the security review, make sure you genuinely address all three layers — it's easy to default to focusing mainly on prompt injection and giving the data and serving layers less real attention.",
      "For checklist verification, be honest about any item you can't fully verify — an honestly incomplete checklist with a clear gap noted is more valuable than a fully checked-off one that isn't actually true.",
    ],
    solution_notes: "A strong capstone submission has a genuinely working, deployed full-stack feature with real evidence behind every layer's checklist items (not just Module 1-3's individual checklists, but this module's full cross-layer synthesis), unified cost monitoring that actually aggregates all three layers, and a security review that gives genuine, specific attention to the data and serving layers, not just prompt injection. Submissions that complete individual layers well but never actually integrate and ship them as one deployed, verified feature haven't met the actual bar this capstone and course are built around — full-stack ownership demonstrated end to end, not layer expertise demonstrated in isolation.",
  },
];

const interviewQuestions = [
  { category: "project_defence", order_index: 0, question: "Walk me through your capstone's cost monitoring. What does it actually track, and how did you make sure you weren't missing a cost source?", what_is_tested: "Whether cost monitoring genuinely spans all layers, not just the most obvious one.", strong_answer_structure: "Describe the specific cost sources tracked (model API, vector store, compute) and the actual process used to confirm none were missed — ideally by explicitly auditing each layer for a real, billed cost rather than assuming only the model API matters.", weak_answer_example: "\"I track our OpenAI/Anthropic bill\" — describes only the most obvious cost source without addressing the others.", follow_up_question: "If your vector store costs suddenly spiked, how would your monitoring surface that?" },
  { category: "project_defence", order_index: 1, question: "Walk me through your security review. What did you check at the data and serving layers specifically, beyond prompt injection?", what_is_tested: "Whether the security review genuinely covered all layers, not just the most commonly discussed one.", strong_answer_structure: "Describe specific checks performed at the data layer (RAG poisoning considerations) and serving layer (authentication, rate limiting, input validation), not just a restatement of prompt injection considerations.", weak_answer_example: "\"I made sure prompt injection wasn't possible\" — doesn't address the data and serving layers this question specifically asks about.", follow_up_question: "What would you do if you found your serving layer had no rate limiting at all?" },
  { category: "fundamentals", order_index: 2, question: "Why is full-stack ownership of an AI feature a genuinely different and valuable skill from deep expertise in any single layer?", what_is_tested: "Understanding of the actual value proposition this course is built around.", strong_answer_structure: "Explain that full-stack ownership means being able to navigate and integrate across layers, diagnose a problem to its actual responsible layer, and ship a complete, working thing — a distinct and complementary skill to deep single-layer expertise, not a replacement for it.", weak_answer_example: "\"It's just knowing a bit about everything\" — undersells the actual integration and diagnostic value of genuine cross-layer competence.", follow_up_question: "How would you work effectively with a team that includes deep single-layer specialists, given your full-stack perspective?" },
  { category: "debugging", order_index: 3, question: "A full-stack AI feature is misbehaving in production, and it's not immediately obvious which layer is responsible. Walk me through your investigation.", what_is_tested: "Application of layer-isolation debugging discipline established across this course.", strong_answer_structure: "Describe methodically checking each layer independently (model/prompting, orchestration/data, serving/infrastructure) rather than a broad, unfocused investigation, using each layer's specific debugging approach from earlier in this course.", weak_answer_example: "\"I'd just start trying different things until something works\" — an unfocused approach that doesn't apply the layer-isolation discipline.", follow_up_question: "How would this investigation differ if the issue was intermittent rather than consistently reproducible?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you owned a complete feature or system end to end, across multiple technical layers or domains. What was the hardest part of the integration?", what_is_tested: "Real or realistic experience with genuine cross-layer ownership and integration challenges.", strong_answer_structure: "A strong answer describes the specific system, the actual integration challenge encountered (a boundary mismatch, a cross-cutting concern like cost or security that spans layers), and how it was resolved.", weak_answer_example: "A vague answer describing only one layer in depth without addressing the actual integration challenge across layers.", follow_up_question: "What would you do differently if you were designing that system's cross-layer integration from scratch again?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "Model Observability").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AI Stack Module 4 (Full-Stack Integration) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
