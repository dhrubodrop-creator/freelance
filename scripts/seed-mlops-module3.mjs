import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "0678ff8b-1b79-4f88-b498-b1d2fc065975"; // Deployment & Containerization

const sections = [
  {
    section_type: "the_field",
    title: "What making a model usable by something other than a notebook actually looks like",
    order_index: 0,
    content: "A trained model saved as a pickle file on your laptop has zero value to anyone else. The gap between \"I have a model that works\" and \"I have a model something else can actually use\" is exactly what this module closes: packaging the model in a portable, reliable format, serving it behind an API another system can call, and containerizing the whole thing so it runs consistently regardless of whose machine it's on.\n\nThis directly extends the same containerization discipline from the AI Engineering course track — multi-stage builds, no secrets baked into images, health checks, reproducible pinned dependencies — but applied specifically to an ML model serving context, which has its own particular wrinkles: model files can be large and need to be loaded once at startup (not per-request), the exact preprocessing pipeline from training needs to be applied identically at inference time (the training/serving skew problem), and the API contract needs to make sense to whatever system is actually going to call it, not just be a thin wrapper around the model's raw predict function.\n\nThe practical skill is treating \"deploy the model\" as a real engineering task with its own correctness requirements, not an afterthought tacked onto the end of a successful training run. A model that scores well in evaluation and then gets deployed carelessly — with training/serving skew, no input validation, or brittle dependency handling — throws away all the rigor invested in the earlier stages of the pipeline.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about deploying ML models",
    order_index: 1,
    content: "**1. The exact preprocessing used in training must be reused identically at inference time — this is the single highest-risk gap in ML deployment.** Any divergence between how a feature was computed during training and how it's computed at serving time produces predictions that don't reflect what the model was actually evaluated on, silently.\n\n**2. Load the model once, at startup — not on every request.** Loading a model file is relatively expensive; a service that reloads it per-request will be needlessly slow and won't scale, while a service that loads it once into memory at startup and reuses it is both faster and simpler.\n\n**3. An API's contract should be designed for its actual consumer, not just expose the model's raw interface.** A well-designed prediction endpoint takes input in a shape the calling system naturally has, returns output in a shape it can naturally use, and handles errors in a way that's actionable — not a thin, literal wrapper around whatever the model's predict function happens to expect.\n\n**4. A model's version and the serving code's version need to be tracked together, connected back to the experiment tracking record.** \"Which exact model is currently being served\" should be a question the deployment itself can answer, not something requiring a separate investigation.\n\n**5. Input validation at the API boundary is not optional — it's the thing standing between a malformed request and either a crash or, worse, a silent nonsensical prediction.** The model itself generally won't validate that its inputs make sense; the serving layer around it needs to.\n\n**6. Containerization for ML services follows the same discipline as any other containerized service, with model-artifact handling as the one genuinely new consideration.** Multi-stage builds, no secrets in the image, health checks, pinned dependencies — all still apply, plus a deliberate decision about how the (potentially large) model file itself gets into the running container.",
  },
  {
    section_type: "decision_framework",
    title: "How should I package, serve, and containerize this model?",
    order_index: 2,
    content: "IF you're preparing a trained model for deployment THEN save the model together with its exact preprocessing pipeline as one artifact (e.g., a scikit-learn Pipeline object serialized as a whole, not the model alone), and record the exact library versions used to save it. BECAUSE separating the model from its preprocessing invites the training/serving skew failure mode — bundling them as one artifact makes correct reuse the default, not something that has to be manually remembered. WATCH OUT FOR: a model serialization format can be version-sensitive — loading a model saved with one library version using a meaningfully different version can fail or behave unexpectedly; pin and record the exact version.\n\nIF you're designing the API for serving predictions THEN design the request/response shape around what the calling system actually needs, with explicit input validation, rather than a bare pass-through of the model's raw interface. BECAUSE a well-designed API contract is both safer (validated input) and genuinely more usable by whatever system consumes it, versus a fragile, literal wrapper that breaks on any unexpected input shape. WATCH OUT FOR: over-designing the API with speculative features the actual consumer doesn't need yet — match the contract to real, current requirements, and extend it deliberately later if needed.\n\nIF the model file is large, or startup time matters for how the service scales THEN load the model once at process startup, held in memory for the life of the process, rather than reloading it per request. BECAUSE per-request loading adds unnecessary latency and resource cost that compounds under real load. WATCH OUT FOR: if the model needs to be updated without downtime, a naive \"load once at startup\" design needs an explicit reload/redeploy strategy — this is a real design consideration, not automatically solved by loading once.\n\nIF you're containerizing the serving application THEN apply the same containerization discipline as any production service (multi-stage build, no baked-in secrets, health checks, pinned dependencies, non-root user), and make a deliberate choice about how the model artifact enters the image (baked into the image at build time, or fetched from external storage at container startup). BECAUSE both approaches are valid but have different trade-offs — baking in the model produces a larger, self-contained image with no runtime dependency on external storage; fetching at startup keeps images smaller and lets the model be updated without rebuilding, at the cost of a startup dependency on that external storage being available.",
  },
  {
    section_type: "workflow",
    title: "The real process for packaging, serving, and containerizing a model",
    order_index: 3,
    content: "1. Bundle the trained model with its exact preprocessing pipeline as one serialized artifact, recording the exact library versions used.\n\n2. Design the serving API's request/response contract around the actual consuming system's real needs, including explicit input validation.\n\n3. Build the serving application (e.g., FastAPI) to load the model artifact once at startup, not per-request.\n\n4. Implement the prediction endpoint to apply the bundled preprocessing identically to how it was applied in training, then run inference and return a validated, well-shaped response.\n\n5. Add meaningful error handling: what happens on malformed input, and what does the caller receive in that case (a clear error, not a cryptic stack trace or a silently wrong prediction).\n\n6. Containerize the application using a multi-stage build, with the model-artifact inclusion strategy (baked-in vs. fetched-at-startup) deliberately chosen per this module's decision framework.\n\n7. Add a real health check that verifies the model is actually loaded and able to serve a prediction, not just that the process is running.\n\n8. Test the fully containerized service standalone: send it real (and deliberately malformed) requests and confirm the responses match what training-time evaluation would predict for the same inputs, catching any training/serving skew before it reaches production.",
  },
  {
    section_type: "failure_modes",
    title: "How model deployment and containerization actually break",
    order_index: 4,
    content: "Failure 1 — Training/serving skew from reimplementing preprocessing separately for the serving layer.\nWhat: the preprocessing logic applied at inference time doesn't exactly match what was applied during training, because it was rewritten (even slightly differently) for the serving application instead of reused.\nHow it happens: the training pipeline's preprocessing often lives in notebook cells or training-specific scripts, and it's easy to \"just rewrite it quickly\" for the serving application rather than extracting and reusing the exact same logic.\nWhy beginners miss it: the serving application runs successfully and returns predictions — there's no error, just silently different (and generally worse) predictions than what the model was actually evaluated on.\nDetect: send the exact same raw input through both the original training-time preprocessing and the serving application's preprocessing, and confirm the resulting feature vectors are identical, not just similar.\nPrevent: bundle the model and its preprocessing as one serialized artifact from the start, so there's no separate reimplementation for the serving layer to diverge from.\nInterview question: \"Your model performs well in evaluation but noticeably worse once deployed, with no data drift involved. What's your first hypothesis?\"\nReal-world consequence: a deployed model that quietly underperforms its evaluated capability from the very first request, often undetected for a long time since nothing crashes or looks obviously wrong.\n\nFailure 2 — No input validation, so malformed requests cause crashes or silently wrong predictions.\nWhat: the serving API accepts a request that doesn't match the expected shape or value ranges, and either crashes ungracefully or, worse, produces a prediction from nonsensical input without any warning.\nHow it happens: it's straightforward to build a serving endpoint that simply passes whatever it receives into the model, without an explicit validation step in between.\nWhy beginners miss it: normal, well-formed requests work fine throughout development and testing — the gap only shows up when a real caller sends something unexpected.\nDetect: deliberately send malformed, missing-field, or out-of-range requests to the serving endpoint and check what happens — a crash with a cryptic error, or worse, a confident-looking prediction from garbage input.\nPrevent: add explicit input validation at the API boundary, returning a clear, actionable error for invalid input rather than letting it reach the model.\nInterview question: \"What should your prediction API do if it receives a request missing a required field?\"\nReal-world consequence: either poor reliability (frequent crashes on real-world request variety) or, more dangerously, confidently wrong predictions served from invalid input with no indication anything was wrong.\n\nFailure 3 — Reloading the model on every request instead of once at startup.\nWhat: the serving application loads the model file fresh for every incoming request, adding significant unnecessary latency and resource cost, and failing to scale under real load.\nHow it happens: this is often how the simplest possible first version of a serving endpoint gets written — load the model, then predict, all within the request handler.\nWhy beginners miss it: it works correctly for a single test request, and the performance cost isn't obvious until the service faces real concurrent traffic.\nDetect: check whether the model-loading code executes once at application startup or inside the request-handling function itself.\nPrevent: restructure the application to load the model once during startup, holding it in memory for the life of the process, with the request handler only performing inference against the already-loaded model.\nInterview question: \"Why would loading a model on every request be a problem, even if each individual request still returns the correct prediction?\"\nReal-world consequence: unnecessarily slow response times and poor scalability under real concurrent load, discovered only once the service faces meaningful traffic rather than isolated test requests.\n\nFailure 4 — Baking secrets or excessive size into the container image, or no deliberate strategy for the model artifact.\nWhat: the same containerization mistakes from the AI Engineering course track (secrets baked into image layers, an unnecessarily large image from skipping multi-stage builds) recur specifically in an ML-serving context, sometimes compounded by an oversized model artifact with no deliberate inclusion strategy.\nHow it happens: the same pressures apply here as in any containerization work — convenience during initial development, without revisiting the pattern once it's \"working.\"\nWhy beginners miss it: the same reasons as general containerization mistakes — the container runs correctly, so the cost (security exposure, deploy latency) isn't immediately visible.\nDetect: apply the same containerization checklist from the AI Engineering course track, plus specifically check how the model artifact enters the image and whether that was a deliberate choice.\nPrevent: apply the established multi-stage build, secrets-handling, and image-size discipline, and make the model-artifact inclusion strategy (baked-in vs. fetched-at-startup) an explicit decision per this module's framework, not a default nobody chose on purpose.\nInterview question: \"How would you decide whether to bake a 500MB model file into your Docker image, or fetch it from external storage at container startup?\"\nReal-world consequence: slow deploys, unnecessary security exposure, or (if using the fetch-at-startup approach without considering it deliberately) a service that fails to start if the external storage it depends on is unavailable, with no fallback considered.",
  },
  {
    section_type: "debugging_playbook",
    title: "The deployed model isn't behaving correctly — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"The deployed model's predictions don't match what I'd expect from its evaluation metrics.\"\nDiagnostic: send an identical input through both the original training-time preprocessing and the serving application's actual code path, and compare the resulting feature vectors directly, not just the final predictions.\nFix: if they diverge, this is training/serving skew — consolidate to one shared, bundled preprocessing artifact used identically by both training and serving.\n\nSymptom: \"The serving API crashes or returns a strange error on certain real-world requests.\"\nDiagnostic: check what specifically was different about the failing request compared to the well-formed ones that work — a missing field, an unexpected type, an out-of-range value.\nFix: add explicit validation for the specific gap found, and more generally review whether the endpoint has real input validation at all, or only handles the happy-path shape.\n\nSymptom: \"The serving application is slower than expected, especially under concurrent load.\"\nDiagnostic: check whether the model is being loaded once at startup or reloaded on every request.\nFix: restructure to load the model once at startup if it isn't already, and re-test latency under concurrent load to confirm the fix actually addresses the bottleneck.\n\nSymptom: \"The container fails to start in a new environment even though it worked before.\"\nDiagnostic: check whether the model artifact is fetched from external storage at startup, and whether that storage is actually reachable and correctly configured in the new environment.\nFix: verify the external dependency's availability and credentials in the new environment, or consider whether baking the model artifact into the image would be a more robust choice for this specific deployment context.",
  },
  {
    section_type: "checklist",
    title: "Model Deployment & Containerization Checklist",
    order_index: 6,
    content: "- [ ] The model and its exact preprocessing pipeline are bundled as one artifact, not separately reimplemented for serving\n- [ ] Exact library versions used to save the model artifact are recorded, to prevent version-mismatch loading issues\n- [ ] The serving API's request/response contract is designed around the actual consumer's needs, with explicit input validation\n- [ ] The model is loaded once at application startup, not reloaded per request\n- [ ] Malformed or invalid requests receive a clear, actionable error, not a crash or a silent wrong prediction\n- [ ] The container follows established multi-stage build, secrets-handling, and image-size discipline\n- [ ] The model-artifact inclusion strategy (baked-in vs. fetched-at-startup) was a deliberate choice, with trade-offs considered\n- [ ] A real health check verifies the model is actually loaded and able to serve a prediction, not just that the process is running\n- [ ] The fully containerized service has been tested standalone against both well-formed and deliberately malformed input",
  },
  {
    section_type: "template",
    title: "Model Serving Readiness Review",
    order_index: 7,
    content: "Model / service name: ___\n\nModel artifact: bundled with preprocessing? ___ — library versions recorded: ___\n\nAPI contract: request shape ___, response shape ___, validation rules ___\n\nModel loading strategy: once at startup / per-request — confirmed: ___\n\nError handling: what happens on malformed input: ___\n\nContainer: multi-stage build? ___ secrets handling confirmed? ___ non-root user? ___\nModel-artifact strategy: baked-in / fetched-at-startup — reasoning: ___\n\nHealth check: what specifically does it verify: ___\n\nTraining/serving skew check: confirmed identical feature vectors for a test input? ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [FastAPI: Deployment documentation](https://fastapi.tiangolo.com/deployment/docker/) (referenced in the Data Science course track's Module 4) — revisit specifically for model-serving application structure.\n- [scikit-learn: Model persistence](https://scikit-learn.org/stable/model_persistence.html) — the official reference for correctly bundling and saving a model together with its preprocessing pipeline.\n\nRecommended\n- [Docker: Multi-stage builds](https://docs.docker.com/build/building/multi-stage/) and [Docker: Best practices](https://docs.docker.com/build/building/best-practices/) (both referenced in the AI Engineering course track) — the same containerization discipline applies directly here.\n\nReference\n- [BentoML documentation](https://docs.bentoml.com/) or [MLflow: Model serving](https://mlflow.org/docs/latest/deployment/index.html) — purpose-built ML model serving frameworks that handle much of this module's packaging and API-contract discipline out of the box, worth evaluating once a hand-built FastAPI approach outgrows what's practical to maintain manually.\n\nAdvanced\n- [ONNX documentation](https://onnx.ai/onnx/intro/) — for teams needing model portability across different serving runtimes/languages beyond a single Python environment, a more advanced packaging approach than this module's baseline.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Bundle and serve a model with a FastAPI endpoint",
    problem_statement: "Take a model you trained earlier in this course track (or train a simple one now on any dataset), bundle it with its preprocessing pipeline as one serialized artifact using scikit-learn's Pipeline, and build a minimal FastAPI application with a single /predict endpoint that loads the artifact once at startup, validates incoming requests, and returns a prediction.\n\nTest it with both a well-formed request and at least one deliberately malformed request, confirming the API handles both appropriately.",
    starter_context: "Keep the API contract simple for this exercise — one clear input shape, one clear output shape.",
    hints: [
      "Use Pydantic models (FastAPI's native validation approach) to define your expected request shape explicitly, rather than accepting arbitrary unvalidated JSON.",
      "Confirm the model loading happens at application startup (module level, or a startup event), not inside the endpoint function itself.",
    ],
    solution_notes: "A strong submission has a genuinely bundled model+preprocessing artifact, a FastAPI endpoint with explicit request validation via Pydantic, model loading confirmed to happen once at startup (not per-request, verifiable by checking where the loading code executes), and a malformed-request test that returns a clear validation error rather than crashing or silently producing a nonsensical prediction.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Detect and fix a deliberately introduced training/serving skew bug",
    problem_statement: "Take your bundled model-serving setup from the guided exercise (or a similar one). Deliberately introduce a training/serving skew bug: modify the serving application to compute one feature slightly differently than how it was computed during training (e.g., a different rounding, a different missing-value default).\n\nThen detect this bug the way a real investigation would: by comparing predictions for identical inputs between the correct training-time pipeline and your now-buggy serving code, without looking at the bug you just introduced. Fix it, and confirm the predictions now match.",
    starter_context: "The deliberate 'forget what bug you introduced' framing matters — try to detect it purely through the comparison method, the way you'd have to in a real, undiagnosed skew situation.",
    hints: [
      "Pick a bug subtle enough that it wouldn't cause an obvious crash — a rounding difference or a slightly different default value is realistic and appropriately subtle.",
      "Compare the actual feature vectors produced by each path, not just the final predictions — this localizes exactly where the divergence originates, not just that one exists.",
    ],
    solution_notes: "A strong submission successfully detects the introduced skew through direct feature-vector comparison (not by simply recalling what bug was planted), correctly localizes which specific feature diverges, and fixes it by consolidating back to one shared preprocessing implementation — directly demonstrating the detection method this module's debugging playbook recommends for real training/serving skew incidents.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Design a deployment strategy for a large, frequently-updated model",
    problem_statement: "You're deploying a model that's 2GB in size, gets retrained and updated weekly, and needs to serve real-time predictions with under 200ms latency. Design (in writing) your model-artifact strategy (baked-in vs. fetched-at-startup), your container update process, and your health-check design — justifying each decision against these specific constraints.",
    starter_context: "This is a design exercise — the constraints given (size, update frequency, latency requirement) should directly drive your specific recommendations, not generic best practices.",
    hints: [
      "A 2GB baked-in image rebuilt weekly has real trade-offs (slower CI builds, larger registry storage) compared to fetching from external storage at startup — reason through both explicitly for this specific case.",
      "Consider what your health check needs to verify specifically, given the real-time latency requirement — a health check should probably confirm the model can actually serve a prediction within an acceptable time, not just that it loaded.",
    ],
    solution_notes: "A strong submission reasons explicitly about the size/update-frequency trade-off (likely leaning toward fetch-at-startup given weekly updates, to avoid slow rebuild-and-redeploy cycles for a 2GB artifact, while acknowledging the startup-dependency risk this introduces), and designs a health check that verifies actual prediction capability and rough latency, not just successful model loading — directly connecting the design to the stated real-time latency requirement rather than proposing a generic, unconditioned answer.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why should a model and its preprocessing pipeline be bundled together as one artifact rather than the preprocessing being reimplemented separately for the serving application?", what_is_tested: "Understanding of training/serving skew as a real, common, and avoidable failure mode.", strong_answer_structure: "Explain that reimplementing preprocessing separately creates an opportunity for subtle divergence between training-time and serving-time feature computation, which silently produces predictions that don't reflect the model's actual evaluated behavior — bundling as one artifact removes the opportunity for that divergence entirely.", weak_answer_example: "\"It's more convenient\" without explaining the actual correctness risk being avoided.", follow_up_question: "How would you detect training/serving skew if you suspected it was happening in an existing deployed system?" },
  { category: "applied", order_index: 1, question: "Why is loading a model once at application startup better than loading it fresh on every request?", what_is_tested: "Understanding of the performance and scalability implications, not just a memorized best practice.", strong_answer_structure: "Explain that model loading has real cost (I/O, deserialization time), and repeating that cost on every single request adds unnecessary latency and won't scale under concurrent load, while loading once and holding the model in memory for the life of the process avoids this repeated cost entirely.", weak_answer_example: "\"It's just standard practice\" without explaining the actual performance mechanism.", follow_up_question: "How would you handle updating the model without downtime, given it's loaded once at startup?" },
  { category: "scenario", order_index: 2, question: "A model performs well in evaluation but noticeably worse once deployed, and you've ruled out data drift. Walk me through your investigation.", what_is_tested: "Diagnostic instinct toward training/serving skew as a leading hypothesis for this specific symptom pattern.", strong_answer_structure: "Compare the feature vectors produced by the training-time preprocessing versus the serving application's actual preprocessing code, for identical raw input, to check for any divergence — rather than assuming the model itself has degraded.", weak_answer_example: "Assuming the model needs retraining without first checking whether the deployed serving code actually reproduces training-time behavior.", follow_up_question: "If you find the feature vectors do diverge, what's your fix, and how do you prevent this from happening again?" },
  { category: "debugging", order_index: 3, question: "Your model-serving API occasionally crashes with a cryptic error on certain real-world requests. What's your first move?", what_is_tested: "Instinct toward input validation as the likely gap, rather than assuming a deeper model or infrastructure problem.", strong_answer_structure: "Investigate what's specifically different about the failing requests compared to well-formed ones, and check whether the API has explicit input validation at all, or is passing raw, unvalidated input directly into the model.", weak_answer_example: "Assuming it's an infrastructure or model reliability issue without first checking the actual malformed request content.", follow_up_question: "What would a good error response look like for a request missing a required field, versus letting it crash the service?" },
  { category: "system_design", order_index: 4, question: "Design a deployment approach for a model that's frequently retrained and needs to be updated without service downtime.", what_is_tested: "Ability to synthesize this module's packaging, loading, and containerization principles into a coherent update strategy.", strong_answer_structure: "Propose a strategy where new model versions can be deployed via a rolling update (new containers with the updated artifact replace old ones gradually, per the Kubernetes rollout patterns from the AI Engineering course track) or a fetch-at-startup approach where new containers pick up the latest model version on launch — either way, ensuring the health check confirms the new model is actually loaded and functional before it receives real traffic.", weak_answer_example: "Describing manually swapping the model file on a running server, which introduces downtime and risk of a partially-updated inconsistent state.", follow_up_question: "How would you roll back quickly if a newly deployed model version turns out to be broken?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "AI Service Deployment").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("MLOps Module 3 (Deployment & Containerization) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
