import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "2f9153ae-979e-4d9f-8521-521ae51628c1"; // Shipping a Real Product

const sections = [
  {
    section_type: "the_field",
    title: "What taking an idea to a deployed build actually looks like",
    order_index: 0,
    content: "Everything in Modules 1-3 — good context, delegated task loops, reusable workflows — comes together (or falls apart) the moment you try to take a real product idea from nothing to something deployed and working. This is where the gap between \"I can prompt an AI to write code\" and \"I can use this tool to actually ship something\" becomes obvious.\n\nShipping a real product with Claude Code as the primary tool means: planning the architecture before diving into code (even a small project benefits from deciding how pieces fit together up front), working inside an existing large codebase without breaking things you don't fully understand yet, building in testing and validation as you go rather than at the very end, and actually handling deployment — the unglamorous last mile that turns working code on your machine into something a real user can access.\n\nThe capstone in this module is deliberately open-ended in the way real work is open-ended: you're given a guided session to take an idea to a deployed build, and success is measured by whether it actually works when someone else tries it, not by how impressive the prompting looked along the way.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about shipping with an AI-native workflow",
    order_index: 1,
    content: "**1. Planning is cheap; unwinding a wrong architecture is expensive.** A short planning pass before writing code — even informally, even just talking through the approach with the agent before executing — catches structural problems while they're still just ideas, not hundreds of lines of code.\n\n**2. Working inside a large existing codebase requires humility about what you don't know yet.** The instinct to move fast needs to be balanced against reading and understanding existing patterns before changing them — an AI tool can help you understand unfamiliar code quickly, but that's a step, not something to skip.\n\n**3. Testing and validation loops are part of building, not a separate phase that happens after.** Code that's \"done\" but never actually run, tested, or validated against real usage isn't done — it's a draft.\n\n**4. Deployment is a real skill, not an afterthought.** Getting code from your machine to somewhere a real user can reach it involves genuine decisions (environment configuration, secrets management, what happens when it breaks in production) that deserve the same care as the application code itself.\n\n**5. \"Guided\" doesn't mean \"unreviewed.\"** Even in a single focused session taking an idea to deployment, the review discipline from Modules 1-2 doesn't relax — if anything, a capstone project benefits most from careful review, since it's the artifact you'll actually be judged on.\n\n**6. A shipped product that works end-to-end beats a more ambitious one that's 80% finished.** Scope the capstone to something genuinely completable, deployed, and working — an impressive but non-functional prototype demonstrates less real capability than a modest but fully working product.",
  },
  {
    section_type: "decision_framework",
    title: "How should I approach a real shipping session?",
    order_index: 2,
    content: "IF you're starting a genuinely new project THEN spend real time on architecture planning before writing implementation code, even if it feels like it's slowing you down. BECAUSE the cost of a wrong early structural decision compounds across everything built on top of it, while the cost of planning is small and one-time. WATCH OUT FOR: planning so extensively that you never actually start building — the plan should be enough to start confidently, not exhaustive.\n\nIF you're working inside an existing, unfamiliar codebase THEN spend time understanding relevant existing patterns before making changes, and let the agent help you read and explain the code, not just write new code on top of it blind. BECAUSE changes that don't respect existing patterns create inconsistency and often subtle bugs where the new code's assumptions don't match the old code's reality. WATCH OUT FOR: assuming a pattern you see in one part of the codebase applies everywhere — large codebases are often inconsistent, and it's worth checking more than one example.\n\nIF you're deciding how much testing to build in THEN match the testing effort to how much you're relying on the code working correctly without you personally re-checking it — a throwaway prototype needs less; something you're deploying for real users needs more. BECAUSE testing is an investment that should scale with how much the cost of being wrong actually matters. WATCH OUT FOR: skipping testing entirely on a capstone project specifically because it's \"just an exercise\" — the whole point is demonstrating a real, professional workflow.\n\nIF deployment is part of the scope THEN treat it as a real step requiring its own attention — environment configuration, secrets, and a basic plan for what happens if something goes wrong after deployment — not something to rush through at the very end when you're already out of time. BECAUSE a project that works locally but was never actually deployed correctly hasn't actually demonstrated the full \"idea to deployed build\" capability this module is testing.",
  },
  {
    section_type: "workflow",
    title: "The real idea-to-deployed-build workflow",
    order_index: 3,
    content: "1. Scope the project to something genuinely completable in the time available — a smaller, fully-working product beats a larger, unfinished one.\n\n2. Plan the architecture before writing implementation code: what are the main pieces, how do they connect, what does 'done' actually look like for this specific project.\n\n3. If working inside an existing codebase, spend real time understanding relevant existing patterns before adding to them.\n\n4. Build in small, verifiable increments — using the delegation and checkpoint principles from Module 2 — rather than one giant unreviewed push.\n\n5. Test and validate as you go, not only at the end. Each meaningful increment should be checked before building the next thing on top of it.\n\n6. Review everything the same way you would any other AI-assisted work — shipping quickly doesn't lower the review bar, it raises the importance of not skipping it.\n\n7. Handle deployment as a real, deliberate step: environment configuration, secrets handled correctly, and a basic understanding of what you'd do if something goes wrong after it's live.\n\n8. Confirm the deployed product actually works by using it the way a real user would — not just checking that the deployment process completed without errors.",
  },
  {
    section_type: "failure_modes",
    title: "How shipping sessions actually break",
    order_index: 4,
    content: "Failure 1 — Skipping architecture planning and discovering the structural problem after a lot of code exists.\nWhat: implementation starts immediately without deciding how the main pieces should fit together, and a fundamental structural mismatch surfaces only after significant code has been written on top of it.\nHow it happens: planning feels like it's delaying real progress, especially under time pressure, so it gets skipped or rushed.\nWhy beginners miss it: early progress feels fast and productive right up until the structural problem forces a costly rework.\nDetect: before writing implementation code, check whether you can clearly describe how the main pieces of the system will interact — if not, that's a sign planning isn't actually done yet.\nPrevent: treat a short, explicit planning pass as a mandatory first step for any project beyond the most trivial, even under time pressure.\nInterview question: \"How much planning is 'enough' before you start writing code, and how do you know when you've done enough?\"\nReal-world consequence: significant rework or an unstable final architecture, discovered late enough that fixing it costs far more than planning would have.\n\nFailure 2 — Making changes inside an existing codebase without understanding its actual patterns.\nWhat: new code is added that technically works but doesn't match the existing codebase's conventions, creating inconsistency or interacting badly with assumptions the new code doesn't respect.\nHow it happens: it's faster in the moment to write something new than to first understand what's already there.\nWhy beginners miss it: the new code often works in isolation, so the mismatch with the rest of the system isn't immediately visible.\nDetect: check whether the new code's approach matches how similar things are already done elsewhere in the same codebase — and check more than one existing example, since large codebases can be inconsistent.\nPrevent: spend real time reading and understanding relevant existing patterns before adding new code, using the agent to help explain unfamiliar sections rather than skipping straight to writing.\nInterview question: \"How do you approach adding a new feature to a large codebase you didn't build and don't fully understand yet?\"\nReal-world consequence: a codebase that becomes progressively more inconsistent and harder to maintain as new contributions don't respect existing structure.\n\nFailure 3 — Treating testing as a final step instead of part of building.\nWhat: a project is built entirely, end to end, before any testing or validation happens — meaning problems discovered at that point can be scattered across the whole system rather than isolated to one recent change.\nHow it happens: testing at the end feels like it fits better around a tight deadline than pausing to validate along the way.\nWhy beginners miss it: skipping testing along the way doesn't cause an immediate visible problem — the cost only shows up when final testing reveals accumulated issues all at once.\nDetect: ask, at any point mid-project, when the most recent piece of work was actually verified to work correctly — if the answer is \"not yet, I'll check everything at the end,\" that's the pattern.\nPrevent: build validation into each meaningful increment, matching the checkpoint discipline from Module 2, rather than deferring all verification to the end.\nInterview question: \"Why is testing throughout a project generally better than testing only at the end, even under time pressure?\"\nReal-world consequence: a large, tangled set of issues discovered right before a deadline, when there's the least time available to fix them properly.\n\nFailure 4 — Rushing or skipping deployment because the \"real\" work felt finished once the code worked locally.\nWhat: deployment is treated as a formality tacked on at the end, without real attention to configuration, secrets, or failure handling — resulting in a deployed product that doesn't actually work the way the local version did.\nHow it happens: the application code feels like the substantive part of the work, and deployment feels like a mechanical last step.\nWhy beginners miss it: \"it works on my machine\" creates false confidence that deployment will be a formality rather than its own real set of decisions.\nDetect: check whether deployment was actually planned and configured deliberately, or whether it was done hastily at the very end with minimal attention.\nPrevent: treat deployment as a first-class part of the project scope from the start, not an afterthought squeezed into remaining time.\nInterview question: \"What's the difference between code that 'works' and a product that's actually shipped?\"\nReal-world consequence: a project that looks complete in a demo but doesn't actually function correctly for a real user hitting the deployed version, undermining the entire point of the capstone.",
  },
  {
    section_type: "debugging_playbook",
    title: "The build isn't coming together — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"I keep having to rework the same part of the architecture as new requirements come up.\"\nDiagnostic: this usually means the initial planning pass didn't account for a requirement that was actually foreseeable, or the plan was skipped entirely.\nFix: pause active building, do a proper (even if short) architecture planning pass now, accounting for what you've learned so far, before continuing.\n\nSymptom: \"My new feature works in isolation but breaks something else in the existing codebase.\"\nDiagnostic: this is a sign the new code doesn't respect an existing assumption or pattern it should have accounted for.\nFix: use the agent to trace what the existing code actually assumes about the area you changed, before writing a fix — don't just patch the symptom.\n\nSymptom: \"Everything worked locally but broke immediately after deployment.\"\nDiagnostic: check environment configuration and secrets first — this is the most common gap between 'works locally' and 'works deployed,' since local environments often have configuration that isn't automatically replicated in production.\nFix: explicitly verify every piece of configuration the deployed environment needs, rather than assuming it matches local defaults.\n\nSymptom: \"I'm running out of time and the project still isn't fully working end to end.\"\nDiagnostic: this is often a scoping problem, not an execution problem — the project may have been scoped larger than what was actually completable.\nFix: cut scope decisively to the smallest version that still genuinely works end to end and is deployed, rather than continuing to push on an overscoped version that risks finishing with nothing fully working.",
  },
  {
    section_type: "checklist",
    title: "Ship-a-Real-Product Checklist",
    order_index: 6,
    content: "- [ ] The project is scoped to something genuinely completable, not an ambitious version that risks finishing unfinished\n- [ ] Architecture was planned, even briefly, before implementation code was written\n- [ ] If working in an existing codebase, relevant existing patterns were understood before being built on top of\n- [ ] Testing and validation happened throughout the build, not only at the very end\n- [ ] Every meaningful piece of AI-generated code was actually reviewed, not just checked for whether it ran\n- [ ] Deployment was treated as a real, deliberate step — environment configuration and secrets handled explicitly\n- [ ] The deployed product was actually used the way a real user would, not just confirmed to have deployed without errors\n- [ ] There's a basic plan for what happens if something breaks after deployment",
  },
  {
    section_type: "template",
    title: "Ship Session Plan",
    order_index: 7,
    content: "Product idea (one sentence): ___\n\nMinimum scope that still counts as genuinely working end to end: ___\n\nMain architectural pieces and how they connect: ___\n\nExisting codebase patterns to respect (if applicable): ___\n\nValidation plan per increment: ___ (how you'll check each meaningful piece works before building the next)\n\nDeployment target and configuration needed: ___\n\nWhat you'll do if something breaks after deployment: ___\n\nHow you'll confirm it actually works once deployed: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Claude Code: Common workflows](https://docs.claude.com/en/docs/claude-code/common-workflows) — includes guidance directly relevant to multi-file architecture and working inside large existing codebases.\n- [Claude Code: Deployment options](https://docs.claude.com/en/docs/claude-code/third-party-integrations) — practical reference for taking a project from local development to deployed.\n\nRecommended\n- [Claude Code: Troubleshooting](https://docs.claude.com/en/docs/claude-code/troubleshooting) — useful when a build session runs into an unexpected blocker under time pressure.\n\nReference\n- [Claude Code: Settings](https://docs.claude.com/en/docs/claude-code/settings) — for configuring environment and secrets handling correctly as part of a real deployment.\n\nAdvanced\n- [Claude Code: GitHub Actions integration](https://docs.claude.com/en/docs/claude-code/github-actions) — for teams wanting to extend a single guided shipping session into a repeatable, automated deployment pipeline.",
  },
];

const exercises = [
  {
    level: "capstone",
    order_index: 0,
    title: "Capstone: idea to deployed build in one guided session",
    problem_statement: "Pick a small, genuinely useful product idea you can realistically complete and deploy in a single focused session — something with a clear, narrow scope (examples: a personal habit tracker with a simple database, a small internal tool, a focused single-purpose utility with a UI). You choose the idea; it should be small enough to actually finish and deploy, not just prototype.\n\nUsing Claude Code as your primary tool, and applying this course's full workflow: plan the architecture briefly before building, work in small reviewed increments, test and validate as you go (not only at the end), and actually deploy it somewhere a real URL can reach.\n\nDeliverable: a working, deployed product plus a short written account covering: your architecture plan, at least one moment where you deliberately scoped down or cut something to stay completable, how you validated the build along the way (not just at the end), and how you configured and verified the deployment.",
    starter_context: "The bar here is 'small and genuinely working end to end' over 'ambitious and partially working.' A working single-page tool that's actually deployed and usable demonstrates more real capability than a half-finished multi-feature app.",
    hints: [
      "Write your architecture plan down, even briefly, before starting implementation — this is checkable evidence for your write-up, not just a mental step.",
      "Deliberately test the deployed version by using it the way a stranger would, not just confirming the deploy command succeeded.",
      "If you find yourself significantly behind where you expected to be partway through, that's the moment to cut scope — don't wait until you're completely out of time.",
    ],
    solution_notes: "A strong capstone submission is small in scope but genuinely complete: it deploys successfully, functions correctly when used the way a real user would, and the written account demonstrates real application of this module's principles — a concrete architecture decision made up front, a specific scope cut made deliberately (not just something left unfinished by accident), validation evidence from during the build rather than only at the end, and a clear account of the actual deployment configuration. Submissions that are more ambitious in scope but don't actually work end to end when deployed have not met the bar this capstone is testing, regardless of how sophisticated the unfinished parts look.",
  },
];

const interviewQuestions = [
  { category: "project_defence", order_index: 0, question: "Walk me through your architecture planning for this capstone. What did you decide up front, and why?", what_is_tested: "Whether real planning happened, versus implementation starting immediately with no deliberate structure.", strong_answer_structure: "Describe the actual pieces planned (data model, main components, how they connect) and the reasoning behind at least one specific structural decision, not just a restatement of what was eventually built.", weak_answer_example: "\"I just started building and it worked out\" — no evidence of a deliberate planning step.", follow_up_question: "What would you have planned differently if you'd had more time up front?" },
  { category: "project_defence", order_index: 1, question: "You mentioned cutting scope at some point. Walk me through that decision.", what_is_tested: "Whether scope management was a deliberate, reasoned decision or an accident of running out of time.", strong_answer_structure: "Describe the specific point where scope was cut, what triggered the decision, and why the resulting smaller scope was chosen over continuing to push on the original plan.", weak_answer_example: "\"I ran out of time so some things didn't get finished\" — describes an outcome, not a deliberate decision.", follow_up_question: "How would you decide, next time, when to cut scope earlier rather than later?" },
  { category: "system_design", order_index: 2, question: "How did you validate your build along the way, rather than only at the end?", what_is_tested: "Real evidence of incremental testing discipline, not just a claim that it happened.", strong_answer_structure: "Describe specific checkpoints where something was actually tested or verified before moving on to the next piece, ideally with a concrete example of a problem caught early because of this.", weak_answer_example: "\"I tested everything at the end and it was fine\" — contradicts the module's core workflow and suggests the checkpoint discipline wasn't actually applied.", follow_up_question: "What's an example of a bug that incremental testing caught early, that end-of-project testing alone might have missed or made more expensive to fix?" },
  { category: "debugging", order_index: 3, question: "Did anything break during deployment that didn't happen locally? How did you diagnose and fix it?", what_is_tested: "Real, honest engagement with the deployment step, rather than a suspiciously smooth account.", strong_answer_structure: "A strong, credible answer describes a real (even if minor) deployment issue — a configuration mismatch, a missing environment variable, a difference between local and production behavior — and the actual diagnostic steps taken to resolve it.", weak_answer_example: "\"Deployment was completely smooth with zero issues\" — possible, but often signals that deployment wasn't tested rigorously enough to surface real issues, or wasn't actually completed.", follow_up_question: "If nothing broke, how did you specifically verify the deployed version actually works, beyond the deploy command succeeding?" },
  { category: "applied", order_index: 4, question: "If you were handing this project off to another engineer right now, what would they need to know that isn't obvious from the code alone?", what_is_tested: "Whether the candidate can articulate tacit knowledge from the build process, connecting back to this course's Module 1 context-setting principle.", strong_answer_structure: "Name specific non-obvious context: a deliberate architectural trade-off, a known limitation, deployment configuration that isn't self-evident from the code.", weak_answer_example: "\"The code is pretty self-explanatory\" — underestimates how much context a real handoff actually requires.", follow_up_question: "How would you capture that context so the next person doesn't have to ask you directly?" },
  { category: "behavioural", order_index: 5, question: "What was the hardest moment in building and shipping this capstone, and how did you get through it?", what_is_tested: "Honest reflection on a real difficulty, and the actual problem-solving process used.", strong_answer_structure: "A strong answer names a specific, concrete obstacle (a stuck iteration loop, an architecture decision that needed revisiting, a deployment issue) and the actual steps taken to resolve it, connecting back to principles from earlier modules where relevant.", weak_answer_example: "A vague answer claiming nothing was particularly difficult, which is uncommon for a genuinely end-to-end capstone and suggests limited depth of engagement.", follow_up_question: "What would you do differently if you started this same project again from scratch?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill1 } = await supabase.from("skills").select("id").eq("name", "AI Pair Programming (Claude Code)").single();
const { data: skill2 } = await supabase.from("skills").select("id").eq("name", "Codebase Context Engineering").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert([
  { module_id: MODULE_ID, skill_id: skill1.id },
  { module_id: MODULE_ID, skill_id: skill2.id },
]);
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("Claude Code Module 4 (Shipping a Real Product) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 2 skill mappings.");
