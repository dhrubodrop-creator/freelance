import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "6cb68990-ac07-455b-bb6b-c23a6f8247e5"; // Model & Prompting Layer

const sections = [
  {
    section_type: "the_field",
    title: "What picking the right model for the job actually looks like",
    order_index: 0,
    content: "Most engineers default to whichever model provider they first learned, and use it for everything, regardless of whether it's actually the best fit for a given task. This is understandable — switching costs feel real, and the default choice is rarely catastrophically wrong — but it leaves real value on the table: a smaller, faster, cheaper model is often genuinely sufficient for a simple classification task, while a more capable (and more expensive) model is warranted for something requiring nuanced reasoning, and the API landscape across providers has real, meaningful differences in cost, latency, and capability that a provider-agnostic design lets you actually take advantage of.\n\nThis course is explicitly about the full stack — the model/prompting layer, orchestration/data layer, serving/infrastructure layer, and how they connect — not about going deeper on any single layer than the dedicated courses in this catalog already do (Data Science, LangChain/LangGraph, MLOps, and AI Engineering each cover their respective layers in much greater depth). This module's specific job is the model and prompting layer: understanding the real trade-offs across providers, and building a provider-agnostic abstraction so the rest of your stack doesn't get needlessly coupled to one specific vendor's API.\n\nThe practical skill is thinking about model selection as an actual engineering decision with real trade-offs to weigh — cost per token, latency, capability for the specific task — rather than a default that's never revisited, and building the prompting layer so swapping models (to optimize cost, to compare quality, or in response to a provider outage) is a real, low-cost option rather than a significant rework.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about the model and prompting layer",
    order_index: 1,
    content: "**1. Model choice is a real engineering trade-off, not a default to leave unexamined.** Cost per token, latency, and actual capability for the specific task at hand are all real, measurable dimensions worth deliberately weighing, rather than reflexively reaching for whichever provider you first learned.\n\n**2. Not every task needs the most capable available model.** A simple classification or extraction task often performs just as well on a smaller, cheaper, faster model as on the most capable frontier model — using the frontier model everywhere is a real, avoidable cost.\n\n**3. Provider-agnostic design is an architectural investment that pays back at multiple points.** It lets you optimize cost by routing different tasks to different providers/models, compare quality directly, and respond to a provider outage or price change without a significant rework — this is the same principle established in the LLMOps course track's multi-provider guidance, now built into the architecture from the start.\n\n**4. Structured output and prompting patterns are largely portable concepts, but their exact implementation differs meaningfully by provider.** The underlying discipline (specific instructions, structured schemas, few-shot examples where helpful) transfers, but the exact API mechanism for enforcing structured output differs across providers and needs explicit handling in a provider-agnostic layer.\n\n**5. Cost and latency are not the same optimization target, and sometimes trade off against each other.** A cheaper model might be slower; a faster model might cost more per token — the right choice depends on which dimension actually matters more for this specific application's real requirements.\n\n**6. A provider-agnostic layer's value is proven by actually testing a provider swap, not by its design looking clean.** The real test of whether an abstraction achieves genuine swappability is attempting an actual swap and measuring how much has to change.",
  },
  {
    section_type: "decision_framework",
    title: "How should I choose a model and design the prompting layer?",
    order_index: 2,
    content: "IF a task is well-defined, narrow in scope (classification, extraction, simple formatting) THEN evaluate whether a smaller, faster, cheaper model handles it adequately before defaulting to the most capable available model. BECAUSE the cost and latency savings across real production volume can be substantial, and a smaller model is often genuinely sufficient for well-scoped tasks. WATCH OUT FOR: \"adequately\" needs to be verified with real evaluation (per the LLMOps course track's discipline), not assumed — test the smaller model against a genuine golden dataset before committing to it in production.\n\nIF a task requires nuanced reasoning, synthesis across multiple pieces of information, or handling genuinely ambiguous input THEN a more capable model is likely warranted, and the cost trade-off is justified by the task's actual complexity. BECAUSE forcing a genuinely hard reasoning task onto an underpowered model produces worse results that cost more in downstream correction than the model cost saved. WATCH OUT FOR: don't assume a task is \"nuanced\" without actually testing whether a cheaper model handles it — intuition about task difficulty and actual model performance don't always align.\n\nIF you anticipate needing to compare providers, respond to a provider outage, or optimize cost by routing different tasks to different models THEN build a provider-agnostic prompting layer from the start, isolating model-specific configuration from your application's core logic. BECAUSE retrofitting this abstraction after the application is already tightly coupled to one provider's specific API is a much larger, riskier rework than building it in from the beginning. WATCH OUT FOR: a provider-agnostic layer that only abstracts the trivial parts (the basic API call) while leaving provider-specific structured-output handling or prompt formatting tightly coupled provides much less real swappability than it appears to.\n\nIF cost and latency genuinely trade off against each other for your specific use case THEN make an explicit decision about which matters more, grounded in the actual user experience or business requirement, rather than defaulting to whichever the current provider happens to optimize for. BECAUSE this is a real product decision, not a purely technical one — a customer-facing chat feature likely weighs latency more heavily; a batch analytics job likely weighs cost more heavily.",
  },
  {
    section_type: "workflow",
    title: "The real process for building a provider-agnostic model layer",
    order_index: 3,
    content: "1. Catalog the actual providers and models you're considering, with real, current data on cost per token, typical latency, and known capability strengths/weaknesses for your specific task types.\n\n2. For each distinct task type in your application, evaluate whether a smaller/cheaper model is genuinely sufficient, using a real golden-dataset-style evaluation (per the LLMOps course track), not intuition alone.\n\n3. Design a prompting layer abstraction that isolates model-specific configuration (API client, structured-output mechanism, prompt formatting quirks) from your application's core logic.\n\n4. Implement the abstraction for at least two providers, even if you're currently only using one in production, to genuinely test that the abstraction provides real swappability rather than a superficial one.\n\n5. Explicitly test a provider swap: change which provider/model handles a given task and measure exactly how much application code had to change.\n\n6. Make an explicit, documented decision for each task type about whether cost or latency is the more important optimization target, grounded in real business/user requirements.\n\n7. Build in the ability to route different task types to different models/providers based on the decisions made in steps 2 and 6, rather than a single global model choice for the entire application.\n\n8. Document the model/provider decisions and their reasoning, so a future revisit (a new model release, a price change) has clear context to reason from.",
  },
  {
    section_type: "failure_modes",
    title: "How model and prompting layer decisions actually go wrong",
    order_index: 4,
    content: "Failure 1 — Defaulting to the most capable (and most expensive) model for every task, without evaluating cheaper alternatives.\nWhat: every task in the application, regardless of actual complexity, routes to the same top-tier, most expensive model, when many tasks would perform just as well on a cheaper, faster option.\nHow it happens: it's simpler to use one model everywhere than to evaluate and maintain multiple, and the most capable model is a \"safe\" default that avoids the risk of underpowering a task.\nWhy beginners miss it: the application works correctly with the expensive model everywhere — the cost is purely financial and doesn't manifest as a functional problem, so it's easy to never revisit.\nDetect: audit actual task types in the application and estimate what fraction could plausibly be handled by a cheaper model, based on their real complexity.\nPrevent: evaluate cheaper model options against a real golden dataset for each distinct task type, per this module's workflow, rather than defaulting to uniform use of the most capable model.\nInterview question: \"Your application uses the most expensive available model for every single task, including simple classification. What would you investigate before recommending a change?\"\nReal-world consequence: significantly higher operating costs than necessary, with no corresponding quality benefit for the tasks that didn't actually need the more expensive model's extra capability.\n\nFailure 2 — Tightly coupling application logic to one provider's specific API, making a future swap costly.\nWhat: prompt formatting, structured-output handling, and API calls are written directly against one provider's specific interface throughout the application, rather than behind an abstraction layer.\nHow it happens: it's simplest to just call the provider's API directly when first building the application, without deliberately isolating that coupling behind an abstraction.\nWhy beginners miss it: the application works correctly with the chosen provider — the cost of tight coupling is invisible until there's an actual need to swap or compare providers.\nDetect: check how much of the application's code directly references provider-specific API details, versus going through an isolated abstraction layer.\nPrevent: build a provider-agnostic prompting layer from the start, per this module's workflow, and validate it against at least two providers early, not just the one currently in production use.\nInterview question: \"How would you design your model integration so that switching providers later requires minimal changes to the rest of your application?\"\nReal-world consequence: a costly, risky, time-pressured rework required to switch providers when circumstances demand it (a price change, an outage, a capability need the current provider doesn't meet).\n\nFailure 3 — Assuming a smaller model is 'good enough' without actually evaluating it.\nWhat: a cost-saving decision to use a smaller/cheaper model is made based on intuition about task simplicity, without real evaluation confirming the smaller model actually performs adequately.\nHow it happens: switching to a cheaper model feels like an obviously safe cost optimization for a task that seems simple, making formal evaluation feel like unnecessary overhead.\nWhy beginners miss it: the switch might work fine for the specific cases informally checked during the decision — the gap only becomes visible against the real variety of production input, which formal evaluation is specifically designed to surface.\nDetect: check whether the decision to use a cheaper model was backed by real evaluation against a representative test set, or based on an informal impression.\nPrevent: apply the LLMOps course track's evaluation discipline to any model-swap decision, treating cost optimization with the same evaluation rigor as any other model or prompt change.\nInterview question: \"Someone proposes switching to a cheaper model to save costs, based on a few examples that looked fine. What would you want to see before agreeing?\"\nReal-world consequence: a quality regression introduced by an under-evaluated model swap, discovered only after it's already affecting real users.\n\nFailure 4 — Optimizing for the wrong dimension (cost vs. latency) relative to the actual use case's real requirements.\nWhat: a model/provider choice optimizes for cost when the use case actually needs low latency (or vice versa), because the trade-off was never explicitly considered against the real business or user requirement.\nHow it happens: cost is often the more visible, easily-measured concern (it shows up on a bill), while latency's real importance to user experience can be less obviously quantified without deliberate consideration.\nWhy beginners miss it: both cost and latency numbers look reasonable in isolation — the mismatch is only visible when compared against what the specific use case actually needs.\nDetect: for each task type, explicitly ask whether cost or latency is the more critical constraint given its real business/user context, and check whether the current model choice actually reflects that priority.\nPrevent: make the cost-versus-latency trade-off an explicit, documented decision per task type, per this module's workflow, rather than an implicit default.\nInterview question: \"You have a customer-facing real-time chat feature and a nightly batch analytics job. Would you optimize each for cost or latency, and why might the answer differ?\"\nReal-world consequence: a customer-facing feature that feels sluggish because it was optimized for cost when users actually needed speed, or an unnecessarily expensive batch job optimized for latency it didn't actually need.",
  },
  {
    section_type: "debugging_playbook",
    title: "Your model/provider choice isn't working well — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"Our LLM costs are much higher than expected for the actual volume of requests we're handling.\"\nDiagnostic: audit which task types are routed to the most expensive model, and check whether any of them could plausibly be handled by a cheaper option based on their actual complexity.\nFix: evaluate cheaper models against a real golden dataset for candidate task types, and route accordingly for any that pass evaluation.\n\nSymptom: \"We need to switch providers (price change, outage, capability need) and it's turning into a much larger rework than expected.\"\nDiagnostic: check how tightly the application's code is coupled to the current provider's specific API, versus abstracted behind a provider-agnostic layer.\nFix: for this instance, complete the necessary rework; going forward, build new integrations behind a genuinely provider-agnostic abstraction from the start.\n\nSymptom: \"We switched to a cheaper model to save cost, and quality seems to have dropped, but we're not sure by how much.\"\nDiagnostic: check whether the switch was backed by real evaluation against a representative test set, or based on informal impression.\nFix: run a proper evaluation (per the LLMOps course track) comparing the two models on your actual golden dataset, and make the swap decision based on that evidence, reverting if the quality gap is unacceptable.\n\nSymptom: \"A customer-facing feature feels slow, but our model choice was optimized for cost.\"\nDiagnostic: check whether latency was ever explicitly weighed against cost for this specific use case, or whether cost was the only dimension considered.\nFix: re-evaluate the model/provider choice specifically for this use case with latency as the primary consideration, even if it costs more per request.",
  },
  {
    section_type: "checklist",
    title: "Model & Prompting Layer Checklist",
    order_index: 6,
    content: "- [ ] Model choice for each task type is based on an actual evaluated trade-off, not a uniform default\n- [ ] Cheaper/smaller model options are evaluated against a real golden dataset before being ruled out or adopted\n- [ ] The prompting layer is genuinely provider-agnostic, validated by testing an actual swap between at least two providers\n- [ ] Structured output and prompt formatting are handled through the abstraction, not directly coupled to one provider's API throughout the application\n- [ ] Cost versus latency priority is an explicit, documented decision per task type, grounded in real business/user requirements\n- [ ] Model/provider decisions and their reasoning are documented for future revisiting",
  },
  {
    section_type: "template",
    title: "Model Selection Decision Record",
    order_index: 7,
    content: "Task type: ___\n\nCandidate models/providers evaluated: ___\n\nCost per token (each candidate): ___\nTypical latency (each candidate): ___\n\nEvaluation results (against golden dataset): ___\n\nPriority for this task: cost / latency — reasoning: ___\n\nChosen model/provider: ___\n\nProvider-agnostic abstraction: confirmed swappable (tested against a second provider)? ___\n\nReview date (for revisiting as new models/prices emerge): ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Artificial Analysis: LLM comparison](https://artificialanalysis.ai/) — a real, regularly updated comparison of model providers across cost, latency, and benchmark capability, directly useful for the evaluation this module calls for.\n- [OpenAI: Pricing](https://openai.com/api/pricing/) and [Anthropic: Pricing](https://www.anthropic.com/pricing) — current, authoritative cost references for the two most commonly compared providers.\n\nRecommended\n- [LiteLLM documentation](https://docs.litellm.ai/) — a real, widely-used open-source library specifically for building the provider-agnostic abstraction layer this module's build deliverable requires.\n\nReference\n- The LLMOps course track's Module 1 and 2 (Prompt & Model Lifecycle Management, Evaluation at Scale) — directly relevant to the model-swap evaluation discipline this module depends on.\n\nAdvanced\n- Search for published engineering blog posts on \"LLM cost optimization\" or \"model routing\" from companies operating at real production scale — useful for seeing this module's cost/latency trade-off principles applied with real production numbers.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Build a provider-agnostic prompting layer and test an actual swap",
    problem_statement: "Build a thin abstraction layer that lets you call either of two model providers (or two model tiers from one provider, if you only have access to one) through the same interface, for a simple task (e.g., summarizing a short passage). Your application code should call this abstraction, not the provider APIs directly.\n\nActually swap the underlying provider/model and measure exactly how many lines of application code (outside the abstraction layer itself) had to change.",
    starter_context: "The measured swap is the actual point of this exercise — a genuinely provider-agnostic layer should require zero or near-zero application code changes for a swap.",
    hints: [
      "Make sure structured output handling (if your task uses it) is also abstracted, not just the basic API call — this is often where 'provider-agnostic' layers quietly leak provider-specific details.",
      "If your swap required significant application code changes, that's a useful, honest finding about where your abstraction leaked — document what leaked and why.",
    ],
    solution_notes: "A strong submission has application code that genuinely doesn't reference provider-specific details, and the measured swap requires zero or minimal changes outside the abstraction layer. If changes were required, a strong submission honestly documents exactly what leaked through the abstraction and why, rather than glossing over an imperfect result — this honest diagnosis is itself valuable, directly connecting to this module's point about verifying genuine swappability through an actual test.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Evaluate whether a cheaper model is sufficient for a real task",
    problem_statement: "Pick a real task (reuse a structured-output agent design from earlier in this catalog, e.g., the Lead Qualifier from the n8n course track) and build a small golden dataset (5-10 examples with known-correct expected output). Run the task against both a more capable model and a smaller/cheaper one, and compare results directly.\n\nMake an explicit, evidence-based recommendation: is the cheaper model sufficient for this task, or does it need the more capable model's extra capability?",
    starter_context: "Choose a task with genuine complexity — a trivially easy task won't produce an interesting comparison, and neither will an obviously very hard one.",
    hints: [
      "Include at least one genuinely ambiguous or edge-case example in your golden dataset — this is often where model capability differences show up most clearly.",
      "Report your actual cost/latency numbers for both models alongside the quality comparison, so your final recommendation weighs all three dimensions explicitly.",
    ],
    solution_notes: "A strong submission has a genuinely varied golden dataset, a real side-by-side comparison with actual results (not assumed), and a final recommendation that explicitly weighs quality, cost, and latency together rather than optimizing for just one dimension — directly applying this module's decision framework to a concrete evaluated case rather than an assumption.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Design a multi-model routing strategy for a realistic application",
    problem_statement: "Design (in writing) a model routing strategy for a hypothetical customer support application with three distinct task types: (1) classifying incoming tickets by urgency, (2) drafting a full response to a technical question, (3) real-time chat with a customer. For each, specify which kind of model tier you'd use and why, and whether cost or latency is the priority.",
    starter_context: "This is a reasoning exercise applying this module's decision framework to three genuinely different task profiles.",
    hints: [
      "Consider which of the three tasks is genuinely simple/classification-like (a strong candidate for a smaller model) versus which requires more nuanced reasoning.",
      "Be explicit about why the real-time chat task's priority differs from the batch-style ticket classification task's priority, if it does.",
    ],
    solution_notes: "A strong submission likely recommends a smaller/cheaper, low-latency model for ticket classification (a well-scoped, simple task where speed matters for a responsive triage system), a more capable model for drafting technical responses (genuine reasoning/synthesis needed, less latency-sensitive since it's not real-time), and a model balancing capability with low latency for real-time chat (both quality and speed matter directly to user experience) — with explicit reasoning for each choice tied to the task's actual complexity and the use case's real cost/latency priority, not a uniform answer applied to all three.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why might using the most capable available model for every task in your application be a mistake, even if cost isn't a major concern?", what_is_tested: "Understanding that model choice should be tied to actual task requirements, not just capability maximization.", strong_answer_structure: "Explain that a more capable model doesn't just cost more — it's often also slower, and using it uniformly regardless of task complexity is a missed opportunity to optimize latency and cost for the many tasks that don't actually need that extra capability.", weak_answer_example: "\"More capable is always better\" — doesn't consider the latency and unnecessary-cost trade-offs.", follow_up_question: "How would you decide which specific tasks genuinely warrant the more capable model?" },
  { category: "applied", order_index: 1, question: "How would you design your application's model integration to make switching providers a low-risk, low-effort change?", what_is_tested: "Practical architecture knowledge for provider-agnostic design.", strong_answer_structure: "Describe building a thin abstraction layer that isolates provider-specific API calls, prompt formatting, and structured-output handling from the rest of the application, so application logic never directly references provider-specific details.", weak_answer_example: "\"I'd just rewrite the integration when we need to switch\" — accepts unnecessary cost and risk that could be avoided with better upfront design.", follow_up_question: "How would you actually verify your abstraction achieves real provider-agnostic swappability, rather than just looking clean?" },
  { category: "scenario", order_index: 2, question: "A teammate proposes switching to a cheaper model to save costs, based on a few examples that looked fine. How do you respond?", what_is_tested: "Whether the candidate insists on real evaluation before a model-swap decision.", strong_answer_structure: "Propose evaluating the cheaper model against a genuine golden dataset representative of real task variety before making the switch, rather than relying on a small informal sample.", weak_answer_example: "\"Sounds reasonable, let's just switch\" — accepts an under-evaluated decision.", follow_up_question: "What would you do if the cheaper model performs well on most cases but noticeably worse on a specific subset?" },
  { category: "debugging", order_index: 3, question: "Your customer-facing chat feature feels sluggish. You discover your model choice was optimized for cost. What's your investigation and recommended fix?", what_is_tested: "Recognition of a mismatch between the use case's real requirement (latency) and the actual optimization applied (cost).", strong_answer_structure: "Confirm that latency, not cost, is actually the more critical dimension for this real-time, user-facing use case, and recommend re-evaluating the model choice specifically prioritizing latency, even if it costs more per request.", weak_answer_example: "Assuming the issue must be elsewhere (infrastructure, network) without first questioning whether the model choice itself matches the use case's real priority.", follow_up_question: "How would you balance this against the cost increase from switching to a faster model?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to make a real trade-off decision between cost, latency, and quality for a system you built or worked on.", what_is_tested: "Real or realistic experience making an explicit, reasoned trade-off decision.", strong_answer_structure: "A strong answer describes the specific trade-off, the actual data or evaluation used to inform the decision, and the reasoning behind the final choice — not just a vague description of 'balancing priorities.'", weak_answer_example: "A vague answer with no specific trade-off or no concrete decision process described.", follow_up_question: "How did you validate that your trade-off decision was actually the right one, after the fact?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLM API Integration").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AI Stack Module 1 (Model & Prompting Layer) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
