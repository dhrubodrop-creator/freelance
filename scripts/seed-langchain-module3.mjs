import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "92589733-3a0d-42e6-b387-dea2be3ac0d5"; // LangGraph Orchestration

const sections = [
  {
    section_type: "the_field",
    title: "What controlling multi-step agent behavior precisely actually looks like",
    order_index: 0,
    content: "A simple chain (Module 1) executes a fixed, linear sequence of steps every time. Real agentic behavior often needs something a linear chain can't express: looping back to retry a step, branching based on a decision the model or the application logic makes, pausing for a human to weigh in mid-process, and remembering state across many steps or even across sessions. LangGraph exists specifically to model this as a graph — nodes representing steps, edges representing the possible transitions between them, including conditional and cyclic ones a linear chain structurally can't represent.\n\nThe shift this module represents is philosophical as much as technical: instead of writing a prompt and hoping the model \"does the right thing\" through pure reasoning, you're explicitly modeling the actual state machine of what the application should do — which states exist, what triggers a transition between them, and where a human needs to be brought in. This gives you real, inspectable control over multi-step behavior, rather than trusting an unconstrained agent loop to reliably converge on the right sequence of actions every time.\n\nMemory management in a graph context is a genuinely distinct design concern from a simple chain's context window: short-term memory (state within a single run) versus long-term memory (state persisted across sessions) are different mechanisms with different trade-offs, and human-in-the-loop nodes — pausing graph execution for real human input before continuing — are how the design principles from earlier in this catalog (checkpoints for consequential actions) get implemented concretely in a stateful, multi-step system.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about LangGraph orchestration",
    order_index: 1,
    content: "**1. A graph makes implicit control flow explicit.** Instead of hoping a single large prompt reliably reasons through a multi-step process correctly every time, a graph explicitly defines the actual states and transitions — this trades some of an unconstrained agent's flexibility for real, inspectable, debuggable predictability.\n\n**2. Nodes and edges map to a genuine state machine, and thinking in those terms is the actual skill.** A node represents a distinct step or decision point; an edge (including conditional edges) represents what determines the transition to the next node — designing a graph well means genuinely modeling the application's real states, not just chaining prompts together with extra syntax.\n\n**3. Cycles are a first-class capability a linear chain can't express.** The ability to loop back to a previous node (to retry, to re-evaluate after new information, to iterate toward a goal) is exactly what distinguishes graph-based orchestration from linear chain composition, and it's what enables genuinely iterative agent behavior.\n\n**4. Short-term and long-term memory are different mechanisms serving different needs.** State within a single graph execution (short-term) doesn't automatically persist across separate invocations; if the application needs to remember something across sessions (long-term), that requires a deliberate persistence mechanism, not an assumption that state \"just carries over.\"\n\n**5. A human-in-the-loop node is where the design principle of appropriate approval checkpoints (established throughout this catalog) becomes concrete graph structure.** Rather than a soft instruction hoping the model pauses appropriately, a human-in-the-loop node structurally halts execution until real human input is received.\n\n**6. Conditional routing should reflect real decision criteria, not become an unmanageable tangle of edge cases.** As a graph grows, keeping conditional logic clear and traceable (rather than an accumulating pile of special-case branches) is what keeps a complex graph actually debuggable.",
  },
  {
    section_type: "decision_framework",
    title: "When does an agent's behavior actually need a graph, versus a linear chain?",
    order_index: 2,
    content: "IF the application's logic is a fixed, linear sequence of steps with no branching, looping, or need to pause for human input THEN a linear chain (Module 1) is sufficient, and a graph adds unnecessary structural overhead. BECAUSE the graph abstraction's value is specifically in modeling branching, cycles, and stateful pauses — a genuinely linear process doesn't benefit from it. WATCH OUT FOR: underestimating whether your process will need to branch or loop as it matures — if you can already see that need coming, starting with a graph avoids a later, more disruptive migration.\n\nIF the application needs to make a decision that determines which of several different subsequent steps to take THEN model this as conditional edges in a graph, making the decision logic explicit and inspectable rather than buried in a single large prompt's reasoning. BECAUSE explicit conditional routing is debuggable and testable in a way that \"trust the model's reasoning to branch correctly\" is not. WATCH OUT FOR: conditional logic that's too fine-grained (many near-identical branches for minor variations) can become as hard to maintain as an unstructured approach — aim for conditions that reflect genuinely distinct decision points.\n\nIF the application needs to iterate — retry a step with adjusted input, re-evaluate after new information, or loop toward a goal across multiple attempts THEN this requires a graph's cyclic structure, which a linear chain cannot express. BECAUSE this is precisely the capability that distinguishes graph orchestration from chain composition. WATCH OUT FOR: an unbounded cycle with no exit condition or maximum-iteration limit risks the agent looping indefinitely — always design an explicit termination condition.\n\nIF any step in the process should pause for real human review or approval before continuing (per the human-in-the-loop principle established throughout this catalog) THEN model this as an explicit human-in-the-loop node in the graph, structurally halting execution rather than relying on a soft instruction. BECAUSE a graph node that genuinely pauses execution provides real, structural guarantee that a human reviews before the process continues, versus a prompt-level instruction that depends on model compliance.",
  },
  {
    section_type: "workflow",
    title: "The real process for converting an agent to a stateful LangGraph application",
    order_index: 3,
    content: "1. Map the actual states/steps your agent's process goes through, and the genuine decision points between them — this is design work done before writing graph code, the same discipline as the Agent Design Document from the n8n course track.\n\n2. Identify which transitions are genuinely conditional (branching based on a real decision) versus which are simple, fixed sequences — model only the genuine branches as conditional edges.\n\n3. Identify any point where the process genuinely needs to iterate/cycle, and design an explicit termination condition (a maximum iteration count, a success criterion) to prevent unbounded loops.\n\n4. Decide what state needs to be maintained within a single execution (short-term memory) versus what needs to persist across separate sessions (long-term memory), and implement the appropriate mechanism for each.\n\n5. Identify every point where a human-in-the-loop checkpoint is warranted (per this catalog's earlier risk-based framework) and implement it as an explicit graph node that genuinely halts execution.\n\n6. Build the graph incrementally, testing each node and transition individually before testing the full multi-step flow, connecting to the checkpoint-sizing discipline from the n8n course track.\n\n7. Test the graph against scenarios that specifically exercise its branching and cyclic behavior, not just the simplest single-path-through-the-graph case.\n\n8. Document the graph's structure (states, transitions, decision criteria) so someone else could understand and modify it without having to reverse-engineer the logic from code alone.",
  },
  {
    section_type: "failure_modes",
    title: "How LangGraph applications actually break",
    order_index: 4,
    content: "Failure 1 — An unbounded cycle with no termination condition.\nWhat: a graph designed to loop (retry, iterate toward a goal) has no explicit exit condition or maximum iteration limit, and can loop indefinitely under certain conditions, consuming resources without ever completing.\nHow it happens: the cycle's happy-path exit condition (the goal is achieved, the retry succeeds) is designed, but the unhappy path (the goal is never achieved, the retry never succeeds) isn't explicitly handled.\nWhy beginners miss it: the graph works correctly in normal testing, where the cycle does terminate as expected — the gap only shows up in an edge case where the normal exit condition is never actually met.\nDetect: for every cyclic structure in the graph, explicitly check what happens if the intended exit condition is never satisfied.\nPrevent: design an explicit maximum-iteration or timeout-based termination condition for every cycle, independent of the intended success-based exit condition, per this module's workflow.\nInterview question: \"Your graph has a retry loop that exits when a step succeeds. What happens if it never succeeds?\"\nReal-world consequence: a runaway process that consumes resources (and, in an LLM context, real cost per iteration) indefinitely, potentially going unnoticed until someone specifically investigates why a task never completed.\n\nFailure 2 — Confusing short-term and long-term memory, losing state that should have persisted.\nWhat: state that was assumed to carry over across separate sessions or invocations is actually only short-term (scoped to a single graph execution), and is unexpectedly lost between interactions.\nHow it happens: short-term state within one execution can feel like it should \"just work\" the same way across separate invocations, without deliberately implementing a persistence mechanism for genuinely long-term needs.\nWhy beginners miss it: within a single execution, memory behaves as expected — the gap only becomes visible when a user returns in a later, separate session and the application has no memory of the prior interaction.\nDetect: explicitly test the application across separate, distinct invocations (not just within one continuous run) and check whether expected state actually persisted.\nPrevent: deliberately design and implement a long-term persistence mechanism for any state that genuinely needs to survive across sessions, rather than assuming it happens automatically.\nInterview question: \"A user reports your agent 'forgot' something they told it in a previous conversation. What's your first hypothesis?\"\nReal-world consequence: a user-facing experience that feels broken or forgetful, because state that should have been persisted long-term was only ever short-term.\n\nFailure 3 — A human-in-the-loop node that's easily bypassed or doesn't structurally halt execution.\nWhat: a node intended to require human review before continuing doesn't actually block execution reliably — perhaps it logs a notification but the graph proceeds regardless, or there's a code path that skips the node under certain conditions.\nHow it happens: implementing a genuinely blocking human-in-the-loop node (with real state persistence while waiting for external input) is more involved than a soft instruction or a notification-only step, and it's easy to build something that looks like a checkpoint without actually being one.\nWhy beginners miss it: the node exists in the graph's code and looks like a real checkpoint on review — its actual blocking behavior isn't obvious without deliberately testing whether execution genuinely halts.\nDetect: deliberately test whether the graph actually stops and waits at the intended human-in-the-loop node, or whether it proceeds regardless.\nPrevent: use LangGraph's actual interrupt/checkpoint mechanisms designed for genuine human-in-the-loop pausing, rather than approximating the behavior with a notification step that doesn't truly block.\nInterview question: \"How do you verify a human-in-the-loop node in your graph actually halts execution, rather than just notifying someone while the process continues?\"\nReal-world consequence: a consequential action proceeds without the human review it was supposedly gated on, defeating the entire purpose of the checkpoint.\n\nFailure 4 — Conditional routing logic that accumulates into an unmanageable tangle of special cases.\nWhat: as the graph grows, new conditional branches are added for each new edge case encountered, without periodically reconsidering whether the overall routing logic still reflects clear, genuine decision points.\nHow it happens: each individual added branch feels like a reasonable, incremental fix for a specific case encountered, without stepping back to assess the accumulating complexity.\nWhy beginners miss it: each individual addition is locally reasonable — the problem is only visible in aggregate, once the routing logic has become genuinely hard to trace through.\nDetect: periodically review the graph's conditional routing logic and ask whether someone unfamiliar with its history could still understand the actual decision criteria.\nPrevent: periodically refactor conditional routing logic to consolidate and clarify decision points, rather than only ever adding new branches without revisiting the overall structure.\nInterview question: \"Your graph's conditional routing has grown to 15 special-case branches over several months. What would you do?\"\nReal-world consequence: a graph that becomes progressively harder to debug, modify, or hand off to another engineer, as its actual decision logic becomes effectively undocumented tribal knowledge embedded in an increasingly tangled structure.",
  },
  {
    section_type: "debugging_playbook",
    title: "The LangGraph application isn't behaving as designed — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"A retry loop in our graph seems to run much longer than expected on certain inputs.\"\nDiagnostic: check whether the loop has an explicit maximum-iteration or timeout termination condition, independent of the intended success-based exit.\nFix: add an explicit termination condition if missing, and test specifically against an input that would never satisfy the success-based exit to confirm the loop now terminates correctly.\n\nSymptom: \"The application seems to 'forget' things between separate user sessions.\"\nDiagnostic: check whether the state in question is only maintained as short-term (within-execution) memory, with no deliberate long-term persistence mechanism.\nFix: implement explicit long-term memory persistence for the specific state that needs to survive across sessions.\n\nSymptom: \"A consequential action happened without the human approval step we thought was required.\"\nDiagnostic: check whether the human-in-the-loop node actually uses a genuine blocking/interrupt mechanism, or just a notification that doesn't structurally halt execution.\nFix: reimplement the checkpoint using LangGraph's actual interrupt/pause mechanisms, and explicitly test that execution genuinely halts until human input is provided.\n\nSymptom: \"Our graph's routing logic has become hard for anyone but its original author to follow.\"\nDiagnostic: review the actual conditional edges and check whether they still reflect clear, genuinely distinct decision points, or have accumulated into unmanaged special-case sprawl.\nFix: refactor and consolidate the routing logic, documenting the actual decision criteria clearly, per this module's workflow.",
  },
  {
    section_type: "checklist",
    title: "LangGraph Application Checklist",
    order_index: 6,
    content: "- [ ] The application's actual states and genuine decision points were mapped before building the graph\n- [ ] Only genuinely conditional transitions are modeled as conditional edges, not artificial branching\n- [ ] Every cycle has an explicit termination condition, independent of its intended success-based exit\n- [ ] Short-term versus long-term memory needs are explicitly identified and appropriately implemented\n- [ ] Human-in-the-loop nodes use genuine blocking/interrupt mechanisms, verified by testing, not just present in code\n- [ ] The graph has been tested against branching and cyclic scenarios, not just the simplest single path\n- [ ] Conditional routing logic is periodically reviewed and refactored, not left to accumulate unmanaged complexity\n- [ ] The graph's structure is documented for someone other than its original author to understand and modify",
  },
  {
    section_type: "template",
    title: "LangGraph Application Design Record",
    order_index: 7,
    content: "Application name: ___\n\nStates/nodes and their responsibility:\n| Node | Responsibility |\n|---|---|\n| ... | ... |\n\nConditional edges and their decision criteria:\n| From node | Condition | To node |\n|---|---|---|\n| ... | ... | ... |\n\nCycles and their termination conditions: ___\n\nMemory: short-term state maintained ___, long-term persistence mechanism (if needed) ___\n\nHuman-in-the-loop nodes: ___ — verified to genuinely block execution? ___\n\nTest scenarios covered (including branching/cyclic paths, not just the simplest path): ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [LangGraph documentation](https://langchain-ai.github.io/langgraph/) — the official, authoritative reference for graph-based orchestration this module is built around.\n- [LangGraph: Human-in-the-loop](https://langchain-ai.github.io/langgraph/concepts/human_in_the_loop/) — the direct reference for implementing genuine, blocking human-in-the-loop nodes.\n\nRecommended\n- [LangGraph: Persistence](https://langchain-ai.github.io/langgraph/concepts/persistence/) — the official reference for short-term versus long-term memory mechanisms covered in this module.\n\nReference\n- [LangGraph: Conditional edges](https://langchain-ai.github.io/langgraph/concepts/low_level/#conditional-edges) — the direct API reference for the branching logic this module's decision framework centers on.\n\nAdvanced\n- [LangGraph: Time travel and checkpoints](https://langchain-ai.github.io/langgraph/concepts/persistence/#checkpoints) — for teams needing to debug or replay past graph executions, an advanced capability built on LangGraph's state-persistence model.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Convert a linear chain into a LangGraph with a retry cycle",
    problem_statement: "Take a chain you built in Module 1 or 2 (or a similar simple one), and convert it into a LangGraph application with an added retry cycle: if the output fails a simple validation check (you define what 'valid' means for your case), loop back to retry with adjusted input, up to a maximum of 3 attempts, after which it should exit with a clear failure state rather than looping indefinitely.\n\nTest it against an input that succeeds on the first attempt, and one deliberately designed to fail all 3 attempts, confirming the termination condition works correctly.",
    starter_context: "Your validation check can be simple (e.g., output length within a range, output matches an expected format) — the point is the cycle-and-termination structure, not a sophisticated validation logic.",
    hints: [
      "Make sure your maximum-iteration termination condition is tested explicitly with an input designed to never pass validation — this is the case most likely to be overlooked if only tested with successful inputs.",
      "Design a clear, distinct 'failure after max retries' state, separate from the success state, so downstream logic can handle each appropriately.",
    ],
    solution_notes: "A strong submission has a genuinely working graph with a real retry cycle, explicit and tested termination logic (confirmed via the deliberately-failing test case, not just the successful one), and a clearly distinct failure state — directly demonstrating the termination-condition discipline this module's failure modes emphasize as commonly overlooked.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Add a genuine human-in-the-loop node and verify it actually blocks",
    problem_statement: "Extend your graph (or a new one) with a human-in-the-loop node before a 'consequential' step (mocked, e.g., logging a simulated action rather than performing a real one). Use LangGraph's actual interrupt mechanism, not just a notification step.\n\nDeliberately test whether execution genuinely halts at this node until you provide input to resume it — document exactly how you verified this, not just that you implemented it.",
    starter_context: "The verification step (proving the halt is genuine) is the actual point of this exercise, connecting to this module's third failure mode.",
    hints: [
      "Try to advance the graph past the human-in-the-loop node without providing the expected input, and confirm it genuinely doesn't proceed — this is the specific test that distinguishes a real checkpoint from an approximated one.",
      "Document the exact mechanism you used (LangGraph's specific interrupt/checkpoint API) so the verification is reproducible.",
    ],
    solution_notes: "A strong submission implements a genuine LangGraph interrupt-based human-in-the-loop node, and explicitly demonstrates (not just claims) that execution halts and does not proceed without the expected human input — directly closing the gap this module's failure modes describe between a node that looks like a checkpoint and one that actually functions as one.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose and refactor a tangled conditional-routing graph",
    problem_statement: "You're given this description: \"Our support-routing graph started with 3 clear categories (billing, technical, general) but over 8 months has grown to 22 conditional branches as edge cases were added one by one. New team members find it very hard to understand or safely modify.\" You're not given the actual graph code.\n\nPropose a specific refactoring approach: how would you go about consolidating and clarifying this routing logic without necessarily starting from scratch?",
    starter_context: "This is a process-design exercise based on the description alone — reason about how you'd approach untangling accumulated complexity in a real system.",
    hints: [
      "Consider grouping the 22 branches by whether they represent genuinely distinct decision criteria versus minor variations that could be consolidated under a shared condition with an internal sub-check.",
      "Consider what documentation or diagramming approach would help make the actual decision logic legible again, separate from the code refactoring itself.",
    ],
    solution_notes: "A strong submission proposes auditing the 22 branches to identify which represent genuinely distinct decision points (kept as separate conditional edges) versus which are minor variations that could be consolidated (e.g., under one condition with clearer internal logic, or grouped into a smaller number of genuinely meaningful categories), plus a documentation step (a visual diagram of the resulting simplified graph) to make the decision logic legible to new team members going forward — directly applying this module's fourth failure mode's remediation, rather than proposing either a full from-scratch rewrite or accepting the tangle as unavoidable.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "What can a LangGraph express that a linear LCEL chain cannot?", what_is_tested: "Understanding of the specific structural capability graphs add — branching, cycles, and stateful pausing.", strong_answer_structure: "Explain that a graph can express conditional branching (different next steps based on a decision), cycles (looping back to retry or iterate), and genuine stateful pauses (human-in-the-loop), none of which a strictly linear chain structure can represent.", weak_answer_example: "\"Graphs are just more powerful chains\" — doesn't name the actual specific structural capabilities added.", follow_up_question: "Give an example of a real application where a linear chain would genuinely be insufficient." },
  { category: "applied", order_index: 1, question: "Why does every cycle in a graph need an explicit termination condition beyond its intended success-based exit?", what_is_tested: "Understanding that the unhappy path (the success condition never being met) needs explicit handling.", strong_answer_structure: "Explain that without an independent termination condition (a max iteration count or timeout), a cycle whose intended exit condition is never actually satisfied will loop indefinitely, consuming resources and never completing.", weak_answer_example: "\"The loop will exit once it succeeds\" — only addresses the happy path, not what happens if success never occurs.", follow_up_question: "What should happen when a cycle hits its maximum iteration limit without succeeding?" },
  { category: "scenario", order_index: 2, question: "A user reports your stateful agent 'forgot' something from a previous conversation. How do you investigate?", what_is_tested: "Diagnostic instinct toward the short-term-versus-long-term memory distinction.", strong_answer_structure: "Check whether the specific information was ever intended to be persisted long-term (across sessions), or whether it was only ever short-term state scoped to a single execution that wasn't expected to carry over.", weak_answer_example: "Assuming this is a model reliability issue without first checking whether long-term persistence was actually implemented for this state.", follow_up_question: "How would you design the persistence mechanism if this information does need to be remembered across sessions?" },
  { category: "debugging", order_index: 3, question: "You need to verify that a human-in-the-loop node in your graph actually blocks execution, not just notifies someone. How do you test this?", what_is_tested: "Practical verification approach distinguishing a genuine checkpoint from an approximated one.", strong_answer_structure: "Deliberately attempt to let the graph continue past the node without providing the expected human input, and confirm it genuinely does not proceed — using this as the actual test of whether the checkpoint is real.", weak_answer_example: "Assuming the node works as intended because it's present in the code, without an explicit test of the blocking behavior.", follow_up_question: "What LangGraph-specific mechanism would you use to implement a genuinely blocking checkpoint, rather than approximating one?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to refactor or simplify logic that had grown overly complex through incremental additions over time.", what_is_tested: "Real or realistic experience recognizing and addressing accumulated complexity, not just adding to it indefinitely.", strong_answer_structure: "A strong answer describes the specific situation, how the complexity was recognized (ideally proactively, not just when it became a crisis), and the actual approach taken to consolidate and clarify it.", weak_answer_example: "A vague answer with no specific situation or no described refactoring approach.", follow_up_question: "How do you now try to prevent this kind of unmanaged complexity growth from happening again?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LangGraph").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("LangChain Module 3 (LangGraph Orchestration) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
