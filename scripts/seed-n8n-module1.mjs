import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "c0274b43-a913-426b-b86f-058cf0ae3307"; // Agentic System Design Mindset

const sections = [
  {
    section_type: "the_field",
    title: "What scoping an agent project actually looks like before opening any tool",
    order_index: 0,
    content: "The most expensive mistake in no-code agentic automation isn't a broken workflow — it's building a sophisticated multi-step AI agent to solve a problem that a simple deterministic automation (or no automation at all) would have solved better, faster, and more reliably. The tools make it easy to reach for an agent by default. The actual professional skill is knowing when not to.\n\nBefore opening n8n or designing a single prompt, the real work is asking: is this task actually deterministic (the same input always should produce the same output through a fixed set of steps), or does it genuinely require judgment and reasoning under ambiguity? A workflow that routes a support ticket to \"billing\" whenever the subject line contains the word \"invoice\" is a simple rule — wrapping it in an LLM call adds cost, latency, and a new failure mode (misclassification) for no real benefit. A workflow that has to read a rambling customer email and infer the actual underlying issue, weighing tone and context, is a genuine reasoning task suited to an agent.\n\nGood practitioners write this scoping down before building anything — an Agent Design Document that states the goal, why it needs reasoning (not just rules), what tools/data it needs access to, what its constraints and failure boundaries are, and where a human needs to stay in the loop. This single artifact is what turns \"I know how to use these tools\" into \"I know when and why to use them,\" which is the actual differentiator clients and employers are paying for.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about agentic system design",
    order_index: 1,
    content: "**1. Automation and agentic AI solve different problems, and defaulting to the more sophisticated one is a real cost, not a safety margin.** Deterministic automation is faster, cheaper, and more predictable for anything genuinely rule-based. Reach for an agent only when the task requires actual judgment a fixed rule set can't capture.\n\n**2. A chatbot and a tool-using agent are architecturally different things wearing the same conversational interface.** A chatbot answers questions from what it knows or was told; a tool-using agent takes real actions in the world (calling APIs, updating records, sending messages) — the design and safety considerations for the second are substantially higher-stakes than the first.\n\n**3. Every agent is defined by four things: goal, memory, tools, and constraints.** If you can't state each of these explicitly for a proposed agent, it isn't actually designed yet — it's a vague idea about \"AI doing something.\"\n\n**4. Deterministic and probabilistic components should be deliberately separated, not blended by default.** The parts of a workflow that genuinely need reasoning should call an LLM; the parts that are actually fixed logic (routing, formatting, simple lookups) should stay as plain deterministic steps — mixing them unnecessarily makes the whole system harder to debug and less reliable than it needs to be.\n\n**5. A human-in-the-loop checkpoint is a design decision, not a fallback for when you don't trust the system yet.** Deciding where a human must approve, review, or intervene is part of the actual architecture — driven by the real cost of a wrong autonomous action, not just general caution.\n\n**6. The goal of scoping is to make failure boundaries explicit before they're discovered in production.** What should this agent never be allowed to do unsupervised? What happens when it's uncertain? These questions have real, specific answers, and finding them out during design is far cheaper than finding them out after a client's system does something wrong.",
  },
  {
    section_type: "decision_framework",
    title: "Should this task be a deterministic automation, an agent, or a hybrid?",
    order_index: 2,
    content: "IF the task always follows the same fixed logical steps for a given category of input, with no real judgment required THEN build it as a deterministic automation (plain conditional logic, no LLM call). BECAUSE deterministic logic is faster, cheaper, more predictable, and easier to debug than an LLM call for anything genuinely rule-based. WATCH OUT FOR: mistaking \"this feels complex\" for \"this needs an agent\" — many seemingly complex tasks are actually a long chain of simple deterministic rules, not a reasoning problem.\n\nIF the task requires interpreting ambiguous, unstructured input and making a judgment call that can't be reduced to a fixed rule set THEN this is a genuine candidate for an agent with reasoning capability. BECAUSE this is exactly the class of problem LLM-based reasoning is suited for, where hardcoded rules would be brittle or incomplete. WATCH OUT FOR: even a genuine reasoning task usually has deterministic sub-steps (formatting, routing to a specific system, logging) — decompose the workflow rather than routing the entire thing through the agent.\n\nIF an agent's action would be costly, hard to reverse, or affects something outside your own system (sending a client-facing message, executing a financial transaction, modifying a production record) THEN require a human-in-the-loop approval checkpoint before that specific action executes. BECAUSE the cost of an autonomous mistake in this category is high enough that the latency cost of human review is clearly worth it. WATCH OUT FOR: applying blanket human review to every single action regardless of actual risk defeats much of the value of automation — scope the checkpoint to the genuinely high-stakes actions, not everything.\n\nIF you're unsure whether a task needs an agent at all THEN default to the simplest solution that could plausibly work (deterministic automation, or even no automation) and only add agentic reasoning once you've confirmed the simpler approach genuinely can't handle the real variability in the input. BECAUSE the cost of under-building is usually lower and faster to fix than the cost of over-building — a simple automation that turns out insufficient is easy to upgrade; an overengineered agent system is harder to simplify later.",
  },
  {
    section_type: "workflow",
    title: "The real agent-scoping workflow, before opening any tool",
    order_index: 3,
    content: "1. State the actual business goal in one sentence — not \"build an AI agent,\" but the real outcome (\"reduce time to route a support ticket to the right team from 4 hours to under 5 minutes\").\n\n2. Determine whether the task is genuinely deterministic or requires real reasoning, using the decision framework above — and be honest if the answer is \"this doesn't need an agent at all.\"\n\n3. If it's agentic, define the four components explicitly: goal (what specifically it's trying to achieve), memory (what context it needs to retain across steps or sessions), tools (what real systems/APIs it needs access to), and constraints (what it must never do, and any hard limits).\n\n4. Decompose the full workflow into its deterministic and probabilistic parts, and design each separately — don't route everything through the LLM by default.\n\n5. Identify every action with real-world consequence (sending something external, modifying a record, spending money) and explicitly decide, for each, whether it needs a human-in-the-loop checkpoint.\n\n6. Define what \"the agent is uncertain\" looks like for this specific task, and what should happen in that case — escalate, ask for clarification, or take a conservative default action.\n\n7. Write all of this down as an Agent Design Document before building anything — this artifact is the actual deliverable of the scoping phase, and should be reviewable by a non-technical stakeholder.\n\n8. Only after this document exists and has been reviewed, move to actually building in n8n and the intelligence layer covered in later modules.",
  },
  {
    section_type: "failure_modes",
    title: "How agentic system design actually breaks",
    order_index: 4,
    content: "Failure 1 — Reaching for an agent when a simple deterministic rule would have worked.\nWhat: an LLM call is used to make a decision that could have been handled by a straightforward conditional rule, adding cost, latency, and a new source of unpredictable errors.\nHow it happens: agentic tools are exciting and available, and it's tempting to use them by default rather than asking whether the task actually needs reasoning.\nWhy beginners miss it: the agent-based solution does work, most of the time — the cost is invisible unless you specifically compare it against the simpler alternative that was never built.\nDetect: for any proposed agent step, ask explicitly: could a fixed rule (a conditional, a lookup table) handle this instead? If yes, that's a strong signal the agent is unnecessary.\nPrevent: apply the decision framework's default-to-simplest principle deliberately, every time, rather than reaching for an agent as the default starting point.\nInterview question: \"When would you deliberately choose NOT to use an AI agent for a task, even though it's technically capable of handling it?\"\nReal-world consequence: unnecessary cost, latency, and reliability risk in a system that could have been simpler, more predictable, and cheaper to run and maintain.\n\nFailure 2 — Building an agent without explicitly defining its constraints and failure boundaries.\nWhat: the agent is given tools and a goal, but no explicit statement of what it must never do or how it should behave when uncertain — so it improvises in situations the designer never actually considered.\nHow it happens: constraints feel like a step that can be added \"once we see how it behaves,\" rather than a mandatory part of the initial design.\nWhy beginners miss it: the agent behaves reasonably in early testing with expected inputs — the gap only shows up when it encounters something genuinely unexpected.\nDetect: check whether the design explicitly answers \"what should this agent never do unsupervised\" and \"what happens when it's uncertain\" — if these are unanswered, the design isn't actually complete.\nPrevent: treat constraint and failure-boundary definition as a mandatory section of the Agent Design Document, not an optional add-on.\nInterview question: \"How do you decide what an autonomous agent should never be allowed to do without human approval?\"\nReal-world consequence: an agent takes an unintended, costly, or embarrassing action in a situation the designer never anticipated, because no explicit boundary was ever defined for that case.\n\nFailure 3 — Routing an entire workflow through the LLM instead of decomposing deterministic and probabilistic parts.\nWhat: steps that are actually fixed logic (formatting a message, routing based on a clear category, looking up a record) get bundled into the same LLM call as the genuinely judgment-requiring parts, making the whole system less predictable and harder to debug than necessary.\nHow it happens: it's simpler in the moment to describe the whole task to the LLM in one prompt than to carefully separate which parts are deterministic.\nWhy beginners miss it: the combined approach often works in testing, and the inefficiency (unnecessary variability in parts that should be perfectly consistent) isn't obviously visible without a side-by-side comparison.\nDetect: for any single LLM call handling multiple responsibilities, check whether some of those responsibilities could be extracted into deterministic pre/post-processing steps instead.\nPrevent: explicitly decompose the workflow into deterministic and reasoning components during the design phase, before building, per this module's workflow.\nInterview question: \"Why might you deliberately keep some parts of a workflow outside the LLM call, even when the LLM is technically capable of handling them?\"\nReal-world consequence: unnecessary inconsistency in outputs that should have been perfectly predictable (formatting, simple categorization), and a harder-to-debug system overall.\n\nFailure 4 — No human-in-the-loop checkpoint for high-stakes actions.\nWhat: an agent is given full autonomy to take a costly or hard-to-reverse action (sending a client-facing email, modifying a financial record) with no review step, based purely on how well it performed in testing.\nHow it happens: once an agent seems reliable in testing, it's tempting to remove review steps to increase speed and reduce friction.\nWhy beginners miss it: the agent may perform correctly the vast majority of the time — the cost of the rare failure is what makes this dangerous, and rare failures don't show up in a small testing sample.\nDetect: list every action the agent can take with real-world consequence, and check explicitly whether each one has an appropriate level of human oversight given its actual cost if wrong.\nPrevent: treat human-in-the-loop checkpoints as a deliberate design decision tied to the real cost of a mistake for that specific action, decided during scoping — not removed later purely for speed.\nInterview question: \"An agent has performed flawlessly in testing for two weeks. A stakeholder wants to remove the human approval step to speed things up. How do you respond?\"\nReal-world consequence: a rare but costly autonomous mistake reaches a real client or production system, with no review step that could have caught it.",
  },
  {
    section_type: "debugging_playbook",
    title: "The agent design isn't working as expected — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"The agent is unreliable and gives inconsistent results for what feels like the same input.\"\nDiagnostic: check whether the task was actually a good fit for agentic reasoning in the first place, or whether it's actually a deterministic task that should have been handled with fixed rules for consistency.\nFix: re-apply the decision framework to this specific step — if it's genuinely deterministic, replace the LLM call with fixed logic; if it's genuinely a reasoning task, the inconsistency may point to insufficiently specific instructions rather than the wrong tool choice.\n\nSymptom: \"The agent took an action we didn't want, in a situation we hadn't specifically thought about.\"\nDiagnostic: this is a sign the constraints and failure-boundary definition from the design phase were incomplete.\nFix: go back to the Agent Design Document, explicitly add the newly-discovered boundary case, and decide what should happen in that situation going forward — treat this as expected, iterative refinement, not a one-off patch.\n\nSymptom: \"The system is more expensive and slower than expected, given what it's actually doing.\"\nDiagnostic: check whether deterministic sub-steps (formatting, simple routing, lookups) are unnecessarily being routed through LLM calls instead of plain logic.\nFix: decompose the workflow and move any genuinely deterministic steps out of the LLM call into plain conditional logic.\n\nSymptom: \"A stakeholder is uncomfortable trusting the agent with a specific action, even though it's tested well.\"\nDiagnostic: this discomfort is often a legitimate signal about the real cost of a rare failure for that specific action, not just unfamiliarity with the tool.\nFix: add or strengthen a human-in-the-loop checkpoint for that specific action, and treat the stakeholder's discomfort as useful input into the actual risk assessment, not something to just reassure away.",
  },
  {
    section_type: "checklist",
    title: "Agent Design Pre-Build Checklist",
    order_index: 6,
    content: "- [ ] The actual business goal is stated in one clear sentence, not just \"build an AI agent\"\n- [ ] Whether this task genuinely requires reasoning (versus deterministic rules) has been explicitly assessed, not assumed\n- [ ] Goal, memory, tools, and constraints are all explicitly defined for any agentic component\n- [ ] The workflow is decomposed into deterministic and probabilistic parts, each designed appropriately\n- [ ] Every action with real-world consequence has an explicit decision about whether it needs human-in-the-loop review\n- [ ] What happens when the agent is uncertain is explicitly defined, not left to improvisation\n- [ ] The full design is written down in a reviewable Agent Design Document before any building starts\n- [ ] A non-technical stakeholder could read the design document and understand what the system will and won't do",
  },
  {
    section_type: "template",
    title: "Agent Design Document",
    order_index: 7,
    content: "Use case name: ___\n\nBusiness goal (one sentence): ___\n\nDeterministic or agentic? Reasoning: ___\n\nIf agentic —\nGoal: ___\nMemory (what context it retains, and for how long): ___\nTools (what systems/APIs it can access): ___\nConstraints (what it must never do): ___\n\nDeterministic sub-steps (kept as plain logic, not routed through the LLM): ___\n\nActions with real-world consequence and their human-in-the-loop requirement:\n| Action | Cost if wrong | Human review required? |\n|---|---|---|\n| ... | ... | ... |\n\nWhat happens when the agent is uncertain: ___\n\nSuccess criteria for this use case: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [n8n: AI agent concepts](https://docs.n8n.io/advanced-ai/intro-tutorial/) — n8n's own introduction to when and how to structure agentic workflows, a natural companion to this module's design-first approach before touching the tool.\n- [OpenAI: Agents guide](https://platform.openai.com/docs/guides/agents) — a vendor-neutral-in-spirit but concrete treatment of what actually constitutes an agent (goal, tools, memory) versus a simple prompt-response system.\n\nRecommended\n- [Anthropic: Building effective agents](https://www.anthropic.com/research/building-effective-agents) — directly addresses the deterministic-versus-agentic decision this module centers on, with the same core recommendation to prefer simpler, more composable patterns over full agentic autonomy where possible.\n\nReference\n- [n8n: Human-in-the-loop patterns](https://docs.n8n.io/advanced-ai/examples/) (browse the AI examples section for approval/review workflow patterns) — practical reference for implementing the human-in-the-loop checkpoints this module's decision framework calls for.\n\nAdvanced\n- Search for published case studies or write-ups from agencies building client agentic automation (several no-code/automation agencies publish their design frameworks publicly) — useful for seeing how the scoping discipline in this module gets applied to real, paid client engagements.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Classify five tasks as deterministic, agentic, or hybrid",
    problem_statement: "For each of these five tasks, decide whether it should be built as a deterministic automation, a genuine agent, or a hybrid (with specific deterministic and agentic parts identified) — and justify each with one or two sentences using this module's decision framework:\n\n1. Route a support ticket to \"billing\" if the subject line contains \"invoice,\" \"payment,\" or \"charge.\"\n2. Read an open-ended customer complaint email and draft an appropriate, context-aware response.\n3. Post a daily summary of yesterday's sales numbers to a Slack channel.\n4. Review a batch of job applications and shortlist candidates based on a nuanced set of unwritten preferences a hiring manager has described verbally.\n5. Send a follow-up email to a lead exactly 3 days after their last contact if they haven't responded.",
    starter_context: "This is a pure reasoning exercise — no building required, just classification and justification.",
    hints: [
      "Tasks 1, 3, and 5 have a strong 'fixed rule' quality to them — ask specifically what judgment, if any, they actually require.",
      "Task 4 is a strong agentic candidate specifically because the criteria are nuanced and not fully specified — but consider whether parts of it (initial filtering by hard requirements like years of experience) could still be deterministic.",
    ],
    solution_notes: "A strong submission correctly identifies tasks 1, 3, and 5 as deterministic (clear fixed rules, no real judgment required), task 2 as genuinely agentic (requires interpreting open-ended, context-dependent input), and task 4 as a hybrid — a deterministic pre-filter for hard requirements, with genuine agentic reasoning applied only to the nuanced, harder-to-specify judgment calls among the remaining candidates. Submissions that classify everything as agentic, or that don't identify the hybrid opportunity in task 4, haven't yet internalized this module's core distinction.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Write a full Agent Design Document for a real use case",
    problem_statement: "Pick a real or realistic business process you're familiar with (from your own work, a hypothetical small business, or a domain you know well) that could plausibly benefit from agentic automation. Write a complete Agent Design Document using this module's template, including an explicit table of actions and their human-in-the-loop requirements.",
    starter_context: "Choose something specific and real enough that the design decisions are genuinely non-obvious — a use case that's trivially deterministic won't give you much to design.",
    hints: [
      "Make sure your constraints section names specific things the agent must never do, not just a generic 'be careful' statement.",
      "For the human-in-the-loop table, genuinely vary the risk level across the actions you list — if everything ends up needing review, or nothing does, reconsider whether you've picked actions with meaningfully different real-world consequences.",
    ],
    solution_notes: "A strong submission produces a document specific enough to this use case that it couldn't be reused unchanged for a different one — concrete goals, named tools/systems, specific constraints, and a human-in-the-loop table with genuine variation in risk and review requirement across different actions, directly reflecting the module's principle that human oversight should be scoped to actual risk, not applied uniformly.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Critique an overbuilt agent proposal",
    problem_statement: "A colleague proposes: \"Let's build an AI agent that reads every incoming email, decides which folder to file it in based on sender and subject patterns we've already documented as simple rules, and also decides whether to auto-reply with our standard out-of-office message during declared holiday periods.\"\n\nCritique this proposal using this module's principles. Identify what, if anything, should actually be agentic versus deterministic, and propose a redesigned, more appropriately-scoped version.",
    starter_context: "This is a diagnostic and redesign exercise — the deliverable is your critique and redesigned proposal, not working code.",
    hints: [
      "Notice the phrase 'patterns we've already documented as simple rules' — this is a strong signal that at least part of this proposal doesn't need an agent at all.",
      "Consider whether there's any genuinely ambiguous, judgment-requiring part of this proposal, or whether the whole thing as described is actually deterministic.",
    ],
    solution_notes: "A strong submission recognizes that both parts of this proposal — email filing based on already-documented rules, and auto-reply based on a declared holiday calendar — are genuinely deterministic tasks with no real ambiguity requiring reasoning, despite being framed as an 'AI agent' proposal. The redesign should recommend simple conditional automation instead, correctly applying this module's default-to-simplest principle, and should explicitly note that if a genuinely ambiguous case later emerges (e.g. an email that doesn't clearly match any documented pattern), that specific case — not the whole system — might be a legitimate candidate for agentic handling.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "What's the practical difference between an automation and an agentic AI system?", what_is_tested: "Whether the candidate has a real, applicable distinction rather than treating the terms as interchangeable marketing language.", strong_answer_structure: "Explain that automation follows fixed, deterministic logic for a given input, while an agentic system involves genuine reasoning or judgment to handle ambiguity that fixed rules can't capture — and that choosing between them should be a deliberate decision based on the actual nature of the task.", weak_answer_example: "\"Agentic just means it uses AI\" — doesn't capture the actual distinction in when each is appropriate.", follow_up_question: "Give an example of a task that looks like it needs an agent but is actually better handled as plain automation." },
  { category: "applied", order_index: 1, question: "How do you decide which parts of a workflow should go through an LLM call and which should stay as deterministic logic?", what_is_tested: "Practical decomposition skill, not just theoretical awareness of the distinction.", strong_answer_structure: "Describe identifying the specific sub-steps that genuinely require interpreting ambiguous input or exercising judgment, and keeping everything else (formatting, simple routing, fixed lookups) as plain logic outside the LLM call.", weak_answer_example: "\"I'd just send the whole task to the LLM in one prompt\" — misses the value of decomposition this module teaches.", follow_up_question: "What's the cost of NOT decomposing a workflow this way, even if the combined approach technically works?" },
  { category: "scenario", order_index: 2, question: "A stakeholder wants an agent to have full autonomy to send client-facing emails without any human review, because it's performed well in two weeks of testing. How do you respond?", what_is_tested: "Whether the candidate treats testing performance as sufficient evidence for removing a human-in-the-loop checkpoint on a high-consequence action.", strong_answer_structure: "Explain that two weeks of good performance doesn't rule out rare failure modes, and that the decision to require human review should be based on the actual cost of a rare mistake for this specific, client-facing, hard-to-reverse action — not solely on observed reliability so far.", weak_answer_example: "\"If it's tested well, I'd remove the review step to save time\" — treats testing performance as sufficient justification for removing oversight on a high-stakes action.", follow_up_question: "Is there a testing regimen or monitoring approach that would change your recommendation over time?" },
  { category: "debugging", order_index: 3, question: "An agent-based workflow is giving inconsistent results for what seem like very similar inputs. What's your first hypothesis?", what_is_tested: "Instinct to question whether the task was correctly scoped as agentic in the first place.", strong_answer_structure: "Consider whether this task was actually deterministic and better suited to fixed rules for consistency, or whether the reasoning task's instructions are underspecified in a way that's producing genuine but undesired variability.", weak_answer_example: "Assuming the model itself is simply unreliable, without considering whether the task was scoped correctly to begin with.", follow_up_question: "How would you distinguish between 'this should have been deterministic' and 'the agent's instructions need to be more specific'?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you (or a team you were part of) built something more complex than a problem actually required. What did you learn?", what_is_tested: "Self-awareness about overengineering and what changed as a result, connecting directly to this module's core lesson.", strong_answer_structure: "A strong answer names a specific instance of overbuilding, what made it more complex than necessary, and what changed in how the candidate approaches scoping as a result — ideally connecting to a habit like defaulting to the simplest viable approach first.", weak_answer_example: "A vague or hypothetical answer with no specific instance or lasting change in approach.", follow_up_question: "How do you now catch yourself before overbuilding on a new project?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "Agent Architecture Design").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("n8n Module 1 (Agentic System Design Mindset) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
