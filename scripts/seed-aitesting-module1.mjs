import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "304c3f6a-fe08-48be-aa14-19ce16252e4c"; // Why AI Testing Is Different

const sections = [
  {
    section_type: "the_field",
    title: "What breaks when a QA engineer applies deterministic testing logic to an AI feature",
    order_index: 0,
    content: "Traditional QA has a bedrock assumption baked into almost every practice: given the same input, the system under test should produce the same output, every time. A test case passes or fails, deterministically, and a regression is something that used to pass and now doesn't. An LLM-powered feature breaks this assumption at the root — the same exact input can legitimately produce different, but equally valid, outputs on different runs, and \"pass/fail\" for a single test case stops being a meaningful concept the way it was for deterministic software.\n\nThis isn't a reason to abandon rigor — it's a reason to build genuinely different rigor. A QA engineer moving into AI testing needs a real, working test plan adapted for non-determinism: not \"does this exact output match,\" but \"does this output satisfy the actual quality attributes that matter\" — accuracy (is it factually correct), fairness (does it treat different groups/inputs equitably), robustness (does it handle adversarial or edge-case input gracefully), and explainability (can the reasoning behind an output be understood and defended).\n\nData drift adds a second dimension deterministic QA doesn't have to deal with: a feature that passed all its tests last month can start failing in ways those same tests won't catch, because the real-world data the model encounters has shifted (the same drift concept from the MLOps course track, now viewed through a QA lens — what does this mean for how often and how you re-test). The practical skill this module builds is genuinely re-architecting how \"testing\" is conceived for a probabilistic system, not just applying the old QA playbook with a few AI-flavored test cases added.",
  },
  {
    section_type: "mental_models",
    title: "How QA professionals think about testing AI features",
    order_index: 1,
    content: "**1. Pass/fail for a single test case is the wrong mental model; distribution and rate are the right ones.** For a non-deterministic system, the meaningful question isn't \"did this exact test case pass\" but \"across a representative set of runs and inputs, what fraction meets the quality bar\" — this is a genuine shift in how test results need to be interpreted and reported.\n\n**2. Accuracy, fairness, robustness, and explainability are distinct quality attributes, and a feature can be strong on one and weak on another.** A model can be highly accurate on typical inputs while being unfair across demographic groups, or accurate but unable to explain its reasoning — testing needs to assess each attribute separately, not collapse them into one \"quality\" score.\n\n**3. A regression in an AI feature can be caused by a change nobody in engineering made.** Data drift means the world the model encounters shifted, not the code — this requires a genuinely different mental model of what \"something broke\" means, since the traditional QA assumption (a regression traces back to a recent code change) doesn't reliably hold.\n\n**4. Testing needs to cover the input distribution's real variety, not just the happy path plus a few edge cases.** The same input-variety discipline established throughout this catalog's engineering-focused course tracks applies directly to QA: a test plan built only around clean, expected inputs will systematically miss how the feature actually behaves on the messier real-world variety.\n\n**5. Robustness testing means deliberately trying to break the system, not just confirming it works normally.** This connects directly to the AI Security course track's red-teaming discipline — a QA engineer testing robustness is doing adversarial testing, not just functional verification.\n\n**6. Explainability is testable, even though it feels more subjective than accuracy.** Whether a model's stated reasoning is genuinely coherent and defensible, or just plausible-sounding, is a real thing a QA engineer can and should evaluate systematically, not treat as unquantifiable.",
  },
  {
    section_type: "decision_framework",
    title: "How should I design a test plan for a non-deterministic AI feature?",
    order_index: 2,
    content: "IF you're testing whether a feature meets a quality bar THEN test across a representative set of runs and inputs and report a pass rate or distribution, not a single pass/fail verdict on one test case. BECAUSE a single run's result for a non-deterministic system doesn't tell you whether the feature reliably meets the bar — only a distribution across many runs and inputs does. WATCH OUT FOR: a sample size too small to be statistically meaningful can produce a misleadingly confident-looking pass rate — apply the same confidence-interval reasoning established in the Data Science course track's evaluation module.\n\nIF you're assessing an AI feature's quality THEN test accuracy, fairness, robustness, and explainability as distinct, separately-evaluated attributes, rather than one blended \"does it work\" assessment. BECAUSE a feature can be strong on one attribute and genuinely weak on another, and a blended assessment can mask a real, specific gap. WATCH OUT FOR: not every feature needs deep testing on every attribute — a low-stakes internal tool may reasonably deprioritize fairness testing that a customer-facing, consequential feature genuinely needs; scale testing depth to actual real-world stakes.\n\nIF a feature that previously passed testing starts failing in production THEN consider data drift as a real, distinct hypothesis alongside a recent code change, rather than assuming a code regression by default. BECAUSE this is a genuinely different failure mode from traditional software, and defaulting to the traditional assumption (it must be a recent code change) can send investigation down the wrong path. WATCH OUT FOR: distinguishing drift from a code regression requires actually checking both — comparing current input data characteristics against what the tests were built around, not just assuming one or the other.\n\nIF you're testing robustness THEN deliberately attempt adversarial and edge-case inputs designed to break the system, not just confirm it handles typical inputs correctly. BECAUSE robustness specifically means resilience under genuinely difficult conditions, which functional testing against typical inputs doesn't actually assess.",
  },
  {
    section_type: "workflow",
    title: "The real process for building a non-deterministic test plan",
    order_index: 3,
    content: "1. Identify which quality attributes (accuracy, fairness, robustness, explainability) actually matter for this specific feature, and how deeply each needs to be tested given the feature's real-world stakes.\n\n2. For accuracy, build a representative test set covering the real input variety the feature will actually face, not just clean happy-path examples.\n\n3. For fairness, identify the relevant groups or input categories where equitable treatment matters, and test explicitly across them, not just in aggregate.\n\n4. For robustness, deliberately construct adversarial and edge-case inputs designed to stress the system, connecting to the AI Security course track's red-teaming discipline where genuinely applicable.\n\n5. For explainability, assess whether the model's stated reasoning (where applicable) is coherent and genuinely defensible, not just plausible-sounding.\n\n6. Run tests across enough samples/runs to produce a meaningful distribution or pass rate, not a single pass/fail verdict.\n\n7. When a production failure occurs, explicitly consider data drift as a hypothesis alongside code regression, checking actual input data characteristics against the test set's assumptions.\n\n8. Document the test plan's coverage across all four quality attributes explicitly, so gaps (attributes not deeply tested) are visible and deliberate, not accidental.",
  },
  {
    section_type: "failure_modes",
    title: "How AI testing actually goes wrong when approached with deterministic QA habits",
    order_index: 4,
    content: "Failure 1 — Treating a single test run's pass/fail as a meaningful verdict for a non-deterministic feature.\nWhat: a test case is run once, the output looks acceptable, and it's marked as \"passing,\" with no consideration that a different run of the exact same input might have produced a meaningfully different result.\nHow it happens: single-run pass/fail is the deeply ingrained default from deterministic QA, and it requires deliberate unlearning to recognize it doesn't transfer.\nWhy beginners miss it: the single test run genuinely does look fine — the gap is only visible if you actually run the same input multiple times and observe the variation, which a deterministic-QA habit doesn't naturally prompt you to do.\nDetect: for any \"passing\" test case, ask whether it was actually run multiple times to assess consistency, or evaluated once.\nPrevent: build the distribution/pass-rate mental model explicitly into the test plan from the start, per this module's decision framework, rather than defaulting to single-run pass/fail.\nInterview question: \"You tested an AI feature once and it passed. What's wrong with concluding it's reliable based on that alone?\"\nReal-world consequence: a feature that's actually unreliable across the real distribution of possible outputs gets shipped as \"tested and passing,\" based on a single lucky (or unlucky) run.\n\nFailure 2 — Testing only accuracy, missing fairness, robustness, or explainability gaps entirely.\nWhat: the test plan thoroughly assesses whether the model gives correct answers on typical inputs, but never tests whether it treats different groups equitably, handles adversarial input gracefully, or provides genuinely defensible reasoning.\nHow it happens: accuracy is the most intuitive, most \"testing-like\" quality attribute to assess, and the other three require deliberately different testing approaches that aren't automatically prompted by traditional QA habits.\nWhy beginners miss it: a feature with strong accuracy testing results looks well-tested — the gaps in the other attributes aren't visible unless someone deliberately designs tests specifically for them.\nDetect: check the test plan against all four quality attributes explicitly — if fairness, robustness, or explainability testing is absent or shallow, that's a real, documentable gap.\nPrevent: treat all four quality attributes as required considerations in every test plan, per this module's workflow, scaling depth to actual stakes rather than skipping entirely.\nInterview question: \"Your test plan for a customer-facing AI feature only covers accuracy. What's missing, and why does it matter?\"\nReal-world consequence: a feature ships with a real fairness or robustness gap that accuracy-only testing never had a chance to catch, discovered only when it causes real harm or a public incident.\n\nFailure 3 — Assuming a production failure must be a code regression, missing data drift as the actual cause.\nWhat: when a previously-passing feature starts failing in production, the investigation focuses entirely on recent code changes, missing that the real cause is a shift in the input data the feature now encounters.\nHow it happens: \"a regression traces back to a recent code change\" is the deeply ingrained deterministic-QA assumption, and it doesn't automatically prompt consideration of a genuinely different AI-specific failure mode.\nWhy beginners miss it: code-change investigation is the natural first instinct from traditional QA experience, and if no clear code change is found, the investigation can stall rather than pivoting to consider drift.\nDetect: when a code-change investigation doesn't turn up an obvious cause, explicitly check whether the input data's characteristics have shifted relative to what the original test set assumed.\nPrevent: treat data drift as a standing, real hypothesis for any AI feature regression, per this module's decision framework, not an afterthought considered only once code investigation fails.\nInterview question: \"An AI feature's quality has degraded, but you can't find a recent code change that explains it. What's your next hypothesis?\"\nReal-world consequence: a genuine, ongoing drift-caused quality problem goes undiagnosed and unaddressed because the investigation was anchored entirely on the wrong (code-change) hypothesis.\n\nFailure 4 — Robustness testing limited to typical, well-formed inputs, never genuinely adversarial ones.\nWhat: the test plan confirms the feature works correctly for normal, expected inputs, but never deliberately attempts inputs designed to be difficult, ambiguous, or adversarial, leaving a real robustness gap untested.\nHow it happens: constructing genuinely adversarial test cases requires a different mindset (deliberately trying to break the system) than functional verification (confirming it works as expected), and this shift doesn't happen automatically.\nWhy beginners miss it: functional testing against typical inputs does produce a genuinely passing result — the robustness gap is only visible if someone specifically constructs and tries adversarial inputs, which a purely functional-verification mindset doesn't prompt.\nDetect: check whether the test plan includes any deliberately adversarial or edge-case inputs, or only typical, well-formed ones.\nPrevent: explicitly build a robustness testing phase into the test plan, connecting to the AI Security course track's red-teaming discipline, per this module's workflow.\nInterview question: \"Your feature passes all its functional tests with typical inputs. Does that tell you it's robust? Why or why not?\"\nReal-world consequence: a feature that behaves reasonably for typical use but breaks or produces genuinely bad output under real-world edge cases or adversarial input, discovered only once actual users (or attackers) encounter those conditions.",
  },
  {
    section_type: "debugging_playbook",
    title: "Something's wrong with an AI feature's quality — a troubleshooting guide for QA",
    order_index: 5,
    content: "Symptom: \"A test case that used to pass now fails, but there's no recent code change that would explain it.\"\nDiagnostic: compare the characteristics of current real input data against what the test set was originally built around — this is a strong candidate for data drift, not a code regression.\nFix: if drift is confirmed, update the test set and/or the model to reflect the current real data distribution, and flag this to the engineering team as a drift-driven issue, not a code bug.\n\nSymptom: \"A feature passes accuracy testing consistently but a user reported it behaved very differently, in a way that seems unfair to a specific group.\"\nDiagnostic: check whether fairness testing across relevant groups was ever explicitly performed, or whether testing was accuracy-only.\nFix: add explicit fairness testing across the relevant groups, and investigate the specific reported case as a real, documentable finding.\n\nSymptom: \"A feature that seemed well-tested broke immediately when a user provided an unusual or adversarial input.\"\nDiagnostic: check whether the test plan included any deliberately adversarial or edge-case testing, or only typical, well-formed inputs.\nFix: add robustness testing with deliberately constructed adversarial/edge-case inputs, connecting to the AI Security course track's red-teaming approach.\n\nSymptom: \"We're not confident a feature is actually reliable, even though the one test we ran passed.\"\nDiagnostic: check whether the assessment was based on a single run, or a genuine distribution across multiple runs and varied inputs.\nFix: re-test across a representative sample of runs and inputs, reporting a real pass rate or distribution rather than trusting a single-run result.",
  },
  {
    section_type: "checklist",
    title: "Non-Deterministic AI Test Plan Checklist",
    order_index: 6,
    content: "- [ ] Test results are reported as a distribution/pass rate across multiple runs and inputs, not a single pass/fail verdict\n- [ ] All four quality attributes (accuracy, fairness, robustness, explainability) are explicitly considered, with depth scaled to real-world stakes\n- [ ] The accuracy test set covers real input variety, not just clean happy-path examples\n- [ ] Fairness is tested explicitly across relevant groups/categories, not just in aggregate\n- [ ] Robustness testing includes deliberately adversarial and edge-case inputs\n- [ ] Explainability (where applicable) is assessed for genuine coherence, not just plausibility\n- [ ] Data drift is considered as a standing hypothesis for any production regression, alongside code changes",
  },
  {
    section_type: "template",
    title: "Non-Deterministic AI Feature Test Plan",
    order_index: 7,
    content: "Feature name: ___\n\nAccuracy testing: test set size/variety ___, sample size for pass-rate reporting ___\n\nFairness testing: relevant groups/categories ___, results per group ___\n\nRobustness testing: adversarial/edge-case inputs used ___, results ___\n\nExplainability testing: how reasoning coherence was assessed ___, results ___\n\nDrift monitoring: how current input data is compared against test-set assumptions ___\n\nOverall pass rate / distribution summary: ___\n\nDeliberately deprioritized attributes (if any) and reasoning: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Google: Responsible AI practices](https://ai.google/responsibility/responsible-ai-practices/) — a practical, well-established reference covering fairness, explainability, and robustness assessment approaches directly relevant to this module.\n- [ISTQB: AI Testing (CT-AI) syllabus overview](https://www.istqb.org/certifications/artificial-intelligence-tester) — the established software-testing profession's own framework for AI-specific testing competency, useful context for how this discipline is formally recognized.\n\nRecommended\n- The MLOps course track's Module 4 (Monitoring & Production Operations) — its drift-detection discipline, viewed from an engineering lens, directly complements this module's QA-lens treatment of the same concept.\n\nReference\n- The AI Security course track's Module 2 (Attacking Your Own Systems) — directly relevant to designing genuinely adversarial robustness test cases.\n\nAdvanced\n- Search for published fairness metrics research (e.g., \"demographic parity,\" \"equalized odds\") — for teams needing a more rigorous, quantitative approach to fairness testing beyond this module's qualitative introduction.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Convert a deterministic test case into a non-deterministic test plan",
    problem_statement: "Take a simple LLM feature (e.g., a summarization tool, or reuse the Lead Qualifier agent from the n8n course track). Write a traditional, deterministic-style test case for it first (a single input, one expected exact output). Then rewrite it as a proper non-deterministic test: run the same input 5 times, and report a distribution/pass-rate assessment based on whether each run meets a defined quality bar (not an exact match).",
    starter_context: "The contrast between the two versions is the actual point of this exercise — writing the deterministic version first makes the shift to the non-deterministic version concrete and deliberate.",
    hints: [
      "Your quality bar for the non-deterministic version needs to be more nuanced than 'exact match' — define what actually counts as an acceptable output, even if the exact wording varies.",
      "If your 5 runs show meaningful variation, that's a real, useful finding to report, not something to paper over.",
    ],
    solution_notes: "A strong submission clearly demonstrates the shift from single-input/exact-match testing to a genuine distribution-based assessment across 5 runs, with a real, defined quality bar (not exact match) and an honest report of the actual variation observed — directly practicing the mental-model shift this module's first failure mode identifies as the most fundamental one to unlearn.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Design fairness and robustness tests for a real feature",
    problem_statement: "For a hiring-screening AI feature (hypothetical: it reads a resume and cover letter and produces a fit score), design: (1) a fairness test plan covering at least 3 relevant categories to test for equitable treatment, and (2) a robustness test plan with at least 3 deliberately adversarial or edge-case inputs.",
    starter_context: "This exercise specifically targets the fairness and robustness attributes this module's failure modes identify as most commonly skipped in favor of accuracy-only testing.",
    hints: [
      "For fairness, consider categories where a hiring tool could plausibly produce inequitable results if something in the training data or prompt design introduced bias.",
      "For robustness, consider inputs like a resume in an unusual format, a cover letter attempting to game the system with keyword stuffing, or genuinely ambiguous/incomplete information.",
    ],
    solution_notes: "A strong submission identifies genuinely relevant fairness categories for a hiring context (not generic placeholders) with a real testing approach for each, and constructs genuinely adversarial robustness test cases (not just slightly unusual inputs) that specifically probe for gaming attempts or edge-case failures — directly demonstrating the deliberate, adversarial mindset this module's decision framework requires for robustness testing specifically.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose a mysterious production regression",
    problem_statement: "You're told: \"Our AI feature passed all its tests last quarter with a 94% quality score. This quarter, the same test suite shows 78%, but we haven't deployed any code changes to this feature in two months.\" You're given no further detail.\n\nDiagnose the most likely explanation, and propose a specific investigation plan.",
    starter_context: "This is a diagnostic exercise based on the description alone, specifically probing the code-regression-versus-drift distinction this module emphasizes.",
    hints: [
      "The explicit statement that no code has changed in two months is a strong signal pointing away from the traditional QA default hypothesis (code regression) and toward this module's AI-specific alternative.",
      "Consider what specifically you'd compare to confirm or rule out drift as the actual cause.",
    ],
    solution_notes: "A strong submission identifies data drift as the most likely explanation given the explicit absence of code changes, and proposes a specific investigation comparing the current real input data's characteristics against what the original test set (and its 94% baseline) was built around — checking for a genuine shift in input distribution, rather than continuing to search for a nonexistent code-change cause.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why can't you meaningfully evaluate an AI feature's reliability from a single test run, the way you could for deterministic software?", what_is_tested: "Understanding of the core non-determinism shift this module centers on.", strong_answer_structure: "Explain that a non-deterministic feature can legitimately produce different outputs for the same input across different runs, so a single run's result doesn't tell you how the feature behaves across its real distribution of possible outputs — you need to test across multiple runs and report a distribution or pass rate.", weak_answer_example: "\"AI is just less predictable\" — doesn't explain the actual testing-methodology implication.", follow_up_question: "How would you decide how many runs are enough to get a meaningful pass-rate estimate?" },
  { category: "applied", order_index: 1, question: "A production AI feature that passed testing starts failing, with no recent code changes. What's your investigation approach?", what_is_tested: "Whether the candidate considers data drift as a real, distinct hypothesis rather than only searching for a code cause.", strong_answer_structure: "Explain investigating both possibilities explicitly — first confirming there's genuinely no code change, then comparing current real input data against what the original test set assumed, to check for drift as the actual cause.", weak_answer_example: "\"I'd keep looking for the code change that caused it\" — doesn't consider the AI-specific drift hypothesis when the traditional assumption doesn't hold.", follow_up_question: "How would you build ongoing monitoring to catch this kind of drift earlier next time?" },
  { category: "scenario", order_index: 2, question: "A stakeholder asks whether your AI feature is 'tested and ready.' Your test plan only covers accuracy. How do you respond?", what_is_tested: "Recognition that accuracy alone doesn't constitute complete AI quality testing.", strong_answer_structure: "Explain that accuracy is only one of several relevant quality attributes (fairness, robustness, explainability), and honestly communicate which of those haven't yet been tested, rather than implying the feature is fully vetted based on accuracy alone.", weak_answer_example: "\"Yes, it's tested\" without qualifying that only accuracy was actually assessed.", follow_up_question: "How would you prioritize which of the untested attributes to address first, given limited time?" },
  { category: "debugging", order_index: 3, question: "You're testing an AI feature's robustness and it passes every test you've run. A colleague says it's not actually robust. How could both be true?", what_is_tested: "Recognition that 'passes tests' depends entirely on what was actually tested.", strong_answer_structure: "Explain that if the tests only covered typical, well-formed inputs rather than genuinely adversarial or edge-case ones, passing them says nothing about actual robustness — the colleague may be right that the feature hasn't actually been robustness-tested at all, just functionally verified.", weak_answer_example: "\"If it passes tests, it's robust\" — conflates functional testing with genuine robustness testing.", follow_up_question: "What specific adversarial inputs would you add to genuinely test robustness for this feature?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to change your testing approach because a traditional QA assumption didn't hold for a new kind of system.", what_is_tested: "Real or realistic experience adapting testing methodology to a genuinely different system type.", strong_answer_structure: "A strong answer describes the specific assumption that broke down, how the gap was discovered, and the concrete change made to the testing approach as a result.", weak_answer_example: "A vague answer with no specific assumption or described change in approach.", follow_up_question: "How did you communicate this shift in testing philosophy to a team used to traditional deterministic QA?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLM Evaluation & Testing").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AI LLM Testing Module 1 (Why AI Testing Is Different) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
