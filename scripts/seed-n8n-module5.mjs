import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "52987923-abf1-4ca6-8192-4959e95785c2"; // Knowledge, Enterprise Context & Multi-Agent Systems

const sections = [
  {
    section_type: "the_field",
    title: "What building AI teams, not single agents, actually looks like",
    order_index: 0,
    content: "A single well-designed agent handles one job well. Real enterprise problems often need more than that: an SOP assistant that has to ground its answers in a company's actual, specific documented procedures rather than making plausible-sounding guesses, or a full Research → Draft → Review → Approve → Send pipeline where multiple distinct reasoning roles hand off to each other with real accountability at each step. This is a different design problem than a single agent, and it's where most of the genuine complexity — and value — in enterprise agentic work actually lives.\n\nGrounding an AI assistant in real company knowledge (an SOP document, an internal wiki, a policy manual) is fundamentally a hallucination-reduction problem: the assistant needs to answer from what's actually documented, not from what a general-purpose model thinks is plausible. This means the system needs real context engineering — retrieving the right document sections and making it clear to the model that it should answer from that retrieved context, not its general training knowledge, and ideally that it should say \"I don't know\" or \"this isn't covered in our documentation\" when the retrieved context genuinely doesn't answer the question.\n\nMulti-agent systems add a manager/worker dimension: a coordinating layer that breaks a complex task into pieces, assigns them to specialized worker agents, and synthesizes or escalates the results. This is powerful but genuinely harder to get right than a single agent — more moving parts means more places for a mistake to hide, and the orchestration logic itself (who does what, in what order, with what handoff) becomes a real design surface, not an afterthought.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about enterprise knowledge and multi-agent design",
    order_index: 1,
    content: "**1. A grounded answer and a plausible-sounding answer are not the same thing, and only one of them is trustworthy for enterprise use.** An assistant answering from a general model's training data can sound just as confident as one genuinely grounded in your company's actual documentation — the difference is entirely in whether it's actually retrieving and citing real, current source material.\n\n**2. \"I don't know\" is a correct, valuable answer that needs to be designed for explicitly.** A grounded assistant that always produces a confident-sounding answer, even when the retrieved context doesn't actually cover the question, is worse than one that honestly says the documentation doesn't address this — the second one is trustworthy; the first one is a liability disguised as helpfulness.\n\n**3. A manager/worker pattern trades single-agent simplicity for the ability to decompose genuinely complex tasks.** It's not automatically better than a single well-designed agent — it's the right tool specifically when a task genuinely benefits from being broken into specialized sub-tasks that a single agent's instructions couldn't cleanly cover at once.\n\n**4. Orchestration logic is itself a real design surface, with its own failure modes.** Deciding who does what, in what order, and how results get synthesized or escalated is not a minor implementation detail once you have a manager coordinating workers — it's core architecture that needs the same design rigor as any individual agent's instructions.\n\n**5. A multi-step Research → Draft → Review → Approve → Send pipeline is an accountability chain, not just a sequence of steps.** Each stage should have a clear, distinct responsibility, and the handoff between stages (especially into Approve and Send) is exactly where the reviewer/checkpoint principle from Module 3 matters most.\n\n**6. Platform-specific tooling (like Microsoft Copilot Studio and Teams deployment) is an implementation detail layered on top of these design principles, not a replacement for them.** The underlying discipline — grounding, escalation, orchestration clarity — applies regardless of which specific enterprise platform ends up hosting the assistant.",
  },
  {
    section_type: "decision_framework",
    title: "How should I ground an assistant, and when do I actually need multiple agents?",
    order_index: 2,
    content: "IF you're building an assistant meant to answer questions about specific company documentation (SOPs, policies, internal knowledge) THEN retrieve the relevant document sections for each query and explicitly instruct the model to answer only from that retrieved context, including a clear instruction to say when the context doesn't cover the question. BECAUSE this is what actually reduces hallucination — a model without retrieval, or one that's allowed to fall back on general knowledge silently, will produce plausible-sounding but potentially wrong answers about company-specific facts it was never actually given. WATCH OUT FOR: retrieval that returns loosely related but not actually relevant document sections can still produce a confidently wrong answer — retrieval quality matters as much as the grounding instruction itself.\n\nIF a task can be handled well by a single agent with clear instructions and a well-scoped job THEN don't introduce a multi-agent manager/worker architecture just because it's more sophisticated. BECAUSE added coordination complexity is a real cost (more places for errors to hide, harder to debug) that should only be paid when it buys something a single agent genuinely couldn't do well. WATCH OUT FOR: a task that feels complex because it has many steps isn't automatically a multi-agent problem — it may just need a well-structured single-agent pipeline (Module 3's Research → Analyze → Summarize pattern) without a full manager/worker architecture.\n\nIF a task genuinely requires distinct specialized reasoning across different domains or responsibilities that a single agent's instructions can't cleanly cover at once (e.g., one worker researching, a differently-specialized worker drafting in a specific tone/format, a distinct reviewer checking accuracy) THEN a manager/worker multi-agent architecture is justified. BECAUSE this is exactly the situation where decomposition genuinely improves reliability, letting each agent have focused, coherent instructions rather than one agent juggling conflicting responsibilities. WATCH OUT FOR: the manager's own coordination logic (deciding what to delegate, how to synthesize worker outputs) needs the same design rigor as any individual agent — it's not just plumbing.\n\nIF a pipeline ends in a consequential, hard-to-reverse action (Send, in a Research → Draft → Review → Approve → Send chain) THEN the Approve stage needs a genuine human or high-confidence automated checkpoint before Send executes, per the reviewer-checkpoint principle from earlier modules. BECAUSE this is the last point where a mistake anywhere earlier in the chain can still be caught before it reaches the outside world.",
  },
  {
    section_type: "workflow",
    title: "The real process for building grounded and multi-agent enterprise systems",
    order_index: 3,
    content: "1. For a knowledge-grounded assistant: index the actual source documentation (SOPs, policies) so relevant sections can be retrieved per query, and explicitly instruct the model to answer only from retrieved context, with a clear fallback for when nothing relevant is found.\n\n2. Test the grounded assistant specifically for hallucination resistance: ask it questions the documentation genuinely doesn't cover, and confirm it says so rather than inventing a plausible answer.\n\n3. For a complex task, first ask whether it's genuinely a multi-agent problem or a well-structured single-agent pipeline would suffice — apply the decision framework before defaulting to a manager/worker architecture.\n\n4. If multi-agent is justified, define each worker's distinct role and instructions clearly (per Module 3's instructions-as-role-definition principle), and design the manager's coordination logic explicitly: what gets delegated, in what order, how results are synthesized.\n\n5. For a Research → Draft → Review → Approve → Send pipeline, define each stage's specific responsibility and the structured handoff between them.\n\n6. Build a genuine checkpoint at the Approve stage before Send executes, matching the consequence of that final action.\n\n7. Test the full multi-agent or pipeline system against realistic, varied inputs — including ones designed to surface where coordination or handoff might break down, not just the smooth happy path.\n\n8. If deploying on a specific enterprise platform (Copilot Studio, Teams), layer the platform-specific implementation on top of an already-sound underlying design, rather than letting platform constraints dictate the design from scratch.",
  },
  {
    section_type: "failure_modes",
    title: "How enterprise knowledge and multi-agent systems actually break",
    order_index: 4,
    content: "Failure 1 — A 'grounded' assistant that still hallucinates because it silently falls back on general knowledge.\nWhat: the assistant answers a company-specific question confidently, but the answer is actually drawn from the model's general training knowledge, not the company's actual documentation — and there's no way to tell the difference from the response alone.\nHow it happens: retrieval is implemented, but the model isn't explicitly instructed to answer only from retrieved context, or the instruction is weak enough that the model defaults to its general knowledge when retrieval doesn't turn up a clean answer.\nWhy beginners miss it: a fluent, confident, plausible-sounding answer gives no signal that it's ungrounded — this failure mode is specifically dangerous because it's invisible without deliberate testing.\nDetect: test the assistant with questions specifically designed to be outside the actual documentation, and check whether it correctly says so or fabricates a plausible answer instead.\nPrevent: explicitly instruct the model to answer only from retrieved context and to state clearly when the context doesn't cover the question, and test this behavior deliberately, not just assume the retrieval step alone solves grounding.\nInterview question: \"How would you test whether an assistant that's supposed to be grounded in company documentation is actually hallucinating?\"\nReal-world consequence: employees or customers receive confident, wrong answers about company policy or procedure, potentially causing real operational or compliance problems that trace back to an assistant everyone assumed was reliably grounded.\n\nFailure 2 — Defaulting to a multi-agent architecture for a task a single well-designed agent could have handled.\nWhat: unnecessary coordination complexity is introduced, adding more places for errors to hide and making the system harder to debug, without actually improving reliability over a simpler single-agent design.\nHow it happens: multi-agent architectures are impressive and sophisticated-sounding, creating a pull toward using them even when the underlying task doesn't genuinely require decomposition.\nWhy beginners miss it: the system does work, technically — the cost is in unnecessary complexity and harder debugging, which isn't obvious until something actually goes wrong and there are more moving parts to untangle.\nDetect: for any proposed multi-agent design, ask explicitly whether a single agent with well-structured instructions could handle the same task — if the honest answer is yes, the multi-agent architecture isn't earning its added complexity.\nPrevent: apply the decision framework's genuine-decomposition-need test before committing to a manager/worker architecture, the same discipline as Module 1's deterministic-vs-agentic distinction applied one level up.\nInterview question: \"When would you deliberately choose a single, well-structured agent pipeline over a full multi-agent manager/worker architecture, even for a fairly complex task?\"\nReal-world consequence: an overengineered system that's harder to build, debug, and maintain than necessary, for no actual gain in reliability or capability.\n\nFailure 3 — No genuine checkpoint before the final consequential action in a multi-stage pipeline.\nWhat: a Research → Draft → Review → Approve → Send pipeline's Approve stage is nominal rather than a real check, so a mistake introduced anywhere earlier in the chain reaches Send unfiltered.\nHow it happens: by the time a pipeline reaches its final stages, there's often confidence built up from the earlier stages appearing to work correctly, making a genuine final review feel redundant.\nWhy beginners miss it: the pipeline works correctly most of the time, so a nominal Approve stage doesn't visibly fail until the specific case where an earlier error should have been, but wasn't, caught.\nDetect: check whether the Approve stage actually involves genuine scrutiny (a human reviewing the actual content, or a high-confidence automated check) or is effectively a pass-through step.\nPrevent: design and test the Approve checkpoint as seriously as any other stage, matching its rigor to the real consequence of the Send action that follows it.\nInterview question: \"In a Research → Draft → Review → Approve → Send pipeline, what specifically should the Approve stage be checking that the earlier Review stage didn't already cover?\"\nReal-world consequence: a client-facing or otherwise consequential message goes out with an error that a genuine final checkpoint would have caught, undermining trust in the whole automated pipeline.\n\nFailure 4 — Manager/worker coordination logic treated as plumbing rather than real design.\nWhat: the manager agent's delegation and synthesis logic is thrown together without the same care given to individual worker agents' instructions, leading to poor task decomposition, redundant work, or lost information between workers.\nHow it happens: individual worker agents get the visible design attention (since their instructions directly determine output quality), while the manager's coordination logic feels like connective tissue rather than a first-class design concern.\nWhy beginners miss it: individual workers may each perform well in isolation, masking that the overall system's output suffers from how poorly their results are being combined or sequenced.\nDetect: evaluate the manager's delegation and synthesis decisions specifically, separate from evaluating individual worker outputs — check whether tasks are being decomposed sensibly and results combined coherently.\nPrevent: design the manager's coordination logic with the same explicit rigor as any individual agent's instructions — its role, its decision criteria for delegation, and its synthesis approach should all be deliberately specified.\nInterview question: \"You have three worker agents that each perform well individually, but the overall multi-agent system's output is still poor. What would you investigate first?\"\nReal-world consequence: a multi-agent system that underperforms what its individual components' quality would suggest is possible, because the actual coordination — the hardest and most novel part of the design — was never given real design attention.",
  },
  {
    section_type: "debugging_playbook",
    title: "The enterprise knowledge or multi-agent system isn't behaving — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"The SOP assistant gave a confident answer that turned out to be wrong or not actually in our documentation.\"\nDiagnostic: test the exact question against your retrieval system directly — did it actually retrieve relevant, correct source material, or did the model answer from general knowledge despite the grounding instruction?\nFix: strengthen the explicit instruction to answer only from retrieved context and to say when context is insufficient, and if retrieval itself failed to surface the right material, investigate the retrieval/indexing quality separately.\n\nSymptom: \"A multi-agent system's overall output is worse than what its individual worker agents seem capable of.\"\nDiagnostic: inspect the manager's delegation and synthesis logic specifically, separate from each worker's individual output quality.\nFix: redesign the manager's coordination logic with explicit criteria for what gets delegated to which worker and how results are combined, rather than treating this as an afterthought.\n\nSymptom: \"A mistake made early in a Research → Draft → Review → Approve → Send pipeline still reached Send.\"\nDiagnostic: check whether the Approve stage involves genuine scrutiny or is effectively a formality that doesn't actually catch errors from earlier stages.\nFix: redesign the Approve checkpoint to be a real, substantive check matched to the consequence of the Send action, and test it deliberately with a known planted error from an earlier stage to confirm it actually catches it.\n\nSymptom: \"We're not sure whether this task actually needed a multi-agent architecture, or if a simpler design would have worked as well.\"\nDiagnostic: honestly assess whether a single, well-structured agent pipeline could plausibly have handled the same task.\nFix: if the answer is genuinely yes, consider simplifying — added coordination complexity that isn't earning its cost is worth removing, not preserving out of sunk-cost attachment to the more sophisticated design.",
  },
  {
    section_type: "checklist",
    title: "Enterprise Knowledge & Multi-Agent Design Checklist",
    order_index: 6,
    content: "- [ ] A knowledge-grounded assistant is explicitly instructed to answer only from retrieved context, not general knowledge\n- [ ] The assistant has been tested specifically for hallucination resistance on questions outside its actual documentation\n- [ ] A multi-agent architecture was chosen only after confirming a single well-structured agent genuinely couldn't handle the task\n- [ ] Each worker agent has clearly distinct, well-defined responsibilities\n- [ ] The manager's delegation and synthesis logic was designed deliberately, not treated as connective plumbing\n- [ ] Each stage in a multi-stage pipeline (Research → Draft → Review → Approve → Send) has a clearly defined, distinct responsibility\n- [ ] The Approve stage before any Send-equivalent action is a genuine checkpoint, not a formality\n- [ ] The full system has been tested against realistic, varied inputs designed to surface coordination or grounding failures, not just the smooth happy path",
  },
  {
    section_type: "template",
    title: "Multi-Agent System Design Record",
    order_index: 7,
    content: "System name: ___\n\nIs this genuinely a multi-agent problem? Reasoning: ___\n\nWorker agents and their distinct roles:\n| Worker | Responsibility | Output schema |\n|---|---|---|\n| ... | ... | ... |\n\nManager coordination logic: what gets delegated, in what order, how results are synthesized: ___\n\nGrounding approach (if applicable): retrieval source, grounding instruction, fallback for insufficient context: ___\n\nPipeline stages (if applicable) and their distinct responsibilities: ___\n\nCheckpoint before final consequential action: ___ (who/what reviews, and what specifically it checks)\n\nTest cases used to validate coordination and grounding (list at least 3, including edge cases): ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [OpenAI: Retrieval-augmented generation guidance](https://platform.openai.com/docs/guides/retrieval) — the foundational reference for the grounding approach central to this module's knowledge-assistant topic.\n- [Anthropic: Building effective agents — orchestrator-workers pattern](https://www.anthropic.com/research/building-effective-agents) — directly covers the manager/worker architecture and when it's actually justified, matching this module's decision framework closely.\n\nRecommended\n- [Microsoft: Copilot Studio documentation](https://learn.microsoft.com/en-us/microsoft-copilot-studio/) — for teams specifically deploying on this platform, covering the implementation-layer details referenced in this module.\n\nReference\n- [n8n: Vector store and RAG examples](https://docs.n8n.io/advanced-ai/examples/) — practical reference for implementing document retrieval and grounding within an n8n-orchestrated workflow.\n\nAdvanced\n- Search for published enterprise RAG hallucination-reduction case studies (several enterprise AI platforms publish these) — useful for seeing how the grounding-instruction and testing discipline in this module gets applied and measured at real organizational scale.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Design a grounded SOP assistant and test it for hallucination",
    problem_statement: "Using a short, real or realistic SOP document you write yourself (a few paragraphs covering a specific fictional company procedure — e.g., an expense-reimbursement policy), design a grounded assistant: retrieval approach (even a simple keyword or manual lookup for this exercise is fine), grounding instructions, and a fallback for insufficient context.\n\nTest it with three questions: one clearly answered by your document, one clearly NOT covered by your document, and one that's superficially related but actually not answered by the specific text. Confirm the assistant correctly distinguishes all three cases.",
    starter_context: "You don't need a full production retrieval system for this exercise — the point is testing the grounding instruction and fallback behavior specifically, which you can do even with a simple manual document lookup.",
    hints: [
      "The third test case (superficially related but not actually answered) is the hardest and most important one — this is exactly where a weak grounding instruction tends to fail by producing a plausible-sounding but ungrounded answer.",
      "If you have API access, actually run this test with a real model call; if not, reason through what the model would likely do given your exact instructions, and identify where the instruction might be insufficiently explicit.",
    ],
    solution_notes: "A strong submission's grounding instructions explicitly direct the model to answer only from the provided document text and to say clearly when something isn't covered, and the third test case (superficially related but not actually documented) specifically probes whether the instruction is strong enough to prevent a plausible-sounding hallucination — correctly identifying this as the hardest and most diagnostic case, not just testing the two easy extremes.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Decide: single agent or multi-agent for three proposed systems",
    problem_statement: "For each of these three proposed systems, decide whether it genuinely needs a multi-agent manager/worker architecture or whether a single, well-structured agent pipeline would suffice, using this module's decision framework:\n\n1. A system that reads a customer support ticket and drafts a response in the company's specific tone and style.\n2. A system that researches a competitor's public pricing, product features, and recent news, then produces a structured competitive analysis combining all three.\n3. A system that reviews a job candidate's resume against a job description and produces a fit assessment.",
    starter_context: "You justify each decision explicitly — there's a real judgment call in at least one of these three.",
    hints: [
      "For system 2, consider whether researching pricing, features, and news are genuinely distinct specialized tasks that benefit from separate focused agents, or whether one well-instructed research agent could handle all three coherently.",
      "For systems 1 and 3, ask whether there's a genuine need for multiple distinct reasoning roles, or whether the task is naturally a single, well-scoped job.",
    ],
    solution_notes: "A strong submission likely recommends single-agent pipelines for systems 1 and 3 (each is a coherent, single well-scoped reasoning task), and reasonably argues either way for system 2 depending on how distinct the three research sub-tasks genuinely are — the key evaluation criterion is whether the reasoning is explicit and grounded in the decision framework, not which specific conclusion is reached for the more genuinely ambiguous case.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Redesign a broken Research → Draft → Review → Approve → Send pipeline",
    problem_statement: "You're told: \"Our system researches a topic, drafts a client email, has a 'review' step that just checks the email isn't empty, has an 'approve' step that's actually just a rubber-stamp automatic pass-through, and then sends it. We had an incident where factually incorrect information reached a client.\"\n\nRedesign this pipeline so that a similar incident couldn't happen again, being specific about what each stage should actually check and why.",
    starter_context: "This is a redesign exercise based on the described failure — identify exactly what's missing at each stage, not just at the end.",
    hints: [
      "Notice that BOTH Review and Approve are nominal here — 'checks the email isn't empty' and 'automatic pass-through' are not real checks. Consider what each stage should actually be responsible for, distinctly from each other.",
      "Consider adding fact-verification specifically as a distinct concern, given the described incident was about factual accuracy, not tone or formatting.",
    ],
    solution_notes: "A strong submission redesigns Review to substantively check factual accuracy (e.g., verifying draft claims against the original research output) and redesigns Approve as a genuine final human or high-confidence checkpoint before Send — explicitly distinguishing what each stage catches so they're not redundant with each other. It should directly name that the original design's failure was two nominal checkpoints creating a false sense of safety, exactly matching this module's third failure mode.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "How would you design an AI assistant to answer only from a company's actual documentation, rather than its general training knowledge?", what_is_tested: "Understanding of the grounding mechanism (retrieval plus explicit instruction), not just familiarity with the term RAG.", strong_answer_structure: "Explain retrieving the relevant document sections for each query and explicitly instructing the model to answer only from that retrieved context, including a clear instruction for what to say when the context doesn't cover the question.", weak_answer_example: "\"I'd just give it access to the documents\" — doesn't explain the retrieval and explicit-grounding-instruction mechanism.", follow_up_question: "How would you test whether this grounding is actually working, rather than just assuming it is?" },
  { category: "applied", order_index: 1, question: "When would you choose a multi-agent manager/worker architecture over a single, well-structured agent pipeline?", what_is_tested: "A real decision framework, not a default preference for either approach.", strong_answer_structure: "Explain that multi-agent architecture is justified when a task genuinely requires distinct specialized reasoning that a single agent's instructions couldn't cleanly cover — and that added coordination complexity should only be paid for when it buys real decomposition value.", weak_answer_example: "\"Multi-agent systems are more powerful so I'd use them for complex tasks\" — treats complexity alone as sufficient justification, missing the actual decomposition criterion.", follow_up_question: "Give an example of a complex-seeming task that's actually better handled by a single well-structured agent." },
  { category: "scenario", order_index: 2, question: "A knowledge-grounded assistant confidently answered a question incorrectly — the answer wasn't actually in the company documentation. How do you investigate?", what_is_tested: "Diagnostic process distinguishing a retrieval failure from a grounding-instruction failure.", strong_answer_structure: "Check first whether the retrieval step actually surfaced relevant documentation for this question, then check whether the grounding instruction was explicit enough to prevent the model from falling back on general knowledge when retrieval was weak or absent — these are two distinct possible root causes requiring different fixes.", weak_answer_example: "Assuming the model is simply unreliable without distinguishing between a retrieval problem and an instruction problem.", follow_up_question: "How would you build a standing test suite to catch this category of failure before it reaches real users?" },
  { category: "debugging", order_index: 3, question: "You have three worker agents in a multi-agent system that each individually produce good output, but the overall system's final result is disappointing. What do you investigate first?", what_is_tested: "Recognition that manager coordination logic is a distinct, first-class point of failure separate from individual worker quality.", strong_answer_structure: "Investigate the manager's delegation and synthesis logic specifically — how tasks are being decomposed and assigned, and how the individual good outputs are being combined — rather than assuming the problem must be in one of the workers.", weak_answer_example: "Re-tuning each worker's instructions individually without first checking whether the coordination logic itself is the actual problem.", follow_up_question: "What would evidence of a genuine coordination-logic problem, as opposed to a worker-quality problem, actually look like?" },
  { category: "behavioural", order_index: 4, question: "Tell me about a time you had to decide whether a task genuinely needed a more sophisticated (multi-agent, multi-step) solution, or whether a simpler approach would suffice. How did you decide?", what_is_tested: "Real judgment about avoiding unnecessary complexity, applied to a genuine past decision.", strong_answer_structure: "A strong answer describes the specific task, the reasoning process for evaluating whether decomposition was genuinely needed, and the actual decision made — including being willing to describe choosing the simpler option even when the more sophisticated one might have seemed more impressive.", weak_answer_example: "A vague answer, or one where the more complex solution was chosen without a clear articulated reason beyond it seeming more sophisticated.", follow_up_question: "Looking back, do you think you made the right call? What would make you decide differently next time?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "Multi-Agent Orchestration").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("n8n Module 5 (Knowledge, Enterprise Context & Multi-Agent Systems) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
