import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "94038ebb-022b-4f3a-851a-318fb3e90c0a"; // End-to-End AI Testing Project

const sections = [
  {
    section_type: "the_field",
    title: "What running AI QA the way a real QA team expects it delivered actually looks like",
    order_index: 0,
    content: "A real AI feature isn't just a model output — it lives inside a UI a real user interacts with, and testing it completely means testing both: does the interface correctly handle and display the AI's output (a traditional UI automation concern), and is the AI's output itself actually correct (the model-output-validation work from Modules 1-3). Testing only one half leaves a real gap — a UI that renders a fabricated answer beautifully is still shipping a fabricated answer, and a correct answer displayed with a broken loading state or a truncated response is still a broken user experience.\n\nThis capstone combines Playwright-style UI automation (interacting with the actual interface the way a user would) with the model-output validation discipline built across this course's earlier modules, into one integrated testing project — because that's what real AI QA work actually requires, not two separate, disconnected testing efforts that never talk to each other.\n\nFull bug reporting for AI findings closes the loop with the professional communication skill this whole course has been building toward: a QA finding about an AI feature needs the same rigor as any other bug report — reproducible steps, clear severity, and enough context for engineering to actually act on it — but adapted for AI-specific findings (a faithfulness violation, a fairness gap, a non-deterministic reproduction rate) that a traditional bug-report template doesn't naturally accommodate.",
  },
  {
    section_type: "mental_models",
    title: "How QA professionals think about complete AI feature testing",
    order_index: 1,
    content: "**1. Testing the AI model's output and testing the UI that presents it are both necessary, and neither is sufficient alone.** A correct model output rendered incorrectly and an incorrect model output rendered beautifully are both real, distinct bugs — a complete testing project catches both.\n\n**2. UI automation for an AI feature needs to account for non-determinism the same way model-output testing does.** A Playwright test asserting an exact text match against a non-deterministic AI response will be flaky by design — UI automation for AI features needs to check structural/behavioral correctness (did a response render, does it meet basic format expectations) separately from content correctness (which belongs to the model-output validation layer).\n\n**3. A bug report for an AI finding needs different fields than a traditional bug report, and omitting them makes the finding much less actionable.** Reproduction rate (not just \"reproducible: yes/no\"), the specific quality attribute violated (faithfulness, fairness, robustness), and the golden-dataset case (if applicable) that surfaced it are all AI-specific context a traditional template doesn't prompt for.\n\n**4. An end-to-end AI testing project is a genuine integration of two testing disciplines, not just running two separate test suites side by side.** The actual skill is designing the project so UI automation and model-output validation together produce one coherent picture of feature quality, not two disconnected reports.\n\n**5. \"Delivered the way a real QA team expects\" means professional communication as much as technical rigor.** A finding that's technically correct but poorly communicated (vague, unreproducible, no severity context) doesn't actually get acted on — this is the same lesson from the AI Security course track's governance module, applied to QA reporting specifically.\n\n**6. This capstone's real test is whether the finished project would genuinely be useful and trustworthy to an engineering team that receives it, not whether it technically covers both UI and model-output testing.**",
  },
  {
    section_type: "decision_framework",
    title: "How should I structure UI automation and bug reporting for a non-deterministic AI feature?",
    order_index: 2,
    content: "IF a UI automation test is checking for exact text content from a non-deterministic AI response THEN redesign it to check structural/behavioral correctness instead (a response rendered within an expected time, correct formatting, absence of an error state) and route content correctness to the model-output validation layer from Modules 1-3. BECAUSE an exact-text-match UI test against non-deterministic output will be flaky by design, and this flakiness is a design flaw, not something to tolerate. WATCH OUT FOR: some UI elements genuinely should be deterministic (a loading spinner, an error message for a failed request) — apply exact-match testing there, and reserve the looser, structural checking specifically for the parts of the UI displaying actual model-generated content.\n\nIF you're reporting an AI-specific quality finding THEN include reproduction rate (not just reproducible/not-reproducible), the specific quality attribute violated, and a link to the relevant golden-dataset case, alongside the traditional bug-report fields (steps, severity, expected vs. actual). BECAUSE these AI-specific fields are what make the finding actually actionable for engineering, and a traditional bug template doesn't prompt for them by default. WATCH OUT FOR: a finding reported without reproduction rate can be misread as either a rare fluke or a consistent failure when it's actually somewhere in between — always report the actual observed rate, even if it's based on a modest sample.\n\nIF you're designing the end-to-end testing project THEN structure it so UI automation and model-output validation results are connected — a UI test that surfaces an unexpected response should be able to feed into (or at least reference) the relevant model-output evaluation, rather than the two running as disconnected, unrelated efforts. BECAUSE the real value of an integrated project is a coherent picture of feature quality, not two separate reports that require manual correlation to be useful together.\n\nIF a finding is genuinely severe (a safety issue, a significant fabrication) THEN escalate it with clear urgency in the bug report, distinct from lower-severity findings, using the same severity-communication discipline established in the AI Security course track's governance module.",
  },
  {
    section_type: "workflow",
    title: "The real process for an end-to-end AI testing project",
    order_index: 3,
    content: "1. Identify the AI feature's actual UI touchpoints — where a user interacts with it, and what the UI needs to correctly render/handle.\n\n2. Build UI automation (Playwright-style) for structural/behavioral correctness of these touchpoints — loading states, error handling, response rendering — deliberately avoiding exact-content assertions against non-deterministic output.\n\n3. Build or reuse model-output validation (per Modules 1-3) for the actual content correctness of the AI's responses, using the golden dataset and eval tooling already established.\n\n4. Design the testing project so UI automation and model-output validation results connect — a UI-surfaced unexpected response links to or triggers the relevant content validation, rather than running as two disconnected efforts.\n\n5. Run the full project against realistic scenarios covering both dimensions, identifying findings from either layer.\n\n6. For each finding, write a complete bug report including traditional fields (steps, severity, expected vs. actual) plus AI-specific fields (reproduction rate, quality attribute violated, relevant golden-dataset case).\n\n7. Prioritize and escalate findings by real severity, distinguishing genuinely urgent safety/fabrication issues from lower-severity, cosmetic ones.\n\n8. Deliver the full project (test suite, findings, bug reports) in a form a real engineering team would find genuinely actionable and trustworthy.",
  },
  {
    section_type: "failure_modes",
    title: "How end-to-end AI testing projects actually go wrong",
    order_index: 4,
    content: "Failure 1 — UI automation with exact-text assertions against non-deterministic AI output, producing chronic flakiness.\nWhat: a Playwright test asserts the exact text of an AI-generated response, and fails intermittently because the response legitimately varies across runs, even when the feature is working correctly.\nHow it happens: exact-text assertion is the traditional, deeply ingrained default for UI testing, and it doesn't automatically prompt the recognition that AI-generated content needs a different assertion strategy.\nWhy beginners miss it: the test does correctly catch a genuinely broken UI state when it happens to also produce unexpected text — the flakiness against normal, legitimate variation isn't distinguished from a real failure without deliberately redesigning the assertion.\nDetect: check whether UI tests assert exact content from AI-generated responses, or structural/behavioral correctness separate from content.\nPrevent: redesign UI automation to check structural correctness (did a response render, is it within expected format bounds) and route content correctness testing to the model-output validation layer, per this module's decision framework.\nInterview question: \"Your Playwright test for an AI chat feature fails intermittently even though the feature works fine. What's the likely design flaw?\"\nReal-world consequence: chronically flaky UI tests that the team learns to ignore or work around, undermining trust in the entire UI automation suite, not just the flawed assertions.\n\nFailure 2 — Bug reports for AI findings using a traditional template with no AI-specific context.\nWhat: a finding is reported with traditional bug-report fields (steps, severity) but no reproduction rate, no specific quality attribute identified, and no connection to the relevant golden-dataset case, leaving engineering unable to actually triage or fix it efficiently.\nHow it happens: the traditional bug-report template is the deeply ingrained default, and it doesn't naturally prompt for the AI-specific fields this module's decision framework identifies as necessary.\nWhy beginners miss it: the traditional fields do provide real, useful information — the gap in AI-specific context isn't obvious unless someone has deliberately worked with the previous modules' distinction between a code regression and an AI-specific quality finding.\nDetect: check whether bug reports for AI findings include reproduction rate, the specific violated quality attribute, and golden-dataset linkage, or just traditional fields.\nPrevent: use an AI-specific bug report template including these fields explicitly, per this module's workflow.\nInterview question: \"You report a bug saying an AI feature 'sometimes gives wrong answers.' What's missing that would make this actionable for engineering?\"\nReal-world consequence: engineering can't efficiently triage or prioritize the finding without knowing how often it actually occurs or which specific quality dimension is affected, wasting investigation time re-deriving context the original report should have included.\n\nFailure 3 — UI automation and model-output validation running as two disconnected efforts with no integration.\nWhat: a Playwright suite and a DeepEval/RAGAS suite both exist and both run, but their results never connect — a UI test surfacing an unexpected response has no link to the relevant content-quality evaluation, requiring manual correlation to get a complete picture.\nHow it happens: the two testing disciplines are genuinely different technical domains, and building them separately (by different people, or at different times) without deliberate integration is the natural default.\nWhy beginners miss it: both suites individually do provide real value — the gap is specifically in the combined, coherent picture that integration would provide, which isn't obvious as missing until someone actually needs to correlate a UI finding with a content-quality one manually.\nDetect: check whether a UI-surfaced unexpected response can be traced to or connected with the relevant model-output validation result, or whether the two are entirely separate, uncorrelated reports.\nPrevent: design the testing project's structure deliberately for integration from the start, per this module's workflow, rather than building two independent suites and hoping they'll be useful together.\nInterview question: \"Your UI tests found an unexpected response and your eval suite separately flagged a faithfulness issue. How would you know if they're actually the same underlying problem?\"\nReal-world consequence: real findings that are actually connected (a UI bug caused by a content-quality issue, or vice versa) are investigated and reported as if unrelated, wasting effort and producing an incomplete, harder-to-act-on picture of the feature's actual quality.\n\nFailure 4 — Delivering findings in a form that's technically complete but not genuinely actionable or trustworthy to the receiving engineering team.\nWhat: the testing project's findings are technically accurate and complete, but presented in a way that's confusing, poorly prioritized, or missing the context an engineering team actually needs to act on them efficiently.\nHow it happens: the actual testing and finding-generation work can feel like the complete deliverable, with the final communication/delivery step treated as a lower-priority formality.\nWhy beginners miss it: the underlying findings are genuinely real and technically correct — the gap in actionability and trust isn't a technical problem with the findings themselves, so it's easy to overlook if the focus stays entirely on the testing work.\nDetect: ask whether an engineering team receiving this delivery could quickly understand what needs fixing, in what priority order, with what evidence — or whether they'd need significant additional back-and-forth to make sense of it.\nPrevent: treat the final delivery and communication as a real, required part of the project, per this module's workflow, with the same rigor as the testing work itself.\nInterview question: \"You've completed thorough AI QA testing with real findings. What would make this delivery something an engineering team actually trusts and acts on quickly, versus something they have to decode first?\"\nReal-world consequence: genuinely valuable testing work fails to translate into actual fixes, because the delivery wasn't structured in a way the receiving team could efficiently act on.",
  },
  {
    section_type: "debugging_playbook",
    title: "The end-to-end testing project isn't producing useful results — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"Our UI tests for the AI feature are chronically flaky.\"\nDiagnostic: check whether they assert exact content from AI-generated responses, rather than structural/behavioral correctness.\nFix: redesign to check structural correctness (rendering, format, error handling) separately from content correctness, routing content validation to the model-output evaluation layer.\n\nSymptom: \"Engineering keeps asking for more context on our AI bug reports before they can act on them.\"\nDiagnostic: check whether reports include reproduction rate, the specific quality attribute violated, and golden-dataset linkage, or just traditional bug-report fields.\nFix: adopt an AI-specific bug report template including these fields, and retroactively add this context to any currently-open findings if feasible.\n\nSymptom: \"We have both UI tests and content-quality evals, but we can't easily tell if a UI issue and a content-quality flag are actually the same underlying problem.\"\nDiagnostic: check whether the two testing efforts were designed with any integration/correlation mechanism, or built entirely independently.\nFix: redesign the project structure to connect UI-surfaced findings with relevant content-quality evaluation results going forward.\n\nSymptom: \"We delivered a complete set of testing findings but engineering seems confused about what to prioritize.\"\nDiagnostic: review the delivery format — does it clearly communicate priority, evidence, and actionable next steps, or just a raw list of findings?\nFix: restructure the delivery with clear prioritization and the AI-specific context needed for efficient triage.",
  },
  {
    section_type: "checklist",
    title: "End-to-End AI Testing Project Checklist",
    order_index: 6,
    content: "- [ ] UI automation checks structural/behavioral correctness, not exact content, for non-deterministic AI output\n- [ ] Model-output content validation (Modules 1-3) is integrated into the project, not run as a disconnected separate effort\n- [ ] UI-surfaced findings connect to or reference relevant content-quality evaluation results\n- [ ] Every bug report includes reproduction rate, the specific quality attribute violated, and golden-dataset linkage where applicable\n- [ ] Findings are prioritized and escalated by real severity, distinguishing urgent safety/fabrication issues from cosmetic ones\n- [ ] The final delivery is structured for genuine actionability and trust by the receiving engineering team, not just technical completeness",
  },
  {
    section_type: "template",
    title: "AI Feature Bug Report",
    order_index: 7,
    content: "Feature: ___\nSteps to reproduce: ___\n\nExpected behavior: ___\nActual behavior: ___\n\nReproduction rate: ___ (e.g., 3 out of 10 runs) — sample size: ___\n\nQuality attribute violated: accuracy / fairness / robustness / explainability / UI-structural — specifics: ___\n\nGolden-dataset case (if applicable): ___\n\nSeverity: ___ — reasoning: ___\n\nUI vs. model-output layer: where does this finding originate? ___\n\nSuggested next step for engineering: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Playwright documentation](https://playwright.dev/docs/intro) — the official reference for the UI automation half of this capstone.\n- [ISTQB: AI Testing (CT-AI) syllabus](https://www.istqb.org/certifications/artificial-intelligence-tester) (referenced in Module 1) — the formal profession's framework, worth revisiting for its coverage of test reporting for AI-specific findings.\n\nRecommended\n- [Playwright: Best practices](https://playwright.dev/docs/best-practices) — includes guidance on avoiding flaky assertions, directly relevant to this module's structural-versus-content testing distinction.\n\nReference\n- The AI Security course track's Module 4 (Governance & Review Process) — its severity-and-remediation reporting structure is a direct model for this module's AI-specific bug reporting discipline.\n\nAdvanced\n- Search for published QA team case studies on \"testing conversational AI\" or \"LLM feature QA processes\" — useful for seeing this module's integrated UI-plus-content testing approach applied at real organizational scale.",
  },
];

const exercises = [
  {
    level: "capstone",
    order_index: 0,
    title: "Capstone: a complete AI testing project covering UI automation and output validation",
    problem_statement: "Choose an AI feature with a real or mockable UI (a chat interface, a search-with-AI-summary feature — reuse work from earlier in this course or this catalog if available). Build: (1) Playwright-style UI automation testing structural/behavioral correctness (loading states, error handling, response rendering) without asserting exact AI-generated content, and (2) model-output content validation using your Modules 1-3 work (golden dataset, faithfulness/relevancy metrics, CI integration).\n\nDesign the project so the two layers connect — demonstrate at least one case where a UI-surfaced finding links to a relevant content-quality evaluation result. Write at least 2 complete bug reports using this module's AI Feature Bug Report template for real or realistic findings from your testing.",
    starter_context: "This capstone is a synthesis of the entire course — reuse your work from Modules 1-3 for the content-validation half rather than starting fresh.",
    hints: [
      "For the UI automation, deliberately test what happens when the AI response is slow, malformed, or the underlying call fails — these are genuinely deterministic UI states worth exact-match testing, distinct from the AI content itself.",
      "For your bug reports, make sure at least one genuinely includes a reproduction rate based on actual repeated testing (not a single occurrence reported as if it were consistent), and connects clearly to a specific quality attribute.",
    ],
    solution_notes: "A strong capstone submission has genuinely working UI automation that correctly avoids exact-content assertions against AI-generated text (while still testing deterministic UI states rigorously), real content validation connected to earlier course work, at least one demonstrated case of the two layers being genuinely connected (not just two independent reports), and complete, professionally-structured bug reports with real reproduction rates and AI-specific context. Submissions that build the two testing layers as entirely disconnected efforts, or that report findings without AI-specific context (reproduction rate, quality attribute), haven't met the actual bar this capstone and course are built around — AI QA delivered the way a real QA team would expect it, not two separate technical exercises.",
  },
];

const interviewQuestions = [
  { category: "project_defence", order_index: 0, question: "Walk me through the case in your capstone where a UI finding connected to a content-quality evaluation result. How did you design that connection?", what_is_tested: "Whether the integration between UI and content testing was genuine, not just two separate suites presented together.", strong_answer_structure: "Describe the specific mechanism used to connect the two layers (a shared identifier, a linked report, a UI test that triggers or references the relevant eval case) and the actual finding that demonstrated the connection working.", weak_answer_example: "\"I have a UI test suite and an eval suite\" — describes two separate things without addressing the actual integration this question asks about.", follow_up_question: "How would this integration scale if you had 50 different UI touchpoints for this feature?" },
  { category: "fundamentals", order_index: 1, question: "Why shouldn't a UI automation test assert the exact text content of an AI-generated response?", what_is_tested: "Understanding of the flakiness this design choice causes and the correct alternative approach.", strong_answer_structure: "Explain that AI-generated content can legitimately vary across runs even when the feature works correctly, so exact-text assertion produces chronic false-positive test failures — UI tests should check structural/behavioral correctness instead, with content correctness handled by the separate model-output validation layer.", weak_answer_example: "\"You just need to make the test more flexible\" — doesn't specify the actual correct testing strategy (structural vs. content separation).", follow_up_question: "What UI states or elements would you still test with exact-match assertions, even for an AI feature?" },
  { category: "scenario", order_index: 2, question: "You've found that an AI feature sometimes gives fabricated answers. How would you write this up as a bug report?", what_is_tested: "Application of the AI-specific bug report template, not a generic bug description.", strong_answer_structure: "Describe including reproduction rate (based on actual repeated testing, not a single occurrence), identifying the specific quality attribute (faithfulness), linking to the relevant golden-dataset case if applicable, and clear severity reasoning — not just 'sometimes gives wrong answers.'", weak_answer_example: "\"I'd write a normal bug report with steps to reproduce\" — misses the AI-specific fields this scenario requires.", follow_up_question: "How would you determine an accurate reproduction rate rather than guessing at 'sometimes'?" },
  { category: "debugging", order_index: 3, question: "A finding is technically correct and well-documented, but engineering seems confused about how urgent it is or what to do with it. What's missing?", what_is_tested: "Recognition that severity communication and actionability are distinct from technical correctness.", strong_answer_structure: "Identify that the report likely lacks clear severity reasoning and a specific suggested next step, and propose restructuring the delivery to clearly communicate priority and actionable guidance, not just technical findings.", weak_answer_example: "Assuming the finding itself must be wrong or incomplete, rather than recognizing the communication/delivery gap.", follow_up_question: "How would you validate that your revised delivery actually resolved the confusion?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to adapt a traditional QA practice (bug reporting, test automation, or another) for a genuinely new kind of system or technology.", what_is_tested: "Real or realistic experience with the specific kind of practice-adaptation this course track is built around.", strong_answer_structure: "A strong answer describes the specific traditional practice, what didn't transfer cleanly to the new context, and the concrete adaptation made — with evidence it actually worked better than blindly applying the old practice.", weak_answer_example: "A vague answer with no specific practice or adaptation described.", follow_up_question: "How did you communicate this adapted practice to a team used to the traditional version?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLM Evaluation & Testing").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AI LLM Testing Module 4 (End-to-End AI Testing Project) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
