import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "ac440e0d-ffc1-4a48-b089-00d5e276970a"; // Data & Model Pipelines

const sections = [
  {
    section_type: "the_field",
    title: "What a real, reproducible data-to-model pipeline actually looks like",
    order_index: 0,
    content: "A notebook that runs top-to-bottom once, on your machine, with your specific local files, is not a pipeline — it's a demonstration that something worked one time. A real pipeline is something that can be re-run, by someone else, on fresh data, and produce a consistent, traceable result. The gap between these two things is most of what this module is about.\n\nThe practical work is turning ad-hoc notebook cells into discrete, well-defined pipeline stages: data ingestion, cleaning, feature engineering, training, evaluation — each with clear inputs and outputs, each independently testable, and each connected to the experiment tracking discipline from Module 1 so every pipeline run produces a traceable record. This isn't about making things unnecessarily complicated; it's about making the pipeline something that survives contact with a second dataset, a second person, or six months of not looking at it.\n\nFeature engineering and data cleaning at scale surface real engineering problems that don't show up in small, clean tutorial datasets: operations that are fast on a thousand rows can be prohibitively slow on a million, transformations need to be fit on training data only and applied consistently to new data (the same leakage concern from the Data Science course track, now at pipeline scale), and the pipeline needs to handle genuinely messy, evolving real-world data without breaking every time something unexpected shows up.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about data and model pipelines",
    order_index: 1,
    content: "**1. A pipeline is defined by its stages having clear, explicit contracts — what goes in, what comes out.** If you can't describe a stage's input and output shape precisely, it's not really a well-defined pipeline stage yet, just a blob of code that happens to run in sequence.\n\n**2. Fit on training data, transform on everything else — always.** Any transformation that learns something from the data (a scaler's mean/variance, an encoder's category mapping) must be fit only on training data and then applied consistently to validation, test, and future production data — fitting on the full dataset before splitting is a leakage bug, the same one covered in the Data Science course track, but easier to introduce accidentally at pipeline scale.\n\n**3. A pipeline that works on a thousand rows may not survive contact with a million.** Operations that are fine at small scale (loading everything into memory, naive loops) can become the actual bottleneck or failure point at real production data volume — scale-awareness needs to be considered deliberately, not discovered as a surprise later.\n\n**4. Idempotency and reproducibility are related but distinct properties, and a real pipeline needs both.** Reproducibility means the same inputs produce the same outputs; idempotency means re-running the pipeline on the same inputs doesn't cause unintended side effects (duplicated records, corrupted state) — both matter for a pipeline that will actually run repeatedly, not just once.\n\n**5. Data quality issues are not edge cases to handle eventually — they're the default state of real data.** A pipeline built assuming clean, well-formed input will break on real data; one built with explicit validation and handling for missing values, unexpected types, and outliers is the actual professional baseline, not an advanced feature.\n\n**6. Evaluation is a pipeline stage too, not a separate, informal step done by eyeballing a printed number.** Treating evaluation with the same rigor (versioned, reproducible, tracked) as training is what makes the whole pipeline's output trustworthy, connecting directly back to Module 1's tracking discipline.",
  },
  {
    section_type: "decision_framework",
    title: "How should I structure this pipeline's stages and handle scale?",
    order_index: 2,
    content: "IF a transformation (scaling, encoding, imputation) learns any statistic or mapping from the data THEN fit it strictly on the training split only, and apply the fitted transformation consistently to validation, test, and future production data. BECAUSE fitting on the full dataset before splitting leaks information from validation/test into training, producing an overly optimistic and untrustworthy evaluation. WATCH OUT FOR: this is easy to get subtly wrong in a pipeline where the split happens after some shared preprocessing step — make sure the actual order of operations in your pipeline code matches this principle, not just your mental model of it.\n\nIF the real production data volume is meaningfully larger than what fits comfortably in memory on a typical development machine THEN design the pipeline stages to process data in a scalable way (chunked/batched processing, or a framework built for larger-than-memory data) from the start, rather than assuming it will \"probably be fine.\" BECAUSE discovering a memory or performance bottleneck only once real-scale data arrives is far more expensive to fix retroactively than designing for it from the start. WATCH OUT FOR: over-engineering for a scale you don't actually have yet adds unnecessary complexity — this is a real judgment call based on your actual current and near-term expected data volume, not a default to always build for massive scale.\n\nIF a pipeline stage's behavior with malformed, missing, or unexpected data hasn't been explicitly defined THEN add explicit validation and a defined handling strategy (reject, impute, flag for review) before considering that stage complete. BECAUSE real data will eventually contain something the \"happy path\" doesn't anticipate, and an undefined behavior there means an unpredictable, hard-to-debug failure later. WATCH OUT FOR: silently dropping or imputing problematic rows without logging that it happened hides a data quality signal that might actually be worth investigating, not just working around.\n\nIF the pipeline will be re-run repeatedly (scheduled retraining, repeated experimentation) THEN design it to be idempotent — re-running it with the same input shouldn't cause duplicated records or corrupted downstream state. BECAUSE a pipeline that behaves differently or destructively on a second run is much harder to operate safely and confidently than one that can simply be re-run to recover from a failure.",
  },
  {
    section_type: "workflow",
    title: "The real process for building a production-style data-to-model pipeline",
    order_index: 3,
    content: "1. Define the pipeline's stages explicitly: ingestion, validation/cleaning, feature engineering, train/validation/test split, training, evaluation — with a clear input/output contract for each.\n\n2. Implement data validation as an explicit, early stage: check for expected schema, types, and reasonable value ranges, with a defined handling strategy for anything that fails validation.\n\n3. Perform the train/validation/test split before fitting any data-dependent transformation, and structure the code so this ordering is enforced, not just assumed.\n\n4. Build feature engineering as a fit-on-train, transform-everywhere component, using tooling (like scikit-learn's Pipeline/ColumnTransformer, or an equivalent) that makes this pattern structurally enforced rather than manually remembered.\n\n5. Consider the pipeline's behavior at realistic production data scale, and use chunked/batched processing or a scale-appropriate framework if the expected volume genuinely warrants it.\n\n6. Connect every pipeline run to the experiment tracking discipline from Module 1 — code version, data version, configuration, and full evaluation metrics, recorded automatically as part of running the pipeline, not as a separate manual step.\n\n7. Test the pipeline's idempotency: run it twice on the same input and confirm it doesn't produce duplicated or corrupted results.\n\n8. Test the pipeline against deliberately messy or malformed sample data, confirming the validation and handling strategy from step 2 actually behaves as intended, not just in the happy-path case.",
  },
  {
    section_type: "failure_modes",
    title: "How data and model pipelines actually break",
    order_index: 4,
    content: "Failure 1 — Fitting a transformation on the full dataset before splitting, leaking information into evaluation.\nWhat: a scaler, encoder, or imputer is fit on the combined training and test data (often inadvertently, as part of a preprocessing step that runs before the split), causing the evaluation metric to be overly optimistic and untrustworthy.\nHow it happens: it's structurally easy to write preprocessing code that runs before the split step, especially when the pipeline evolved incrementally from an original notebook where this ordering wasn't deliberately enforced.\nWhy beginners miss it: the pipeline runs successfully and produces a plausible-looking metric — there's no error, just a subtly inflated number that looks like real success.\nDetect: explicitly trace the actual order of operations in the pipeline code and confirm the split happens strictly before any data-dependent transformation is fit.\nPrevent: use pipeline tooling that structurally enforces fit-on-train (like scikit-learn's Pipeline object) rather than manually sequencing steps in a way that's easy to get wrong.\nInterview question: \"Your pipeline's reported test accuracy is significantly higher than what you observe once deployed. What's your first hypothesis?\"\nReal-world consequence: a model that looks excellent in pipeline evaluation and performs meaningfully worse once deployed on genuinely new data — the classic leakage failure, now happening at pipeline scale rather than in a single notebook.\n\nFailure 2 — A pipeline that works on small sample data but fails or performs terribly at real production scale.\nWhat: an operation that's fast and memory-efficient on a small development sample becomes a genuine bottleneck, or fails entirely (out of memory, excessive runtime), once run against the real, much larger production dataset.\nHow it happens: development and testing naturally happen against a manageable sample, and scale characteristics aren't specifically considered unless deliberately tested.\nWhy beginners miss it: everything works correctly throughout development — the failure only appears once the pipeline actually meets real-scale data, often for the first time in a production or near-production context.\nDetect: explicitly test the pipeline (or at least profile its likely bottleneck operations) against data volume genuinely representative of production scale, not just a convenient development sample.\nPrevent: consider scale requirements deliberately during pipeline design, using this module's decision framework, rather than assuming small-scale success implies large-scale success.\nInterview question: \"Your pipeline runs in 2 minutes on your 10,000-row development sample. The real production dataset has 50 million rows. What would you specifically check before assuming it'll be fine?\"\nReal-world consequence: a pipeline that fails or becomes impractically slow the first time it's actually needed at real scale, often discovered under time pressure rather than during calm development.\n\nFailure 3 — No explicit data validation, so malformed input silently corrupts downstream results.\nWhat: unexpected or malformed data (a missing field, an out-of-range value, a type mismatch) flows through the pipeline without being caught, silently degrading feature engineering, training, or evaluation without any visible error.\nHow it happens: validation feels like extra work relative to just processing the data and moving forward, especially when initial development data happens to be clean.\nWhy beginners miss it: the pipeline runs without crashing, so there's no obvious signal that something is wrong — the degraded result just looks like a somewhat worse model, without an obvious traceable cause.\nDetect: deliberately inject known malformed or edge-case data into the pipeline and check whether it's caught and handled explicitly, or silently passes through and affects the result.\nPrevent: add an explicit, early validation stage with a defined handling strategy (reject, impute with a flag, log) for anything that fails validation.\nInterview question: \"How would you design a pipeline stage to handle a data field that's supposed to always be present but occasionally isn't, in real production data?\"\nReal-world consequence: a model quietly trained or evaluated on corrupted or malformed data, producing degraded and hard-to-diagnose results with no clear signal pointing back to the actual root cause.\n\nFailure 4 — A pipeline that isn't idempotent, causing corruption or duplication on re-runs.\nWhat: re-running the pipeline with the same input produces duplicated records, double-counted features, or otherwise corrupted state, because the pipeline wasn't designed to be safely re-runnable.\nHow it happens: idempotency isn't naturally top-of-mind when a pipeline is initially built and tested by running it once successfully.\nWhy beginners miss it: the first run works perfectly, and the idempotency gap only becomes apparent when the pipeline genuinely needs to be re-run — for recovery after a failure, or as part of a scheduled retraining cadence.\nDetect: deliberately run the pipeline twice on identical input and check whether the second run produces the same clean result or introduces duplication/corruption.\nPrevent: design pipeline stages (especially any that write to persistent storage) to be idempotent from the start — e.g., using upserts rather than blind inserts, or clearing/replacing output rather than appending to it.\nInterview question: \"Why does it matter whether your data pipeline is idempotent, specifically for a pipeline that will be run on a recurring schedule?\"\nReal-world consequence: a scheduled retraining pipeline that corrupts its own output data over successive runs, requiring a manual cleanup and undermining confidence in the automation.",
  },
  {
    section_type: "debugging_playbook",
    title: "The pipeline isn't producing trustworthy results — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"Evaluation metrics from the pipeline look great, but real-world/deployed performance is noticeably worse.\"\nDiagnostic: trace the exact order of operations in the pipeline and check whether any data-dependent transformation is fit before the train/test split, rather than strictly after it.\nFix: restructure the pipeline so all data-dependent fitting happens strictly on the training split, using enforced pipeline tooling rather than manual sequencing, and re-evaluate honestly.\n\nSymptom: \"The pipeline works fine in development but is unacceptably slow or fails outright on the real production dataset.\"\nDiagnostic: profile which specific stage is the actual bottleneck at real scale — it's often not where intuition first suggests.\nFix: redesign the identified bottleneck stage for the actual data volume (chunked processing, a more scale-appropriate library or approach), and re-test specifically against representative production-scale data, not just the original small sample.\n\nSymptom: \"A model trained through the pipeline performs oddly worse than expected, with no obvious cause.\"\nDiagnostic: inject known malformed or edge-case sample data into the pipeline and check whether it's silently passing through unhandled, potentially corrupting features or labels.\nFix: add explicit validation with a defined handling strategy at the point where the malformed data was found to pass through unnoticed.\n\nSymptom: \"Re-running the pipeline on the same input produced a different or corrupted result than the first run.\"\nDiagnostic: identify which stage's output changed or duplicated on the second run — this usually points to a non-idempotent write or state-mutation somewhere in the pipeline.\nFix: redesign that specific stage to be idempotent (upsert instead of append, clear-and-replace instead of accumulate), and re-verify with a deliberate double-run test.",
  },
  {
    section_type: "checklist",
    title: "Data & Model Pipeline Readiness Checklist",
    order_index: 6,
    content: "- [ ] Every pipeline stage has a clear, explicit input/output contract\n- [ ] All data-dependent transformations are fit strictly on the training split, enforced structurally, not just by convention\n- [ ] The pipeline has been tested (or at least profiled) against data volume representative of real production scale\n- [ ] Explicit data validation exists early in the pipeline, with a defined handling strategy for malformed or unexpected data\n- [ ] The pipeline has been tested for idempotency — running it twice on the same input doesn't duplicate or corrupt results\n- [ ] Every pipeline run is connected to experiment tracking (code, data, config, metrics), per Module 1's discipline\n- [ ] Evaluation is treated as a first-class, versioned pipeline stage, not an informal final step",
  },
  {
    section_type: "template",
    title: "Pipeline Stage Contract",
    order_index: 7,
    content: "Stage name: ___\n\nInput (exact shape/schema expected): ___\nOutput (exact shape/schema produced): ___\n\nData-dependent (needs fit-on-train handling)? Yes / No — if yes, what's fit: ___\n\nValidation performed at this stage: ___\nHandling strategy for invalid/malformed input: ___\n\nScale considerations: expected data volume ___, chosen approach for that volume ___\n\nIdempotency: what happens if this stage is re-run on the same input? ___\n\nHow this stage's run is tracked (linked to Module 1's experiment tracking): ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [scikit-learn: Pipeline and ColumnTransformer](https://scikit-learn.org/stable/modules/compose.html) — the standard tooling for structurally enforcing fit-on-train-transform-everywhere, directly relevant to this module's core leakage-prevention principle.\n- [scikit-learn: Common pitfalls](https://scikit-learn.org/stable/common_pitfalls.html) (referenced in the Data Science course track) — revisit specifically its data leakage section in the context of pipeline design at scale.\n\nRecommended\n- [ZenML: Pipelines documentation](https://docs.zenml.io/user-guides/starter-guide/create-an-ml-pipeline) — practical reference for structuring pipeline stages with the explicit contracts this module recommends.\n- [Great Expectations documentation](https://docs.greatexpectations.io/docs/home/) — a widely-used tool specifically for the data-validation stage this module emphasizes.\n\nReference\n- [Dask documentation](https://docs.dask.org/en/stable/) or [Polars documentation](https://docs.pola.rs/) — for teams whose real data volume genuinely exceeds comfortable in-memory pandas processing, referenced in this module's scale-decision framework.\n\nAdvanced\n- [Apache Airflow documentation](https://airflow.apache.org/docs/apache-airflow/stable/index.html) — for teams needing to orchestrate scheduled, idempotent pipeline runs at a scale beyond what a simple script can manage reliably.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Build a leakage-safe pipeline with scikit-learn's Pipeline object",
    problem_statement: "Using any dataset with both numeric and categorical features (the UCI Credit Card Default dataset from the Data Science course track works well), build a full pipeline using scikit-learn's Pipeline and ColumnTransformer: split first, then fit all preprocessing (scaling, encoding) exclusively within the pipeline object so it's structurally impossible to leak test data into fitting.\n\nDeliberately compare this against a 'wrong' version that fits preprocessing on the full dataset before splitting, and quantify the difference in reported evaluation metrics between the two approaches.",
    starter_context: "The comparison step is the actual point of this exercise — seeing the concrete size of the leakage effect, not just building the correct version.",
    hints: [
      "Use scikit-learn's Pipeline and ColumnTransformer specifically, since they structurally enforce the correct fit-on-train ordering rather than relying on you remembering to do it manually.",
      "The leakage effect may be modest on some datasets and larger on others — report your actual observed difference honestly, whatever it is, rather than expecting a specific dramatic result.",
    ],
    solution_notes: "A strong submission has a genuinely correct pipeline built with scikit-learn's Pipeline/ColumnTransformer, a genuinely 'wrong' comparison version with the leakage bug deliberately reintroduced, and an honest report of the actual observed metric difference between them — even if that difference turns out to be small on this particular dataset, correctly noting that the leakage risk (and its potential size) depends on the specific data and features involved.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Add data validation and test it against injected malformed data",
    problem_statement: "Extend a pipeline (your guided-exercise pipeline, or a new one) with an explicit data validation stage that checks for expected schema, reasonable value ranges, and flags or rejects rows that fail validation. Then deliberately inject at least 3 different kinds of malformed rows (a missing required field, an out-of-range value, an unexpected data type) into your input and confirm the validation stage correctly catches and handles each one.",
    starter_context: "You decide the specific handling strategy (reject, flag, impute) for each malformed case — justify your choice for each.",
    hints: [
      "Make sure your validation actually runs before the malformed data reaches feature engineering or training — validation added but not actually wired into the pipeline's execution order won't catch anything.",
      "Log or report when validation catches something, rather than silently discarding it — this connects back to the module's point about hidden data quality signals.",
    ],
    solution_notes: "A strong submission's validation stage correctly identifies and handles all three deliberately injected malformed cases, with each handling decision justified (why reject versus impute versus flag for this specific case), and with visible logging/reporting when validation catches something — rather than a validation step that exists in code but either isn't actually wired into the execution path or silently discards problems with no record.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Redesign a pipeline description for idempotency and scale",
    problem_statement: "You're given this description: \"Our nightly pipeline reads new customer records from a CSV export, appends engineered features to a growing feature table, retrains the model, and writes results to a dashboard. It's been running for two months, but sometimes the feature table has duplicate rows for the same customer, and it took 45 minutes last week when the CSV was unusually large.\"\n\nDiagnose the likely causes of both reported problems, and propose specific redesigns to fix each.",
    starter_context: "This is a diagnostic and redesign exercise based on the description alone.",
    hints: [
      "The duplicate-rows symptom is a strong, direct signal about idempotency specifically — connect it to what 'appends' as a write pattern implies about re-runs.",
      "The 45-minute runtime on an 'unusually large' CSV is worth investigating as a scale-sensitivity issue — consider what about the pipeline's current design might not scale linearly or gracefully.",
    ],
    solution_notes: "A strong submission correctly diagnoses the duplicate-rows issue as a non-idempotent write pattern (blind appends rather than upserts, likely triggered by retries or partial re-runs) and proposes switching to an upsert-based or clear-and-replace write pattern keyed on customer ID. It should diagnose the runtime issue as a likely scale-sensitivity problem in how the CSV is processed (e.g., loading everything into memory at once, or an inefficient join/lookup pattern) and propose a specific, scale-appropriate fix (chunked processing, a more efficient join strategy) rather than a vague 'optimize it' recommendation.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why must a transformation like a StandardScaler be fit only on the training split, not the full dataset?", what_is_tested: "Understanding of the leakage mechanism at the pipeline level, not just as an abstract rule.", strong_answer_structure: "Explain that fitting on the full dataset lets information from the test set (its mean, variance, or category distribution) influence the transformation applied to training data, producing an evaluation that no longer reflects genuine generalization to unseen data.", weak_answer_example: "\"It's just best practice\" without explaining the actual leakage mechanism.", follow_up_question: "How would scikit-learn's Pipeline object help prevent this mistake compared to manually sequencing preprocessing and splitting steps?" },
  { category: "applied", order_index: 1, question: "How would you decide whether a pipeline needs to be redesigned for larger-scale data processing, versus being fine as-is?", what_is_tested: "Practical judgment about scale-appropriate design, not a blanket always-optimize-for-scale rule.", strong_answer_structure: "Explain assessing the actual expected production data volume against the pipeline's current approach's realistic limits, and redesigning specifically when there's a genuine, foreseeable gap — not preemptively over-engineering for scale that isn't actually expected.", weak_answer_example: "\"You should always design for massive scale from day one\" — ignores the real cost of unnecessary complexity for data that will never reach that scale.", follow_up_question: "What specific signals would tell you your current pipeline design is about to hit a real scale problem?" },
  { category: "scenario", order_index: 2, question: "A scheduled retraining pipeline occasionally produces duplicate rows in its output feature table. What's your investigation and fix?", what_is_tested: "Recognition of a likely idempotency failure and a concrete remediation approach.", strong_answer_structure: "Investigate whether the pipeline's write pattern is a blind append (non-idempotent) rather than an upsert or replace pattern, especially around retries or partial re-runs, and redesign the write step to be idempotent.", weak_answer_example: "Assuming it's a random, unexplainable glitch rather than investigating the actual write pattern.", follow_up_question: "How would you test that your fix actually resolves the issue, rather than just hoping it does?" },
  { category: "debugging", order_index: 3, question: "A model trained through your pipeline performs unexpectedly poorly, and you suspect a data quality issue rather than a modeling issue. How do you investigate?", what_is_tested: "Diagnostic process for tracing a pipeline-level data quality problem.", strong_answer_structure: "Check whether the pipeline has explicit data validation, and if not, inspect a sample of the actual data that flowed through training for malformed values, unexpected types, or missing fields that might have silently degraded feature quality.", weak_answer_example: "Jumping straight to trying different model architectures without first investigating the data that fed into training.", follow_up_question: "What would you add to the pipeline to catch this category of issue automatically next time, rather than discovering it after the fact?" },
  { category: "behavioural", order_index: 4, question: "Describe a time a pipeline you built or worked on broke unexpectedly when it encountered real production-scale data or real-world messy data for the first time. What happened?", what_is_tested: "Real or realistic experience with the gap between development-scale testing and production reality.", strong_answer_structure: "A strong answer describes the specific failure (a scale bottleneck, an unhandled data quality issue), how it was diagnosed, and a concrete change made to testing or design practices afterward — testing against more representative data or scale earlier in development, for instance.", weak_answer_example: "A vague answer with no specific failure or no described change in how pipelines are tested going forward.", follow_up_question: "How do you now test a pipeline before considering it production-ready, given what you learned from that experience?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "MLOps Pipeline Design").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("MLOps Module 2 (Data & Model Pipelines) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
