import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "8829f707-b945-4632-9b6a-531a98a41ecf"; // Kubernetes & CI/CD for AI Systems

const sections = [
  {
    section_type: "the_field",
    title: "What deploying AI systems to Kubernetes actually looks like",
    order_index: 0,
    content: "A container that runs correctly on your machine solves a different problem than a service that needs to stay up, scale under real load, and recover automatically when something fails, at a client's actual scale. Kubernetes exists to solve that second problem — but it introduces real complexity of its own, and AI workloads specifically add a wrinkle most generic Kubernetes material doesn't cover well: GPU scheduling, which behaves very differently from ordinary CPU/memory scheduling.\n\nA working FDE deployment on Kubernetes involves real, specific decisions: how many replicas of the service should run, what resource requests and limits are correct for a workload that might spike unpredictably when an LLM call takes longer than usual, how autoscaling should actually trigger (CPU-based autoscaling is often the wrong signal for an LLM-bound service, where the bottleneck is usually waiting on an external API call, not local CPU), and — critically — what the deployment strategy is for rolling out a new version without breaking things for users currently mid-conversation with the agent.\n\nCI/CD ties this together: a pipeline that builds the container, runs real tests, pushes to a registry, and deploys — ideally with a rollback path that doesn't require a frantic manual intervention at 2am when a bad deploy makes it to production. The skill here isn't memorizing Kubernetes YAML syntax; it's understanding what each piece is actually protecting against, so a deployment can be debugged and reasoned about, not just copy-pasted from a template.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about Kubernetes and CI/CD for AI systems",
    order_index: 1,
    content: "**1. Resource requests and limits are promises, not suggestions.** A request tells the scheduler \"reserve at least this much\" (used for placement decisions); a limit caps what the container can actually use. Getting these wrong for an LLM-calling service — which can have bursty, unpredictable memory use while waiting on external calls — causes either wasted cluster capacity (over-requesting) or unpredictable evictions (under-requesting).\n\n**2. CPU-based autoscaling is usually the wrong signal for an LLM-bound service.** If your service spends most of its time waiting on an external API call rather than doing local computation, CPU usage stays low regardless of load — autoscaling on CPU will under-scale exactly when you need more capacity. Request-count or queue-depth-based autoscaling is usually a better fit.\n\n**3. GPU scheduling is fundamentally different from CPU/memory scheduling.** GPUs generally can't be fractionally shared the way CPU and memory can by default — a pod either gets a whole GPU (or a specific fraction via specialized device plugins) or none. Planning GPU workload placement requires understanding this constraint explicitly, not assuming it behaves like any other resource.\n\n**4. A rollout strategy is a statement about what \"acceptable risk\" means for this specific service.** Rolling deployments, blue-green, and canary releases trade off deployment speed against blast radius differently — the right choice depends on how costly a bad deployment would actually be for this specific service's users, not a one-size-fits-all default.\n\n**5. Rollback needs to be a tested capability, not a theoretical one.** A rollback strategy that's never actually been exercised is a rollback strategy you don't really have — the first time you need it under real pressure is the worst possible time to discover it doesn't work as expected.\n\n**6. CI/CD pipelines should make the safe path the easy path.** A pipeline that requires someone to remember to run tests, or to manually verify a health check before promoting a deploy, will eventually have someone skip that step under time pressure — the pipeline itself should enforce it.",
  },
  {
    section_type: "decision_framework",
    title: "How should I configure scaling and rollout strategy for this AI service?",
    order_index: 2,
    content: "IF your service is bound primarily by waiting on an external LLM API call rather than local computation THEN configure autoscaling based on request concurrency or queue depth rather than CPU utilization. BECAUSE CPU-based autoscaling won't detect the actual bottleneck, leading to under-provisioning exactly when demand is high. WATCH OUT FOR: some autoscaling setups default to CPU metrics without anyone deliberately choosing that — verify what signal is actually driving your HPA (Horizontal Pod Autoscaler) configuration rather than assuming it's appropriate.\n\nIF your workload genuinely requires GPU acceleration (local model inference, embedding generation at scale) THEN plan GPU resource requests explicitly, understanding that GPUs are typically allocated as whole units per pod unless you're using specific fractional-GPU device plugins. BECAUSE assuming GPUs behave like fractionally-shareable CPU resources leads to either resource contention or wasted capacity. WATCH OUT FOR: cluster autoscaling for GPU node pools is often slower and more expensive than CPU node pools — factor this into how aggressively you scale GPU workloads up and down.\n\nIF a bad deployment for this service would be low-cost to detect and roll back quickly (a well-monitored internal tool, for example) THEN a straightforward rolling deployment with good health checks is usually sufficient. BECAUSE the added complexity of blue-green or canary deployments isn't justified when the blast radius of a mistake is already small and recoverable. WATCH OUT FOR: \"low-cost to detect\" requires actually having the monitoring in place to detect it quickly — don't assume low cost without verifying you'd actually notice a regression fast.\n\nIF a bad deployment would be expensive (client-facing, hard to detect quickly, or costly per-incident) THEN use a canary or blue-green strategy that exposes the new version to a small fraction of traffic (or a shadow environment) before a full rollout, with an automated or fast-manual rollback trigger. BECAUSE this limits the blast radius of a bad deploy to a small, controlled fraction of real traffic instead of exposing everyone at once.",
  },
  {
    section_type: "workflow",
    title: "The real Kubernetes deployment and CI/CD workflow",
    order_index: 3,
    content: "1. Define resource requests and limits based on actual observed usage (from load testing or staging traffic), not guesses — under-provisioning causes instability, over-provisioning wastes cluster capacity.\n\n2. Choose the right autoscaling signal for this specific service's actual bottleneck (request concurrency for I/O-bound LLM-calling services, GPU utilization for local-inference services), rather than defaulting to CPU-based autoscaling.\n\n3. If GPU workloads are involved, explicitly plan node pool sizing and scheduling, accounting for GPU's typically whole-unit allocation model and the slower/more expensive autoscaling of GPU node pools.\n\n4. Build a CI pipeline that runs real tests and builds the container image automatically on every relevant change, pushing to a registry with a clear, traceable versioning/tagging scheme.\n\n5. Choose a rollout strategy (rolling, blue-green, canary) matched to the actual blast radius and detectability of a bad deployment for this specific service, using the decision framework above.\n\n6. Configure real health checks (readiness and liveness probes) that accurately reflect whether the service is actually able to serve traffic correctly, not just whether the process is running.\n\n7. Build and actually test a rollback path — trigger a deliberate bad deployment in a non-production environment and confirm the rollback mechanism genuinely restores service quickly.\n\n8. Set up monitoring and alerting tied to the rollout, so a canary or gradual rollout has a real, automated or fast-manual signal for whether to proceed or roll back — not just a hope that someone notices a problem in time.",
  },
  {
    section_type: "failure_modes",
    title: "How Kubernetes deployments for AI systems actually break",
    order_index: 4,
    content: "Failure 1 — CPU-based autoscaling on an I/O-bound LLM service.\nWhat: the service is under real load (many concurrent requests waiting on slow LLM API calls) but CPU usage stays low, so the autoscaler never triggers additional replicas, and requests queue up or time out.\nHow it happens: CPU-based autoscaling is the default, most commonly documented pattern, so it gets applied without considering whether it matches this specific workload's actual bottleneck.\nWhy beginners miss it: the autoscaler is technically working correctly according to its configured metric — the mismatch is between the metric chosen and what's actually constraining the service, which isn't obvious without specifically thinking through where the bottleneck is.\nDetect: during a load test, watch whether CPU usage actually correlates with real service degradation (increasing latency, queued requests) — if degradation happens while CPU stays low, this is the mismatch.\nPrevent: configure autoscaling based on request concurrency, queue depth, or a custom metric that actually reflects the service's real bottleneck.\nInterview question: \"Your LLM-calling service is degrading under load but your CPU-based autoscaler isn't adding replicas. What's happening?\"\nReal-world consequence: degraded service or timeouts during real traffic spikes, discovered in production because load testing didn't specifically check for this mismatch.\n\nFailure 2 — Treating GPU scheduling like ordinary CPU/memory scheduling.\nWhat: a deployment assumes GPUs can be fractionally shared across pods the way CPU cores can, leading to scheduling failures or unexpected resource contention.\nHow it happens: GPU resource requests get configured using the same mental model as CPU/memory, without accounting for GPU's typically whole-unit allocation behavior.\nWhy beginners miss it: CPU and memory intuitions transfer to most other Kubernetes resources, so it's a reasonable but incorrect assumption to extend that intuition to GPUs without checking.\nDetect: check whether pods requiring GPU are failing to schedule (pending indefinitely) even when the cluster appears to have available GPU capacity in aggregate.\nPrevent: explicitly research and account for how your specific cluster's GPU allocation model works (whole-GPU-per-pod by default, or fractional sharing only via specific device plugins) before planning workload placement.\nInterview question: \"Why might a pod requesting a fraction of a GPU fail to schedule even though the cluster has GPUs with available capacity?\"\nReal-world consequence: GPU-dependent workloads stuck in a pending state, wasting both engineering time diagnosing the issue and, often, the cost of provisioned-but-idle GPU nodes.\n\nFailure 3 — A rollback strategy that's never actually been tested.\nWhat: a bad deployment reaches production, and the team discovers — under real pressure, in the moment — that the rollback process doesn't actually work as assumed (a missing previous image tag, a database migration that isn't safely reversible, a manual step nobody remembered).\nHow it happens: rollback capability is built and assumed to work based on the deployment tooling's general design, without a deliberate test of an actual bad-deployment-and-rollback cycle.\nWhy beginners miss it: rollback mechanisms often look complete and correct on paper, and the gap only shows up under the specific conditions of a real incident.\nDetect: deliberately exercise a full bad-deployment-and-rollback cycle in a non-production environment before trusting it in production.\nPrevent: treat a tested rollback as a hard requirement before considering a deployment pipeline production-ready, not an assumed capability.\nInterview question: \"How do you know your rollback strategy actually works, rather than just assuming it does based on how the pipeline is configured?\"\nReal-world consequence: an extended, high-stress outage during a real incident because the assumed rollback path fails exactly when it's needed most.\n\nFailure 4 — Health checks that reflect process liveness but not actual service readiness.\nWhat: a liveness/readiness probe checks something trivial (the process is running, a basic HTTP endpoint returns 200) that doesn't actually verify the service can correctly serve real traffic — e.g., it doesn't check that the service can actually reach its LLM API or vector database dependency.\nHow it happens: a minimal health check is the fastest thing to implement, and it does technically satisfy Kubernetes' requirement for a readiness probe.\nWhy beginners miss it: the health check passes, so Kubernetes considers the pod ready and routes traffic to it — the gap is only visible when real requests start failing despite a \"healthy\" pod.\nDetect: check whether the health check would actually catch the specific failure modes that matter (a downstream dependency being unreachable, a misconfigured API key) or only the most trivial process-is-running case.\nPrevent: design health checks to verify the specific things that would actually prevent the service from correctly handling real requests, not just that the process started.\nInterview question: \"Your pods are all reporting healthy, but users are getting errors. What might be wrong with your health check design?\"\nReal-world consequence: Kubernetes routes real traffic to pods that are technically \"up\" but functionally broken, because the health check never actually verified the thing that broke.",
  },
  {
    section_type: "debugging_playbook",
    title: "The Kubernetes deployment isn't behaving — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"The service is degrading under load but the autoscaler isn't adding replicas.\"\nDiagnostic: check what metric the HPA is actually configured against, and whether that metric is correlated with the real bottleneck (often it isn't, for an LLM-calling service on CPU-based autoscaling).\nFix: reconfigure autoscaling to use request concurrency, queue depth, or another signal that actually reflects the service's real constraint.\n\nSymptom: \"A pod requiring a GPU is stuck in Pending, even though the cluster seems to have GPU capacity.\"\nDiagnostic: check the specific GPU allocation model of your cluster (whole-unit vs. fractional via device plugins) and whether the requested amount matches what's actually schedulable.\nFix: adjust the resource request to match the cluster's actual GPU allocation granularity, or provision additional GPU node capacity if genuinely needed.\n\nSymptom: \"A deployment went bad and the rollback didn't restore service the way we expected.\"\nDiagnostic: identify exactly which step of the rollback process failed — a missing previous image tag, an irreversible migration, a manual step someone forgot.\nFix: after resolving the immediate incident, fix the specific gap and then actually test the corrected rollback process end-to-end in a non-production environment before trusting it again.\n\nSymptom: \"Pods report healthy but users are experiencing real errors.\"\nDiagnostic: check exactly what the health check verifies — it's very likely checking process liveness without checking the specific dependency (LLM API reachability, vector database connectivity) that's actually failing.\nFix: redesign the readiness probe to check the specific dependencies that, if broken, would actually prevent the service from handling real requests correctly.",
  },
  {
    section_type: "checklist",
    title: "Kubernetes Deployment Readiness Checklist",
    order_index: 6,
    content: "- [ ] Resource requests and limits are based on real observed usage, not guesses\n- [ ] The autoscaling signal actually reflects this service's real bottleneck (not defaulted to CPU without consideration)\n- [ ] GPU workloads (if any) account for the cluster's actual GPU allocation model, not assumed fractional sharing\n- [ ] CI pipeline runs real tests and builds a clearly versioned/tagged image on every relevant change\n- [ ] Rollout strategy (rolling, blue-green, canary) matches the actual blast radius and detectability of a bad deployment for this service\n- [ ] Readiness and liveness probes verify actual service capability, not just process liveness\n- [ ] Rollback has been deliberately tested end-to-end in a non-production environment, not just assumed to work\n- [ ] Monitoring and alerting are in place to detect a bad rollout quickly, especially for canary/gradual strategies",
  },
  {
    section_type: "template",
    title: "Deployment Strategy Decision Record",
    order_index: 7,
    content: "Service: ___\n\nWorkload bottleneck (CPU-bound / I/O-bound waiting on external calls / GPU-bound): ___\nAutoscaling signal chosen and why: ___\n\nGPU requirements (if any) and cluster allocation model: ___\n\nBlast radius if this deployment goes bad: ___ (who's affected, how quickly would it be detected)\nRollout strategy chosen and why: ___ (rolling / blue-green / canary)\n\nReadiness probe checks: ___ (list the specific dependencies/behaviors verified)\n\nRollback process: ___ — last tested on: ___ (date, and result)\n\nMonitoring/alerting tied to this rollout: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Kubernetes: Horizontal Pod Autoscaling](https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/) — the official reference, including custom-metrics autoscaling relevant to I/O-bound services.\n- [Kubernetes: Managing GPUs](https://kubernetes.io/docs/tasks/manage-gpus/scheduling-gpus/) — the canonical reference for GPU scheduling behavior referenced throughout this module.\n\nRecommended\n- [Kubernetes: Rolling updates, and deployment strategies](https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#strategy) — the official deployment strategy reference; pair with a cloud provider's specific canary/blue-green tooling docs for a full implementation.\n- [GitHub Actions documentation](https://docs.github.com/en/actions) — for building the CI/CD pipeline half of this module's workflow.\n\nReference\n- [Kubernetes: Configure liveness, readiness, and startup probes](https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/) — the official reference for designing health checks that actually verify service readiness, not just process liveness.\n\nAdvanced\n- [Google SRE Book: Release Engineering](https://sre.google/sre-book/release-engineering/) — deeper treatment of the risk/speed trade-offs behind rollout strategy choices, applicable beyond any single cloud provider's tooling.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Choose the right autoscaling signal for three different services",
    problem_statement: "You have three services to configure autoscaling for: (1) an LLM-calling chatbot API that spends most of its time waiting on external API responses, (2) a local embedding-generation service that does heavy GPU computation with minimal waiting, (3) a lightweight internal API that does simple database lookups with fast, CPU-light responses.\n\nFor each, state what autoscaling signal you'd use (CPU, request concurrency/queue depth, GPU utilization, or another metric) and justify why, referencing where each service's actual bottleneck lies.",
    starter_context: "This is a reasoning exercise — you don't need to write actual Kubernetes YAML, just correctly identify and justify the right signal for each case.",
    hints: [
      "For service 1, ask: does CPU usage actually track with how loaded the service really is, given that it's mostly waiting on a network call?",
      "For service 2, GPU utilization is likely the most direct signal of real load, unlike the other two.",
    ],
    solution_notes: "A strong submission correctly identifies that service 1 (I/O-bound, waiting on external calls) needs request-concurrency or queue-depth-based autoscaling since CPU stays low regardless of real load; service 2 (GPU-bound computation) should scale on GPU utilization since that's the actual constraint; and service 3 (genuinely CPU-bound, fast local work) is one of the few cases where CPU-based autoscaling is actually the correct default choice, not a mismatched default.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Design a rollout strategy for two different-risk services",
    problem_statement: "You're deploying updates to two services: (1) an internal admin dashboard used by five people on your own team, with good logging and immediate feedback if something breaks, and (2) a client-facing agent that handles real customer conversations, where a bad deployment could mean incorrect responses reaching real customers before anyone notices.\n\nDesign a rollout strategy for each, using this module's decision framework, and justify the difference in your approach between the two.",
    starter_context: "You decide the specific mechanics (percentage of traffic for a canary, how you'd monitor it) — the key deliverable is the reasoning for why the two strategies differ.",
    hints: [
      "For service 1, consider whether the added complexity of canary/blue-green is actually justified given how quickly a problem would be noticed and how low-cost it is if it isn't.",
      "For service 2, be specific about what signal you'd use to decide whether to proceed past the canary stage or roll back — 'monitor it' isn't specific enough on its own.",
    ],
    solution_notes: "A strong submission recommends a simple rolling deployment for the low-risk internal tool (correctly recognizing that added rollout complexity isn't justified there) and a canary or blue-green strategy with a specific, concrete monitoring signal (error rate, response quality sampling, or a similar real metric) for the client-facing service — explicitly connecting the strategy choice to blast radius and detectability, as this module's decision framework requires, rather than applying the same strategy to both out of habit.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose a production incident from a description alone",
    problem_statement: "You're told: \"We deployed a new version of our client-facing agent service at 2pm. By 2:15pm, customers started reporting errors. Our health checks all showed the pods as healthy the entire time. We tried to roll back, but the previous image had already been garbage-collected from our registry, so the rollback failed too.\"\n\nIdentify every distinct failure in this scenario, explain the real-world consequence of each, and describe the specific fix for each one — both the immediate incident response and the process changes needed to prevent a repeat.",
    starter_context: "This is a pure diagnostic and remediation exercise based on the description given.",
    hints: [
      "There are at least two distinct, separate failures described here — a health-check design gap and a rollback/registry-retention gap. Don't treat them as one issue.",
      "Consider what image retention policy would have prevented the second failure, and what the health check should have actually verified to catch the first one sooner.",
    ],
    solution_notes: "A strong submission identifies two distinct failures: the health checks weren't actually verifying the specific thing that broke (a classic 'process liveness vs. real readiness' gap from this module's failure modes), and the registry's image retention/garbage-collection policy destroyed the rollback target before it was needed — a rollback strategy that was never actually tested would likely have caught this gap earlier. The fix should address both: redesigning the health check to verify real service capability, and establishing a minimum image-retention policy (e.g. always keep the last N successful deployments) as a hard requirement, plus actually testing the rollback path going forward per this module's checklist.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why is CPU-based autoscaling often the wrong choice for a service that primarily calls an external LLM API?", what_is_tested: "Understanding that autoscaling signals need to match the actual bottleneck, not default to the most common pattern.", strong_answer_structure: "Explain that CPU usage stays low while the service is waiting on a network call, so CPU-based autoscaling won't detect real load increases, leading to under-provisioning exactly when more capacity is needed.", weak_answer_example: "\"CPU autoscaling is just outdated\" without explaining the actual I/O-bound-versus-CPU-bound mechanism.", follow_up_question: "What signal would you use instead, and how would you implement it?" },
  { category: "applied", order_index: 1, question: "How would you decide between a simple rolling deployment and a canary deployment for a given service?", what_is_tested: "A real decision framework based on blast radius and detectability, not a fixed preference for one approach.", strong_answer_structure: "Reference how costly and how quickly detectable a bad deployment would be for this specific service — low-cost, quickly-detected issues favor simple rolling deployments; expensive or hard-to-detect issues justify the added complexity of a canary.", weak_answer_example: "\"Canary is always safer so I'd always use it\" — ignores that added complexity has real costs too, not justified for every service.", follow_up_question: "What specific signal would trigger an automatic rollback during a canary deployment?" },
  { category: "scenario", order_index: 2, question: "All your pods report healthy, but users are experiencing real errors. Walk me through your investigation.", what_is_tested: "Recognition that a health check design gap is a leading hypothesis, not an application-logic bug by default.", strong_answer_structure: "Check what the health check actually verifies — likely just process liveness — and hypothesize that it's not checking a specific dependency (an external API, a database connection) that's actually failing.", weak_answer_example: "Jumping straight to reviewing application code without first questioning whether the health check itself is inadequate.", follow_up_question: "How would you redesign the health check to catch this category of issue going forward?" },
  { category: "debugging", order_index: 3, question: "A GPU-requiring pod is stuck in Pending even though your cluster shows available GPU capacity in aggregate. What's your first hypothesis?", what_is_tested: "Understanding that GPU scheduling doesn't behave like ordinary fractional CPU/memory scheduling.", strong_answer_structure: "Hypothesize a mismatch between the requested GPU amount and the cluster's actual allocation granularity — GPUs are typically allocated as whole units per pod unless specific fractional-sharing device plugins are configured.", weak_answer_example: "Assuming it's a generic scheduling bug without considering GPU's distinct allocation model specifically.", follow_up_question: "How would you confirm this hypothesis, and what would the fix look like?" },
  { category: "system_design", order_index: 4, question: "Design a CI/CD pipeline for an AI service that makes it hard for someone to accidentally deploy a bad version to production, even under time pressure.", what_is_tested: "Ability to design a pipeline where the safe path is the default, easy path, not one that relies on manual discipline.", strong_answer_structure: "Propose automated testing gates that must pass before a build can be promoted, a deployment strategy appropriate to the service's risk profile, automated health-check-based rollback triggers, and a tested (not just assumed) rollback path — all enforced by the pipeline itself rather than relying on someone remembering a manual step.", weak_answer_example: "Describing a pipeline that relies on someone manually verifying things before promoting a deploy, without automated enforcement.", follow_up_question: "What would you do if the automated tests pass but a real regression still reaches production? How does your pipeline help you respond?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "AI Service Deployment").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("FDE Module 3 (Kubernetes & CI/CD for AI Systems) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
