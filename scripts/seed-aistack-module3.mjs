import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "677617b4-6792-47ba-897f-7e23925a8362"; // Serving & Infrastructure

const sections = [
  {
    section_type: "the_field",
    title: "What moving a notebook prototype into something that survives real traffic actually looks like",
    order_index: 0,
    content: "This module applies the containerization, API-layer, and scaling discipline covered in depth elsewhere in this catalog (the AI Engineering, MLOps, and LLMOps course tracks) specifically to the pipeline you built in this course's Module 2 — the full-stack angle here is making sure you can actually take a working orchestration-plus-data pipeline and make it a real, servable, observable API endpoint, not learning containerization from scratch again.\n\nThe specific full-stack skill is the transition itself: a pipeline that works when you run it directly in a notebook or script has implicit assumptions (it runs once, sequentially, with your local environment) that don't hold once it needs to serve concurrent real traffic behind an API. Making that transition correctly — wrapping the pipeline in a proper API layer, containerizing it with the established discipline, and adding basic observability — is what this module is actually testing.\n\nScaling patterns specifically matter here because a RAG-plus-generation pipeline has a genuinely different resource profile than a typical web API: retrieval (a vector search) and generation (waiting on an LLM call) have very different latency and resource characteristics, and a naive \"just add more instances\" scaling approach doesn't automatically account for that difference.",
  },
  {
    section_type: "mental_models",
    title: "How full-stack AI engineers think about serving and infrastructure",
    order_index: 1,
    content: "**1. A notebook script and a servable API are architecturally different things, not just a packaging difference.** A notebook runs once, sequentially, in a controlled environment; a servable API needs to handle concurrent requests, unexpected input, and failure gracefully — this is a real architectural transition, not just wrapping the same code in a web framework.\n\n**2. Retrieval and generation have different resource profiles, and scaling needs to account for both.** A vector search is typically fast and CPU/memory-bound; an LLM generation call is typically slower and I/O-bound (waiting on an external API) — treating the whole pipeline as one uniform unit for scaling purposes misses this real distinction.\n\n**3. Observability for a full-stack pipeline needs visibility into both layers, not just the API's overall response time.** Knowing the end-to-end latency doesn't tell you whether retrieval or generation is the bottleneck — this connects directly to the distributed-tracing principle established in the AI Engineering and LLMOps course tracks, now applied to distinguishing retrieval time from generation time specifically.\n\n**4. Containerizing a full-stack AI pipeline follows the same established discipline as any AI service** (multi-stage builds, no baked-in secrets, health checks) — the full-stack-specific consideration is making sure the health check actually verifies both layers are functioning, not just that the process is running.\n\n**5. \"Survives real traffic\" means tested under realistic concurrent load, not just confirmed to work for one request at a time.** A pipeline that works correctly for a single sequential test can still fail under concurrent load if it wasn't actually designed and tested for that.\n\n**6. The transition from prototype to servable API is where a lot of hidden assumptions surface.** Local file paths, synchronous blocking calls that don't scale under concurrency, and implicit single-user assumptions are all things that work fine in a notebook and break once real, concurrent traffic arrives.",
  },
  {
    section_type: "decision_framework",
    title: "How should I move this pipeline from prototype to servable API?",
    order_index: 2,
    content: "IF your pipeline currently runs as a sequential notebook or script THEN wrap it in a proper API layer (FastAPI-style) with explicit request/response handling and input validation before considering it servable. BECAUSE a notebook's implicit assumptions (single execution, controlled environment, no concurrent access) don't hold once real traffic needs to reach it, and this transition needs deliberate attention, not just a thin wrapper. WATCH OUT FOR: hidden local-environment assumptions (a hardcoded local file path, an assumption about working directory) that work fine in the notebook but break once the code runs in a different, containerized environment.\n\nIF your pipeline has meaningfully different resource profiles for retrieval versus generation THEN consider whether they should scale independently (e.g., separate services, or at least separate resource allocation within one service) rather than assuming uniform scaling handles both well. BECAUSE forcing two components with different bottlenecks (fast/CPU-bound retrieval, slower/I/O-bound generation) to scale as one undifferentiated unit can under-provision one while over-provisioning the other. WATCH OUT FOR: this differentiated scaling adds real complexity — for a small-scale application, a simpler unified deployment may genuinely be sufficient; apply this consideration when scale actually warrants it, per the general principle of not over-engineering for scale you don't have.\n\nIF you're designing observability for the served pipeline THEN instrument both retrieval and generation stages separately, not just the overall API response time. BECAUSE a single blended latency number can't tell you which layer is actually the bottleneck when something's slow, the same distributed-tracing principle established in the AI Engineering course track applied specifically to this two-layer pipeline. WATCH OUT FOR: building only overall-latency monitoring and discovering the gap only when you actually need to diagnose a slowdown and can't tell which layer is responsible.\n\nIF you're designing the health check for this service THEN verify both layers are actually functioning (retrieval returns something, generation completes), not just that the process is running. BECAUSE (per the AI Engineering course track's health-check discipline) a health check that only confirms process liveness will route real traffic to a pod where one layer is broken, since the health check never actually verified end-to-end functionality.",
  },
  {
    section_type: "workflow",
    title: "The real process for moving a pipeline from prototype to servable API",
    order_index: 3,
    content: "1. Wrap the pipeline in a proper API layer (FastAPI-style) with explicit request/response schemas and input validation, per the deployment discipline established in the MLOps course track.\n\n2. Audit the pipeline code for local-environment assumptions (hardcoded paths, working-directory dependencies) that won't hold in a containerized deployment.\n\n3. Containerize following the established multi-stage build, secrets-handling, and non-root-user discipline.\n\n4. Design a health check that verifies both retrieval and generation are actually functioning, not just that the process is running.\n\n5. Instrument observability for both layers separately — retrieval latency and generation latency as distinct metrics, not just overall response time.\n\n6. Assess whether the two layers' different resource profiles warrant differentiated scaling, per this module's decision framework, given your actual expected scale.\n\n7. Test the deployed service under realistic concurrent load, not just sequential single-request testing — this is where hidden assumptions from the original notebook prototype most commonly surface.\n\n8. Document the deployed service's architecture, resource profile, and observability setup for operational handoff.",
  },
  {
    section_type: "failure_modes",
    title: "How the prototype-to-servable-API transition actually breaks",
    order_index: 4,
    content: "Failure 1 — Hidden local-environment assumptions that break once the code runs in a different environment.\nWhat: a hardcoded local file path, an assumption about the working directory, or a dependency on something only present on the original development machine causes the containerized service to fail, despite the code working fine in the original notebook.\nHow it happens: a notebook or local script naturally accumulates small, implicit environmental assumptions during development that don't get surfaced until the code actually runs somewhere else.\nWhy beginners miss it: the code works correctly throughout local development — these assumptions are invisible until the code is actually run in a genuinely different environment (a container).\nDetect: test the containerized service in complete isolation from the original development environment, checking for any failure related to a missing file, path, or environmental assumption.\nPrevent: audit the pipeline code specifically for local-environment dependencies before containerizing, per this module's workflow, and test in a genuinely isolated environment early.\nInterview question: \"Your pipeline worked perfectly in your notebook but fails immediately when containerized. What's your first hypothesis?\"\nReal-world consequence: a deployment that fails immediately or behaves unexpectedly, requiring debugging time that a deliberate pre-containerization audit would have avoided.\n\nFailure 2 — Only monitoring overall API latency, unable to tell whether retrieval or generation is the bottleneck.\nWhat: when the service is slow, there's no way to tell from the available monitoring whether the retrieval step or the generation step is responsible, wasting investigation time.\nHow it happens: overall response-time monitoring is the simplest, most obvious thing to instrument first, and separately instrumenting each layer takes deliberate additional effort.\nWhy beginners miss it: overall latency monitoring does tell you something is slow — the gap in diagnostic value (which layer specifically) isn't obvious until you're actually trying to investigate a real slowdown.\nDetect: check whether current monitoring can actually answer \"was this specific slow request slow because of retrieval or generation,\" or only \"this request was slow overall.\"\nPrevent: instrument retrieval and generation latency as distinct metrics from the start, per this module's workflow, applying the AI Engineering course track's distributed-tracing discipline to this specific two-layer pipeline.\nInterview question: \"Your service's overall latency increased. How do you determine whether retrieval or generation is responsible?\"\nReal-world consequence: significant wasted investigation time during a real performance issue, because the available monitoring can't actually localize which layer is the bottleneck.\n\nFailure 3 — A health check that only confirms process liveness, not that both layers actually function.\nWhat: the health check passes as long as the process is running, even if retrieval or generation is actually broken (a misconfigured API key, an unreachable vector store), so traffic gets routed to a non-functional pod.\nHow it happens: a minimal health check (a basic HTTP 200 response) is the fastest thing to implement, and it does technically satisfy the requirement for a health check.\nWhy beginners miss it: the health check passes and the pod is marked healthy — the gap is only visible when real requests to that \"healthy\" pod start failing.\nDetect: check whether the health check actually exercises both retrieval and generation (even a lightweight version of each), or just confirms the process started.\nPrevent: design the health check to verify actual end-to-end capability, per the AI Engineering course track's health-check discipline, applied specifically to both layers of this pipeline.\nInterview question: \"Your pods all report healthy, but some requests are failing. What might be wrong with your health check design?\"\nReal-world consequence: Kubernetes (or an equivalent orchestrator) routes real traffic to pods that are technically \"up\" but functionally broken in one layer, because the health check never actually verified that layer.\n\nFailure 4 — Only testing sequentially, never under realistic concurrent load.\nWhat: the service is tested one request at a time during development, and issues that only manifest under concurrent load (a shared resource without proper locking, a connection pool exhaustion) go undiscovered until real production traffic hits it.\nHow it happens: sequential, one-at-a-time testing is the natural default during development, and setting up realistic concurrent load testing takes deliberate additional effort.\nWhy beginners miss it: the service works correctly for every individual test performed sequentially — concurrency-specific issues by definition don't show up under sequential testing.\nDetect: run a real concurrent load test (even a simple one, sending several simultaneous requests) before considering the service production-ready.\nPrevent: include concurrent load testing as a required, non-optional step in the deployment readiness process, per this module's workflow.\nInterview question: \"Your service passed all your sequential manual tests. Why might it still fail under real concurrent traffic?\"\nReal-world consequence: a service that appears fully tested and ready, but fails or behaves unpredictably the moment real concurrent production traffic arrives, exposing gaps that sequential testing never could have caught.",
  },
  {
    section_type: "debugging_playbook",
    title: "The deployed pipeline isn't behaving in production — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"The service fails immediately when containerized, despite working fine locally.\"\nDiagnostic: check for hardcoded local paths, working-directory assumptions, or other environment-specific dependencies in the pipeline code.\nFix: remove the environment-specific assumption, replacing it with a properly configured, environment-agnostic approach (environment variables, relative paths resolved correctly), and re-test in a genuinely isolated container.\n\nSymptom: \"The service is slow, but we can't tell whether retrieval or generation is responsible.\"\nDiagnostic: check whether current monitoring separately instruments each layer's latency, or only tracks overall response time.\nFix: add separate latency instrumentation for retrieval and generation, and re-investigate the slow requests with this new visibility.\n\nSymptom: \"Pods report healthy but some real requests are failing.\"\nDiagnostic: check exactly what the health check verifies — likely just process liveness, not actual retrieval/generation functionality.\nFix: redesign the health check to exercise both layers at a basic level, so a broken layer is correctly reflected as an unhealthy pod.\n\nSymptom: \"The service passed all our manual tests but is behaving unpredictably under real production traffic.\"\nDiagnostic: check whether the service was ever actually tested under realistic concurrent load, or only sequentially.\nFix: run a genuine concurrent load test now to surface the actual issue, and add this as a required step for future deployments.",
  },
  {
    section_type: "checklist",
    title: "Prototype-to-Servable-API Checklist",
    order_index: 6,
    content: "- [ ] The pipeline is wrapped in a proper API layer with explicit request/response schemas and input validation\n- [ ] Local-environment assumptions (hardcoded paths, working-directory dependencies) have been audited and removed\n- [ ] Containerization follows established multi-stage build, secrets-handling, and non-root-user discipline\n- [ ] The health check verifies actual retrieval and generation functionality, not just process liveness\n- [ ] Retrieval and generation latency are instrumented as separate metrics, not just overall response time\n- [ ] Scaling approach (unified vs. differentiated) is a deliberate decision matched to actual expected scale\n- [ ] The deployed service has been tested under realistic concurrent load, not just sequential requests\n- [ ] The service's architecture and observability are documented for operational handoff",
  },
  {
    section_type: "template",
    title: "Prototype-to-Production Readiness Review",
    order_index: 7,
    content: "Pipeline / feature name: ___\n\nAPI layer: framework ___, request/response schema ___, input validation ___\n\nLocal-environment assumptions audited and removed: ___\n\nContainerization: multi-stage build ___, secrets handling ___, non-root user ___\n\nHealth check: verifies retrieval? ___ verifies generation? ___\n\nObservability: retrieval latency metric ___, generation latency metric ___\n\nScaling approach: unified / differentiated — reasoning: ___\n\nConcurrent load test: performed? ___, result: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- The AI Engineering course track's Module 2 and 3 (Containerizing Agentic Systems, Kubernetes & CI/CD) — the deep-dive reference for the containerization and scaling discipline this module applies.\n- The MLOps course track's Module 3 (Deployment & Containerization) — direct reference for the API-layer and model-serving patterns applicable here.\n\nRecommended\n- [FastAPI: Concurrency and async/await](https://fastapi.tiangolo.com/async/) — directly relevant to correctly handling concurrent requests in the API layer this module builds.\n\nReference\n- [Locust documentation](https://docs.locust.io/) — a real, practical tool for the concurrent load testing this module requires before considering a service production-ready.\n\nAdvanced\n- The AI Engineering course track's Module 4 (LLMOps, Observability & Incident Response) — for extending this module's basic observability into full production incident-response readiness.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Wrap your Module 2 pipeline in a FastAPI service with layer-separated observability",
    problem_statement: "Take your RAG-backed pipeline from Module 2 and wrap it in a FastAPI service with a single /query endpoint. Instrument retrieval latency and generation latency as two separate, distinct metrics (even simple logged timestamps are sufficient for this exercise). Add a health check that actually exercises both retrieval and generation at a basic level, not just returns 200.\n\nSend 5 test requests and report the separate retrieval/generation latency for each.",
    starter_context: "Reuse your Module 2 pipeline directly — the point of this exercise is the serving/observability wrapper, not rebuilding the RAG logic.",
    hints: [
      "Make sure your health check genuinely attempts a lightweight retrieval and generation call, not just a hardcoded 200 response — this is the specific gap this module's failure modes describe.",
      "Report your latency numbers honestly, even if they reveal one layer is a clear bottleneck — that's a useful, real finding.",
    ],
    solution_notes: "A strong submission has a genuinely working FastAPI wrapper, a health check that actually exercises both layers (verifiable by deliberately breaking one layer and confirming the health check correctly reports unhealthy), and real, separately-reported retrieval/generation latency numbers across the 5 test requests — directly closing the observability and health-check gaps this module's failure modes identify.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Audit and fix local-environment assumptions before containerizing",
    problem_statement: "Review your Module 2 pipeline's code for any local-environment assumptions (hardcoded file paths, working-directory dependencies, local-only credentials) before containerizing it. Fix any found, then containerize it and confirm it runs correctly in complete isolation from your original development environment (e.g., run the container with a different working directory, or on a different machine/environment if possible).",
    starter_context: "This exercise specifically targets the hidden-assumption failure mode this module identifies as the most common cause of a working-locally-broken-in-container transition.",
    hints: [
      "Check specifically for relative file paths that assume a particular working directory, and any local file the code reads that wouldn't exist inside a fresh container.",
      "The real test is running the container genuinely isolated from your development setup — not just confirming the Dockerfile builds successfully.",
    ],
    solution_notes: "A strong submission identifies and documents at least the environmental assumptions it checked for (even if none were found — a documented, deliberate audit is the point, not necessarily finding a bug), and confirms the containerized version actually runs correctly in genuine isolation from the original development environment, not just that the build succeeded.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Design a differentiated scaling strategy for a high-traffic scenario",
    problem_statement: "Assume your RAG-backed feature from this module needs to scale to handle significant real traffic (design for this hypothetically — you don't need to actually deploy at this scale). Design whether retrieval and generation should scale as one unified service or be separated with independent scaling, with explicit reasoning tied to their actual different resource profiles.",
    starter_context: "This is a design exercise applying this module's differentiated-scaling decision framework to a concrete, if hypothetical, high-traffic scenario.",
    hints: [
      "Consider the actual resource profile difference: retrieval is typically fast and CPU/memory-bound; generation is typically slower and I/O-bound (waiting on an external LLM call) — how does this difference affect the case for separating them?",
      "Consider the added operational complexity of separating them into distinct services, and whether it's genuinely justified at the traffic scale you're designing for.",
    ],
    solution_notes: "A strong submission reasons explicitly about the resource-profile difference and its implications for scaling, and reaches a genuine, justified conclusion about whether differentiated scaling is warranted at the assumed traffic scale — correctly weighing the added complexity of separation against its actual benefit, rather than defaulting to either 'always separate' or 'always keep unified' without real reasoning.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why might a pipeline that works perfectly in a notebook fail immediately when containerized and deployed?", what_is_tested: "Understanding of hidden local-environment assumptions as the common root cause.", strong_answer_structure: "Explain that notebooks and local scripts naturally accumulate implicit environmental assumptions (local file paths, working-directory dependencies) during development that aren't obvious until the code actually runs in a genuinely different environment like a container.", weak_answer_example: "\"Containers are just harder to debug\" — doesn't identify the actual, specific root cause pattern.", follow_up_question: "How would you proactively find these hidden assumptions before they cause a deployment failure?" },
  { category: "applied", order_index: 1, question: "Why might you separately monitor retrieval latency and generation latency, rather than just overall API response time?", what_is_tested: "Understanding of the diagnostic value of layer-separated observability.", strong_answer_structure: "Explain that overall response time can't tell you which specific layer is the actual bottleneck when the service is slow, while separately instrumented metrics let you localize a performance problem to the responsible layer directly.", weak_answer_example: "\"More monitoring is always better\" — doesn't explain the specific diagnostic value this particular separation provides.", follow_up_question: "How would you use this separated data to decide where to invest optimization effort?" },
  { category: "scenario", order_index: 2, question: "Your service's pods all report healthy, but a meaningful fraction of real requests are failing. What's your first hypothesis about the health check?", what_is_tested: "Recognition that a health check might only verify process liveness, not actual functional capability.", strong_answer_structure: "Hypothesize that the health check only confirms the process is running, without actually exercising retrieval and generation — so a pod with a broken layer still reports healthy and continues receiving traffic.", weak_answer_example: "Assuming the health check must be fine since pods report healthy, without questioning what it actually verifies.", follow_up_question: "How would you redesign the health check to catch this category of issue?" },
  { category: "debugging", order_index: 3, question: "Your service passed every manual test you ran but behaves unpredictably under real production traffic. What's your first hypothesis?", what_is_tested: "Recognition that concurrency-specific issues don't surface under sequential testing.", strong_answer_structure: "Hypothesize that the service was never actually tested under realistic concurrent load, and that the unpredictable behavior is likely a concurrency-specific issue (a shared resource, a connection pool limit) that sequential testing couldn't have caught.", weak_answer_example: "Assuming it must be a random, unexplainable production anomaly without considering the sequential-versus-concurrent testing gap.", follow_up_question: "How would you set up a concurrent load test to try to reproduce this issue?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you moved a working prototype into a real, deployed service and discovered something that worked differently than expected.", what_is_tested: "Real or realistic experience with the prototype-to-production transition and its common gaps.", strong_answer_structure: "A strong answer describes the specific gap discovered (an environmental assumption, a concurrency issue, a health-check gap), how it was found, and what changed in the deployment process afterward.", weak_answer_example: "A vague answer with no specific gap or no described process change.", follow_up_question: "What would you check first, from day one, on your next prototype-to-production transition, given what you learned?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "AI Service Deployment").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AI Stack Module 3 (Serving & Infrastructure) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
