import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "557de0ed-2396-466e-bf9c-4d9024191721"; // Fine-Tuning Open Models

const sections = [
  {
    section_type: "the_field",
    title: "What deciding whether fine-tuning is actually worth it looks like",
    order_index: 0,
    content: "Fine-tuning is expensive in ways that aren't always obvious upfront: real compute cost, real data curation effort, real ongoing maintenance burden (a fine-tuned model needs to be re-tuned as requirements or underlying base models evolve), and real opportunity cost against the much simpler alternative of better prompting or RAG. The specific skill this module builds isn't \"how to fine-tune\" in isolation — it's the harder, more valuable skill of honestly deciding whether fine-tuning is the right investment for a given problem before spending the resources to find out.\n\nPrompting and RAG solve a real, large fraction of problems people reach for fine-tuning to solve, at a fraction of the cost and complexity — and the honest default position, matching this catalog's consistent \"prefer the simpler approach\" principle, is to exhaust prompting and RAG improvements first. Fine-tuning genuinely earns its cost specifically when a task requires a narrow, specialized behavior or format that prompting can't reliably achieve, or when there's a real cost/latency benefit to a smaller, specialized model matching a larger general model's performance on a narrow task.\n\nQLoRA and other parameter-efficient fine-tuning techniques matter specifically because they make fine-tuning meaningfully cheaper and more accessible than full fine-tuning — updating a small fraction of a model's parameters rather than the whole thing — which changes the actual cost/benefit calculation this module's decision framework is built around, but doesn't eliminate the real cost and complexity that still needs honest evaluation.",
  },
  {
    section_type: "mental_models",
    title: "How data scientists think about fine-tuning decisions",
    order_index: 1,
    content: "**1. Fine-tuning is a real, ongoing cost, not a one-time investment.** A fine-tuned model needs to be re-tuned as the base model ecosystem evolves and as task requirements shift — this ongoing maintenance burden is a genuine part of the cost calculation, not just the initial training cost.\n\n**2. Prompting and RAG should be exhausted first, as a deliberate default, matching this catalog's consistent simplicity-first principle.** The question isn't \"could fine-tuning help,\" it's \"has a well-executed prompting/RAG approach genuinely failed to achieve what's needed\" — this is a meaningfully higher bar than \"fine-tuning sounds like it would help.\"\n\n**3. Fine-tuning genuinely earns its cost in specific, identifiable scenarios: a narrow behavioral/format requirement prompting can't reliably achieve, or a real cost/latency win from matching a smaller specialized model to a larger general model's narrow-task performance.** These are checkable, specific justifications, not a vague \"it would probably be better.\"\n\n**4. QLoRA and parameter-efficient fine-tuning change the cost calculation but don't eliminate the need for honest evaluation.** Cheaper fine-tuning still requires real data curation, real training time, and real ongoing maintenance — \"cheaper\" doesn't mean \"free\" or \"always worth it now.\"\n\n**5. Evaluating tuned-versus-base-model performance needs the same evaluation rigor established throughout this catalog — a real golden dataset, separately-measured quality dimensions, not an informal \"it seems better\" impression.** A fine-tuning decision should be justified with the same evidence standard as any other model choice.\n\n**6. The decision to fine-tune should be revisited as circumstances change**, since a decision that correctly favored prompting/RAG at one point might genuinely change as the task's requirements or the available base models evolve.",
  },
  {
    section_type: "decision_framework",
    title: "Is fine-tuning actually worth it for this task?",
    order_index: 2,
    content: "IF the task's requirements can plausibly be met through better prompting (more specific instructions, few-shot examples) or RAG (better grounding in relevant context) THEN exhaust these approaches first, and treat fine-tuning as the fallback, not the first option. BECAUSE prompting and RAG are dramatically cheaper and faster to iterate on than fine-tuning, and a well-executed version of either genuinely solves a large fraction of tasks people initially assume need fine-tuning. WATCH OUT FOR: \"we tried prompting once and it didn't work great\" is not the same as \"we exhausted prompting\" — genuinely invest in prompt engineering (few-shot examples, clearer instructions, structured output) before concluding it's insufficient.\n\nIF the task requires a narrow, specific behavior or output format that prompting genuinely can't reliably achieve, even after real investment in prompt engineering THEN fine-tuning is a legitimate, well-justified option. BECAUSE this is exactly the scenario where fine-tuning provides real value prompting structurally can't — baking a specific behavior pattern into the model's weights rather than relying on in-context instruction. WATCH OUT FOR: verify this claim with real evidence (documented prompting attempts and their specific shortcomings), not just an assumption that fine-tuning would be better.\n\nIF there's a genuine, quantifiable cost or latency benefit to using a smaller, fine-tuned model matching a larger general model's performance on a narrow task THEN this is a legitimate business case for fine-tuning, provided the actual savings are estimated and compared against fine-tuning's real ongoing cost. BECAUSE this connects directly to Module 1's cost-consciousness principle — a smaller, cheaper model that performs as well on the specific narrow task is a genuine, measurable win, if the numbers actually support it. WATCH OUT FOR: this calculation needs to include fine-tuning's ongoing maintenance cost (re-tuning as the base model or requirements evolve), not just the one-time training cost versus ongoing inference savings.\n\nIF you decide to fine-tune THEN use parameter-efficient techniques (QLoRA or similar) by default rather than full fine-tuning, unless there's a specific, demonstrated reason full fine-tuning is necessary. BECAUSE parameter-efficient fine-tuning captures most of the benefit at a fraction of the compute cost, and this should be the default starting point, not an advanced option considered only after trying full fine-tuning.",
  },
  {
    section_type: "workflow",
    title: "The real process for deciding on and executing fine-tuning",
    order_index: 3,
    content: "1. Document a genuine, real attempt at solving the task through prompting and/or RAG, including what was tried and its specific shortcomings — this is the actual evidence needed before considering fine-tuning.\n\n2. If prompting/RAG genuinely falls short, identify the specific narrow behavior/format requirement, or quantify the specific cost/latency benefit, that justifies fine-tuning per this module's decision framework.\n\n3. Curate a real, representative training dataset for the narrow task — the same representativeness discipline established in the LLMOps and AI LLM Testing course tracks applies directly to fine-tuning data.\n\n4. Fine-tune using QLoRA or an equivalent parameter-efficient technique by default.\n\n5. Build a real evaluation harness (golden dataset, separately-measured quality dimensions) comparing the tuned model against the base model on the actual task, using the same evaluation rigor established throughout this catalog.\n\n6. Make the ship/don't-ship decision based on real evidence — does the tuned model genuinely outperform the base model (with prompting) enough to justify the fine-tuning investment.\n\n7. Document the decision and its reasoning, including the ongoing maintenance plan (when and how the fine-tuned model will be revisited as the base model ecosystem or task requirements evolve).\n\n8. Periodically re-evaluate whether fine-tuning remains the right choice as circumstances change — a newer base model's improved general capability might close the gap fine-tuning was originally justified by.",
  },
  {
    section_type: "failure_modes",
    title: "How fine-tuning decisions actually go wrong",
    order_index: 4,
    content: "Failure 1 — Reaching for fine-tuning without genuinely exhausting prompting and RAG first.\nWhat: fine-tuning is undertaken based on a quick, informal attempt at prompting that wasn't given real investment, when a more thorough prompting/RAG effort would likely have solved the problem at a fraction of the cost.\nHow it happens: fine-tuning can feel like the more \"serious\" or definitive solution, and a quick prompting attempt that didn't immediately work can be prematurely written off rather than genuinely iterated on.\nWhy beginners miss it: the fine-tuned model does end up working, so the investment feels justified in hindsight — the comparison against what a genuinely thorough prompting effort would have achieved is never actually made.\nDetect: check whether the decision to fine-tune was backed by documented, genuine prompting/RAG attempts and their specific shortcomings, or a quick, informal try.\nPrevent: require documented evidence of a real prompting/RAG effort and its specific limitations before approving a fine-tuning investment, per this module's decision framework.\nInterview question: \"Someone wants to fine-tune a model because 'prompting didn't work well.' What would you want to see before approving this investment?\"\nReal-world consequence: significant unnecessary cost and complexity from a fine-tuning investment that a genuinely thorough prompting effort would have avoided entirely.\n\nFailure 2 — Underestimating fine-tuning's ongoing maintenance cost, treating it as a one-time investment.\nWhat: the fine-tuning decision is justified against the one-time training cost alone, without accounting for the real, ongoing cost of re-tuning as the base model ecosystem evolves or task requirements shift.\nHow it happens: the training cost is the most visible, immediate cost consideration, while the ongoing maintenance burden is a less obvious, deferred cost that's easy to underweight in the initial decision.\nWhy beginners miss it: the initial fine-tuning decision looks justified based on the immediate cost/benefit comparison — the maintenance burden only becomes visible months later, when the base model ecosystem has moved on and the fine-tuned model needs real, additional investment to stay current.\nDetect: check whether the original fine-tuning decision's cost analysis included an estimate of ongoing maintenance, or only the one-time training cost.\nPrevent: include ongoing maintenance cost explicitly in the fine-tuning decision's cost analysis, per this module's decision framework.\nInterview question: \"You fine-tuned a model a year ago and it's now noticeably behind current base model capabilities. What does this reveal about the original decision process?\"\nReal-world consequence: an ongoing, underestimated maintenance burden that makes the fine-tuning investment less favorable in hindsight than the original one-time-cost analysis suggested.\n\nFailure 3 — Evaluating tuned-versus-base-model performance informally, without real evaluation rigor.\nWhat: the decision to keep or ship a fine-tuned model is made based on an informal impression (\"it seems better\") rather than a real, structured evaluation comparing the tuned model against the base model on a genuine golden dataset.\nHow it happens: after investing real effort in the fine-tuning process itself, a rigorous, potentially unflattering evaluation can feel like an unwelcome additional step rather than a required verification.\nWhy beginners miss it: the tuned model often does perform noticeably differently in a quick, informal check, which can feel like sufficient evidence — the gap in rigor (a real golden-dataset comparison) isn't obvious until a more careful evaluation reveals the informal impression was misleading.\nDetect: check whether the fine-tuning decision was backed by a real evaluation harness comparing tuned versus base model performance, or an informal impression.\nPrevent: apply the same evaluation rigor established throughout this catalog (a real golden dataset, separately-measured quality dimensions) to the tuned-versus-base comparison, per this module's workflow.\nInterview question: \"How do you know your fine-tuned model actually outperforms a well-prompted base model, rather than just seeming like it does?\"\nReal-world consequence: a fine-tuning investment that doesn't actually deliver the claimed improvement, discovered only after real evaluation reveals the original informal assessment was wrong.\n\nFailure 4 — Never revisiting a fine-tuning decision as circumstances change.\nWhat: a fine-tuned model continues in production long after a newer base model's improved general capability would have closed the original performance gap, without anyone revisiting whether fine-tuning remains the right choice.\nHow it happens: once a fine-tuning decision is made and deployed, it's easy to treat it as settled, without a deliberate practice of periodically re-evaluating against newer alternatives.\nWhy beginners miss it: the fine-tuned model continues working correctly, so there's no obvious signal prompting a re-evaluation — the missed opportunity (a newer base model that would now be simpler and equally effective) isn't visible without deliberately checking.\nDetect: periodically re-run the original tuned-versus-base comparison using the current best available base model, checking whether the original justification still holds.\nPrevent: build periodic re-evaluation into the fine-tuning decision's maintenance plan, per this module's workflow, rather than treating the original decision as permanent.\nInterview question: \"Your fine-tuned model was justified two years ago against the base models available then. How would you know if that justification still holds today?\"\nReal-world consequence: continued unnecessary maintenance investment in a fine-tuned model that a simpler, updated prompting approach against a newer base model could now replace.",
  },
  {
    section_type: "debugging_playbook",
    title: "Reassessing a fine-tuning decision — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"We're considering fine-tuning because prompting 'didn't work well' for this task.\"\nDiagnostic: check exactly how much genuine prompting/RAG effort was actually invested — specific instructions tried, few-shot examples attempted, structured output enforcement — versus a quick, informal attempt.\nFix: invest in a genuinely thorough prompting/RAG effort first, documenting specific shortcomings, before proceeding to a fine-tuning decision.\n\nSymptom: \"Our fine-tuned model from a while back seems to be falling behind current capability expectations.\"\nDiagnostic: compare current best available base models (with good prompting) against the aging fine-tuned model on the original task.\nFix: if a current base model now closes the gap, consider retiring the fine-tuned model in favor of the simpler, updated approach.\n\nSymptom: \"We're not confident our fine-tuned model actually outperforms a well-prompted base model.\"\nDiagnostic: check whether a real, structured evaluation (golden dataset, separate quality dimensions) was ever performed, or whether the assessment was informal.\nFix: build and run a real evaluation harness comparing both approaches directly, and let the actual evidence determine the decision.\n\nSymptom: \"Our fine-tuning investment is costing more in ongoing maintenance than originally expected.\"\nDiagnostic: review the original cost analysis for whether it included ongoing maintenance estimates.\nFix: for this instance, reassess whether the ongoing cost still justifies the investment; going forward, include maintenance cost explicitly in future fine-tuning decision analyses.",
  },
  {
    section_type: "checklist",
    title: "Fine-Tuning Decision Checklist",
    order_index: 6,
    content: "- [ ] A genuine, documented prompting/RAG attempt (not a quick informal try) preceded the fine-tuning decision\n- [ ] The specific narrow behavior/format requirement, or the specific cost/latency benefit, is explicitly identified and justified\n- [ ] Ongoing maintenance cost (re-tuning as base models/requirements evolve) is included in the cost analysis, not just one-time training cost\n- [ ] Parameter-efficient fine-tuning (QLoRA or equivalent) is the default approach, not full fine-tuning without specific justification\n- [ ] Tuned-versus-base-model performance is evaluated with real rigor (golden dataset, separate quality dimensions), not informal impression\n- [ ] The decision includes a plan for periodic re-evaluation as base models and requirements evolve",
  },
  {
    section_type: "template",
    title: "Fine-Tuning Decision Record",
    order_index: 7,
    content: "Task: ___\n\nPrompting/RAG attempts made and specific shortcomings: ___\n\nFine-tuning justification: narrow behavior/format requirement / cost-latency benefit — specifics: ___\n\nTechnique: QLoRA / other — reasoning: ___\n\nTraining data: size, representativeness: ___\n\nEvaluation results (tuned vs. base):\n| Metric | Base model | Tuned model |\n|---|---|---|\n| ... | ... | ... |\n\nOngoing maintenance cost estimate: ___\n\nDecision: proceed / don't proceed — reasoning: ___\n\nRe-evaluation date: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- Dettmers et al., [\"QLoRA: Efficient Finetuning of Quantized LLMs\"](https://arxiv.org/abs/2305.14314) (2023) — the foundational research paper for the parameter-efficient fine-tuning technique this module centers on.\n- [Hugging Face: PEFT documentation](https://huggingface.co/docs/peft/index) — the direct, practical reference for implementing QLoRA and other parameter-efficient fine-tuning approaches.\n\nRecommended\n- [OpenAI: Fine-tuning guide](https://platform.openai.com/docs/guides/fine-tuning) — practical guidance including OpenAI's own recommendation to try prompting first, directly reinforcing this module's core decision framework from a major provider's own documentation.\n\nReference\n- [Hugging Face: Transformers fine-tuning tutorial](https://huggingface.co/docs/transformers/training) — the direct technical reference for the actual fine-tuning implementation this module's build deliverable requires.\n\nAdvanced\n- Search for published case studies on \"prompting vs. fine-tuning\" cost-benefit analyses — useful for grounding this module's decision framework in real, measured organizational experience.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Attempt prompting thoroughly before considering fine-tuning",
    problem_statement: "Pick a narrow, specific task (e.g., converting informal customer messages into a strict, specific structured format your organization uses). Attempt to solve it through genuinely thorough prompting: try at least 3 distinct prompting strategies (clearer instructions, few-shot examples, structured output enforcement) and document the results and specific shortcomings of each.\n\nBased on this genuine effort, conclude whether fine-tuning would actually be justified for this task, or whether prompting is sufficient.",
    starter_context: "The genuineness of the prompting effort is the actual point of this exercise — a quick, single attempt doesn't count as 'exhausting prompting' per this module's decision framework.",
    hints: [
      "Make your 3 prompting strategies genuinely distinct approaches, not minor variations of the same basic prompt.",
      "Document specific, concrete shortcomings for any strategy that didn't fully work — 'it didn't work well' isn't specific enough to justify a fine-tuning decision.",
    ],
    solution_notes: "A strong submission genuinely attempts 3 distinct prompting strategies with real effort, documents specific, concrete shortcomings for each, and reaches a defensible conclusion about whether fine-tuning is actually justified — a conclusion that prompting is sufficient is just as strong an outcome as one that identifies a genuine, specific gap only fine-tuning could address, since the point is applying real rigor to the decision, not arriving at a predetermined answer.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Fine-tune a small open model and evaluate against the base model rigorously",
    problem_statement: "Using a small open model (accessible via Hugging Face) and QLoRA, fine-tune on a narrow, real task with a small curated dataset (even 50-100 examples is enough for this exercise). Build a real evaluation comparing the tuned model against the base model (with your best prompting from the guided exercise, if applicable) on a held-out test set, reporting results separately by relevant quality dimension.",
    starter_context: "This is a hands-on technical exercise — use a genuinely small model and dataset to keep it tractable, while still going through the real process rigorously.",
    hints: [
      "Make sure your held-out test set is genuinely separate from your training data, applying the same train/test discipline established in the Data Science course track.",
      "Report your results honestly, even if the fine-tuned model doesn't clearly outperform a well-prompted base model — that's a legitimate, useful finding.",
    ],
    solution_notes: "A strong submission actually completes a real QLoRA fine-tuning run and produces a genuine, rigorous comparison against the base model on a held-out test set, with honestly reported results — whether the fine-tuned model wins, ties, or loses against a well-prompted base model, the value is in the real, rigorous comparison process, not a predetermined 'fine-tuning wins' conclusion.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Design a re-evaluation plan for an aging fine-tuning decision",
    problem_factory: undefined,
    problem_statement: "You're told: \"We fine-tuned a model 18 months ago for a specific customer-support classification task. It's worked well, but we haven't compared it against current base models since then, and newer models have come out.\" Design a specific re-evaluation plan: what you'd compare, how, and what would justify keeping versus retiring the fine-tuned model.",
    starter_context: "This is a process-design exercise applying this module's periodic-re-evaluation principle to a realistic, aging fine-tuning decision.",
    hints: [
      "Consider using the original evaluation golden dataset (if it still exists and remains representative) as the basis for a fair, direct comparison against a current best base model with good prompting.",
      "Be specific about the actual decision criteria — what specific performance and cost difference would justify retiring the fine-tuned model versus keeping it.",
    ],
    solution_notes: "A strong submission proposes re-running the original (or an updated, still-representative) golden dataset evaluation against a current best base model with genuinely thorough prompting, and specifies concrete decision criteria (e.g., 'if the current base model with best-effort prompting comes within X% of the fine-tuned model's performance, retire the fine-tuned model given its ongoing maintenance cost') — directly applying this module's periodic re-evaluation principle with real, actionable specificity rather than a vague 'check periodically' recommendation.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why should prompting and RAG be exhausted before considering fine-tuning?", what_is_tested: "Understanding of the real cost asymmetry between these approaches.", strong_answer_structure: "Explain that prompting and RAG are dramatically cheaper and faster to iterate on than fine-tuning, and a well-executed version of either solves a large fraction of tasks people initially assume need fine-tuning — fine-tuning's real, ongoing cost should only be paid when there's genuine, demonstrated evidence prompting/RAG can't achieve what's needed.", weak_answer_example: "\"Fine-tuning is just more work\" — doesn't articulate the actual cost/benefit reasoning or the genuine-evidence requirement.", follow_up_question: "What would constitute genuine evidence that prompting has been exhausted for a given task?" },
  { category: "applied", order_index: 1, question: "What's a legitimate, well-justified reason to fine-tune a model, versus reaching for it prematurely?", what_is_tested: "Ability to distinguish genuine justification from a vague 'it would probably help' rationale.", strong_answer_structure: "Explain the two legitimate scenarios: a narrow behavior/format requirement prompting genuinely can't reliably achieve after real investment, or a quantified cost/latency benefit from a smaller specialized model matching a larger general model's performance on a narrow task.", weak_answer_example: "\"Fine-tuning is good when you want better performance\" — too vague to distinguish legitimate justification from premature adoption.", follow_up_question: "How would you verify a claimed cost/latency benefit is real before committing to a fine-tuning investment?" },
  { category: "scenario", order_index: 2, question: "A fine-tuned model deployed 18 months ago hasn't been re-evaluated against current base models. What's your concern, and what would you do?", what_is_tested: "Recognition that fine-tuning decisions need periodic re-evaluation, not permanent trust.", strong_answer_structure: "Express concern that a newer base model with good prompting might now match or exceed the fine-tuned model's performance, making the ongoing maintenance investment no longer justified, and propose a specific re-evaluation comparing current alternatives.", weak_answer_example: "\"If it's still working, there's no need to check\" — misses that 'still working' doesn't confirm it's still the best or most cost-effective choice.", follow_up_question: "What specific evidence would convince you to retire the fine-tuned model in favor of a current base model?" },
  { category: "debugging", order_index: 3, question: "A fine-tuning investment didn't deliver the expected improvement over a well-prompted base model. What would you investigate?", what_is_tested: "Diagnostic instinct toward checking whether the original decision was actually well-evidenced.", strong_answer_structure: "Investigate whether the original decision to fine-tune was backed by genuine, documented prompting attempts and their specific shortcomings, or whether it was based on an insufficient prompting effort that a more thorough attempt might have actually resolved.", weak_answer_example: "Assuming the fine-tuning technique itself must have been executed incorrectly, without questioning whether the original decision to fine-tune was well-justified in the first place.", follow_up_question: "What would you do differently in the decision process for the next fine-tuning consideration?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to make a build-versus-buy or invest-versus-simplify decision, weighing a more sophisticated but costly approach against a simpler one.", what_is_tested: "Real or realistic judgment applied to a genuine cost/complexity trade-off decision.", strong_answer_structure: "A strong answer describes the specific decision, the real evidence gathered to inform it, and the actual choice made — including being willing to describe choosing the simpler approach when it was genuinely sufficient.", weak_answer_example: "A vague answer with no specific decision or evidence-gathering process described.", follow_up_question: "How did you validate that your decision was actually the right one, after the fact?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const exercisesClean = exercises.map(({ problem_factory, ...e }) => e);
const { error: exErr } = await supabase.from("exercises").insert(exercisesClean.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "Fine-Tuning (LoRA / QLoRA)").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("DS+GenAI Module 3 (Fine-Tuning Open Models) seeded:", sections.length, "sections,", exercisesClean.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
