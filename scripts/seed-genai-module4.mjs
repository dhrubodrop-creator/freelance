import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "78e7a66e-caec-4059-9333-a6fa44b8bb21"; // Fine-Tuning & Deployment

const sections = [
  {
    section_type: "the_field",
    title: "What knowing when to fine-tune versus when prompting or RAG is enough — and executing either — actually looks like",
    order_index: 0,
    content: "This capstone brings together the entire course: Module 1's mechanistic understanding of what a model actually is, Module 2's prompting discipline, and Module 3's RAG/grounding architecture — into the final, practical decision every GenAI engineer eventually faces: is prompting and RAG genuinely sufficient for this task, or does it need fine-tuning, and can you actually execute either path competently.\n\nThis directly applies the Data Science with Generative AI course track's fine-tuning decision discipline — exhaust prompting and RAG first, with genuine documented evidence, before reaching for fine-tuning's real ongoing cost — now as the practical, hands-on capstone test: actually fine-tuning a small model with QLoRA, actually evaluating it rigorously against the base model, and actually deploying it behind a real API with the safety monitoring discipline established across this catalog's engineering tracks.\n\nDeployment and safety monitoring close the loop on the entire course's arc: understanding what's happening inside the model (Module 1) is what lets you reason about a fine-tuned model's actual behavior; reliable prompting (Module 2) and grounding (Module 3) are what a well-executed fine-tuning decision doesn't replace but complements; and real deployment discipline is what turns a fine-tuned model from a research artifact into something a real application can depend on.",
  },
  {
    section_type: "mental_models",
    title: "How engineers think about the fine-tune-versus-prompt-or-RAG decision and its execution",
    order_index: 1,
    content: "**1. Fine-tuning is the last resort in the capability toolkit this course has built, not the first reach.** Prompting (Module 2) and RAG/grounding (Module 3) should be genuinely exhausted first, with real evidence of their specific shortcomings, before fine-tuning's real ongoing cost is justified — the same discipline established in the Data Science with Generative AI course track.\n\n**2. Knowing when fine-tuning is genuinely warranted requires the mechanistic understanding from Module 1** — recognizing a task that requires baking a specific behavior pattern into the model's weights (something in-context prompting structurally can't achieve as reliably) versus a task prompting/RAG could handle with more investment.\n\n**3. Evaluating tuned-versus-base-model performance needs the same evidence-based rigor established throughout this catalog** — a real golden dataset, separately-measured quality dimensions, not an informal impression that the tuned model \"seems better.\"\n\n**4. Deployment for a fine-tuned model applies the same containerization, API layer, and monitoring discipline established across this catalog's engineering tracks, with the additional consideration that a fine-tuned model itself needs safety monitoring specific to its narrow task** — is it still performing well as real usage patterns evolve, the same drift-awareness principle established in the MLOps course track.\n\n**5. \"Can execute either\" is the actual, demonstrated skill this capstone tests.** Not just knowing the decision framework abstractly, but having genuinely built a reliable prompting/RAG solution and a fine-tuned alternative, and being able to compare them with real evidence.\n\n**6. This course's full arc — mechanism understanding, prompting, RAG, fine-tuning — is what makes someone a genuinely capable GenAI engineer, not fluency with any single technique in isolation.**",
  },
  {
    section_type: "decision_framework",
    title: "How should I decide between prompting/RAG and fine-tuning, and execute the chosen path safely?",
    order_index: 2,
    content: "IF a task's requirements can plausibly be met through better prompting (Module 2's discipline) or RAG-based grounding (Module 3's discipline) THEN genuinely exhaust these approaches first, documenting specific attempts and their real shortcomings, before considering fine-tuning. BECAUSE this is the same evidence-based discipline established in the Data Science with Generative AI course track, and a large fraction of tasks people initially assume need fine-tuning are actually solvable this way at a fraction of the cost.\n\nIF the task genuinely requires a narrow, specific behavior or output format that well-executed prompting and RAG can't reliably achieve, backed by documented evidence of the specific shortcoming THEN fine-tuning is a legitimate, well-justified next step. BECAUSE this connects your Module 1 mechanistic understanding directly to a real engineering decision — recognizing when the task genuinely needs the specific kind of change fine-tuning provides (baking a behavior into weights) versus what in-context prompting can achieve.\n\nIF you decide to fine-tune THEN use QLoRA or an equivalent parameter-efficient technique by default, and evaluate the tuned model against the base model (with your best prompting/RAG) using real, rigorous evaluation — a golden dataset, separately-measured quality dimensions — not an informal comparison. BECAUSE this is the evidence standard established throughout this catalog, applied to the actual before-versus-after fine-tuning decision.\n\nIF you're deploying the resulting model (fine-tuned or prompting/RAG-based) THEN apply the established containerization, API-layer, and monitoring discipline from this catalog's engineering tracks, with specific attention to safety monitoring — is the model's behavior on real production input consistent with what evaluation predicted, and is it still performing well as real usage evolves over time.",
  },
  {
    section_type: "workflow",
    title: "The real process for the fine-tuning-and-deployment capstone",
    order_index: 3,
    content: "1. Choose a narrow, real task and genuinely attempt to solve it through prompting (Module 2) and/or RAG (Module 3) first, documenting the actual results and any specific shortcomings.\n\n2. If a genuine shortcoming justifies fine-tuning, curate a real, representative training dataset for the narrow task.\n\n3. Fine-tune a small open model using QLoRA, applying Module 1's mechanistic understanding to reason about what the fine-tuning process is actually doing.\n\n4. Build a real evaluation harness comparing the tuned model against your best prompting/RAG attempt on a genuine held-out test set, reporting results by relevant quality dimension.\n\n5. Make an evidence-based decision: does the tuned model genuinely justify its fine-tuning investment over the prompting/RAG alternative for this specific task.\n\n6. Deploy the chosen solution (fine-tuned or prompting/RAG-based) behind a real API, applying established containerization and deployment discipline.\n\n7. Implement safety monitoring specific to the deployed model — tracking whether real production behavior matches evaluation predictions, and watching for drift over time.\n\n8. Document the full decision process and deployment, so it demonstrates the complete, evidence-based reasoning this course's entire arc has built toward.",
  },
  {
    section_type: "failure_modes",
    title: "How the fine-tune-versus-prompt-or-RAG decision and its execution actually go wrong",
    order_index: 4,
    content: "Failure 1 — Reaching for fine-tuning without a genuine, documented prompting/RAG attempt.\nWhat: fine-tuning is undertaken based on an assumption that prompting/RAG wouldn't work, without a real, documented attempt establishing that assumption as actually true.\nHow it happens: fine-tuning can feel like the more \"serious\" technical solution, especially after this capstone's deep dive into it, creating a pull to reach for it as the default rather than genuinely testing the simpler alternatives first.\nWhy beginners miss it: the fine-tuned model does end up working, which feels like validation — the comparison against what genuinely thorough prompting/RAG effort might have achieved is never actually made.\nDetect: check whether the decision to fine-tune was backed by a real, documented prompting/RAG attempt and its specific shortcomings, or an assumption.\nPrevent: require genuine documented evidence of prompting/RAG shortcomings before proceeding to fine-tuning, per this module's decision framework and the Data Science with Generative AI course track's discipline.\nInterview question: \"You're considering fine-tuning for a new task. What evidence would you want to see before agreeing this is actually necessary?\"\nReal-world consequence: unnecessary fine-tuning investment for a task a genuinely thorough prompting or RAG effort would have solved at a fraction of the cost.\n\nFailure 2 — Evaluating the tuned model against an informal impression of the base model's performance, not a genuine, rigorous comparison.\nWhat: the decision to keep or deploy the fine-tuned model is based on an informal sense that it \"seems better\" than the base model, rather than a real, structured evaluation on a genuine held-out test set.\nHow it happens: after investing real effort in the fine-tuning process, a rigorous, potentially unflattering comparison can feel like an unwelcome additional step.\nWhy beginners miss it: the tuned model often does perform noticeably differently in casual, informal checking — the gap in genuine rigor isn't obvious until a real evaluation reveals the informal impression was misleading.\nDetect: check whether the tuned-versus-base comparison was based on real evaluation against a held-out test set, or informal impression.\nPrevent: apply the same evaluation rigor established throughout this catalog, per this module's workflow, to the tuned-versus-base comparison.\nInterview question: \"How do you know your fine-tuned model actually outperforms your best prompting/RAG attempt, rather than just seeming like it does?\"\nReal-world consequence: a fine-tuning investment that doesn't actually deliver the claimed improvement, discovered only after real evaluation reveals the original informal assessment was wrong.\n\nFailure 3 — Deploying the fine-tuned model without the same containerization, API, and monitoring discipline as any other production service.\nWhat: the fine-tuned model's core capability work is genuinely solid, but it's deployed without proper containerization, an API layer, or real monitoring, treating it as a research artifact rather than a production service.\nHow it happens: the fine-tuning work (the genuinely hard, interesting technical part) can feel like the complete deliverable, with deployment discipline treated as a separate, lower-priority concern.\nWhy beginners miss it: the fine-tuned model genuinely does work correctly in a notebook or local test — the gap in production readiness isn't obvious until real deployment conditions expose it.\nDetect: apply this catalog's established deployment checklists explicitly to the fine-tuned model, checking whether real containerization, API design, and monitoring actually exist.\nPrevent: treat deployment discipline as an equally required part of \"shipping\" the fine-tuned model, per this module's workflow, not a separate, lower-priority concern.\nInterview question: \"Your fine-tuned model works perfectly in your evaluation notebook. What specifically still needs to happen before it can serve real production traffic reliably?\"\nReal-world consequence: a genuinely valuable fine-tuning investment that fails to function reliably once real users and real infrastructure conditions are involved, because the deployment half of the work was never actually done.\n\nFailure 4 — No safety monitoring specific to the fine-tuned model's ongoing behavior.\nWhat: the fine-tuned model is deployed with no ongoing monitoring of whether its real production behavior continues to match evaluation predictions, or whether real usage patterns have shifted in ways that degrade its narrow-task performance over time.\nHow it happens: the fine-tuning and evaluation work feels complete once the tuned-versus-base comparison is done, and ongoing monitoring specifically for the fine-tuned model can be deprioritized after deployment.\nWhy beginners miss it: the model continues producing output that looks reasonable — a specific degradation in narrow-task performance isn't visible without deliberately monitoring against the original evaluation baseline over time.\nDetect: check whether the deployed fine-tuned model has ongoing monitoring comparing real production behavior against evaluation predictions, or was left unmonitored after initial deployment.\nPrevent: implement safety monitoring specific to the fine-tuned model's real ongoing performance, per this module's workflow, connecting to the MLOps course track's drift-awareness principle.\nInterview question: \"Your fine-tuned model was validated against a golden dataset six months ago. How would you know if its real production performance has since degraded?\"\nReal-world consequence: a fine-tuned model's real-world performance silently degrades over time (as usage patterns shift beyond what the original training/evaluation data represented), undetected because no ongoing monitoring specific to the model's narrow task existed.",
  },
  {
    section_type: "debugging_playbook",
    title: "Your fine-tuning-and-deployment decision isn't holding up — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"We fine-tuned a model but aren't confident it was actually necessary.\"\nDiagnostic: check whether a genuine, documented prompting/RAG attempt preceded the fine-tuning decision, or whether fine-tuning was the first thing tried.\nFix: retroactively (or for future decisions) require documented evidence of prompting/RAG shortcomings before committing to fine-tuning.\n\nSymptom: \"We're not confident our fine-tuned model actually outperforms our best prompting/RAG attempt.\"\nDiagnostic: check whether a real, rigorous evaluation on a held-out test set exists, or whether the comparison was informal.\nFix: build and run a real comparison now, and make the deployment decision based on the actual evidence.\n\nSymptom: \"Our fine-tuned model works in evaluation but is fragile in production.\"\nDiagnostic: check whether real deployment discipline (containerization, API layer, monitoring) was actually applied, or whether the model was deployed as a research artifact.\nFix: apply this catalog's established deployment checklists explicitly, closing any real gaps found.\n\nSymptom: \"We're not sure if our fine-tuned model's real-world performance has degraded since deployment.\"\nDiagnostic: check whether ongoing monitoring compares real production behavior against the original evaluation baseline, or whether the model was left unmonitored after deployment.\nFix: implement this monitoring now, and re-run the original evaluation against current real data to check for actual degradation.",
  },
  {
    section_type: "checklist",
    title: "Fine-Tuning-and-Deployment Capstone Checklist",
    order_index: 6,
    content: "- [ ] A genuine, documented prompting/RAG attempt (using Module 2/3 discipline) precedes any fine-tuning decision\n- [ ] Fine-tuning uses QLoRA or an equivalent parameter-efficient technique by default\n- [ ] Tuned-versus-base comparison uses real, rigorous evaluation on a held-out test set, not informal impression\n- [ ] The deployment decision is evidence-based, not assumed\n- [ ] The chosen solution (fine-tuned or prompting/RAG-based) is deployed with real containerization, API layer, and monitoring discipline\n- [ ] Ongoing safety monitoring tracks real production behavior against evaluation predictions, watching for drift over time",
  },
  {
    section_type: "template",
    title: "Fine-Tuning-and-Deployment Capstone Record",
    order_index: 7,
    content: "Task: ___\n\nPrompting/RAG attempts made and specific shortcomings: ___\n\nFine-tuning justification: ___\nTechnique: QLoRA — training data size/representativeness: ___\n\nEvaluation results (tuned vs. best prompting/RAG):\n| Metric | Prompting/RAG | Fine-tuned |\n|---|---|---|\n| ... | ... | ... |\n\nDeployment decision: ___ — reasoning: ___\n\nDeployment: containerization ___, API layer ___, monitoring ___\n\nOngoing safety monitoring: what's tracked ___, baseline for comparison ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- The Data Science with Generative AI course track's Module 3 (Fine-Tuning Open Models) — the direct foundational reference for the fine-tuning decision discipline this capstone applies.\n- [Hugging Face: PEFT documentation](https://huggingface.co/docs/peft/index) (referenced in the Data Science with Generative AI course track) — the direct technical reference for QLoRA implementation.\n\nRecommended\n- The MLOps course track's Module 3 (Deployment & Containerization) — direct reference for the deployment discipline this capstone applies.\n- The AI Engineering course track's Module 4 (LLMOps, Observability & Incident Response) — direct reference for the safety monitoring discipline this capstone requires.\n\nReference\n- Dettmers et al., [\"QLoRA\"](https://arxiv.org/abs/2305.14314) (referenced in the Data Science with Generative AI course track) — the foundational research paper for the technique this capstone applies hands-on.\n\nAdvanced\n- Search for published \"prompting vs. fine-tuning\" case studies (referenced in the Data Science with Generative AI course track) — useful for grounding this capstone's decision framework in real, measured organizational experience.",
  },
];

const exercises = [
  {
    level: "capstone",
    order_index: 0,
    title: "Capstone: fine-tune a small model for a narrow task and deploy it behind an API",
    problem_statement: "Pick a narrow, real task. First, genuinely attempt to solve it with your best prompting (Module 2) and/or RAG (Module 3) work, documenting real results and specific shortcomings. If a genuine shortcoming justifies it, fine-tune a small open model with QLoRA on a curated dataset for this task.\n\nBuild a real evaluation comparing the fine-tuned model against your best prompting/RAG attempt on a held-out test set, reporting results by quality dimension. Deploy whichever solution the evidence actually supports behind a real API, with containerization and monitoring, including safety monitoring specific to tracking real production behavior against your evaluation baseline.",
    starter_context: "This capstone is the full synthesis of the course — genuinely exhausting prompting/RAG first is not optional preamble, it's the actual decision process being tested.",
    hints: [
      "Be honest if your evaluation finds that prompting/RAG was actually sufficient — that's a completely legitimate, valuable capstone outcome demonstrating the actual decision-making skill this course builds, not a failure to have fine-tuned.",
      "For deployment, apply the same rigor as this catalog's engineering-focused capstones — real containerization, a real API, and real monitoring, not a notebook dressed up as a deployed service.",
    ],
    solution_notes: "A strong capstone submission genuinely attempts prompting/RAG first with documented real results, makes an evidence-based fine-tuning decision (whether that's 'yes, justified' or 'no, prompting/RAG was sufficient'), if fine-tuning is done, produces a real rigorous comparison against the prompting/RAG baseline, and deploys the actually-justified solution with real containerization, API, and safety monitoring. Submissions that fine-tune without genuinely attempting the simpler alternatives first, or that deploy without real production discipline, haven't met the actual bar this capstone and course are built around — knowing when to fine-tune versus when prompting or RAG is enough, and being able to execute either competently.",
  },
];

const interviewQuestions = [
  { category: "project_defence", order_index: 0, question: "Walk me through your capstone's decision process. Why did you (or didn't you) end up fine-tuning?", what_is_tested: "Whether the decision was genuinely evidence-based, not a default choice.", strong_answer_structure: "Describe the specific prompting/RAG attempts made, their real results and shortcomings, and how this evidence led to the actual decision — with genuine detail, not a generic justification for whichever path was taken.", weak_answer_example: "\"I wanted to practice fine-tuning so I did it\" — doesn't demonstrate the genuine evidence-based decision process this capstone is meant to test.", follow_up_question: "If you had more time, what additional evidence would you want to further validate your decision?" },
  { category: "fundamentals", order_index: 1, question: "How does your Module 1 understanding of what a model actually is inform when fine-tuning is genuinely appropriate?", what_is_tested: "Whether the mechanistic understanding from earlier in the course actually connects to this practical decision.", strong_answer_structure: "Explain that fine-tuning changes the model's actual weights, baking in a specific behavior pattern in a way in-context prompting (which works within the existing weights) structurally can't achieve as reliably — this is the real, mechanistic reason fine-tuning is warranted specifically when a task needs that kind of durable, weight-level behavioral change.", weak_answer_example: "\"Fine-tuning is just for harder tasks\" — doesn't connect this decision to any genuine mechanistic understanding.", follow_up_question: "Give an example of a task where this mechanistic distinction clearly favors fine-tuning over prompting." },
  { category: "applied", order_index: 2, question: "How did you evaluate your fine-tuned model against your prompting/RAG baseline, and why does this comparison need to be rigorous rather than informal?", what_is_tested: "Application of this catalog's evidence-based evaluation discipline to the specific tuned-versus-base comparison.", strong_answer_structure: "Describe a real, structured evaluation against a held-out test set with metrics broken down by quality dimension, explaining that an informal impression risks a fine-tuning investment that doesn't actually deliver a genuine improvement.", weak_answer_example: "\"I tried a few examples and the fine-tuned version seemed better\" — describes an informal, non-rigorous comparison.", follow_up_question: "What would you do if your rigorous evaluation showed the fine-tuned model wasn't actually better?" },
  { category: "debugging", order_index: 3, question: "Your deployed fine-tuned model's real production behavior seems to have drifted from what your original evaluation predicted. What's your investigation?", what_is_tested: "Recognition that fine-tuned models need the same ongoing drift-monitoring discipline as any production ML system.", strong_answer_structure: "Propose comparing current real production input and output against the original evaluation baseline, checking whether real usage patterns have shifted beyond what the training/evaluation data represented, connecting to the MLOps course track's drift-monitoring discipline.", weak_answer_example: "Assuming the model must have a bug, without considering that real-world drift is a genuine, expected possibility for any deployed model over time.", follow_up_question: "How would you decide whether to re-tune the model or address the drift another way?" },
  { category: "behavioural", order_index: 4, question: "Describe the hardest judgment call you made in this capstone (or a similarly complex project) — a decision where the evidence was genuinely ambiguous.", what_is_tested: "Real engagement with the actual difficulty of this course's central decision-making skill.", strong_answer_structure: "A strong answer names a specific, genuinely ambiguous decision point, the evidence available (and its limits), and the actual judgment call made and its reasoning — not a retrospectively-obvious-seeming decision.", weak_answer_example: "A vague or overly confident answer that doesn't acknowledge any genuine ambiguity or difficulty in the decision.", follow_up_question: "Looking back, do you think you made the right call? What would change your answer?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill1 } = await supabase.from("skills").select("id").eq("name", "Fine-Tuning (LoRA / QLoRA)").single();
const { data: skill2 } = await supabase.from("skills").select("id").eq("name", "AI Service Deployment").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert([
  { module_id: MODULE_ID, skill_id: skill1.id },
  { module_id: MODULE_ID, skill_id: skill2.id },
]);
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("GenAI Module 4 (Fine-Tuning & Deployment) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 2 skill mappings.");
