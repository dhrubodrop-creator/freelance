import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "05271590-c990-4524-ad4d-27588f8cb31c"; // Shipping the Product

const sections = [
  {
    section_type: "the_field",
    title: "What shipping a generative-AI data product end to end, not just a notebook demo, actually looks like",
    order_index: 0,
    content: "Everything in Modules 1-3 — correctly positioning generative AI within a data science workflow, building hallucination-resistant analytics RAG, and making a disciplined fine-tuning decision — culminates here in the actual test: packaging it as a real product real users can access, with genuine evaluation backing the claim that it works, not a notebook someone has to run manually to see the results.\n\nLLM-as-judge and RAGAS-style evaluation get their final, integrated treatment here — this capstone is where the evaluation discipline built across Modules 1-3 (comparing model types, checking faithfulness/relevancy for analytics, comparing tuned versus base models) comes together into one coherent evaluation story for the shipped product as a whole, not separate evaluations for separate pieces that were never actually validated together.\n\nPackaging a generative-AI analytics feature for real users means applying the same deployment discipline established across this catalog's engineering-focused course tracks — containerization, an API layer, monitoring — specifically to a product whose core value proposition is exactly what Module 2 was built to protect: users trusting that the numbers it gives them are real. Shipping this product means proving, with real evidence, that trust is earned, not assumed.",
  },
  {
    section_type: "mental_models",
    title: "How data scientists think about shipping a complete generative-AI product",
    order_index: 1,
    content: "**1. A notebook demo and a shipped product are different deliverables with different actual requirements.** A notebook proves the concept works once, for you, on your machine; a shipped product needs to work reliably for real users, under real conditions, with real monitoring — the gap between these is real engineering work, not a formality.\n\n**2. The evaluation story for a shipped product needs to be integrated, not a collection of separate module-level evaluations that were never validated together.** The real question is whether the complete, end-to-end product — model positioning, RAG grounding, any fine-tuning decision — actually delivers a coherent, trustworthy result for a real user's real question.\n\n**3. Shipping a generative-AI product carries the same trust burden this entire course has been building toward: a wrong number or a fabricated claim is not a minor bug, it's a core failure of the product's actual value proposition.** This shapes what \"ready to ship\" actually means — evaluation rigor here isn't optional polish, it's the product's core promise.\n\n**4. Packaging for real users means real deployment discipline** — the same containerization, API layer, and monitoring established across this catalog's engineering tracks — applied specifically to a product where cost, latency, and reliability all matter to real users, not just to an internal demo audience.\n\n**5. \"Ships end to end\" is a genuinely different, higher bar than \"the individual pieces each work.\"** A capstone that demonstrates model positioning, RAG, and fine-tuning decisions each working in isolation hasn't actually shipped a product — shipping means the complete, integrated thing actually works and is deployed.\n\n**6. This capstone's real test is whether a genuinely skeptical user, asking real questions, would trust the product's answers** — not whether it works for the specific examples used during development.",
  },
  {
    section_type: "decision_framework",
    title: "How should I evaluate and package the complete generative-AI product before shipping?",
    order_index: 2,
    content: "IF the product combines model-positioning decisions (Module 1), RAG-based analytics (Module 2), and possibly a fine-tuning decision (Module 3) THEN evaluate the complete, integrated product end to end, not just each module's piece in isolation. BECAUSE a user's actual experience is with the complete product, and issues can emerge specifically at the integration points between pieces that individually-validated components wouldn't reveal. WATCH OUT FOR: assuming that because each individual piece passed its own module-specific evaluation, the integrated whole is automatically correct — this needs its own, real end-to-end validation.\n\nIF you're deciding whether the product is ready to ship THEN require real evidence across the full evaluation stack — faithfulness and relevancy for any RAG components, tuned-versus-base comparison if fine-tuning was used, and overall end-to-end correctness against a genuine golden dataset of real user-representative questions. BECAUSE this connects and requires all of the evaluation rigor established across Modules 1-3, integrated into one coherent readiness assessment, not a partial subset. WATCH OUT FOR: shipping based on evaluation results from an earlier module's narrower scope, without re-validating against the actual complete, integrated product.\n\nIF you're packaging the product for real users THEN apply the same deployment discipline (containerization, API layer, monitoring) established across this catalog's engineering course tracks, with particular attention to cost monitoring (per the LLMOps and AI Stack course tracks) given generative-AI product costs can scale unpredictably. BECAUSE a generative-AI analytics product genuinely needs the same production rigor as any other AI service, and skipping this because the focus has been on the data-science side leaves real operational risk.\n\nIF the product's evaluation reveals any residual risk (a category of question it handles less reliably, a cost profile that scales concerningly with usage) THEN document this honestly as part of the shipping decision, rather than presenting the product as fully solved. BECAUSE honest communication about real limitations is what this entire catalog's professional standard has been built around, and a generative-AI analytics product's trust burden makes this especially important here.",
  },
  {
    section_type: "workflow",
    title: "The real process for shipping the complete generative-AI product",
    order_index: 3,
    content: "1. Confirm each individual piece (model positioning from Module 1, RAG/analytics from Module 2, any fine-tuning decision from Module 3) is independently sound, per each module's respective checklist.\n\n2. Build an integrated golden dataset of real, user-representative questions covering the complete product's actual scope, not just each module's narrower test set.\n\n3. Run end-to-end evaluation against this integrated dataset, using LLM-as-judge and RAGAS-style metrics as appropriate, checking the complete pipeline's actual behavior, not individual components in isolation.\n\n4. Package the product with real deployment discipline: containerization, an API layer, and monitoring specifically covering cost (given generative-AI cost unpredictability) and the faithfulness/hallucination risk Module 2 was built to prevent.\n\n5. Test the deployed product under realistic conditions — real concurrent usage, real question variety — not just the development-time golden dataset alone.\n\n6. Honestly document any residual risk or limitation discovered, rather than presenting the product as fully solved.\n\n7. Ship with monitoring actively watched, connecting to the production-monitoring discipline established across this catalog's engineering tracks.\n\n8. Establish a plan for ongoing evaluation and potential re-assessment of the model-positioning and fine-tuning decisions as circumstances (new base models, evolving requirements) change, per Modules 1 and 3's respective principles.",
  },
  {
    section_type: "failure_modes",
    title: "How shipping a generative-AI data product actually goes wrong",
    order_index: 4,
    content: "Failure 1 — Treating individually-validated module pieces as sufficient evidence the integrated product works.\nWhat: the model-positioning decision, the RAG grounding, and any fine-tuning were each separately evaluated and passed their own module-level checks, but the complete, integrated product was never actually evaluated end to end together, missing integration-point issues.\nHow it happens: each module's evaluation work feels complete in isolation, and it's easy to assume the sum of individually-correct pieces is automatically a correct whole.\nWhy beginners miss it: each piece genuinely did pass its own evaluation — the gap specifically at integration points (how the pieces actually interact in the complete product) isn't visible without a real, dedicated end-to-end evaluation.\nDetect: check whether the complete, integrated product has its own dedicated evaluation, or only inherits confidence from each module's separate, narrower evaluation.\nPrevent: build and run a genuine end-to-end evaluation against an integrated golden dataset, per this module's workflow, treating this as a required, distinct step, not something the individual module evaluations already covered.\nInterview question: \"Each component of your generative-AI product passed its own evaluation. Why might the complete product still have real issues?\"\nReal-world consequence: a shipped product with a genuine integration-point bug (a mismatch between how RAG grounding and model-positioning decisions interact in practice) that no individual module's evaluation was positioned to catch.\n\nFailure 2 — Shipping without real deployment discipline, treating the notebook-stage work as sufficient.\nWhat: the product's core data-science and evaluation work is genuinely solid, but it's shipped without proper containerization, an API layer, or real monitoring, leaving it fragile in ways a notebook demo never had to confront.\nHow it happens: the data-science work (the genuinely hard, interesting part from this course's perspective) can feel like the complete deliverable, with deployment discipline treated as a lower-priority, separate concern.\nWhy beginners miss it: the notebook-stage product genuinely does work correctly in that context — the gap in production readiness isn't obvious until real deployment conditions (concurrent users, real infrastructure) expose it.\nDetect: apply the AI Engineering and MLOps course tracks' deployment checklists explicitly to this product, checking whether real containerization, API design, and monitoring actually exist.\nPrevent: treat deployment discipline as an equally required part of \"shipping,\" per this module's workflow, not a separate, lower-priority concern from the data-science work.\nInterview question: \"Your generative-AI analytics product works perfectly in your notebook. What specifically still needs to happen before real users can access it reliably?\"\nReal-world consequence: a genuinely valuable data-science capability that fails to function reliably once real users and real infrastructure conditions are involved, because the deployment half of \"shipping\" was never actually done.\n\nFailure 3 — No cost monitoring for a product whose usage-driven cost can scale unpredictably.\nWhat: the product ships without real-time cost monitoring, and usage-driven generative-AI costs (which can scale unpredictably with real user query volume and complexity) accumulate without anyone specifically tracking them.\nHow it happens: cost monitoring can feel like a lower-priority operational concern compared to the core evaluation and functionality work, especially if the initial development-time usage was low and cheap.\nWhy beginners miss it: development-time cost is genuinely low and unremarkable — the risk only becomes visible once real user volume and query complexity scale in ways development testing never exercised.\nDetect: check whether real-time cost monitoring exists for the shipped product, connecting to the LLMOps and AI Stack course tracks' cost-monitoring discipline.\nPrevent: build cost monitoring into the shipping checklist explicitly, per this module's workflow, not as an afterthought once real usage has already begun accumulating unexpected cost.\nInterview question: \"Your generative-AI analytics product's usage has grown 10x since launch. How would you know if this is causing a cost problem?\"\nReal-world consequence: significant, unexpected operating cost discovered only through a surprising bill, rather than through proactive, real-time monitoring that would have caught a cost trajectory problem early.\n\nFailure 4 — Presenting the shipped product as fully solved, without honestly documenting residual risk or limitations.\nWhat: the shipping decision and any accompanying communication present the product as fully reliable, without honestly noting any category of question it handles less well, or any known residual risk from the evaluation process.\nHow it happens: it's tempting to present a shipped product in its most confident light, especially after significant real investment in building and evaluating it.\nWhy beginners miss it: the product genuinely does work well for the majority of cases tested — the temptation to round this up to \"fully solved\" rather than honestly communicating the actual, more nuanced evaluation picture is a real, human pull.\nDetect: check whether the shipping documentation or communication includes any honest limitation or residual-risk disclosure, or presents the product as unqualified success.\nPrevent: require honest documentation of any residual risk found during evaluation as a required part of the shipping decision, per this module's decision framework, matching the professional honesty standard established across this catalog.\nInterview question: \"Your evaluation found the product handles 92% of question types reliably but struggles with a specific category. How would you communicate this in your shipping decision?\"\nReal-world consequence: users encounter the known-weaker category of question without any warning that it's a genuine limitation, damaging trust in the product when the reasonable expectation (that known limitations would be communicated) is violated.",
  },
  {
    section_type: "debugging_playbook",
    title: "Something's wrong with the shipped product — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"Each component passed its own evaluation, but users are reporting issues with the complete product.\"\nDiagnostic: check whether a real, end-to-end evaluation of the integrated product was ever performed, or whether confidence was inherited entirely from individual module-level evaluations.\nFix: build and run a genuine end-to-end evaluation against an integrated golden dataset, specifically looking for integration-point issues the individual evaluations couldn't have caught.\n\nSymptom: \"The product works great in development but is fragile or unreliable for real users.\"\nDiagnostic: check whether real deployment discipline (containerization, API layer, monitoring) was actually applied, or whether the notebook-stage work was shipped with minimal additional engineering.\nFix: apply the established deployment checklists from the AI Engineering and MLOps course tracks explicitly, closing any real gaps found.\n\nSymptom: \"Costs have grown unexpectedly since usage increased.\"\nDiagnostic: check whether real-time cost monitoring exists, or whether cost visibility only comes from periodic billing review.\nFix: implement real-time cost monitoring now, and investigate the specific usage pattern driving the increase.\n\nSymptom: \"Users are frustrated by a specific category of question the product handles poorly, and feel misled that this wasn't communicated.\"\nDiagnostic: check whether this limitation was known from evaluation but not honestly disclosed in the shipping communication.\nFix: communicate the known limitation clearly now, and establish honest residual-risk disclosure as a required, standard part of future shipping decisions.",
  },
  {
    section_type: "checklist",
    title: "Generative-AI Product Shipping Checklist",
    order_index: 6,
    content: "- [ ] Each individual component (model positioning, RAG/analytics, fine-tuning decision) is independently sound per its own module's checklist\n- [ ] The complete, integrated product has been evaluated end to end against a real, integrated golden dataset\n- [ ] Real deployment discipline (containerization, API layer, monitoring) is applied, not just notebook-stage work\n- [ ] Real-time cost monitoring exists, given generative-AI usage-driven cost unpredictability\n- [ ] The product has been tested under realistic conditions (concurrent usage, real question variety), not just development-time testing\n- [ ] Any residual risk or known limitation is honestly documented in the shipping decision and user-facing communication\n- [ ] A plan exists for ongoing evaluation and re-assessment of model-positioning and fine-tuning decisions as circumstances evolve",
  },
  {
    section_type: "template",
    title: "Generative-AI Product Shipping Record",
    order_index: 7,
    content: "Product name: ___\n\nModel positioning (Module 1): confirmed sound? ___\nRAG/analytics grounding (Module 2): confirmed sound? ___\nFine-tuning decision (Module 3, if applicable): confirmed sound? ___\n\nEnd-to-end evaluation: integrated golden dataset size ___, results ___\n\nDeployment: containerized ___, API layer ___, monitoring ___\nCost monitoring: ___\n\nRealistic-conditions testing: ___\n\nKnown limitations / residual risk (honestly documented): ___\n\nOngoing re-evaluation plan: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- The LLMOps course track's Module 2 (Evaluation at Scale) and Module 3 (Deployment & Infrastructure) — the direct, integrated reference for end-to-end evaluation and deployment discipline this capstone requires.\n- The AI Stack course track's Module 4 (Full-Stack Integration) — its cross-layer readiness checklist maps directly onto this module's integrated shipping requirements.\n\nRecommended\n- The AI LLM Testing course track's Module 4 (End-to-End AI Testing Project) — its complete testing-and-reporting discipline complements this module's shipping-readiness focus from a QA angle.\n\nReference\n- [RAGAS documentation](https://docs.ragas.io/) (referenced throughout this catalog) — for the final, integrated evaluation metrics this capstone applies to the complete product.\n\nAdvanced\n- Search for published case studies on shipping \"chat with your data\" or natural-language-analytics products at real companies — useful for seeing this module's complete shipping discipline applied at real production scale.",
  },
];

const exercises = [
  {
    level: "capstone",
    order_index: 0,
    title: "Capstone: ship a generative-AI-powered analytics product, evaluated and deployed",
    problem_statement: "Bring together your work from Modules 1-3 (model positioning decision, RAG-based analytics assistant, and — if you completed it — a fine-tuning evaluation) into one complete, deployed generative-AI analytics product. Build a real, integrated golden dataset covering the product's actual full scope, and run end-to-end evaluation against it.\n\nDeploy the product with real deployment discipline (containerization, API layer, monitoring including cost monitoring). Complete a full Generative-AI Product Shipping Record, including honest documentation of any residual risk or limitation discovered.",
    starter_context: "This capstone is a synthesis of the entire course — reuse your work from Modules 1-3 directly rather than building from scratch.",
    hints: [
      "Your integrated golden dataset should include questions that genuinely exercise the full pipeline (model-positioning decisions, RAG grounding, potentially fine-tuned behavior together), not just re-use each module's narrower test set unchanged.",
      "Be genuinely honest in your residual-risk documentation — a capstone that finds and honestly reports a real limitation demonstrates more of this course's actual lesson than one that claims a flawless result.",
    ],
    solution_notes: "A strong capstone submission has all Module 1-3 components genuinely integrated (not just individually present), a real end-to-end evaluation against a genuinely integrated golden dataset (not a re-run of each module's separate test set), real deployment with cost monitoring specifically addressing generative-AI's unpredictable cost profile, and honest, specific documentation of any residual risk or limitation found. Submissions that ship without real deployment discipline, or that present the product as flawless without honest limitation disclosure, haven't met the actual bar this capstone and course are built around — a shipped generative-AI data product, not a notebook demo dressed up as one.",
  },
];

const interviewQuestions = [
  { category: "project_defence", order_index: 0, question: "Walk me through your capstone's end-to-end evaluation. Why wasn't re-using each module's individual evaluation sufficient?", what_is_tested: "Whether the candidate understands why integrated evaluation is genuinely necessary, not redundant with earlier module work.", strong_answer_structure: "Explain that individual module evaluations couldn't catch integration-point issues (how the model-positioning, RAG, and any fine-tuning decisions actually interact in the complete product), and describe the specific integrated golden dataset and evaluation approach used to genuinely test the complete product.", weak_answer_example: "\"I just combined the results from each module's evaluation\" — misses that this doesn't actually test the integrated product together.", follow_up_question: "Did your end-to-end evaluation reveal anything the individual module evaluations missed?" },
  { category: "project_defence", order_index: 1, question: "What residual risk or limitation did your capstone honestly identify, and how did you communicate it?", what_is_tested: "Whether the candidate genuinely engaged with honest limitation disclosure rather than presenting an idealized result.", strong_answer_structure: "Describe a specific, real limitation found during evaluation, and how it was documented and communicated in the shipping decision — with concrete detail, not a vague acknowledgment.", weak_answer_example: "\"Everything worked great, no real limitations\" — an uncommon and somewhat suspicious result for a genuinely rigorous evaluation, suggesting insufficiently thorough testing.", follow_up_question: "How would a user encountering this limitation be informed, in your actual shipped product?" },
  { category: "fundamentals", order_index: 2, question: "Why does a generative-AI analytics product need real-time cost monitoring specifically, more than a typical software feature might?", what_is_tested: "Understanding of the unpredictable, usage-driven cost profile specific to generative-AI products.", strong_answer_structure: "Explain that generative-AI costs scale directly with usage volume and query complexity in ways that can be less predictable than typical software infrastructure costs, and a bug or usage spike can accumulate significant cost quickly if not monitored in real time.", weak_answer_example: "\"All software should be monitored for cost\" — true generally, but doesn't address why this is a particularly acute concern for generative-AI products specifically.", follow_up_question: "What specific metric would you track, and what threshold would trigger an alert?" },
  { category: "debugging", order_index: 3, question: "Your product's evaluation results looked great, but real users are reporting problems the evaluation didn't predict. What's your investigation?", what_is_tested: "Diagnostic approach connecting to the representativeness principle established across this catalog.", strong_answer_structure: "Investigate whether the golden dataset used for evaluation genuinely represents real user query patterns, comparing it against actual usage logs to check for a representativeness gap, per the AI LLM Testing course track's discipline.", weak_answer_example: "Assuming the evaluation must have been run incorrectly, without first questioning whether the golden dataset itself was representative.", follow_up_question: "How would you update your golden dataset based on what you find?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to decide whether something was genuinely 'ready to ship,' balancing real evidence against the pressure to present a polished, complete result.", what_is_tested: "Real or realistic judgment prioritizing honest readiness assessment over an idealized presentation.", strong_answer_structure: "A strong answer describes the specific project, the real evidence considered in the readiness decision, and the actual choice made — including being willing to describe delaying shipping or honestly flagging a limitation rather than presenting an overly polished picture.", weak_answer_example: "A vague answer with no specific project or honest tension described.", follow_up_question: "How did stakeholders respond to the honest limitation or delay, and did it change the outcome?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLM-Augmented Analytics").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("DS+GenAI Module 4 (Shipping the Product) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
