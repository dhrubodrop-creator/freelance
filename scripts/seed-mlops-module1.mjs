import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "7ebaa47c-07d7-4c4b-8c99-e48f78ed36a9"; // MLOps Foundations

const sections = [
  {
    section_type: "the_field",
    title: "What MLOps actually is, and why it exists as a discipline",
    order_index: 0,
    content: "Ask a team without MLOps discipline \"which exact version of the model is currently in production, what data was it trained on, and what were its evaluation metrics\" and you'll often get a shrug, or an answer pieced together from someone's memory and a few scattered notebook files with names like model_v3_final_ACTUALLY_final.ipynb. This isn't a hypothetical failure mode — it's the default state of ML work without deliberate process, and it's exactly the problem MLOps exists to solve.\n\nMLOps is DevOps discipline applied to the specific lifecycle of machine learning: not just code, but data, and not just one deployment, but a model that needs to be retrained, re-evaluated, and potentially rolled back repeatedly over its lifetime. The traditional DevOps concerns (version control, CI/CD, reproducibility, monitoring) all apply, but ML adds a genuinely new dimension — the model's behavior depends on the data it was trained on, not just the code that trained it, and that data changes over time in ways that source code doesn't.\n\nExperiment tracking is the foundational practice this module centers on, and it's foundational for a real reason: without it, every other MLOps practice (reproducible pipelines, safe deployment, informed retraining decisions) is built on sand. If you can't reliably answer \"what exactly produced this model, and how did it perform,\" you can't make a confident decision about whether to trust it, retrain it, or roll it back — you're just guessing with extra steps.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about the ML lifecycle",
    order_index: 1,
    content: "**1. A model is defined by three things together: code, data, and configuration — and all three need to be tracked, not just the code.** Traditional software version control handles code well; ML work needs the same discipline extended to the dataset version and hyperparameters that actually produced a given model.\n\n**2. 'It worked once' and 'it's reproducible' are different claims, and only the second one is trustworthy.** A model that can't be regenerated from a recorded configuration — because the exact data snapshot, code version, or parameters weren't tracked — isn't a real, dependable artifact, no matter how good its one-time results looked.\n\n**3. The ML lifecycle doesn't end at deployment — it's a loop, not a line.** Data changes, the model's real-world performance drifts, and retraining is a recurring, expected event, not an exception. MLOps practices need to support this ongoing loop, not just a one-time \"ship it\" moment.\n\n**4. Experiment tracking exists to answer a question you will definitely be asked later: \"why does this model behave the way it does, and can we trust it?\"** Without a track record, that question has no good answer beyond memory and guesswork.\n\n**5. Comparing models fairly requires comparing them on recorded, identical conditions.** Without tracked experiments, \"model B is better than model A\" is often not actually a verifiable claim — it might just reflect a different data split, a different evaluation script, or an untracked change nobody remembers making.\n\n**6. MLOps maturity is incremental, not all-or-nothing.** A team doesn't need a full enterprise platform on day one — starting with disciplined experiment tracking and reproducible basics is real, valuable progress, and more sophisticated practices (automated retraining, full CI/CD for models) build on that foundation rather than replacing the need for it.",
  },
  {
    section_type: "decision_framework",
    title: "What actually needs to be tracked, and how rigorously?",
    order_index: 2,
    content: "IF you're running any ML experiment beyond a completely disposable, one-off exploration THEN track, at minimum: the code version, the exact dataset version/snapshot used, the hyperparameters, and the resulting evaluation metrics. BECAUSE without these four things recorded together, you cannot reliably answer what produced a given result or reproduce it later. WATCH OUT FOR: tracking metrics without tracking the exact data version is a common half-measure — a model's metrics are meaningless without knowing precisely what data produced them, since a metric on a slightly different data snapshot isn't a fair comparison.\n\nIF a model or experiment result will inform a real decision (whether to deploy, whether it's actually better than the current production model) THEN it needs to be tracked with enough rigor that the comparison is genuinely fair — same evaluation script, same metric definition, ideally the same held-out test set. BECAUSE an untracked or inconsistently-evaluated comparison can lead to a confidently wrong decision about which model is actually better. WATCH OUT FOR: informal, ad-hoc \"eyeballing\" a metric printed in a notebook cell feels sufficient in the moment but leaves no durable record for anyone (including future you) to verify or challenge later.\n\nIF you're just doing early, disposable exploration (quickly checking whether an idea has any signal at all before committing to it) THEN lightweight tracking is reasonable — full experiment-tracking rigor on every single exploratory attempt can become overhead that discourages fast iteration. BECAUSE the cost of tracking should scale with how much a given experiment's result actually matters, not be applied uniformly regardless of context. WATCH OUT FOR: \"this is just exploration\" is sometimes used to justify skipping tracking on something that later quietly becomes the actual production model — if an exploratory result starts looking genuinely promising, that's the moment to start tracking it properly.\n\nIF your team has more than one person working on models, or more than one model in production THEN invest in a shared, queryable experiment tracking system (MLflow, ZenML, or similar) rather than individual, scattered notebooks or spreadsheets. BECAUSE the value of experiment tracking compounds specifically when multiple people or multiple models need a shared, comparable record — individual informal tracking doesn't scale past one person's memory.",
  },
  {
    section_type: "workflow",
    title: "The real process for setting up disciplined experiment tracking",
    order_index: 3,
    content: "1. Choose an experiment tracking tool appropriate to your team's scale (MLflow and ZenML are both real, widely-used open-source options — the specific choice matters less than actually using one consistently).\n\n2. For every experiment beyond disposable exploration, log the code version (a git commit hash is sufficient), the exact dataset version, all hyperparameters, and the full set of evaluation metrics — not just the one headline number.\n\n3. Standardize on one evaluation script/methodology used consistently across all compared experiments, so comparisons are actually fair.\n\n4. Tag or name experiments meaningfully (not \"run_47\") so a future search through the tracking system is actually navigable.\n\n5. When an experiment result starts looking genuinely promising, retroactively confirm it has full tracking rigor if it was originally logged as lightweight exploration.\n\n6. Periodically review the tracked experiment history — not just to find the current best model, but to understand the actual trajectory of what's been tried and what's been learned.\n\n7. Before promoting any model to production, confirm you can answer, from the tracking system alone (not memory), exactly what produced it and how it performed relative to the current baseline.\n\n8. Treat the tracking system as a durable team asset, not a personal scratch pad — anyone on the team should be able to look up what happened with a given model without asking the person who built it.",
  },
  {
    section_type: "failure_modes",
    title: "How ML lifecycle discipline actually breaks",
    order_index: 4,
    content: "Failure 1 — Tracking metrics without tracking the exact data version that produced them.\nWhat: an evaluation metric is recorded, but the specific dataset snapshot used to compute it isn't, making the metric impossible to trust or reproduce later.\nHow it happens: metrics feel like the important, headline result worth recording, while the underlying data version feels like a background detail.\nWhy beginners miss it: the metric number looks complete and meaningful on its own — the gap only becomes apparent when someone tries to reproduce or fairly compare it later and can't.\nDetect: for any recorded metric, check whether the exact data snapshot that produced it is also recorded and retrievable.\nPrevent: make data version tracking a mandatory, non-optional part of every experiment log entry, at the same priority as the metric itself.\nInterview question: \"You have a model with a recorded 92% accuracy from three months ago. What else do you need to know before trusting that number?\"\nReal-world consequence: a metric that can't actually be reproduced or fairly compared against, undermining every downstream decision that relies on it.\n\nFailure 2 — Comparing models evaluated with inconsistent methodology and treating the comparison as fair.\nWhat: \"model B scored higher than model A\" gets treated as a real finding, when the two were actually evaluated with different scripts, different splits, or slightly different metric definitions.\nHow it happens: evaluation code tends to evolve informally over time, without a deliberate effort to keep it identical across compared experiments.\nWhy beginners miss it: both numbers look like legitimate metrics in isolation — the inconsistency is only visible by actually diffing the evaluation methodology behind each one.\nDetect: before trusting any model comparison, verify both were evaluated with the identical script, split, and metric definition.\nPrevent: standardize and version-control one shared evaluation methodology used consistently across all experiments meant to be compared.\nInterview question: \"How do you know a claimed model improvement is real, and not just an artifact of evaluating it differently than the baseline?\"\nReal-world consequence: a worse model gets promoted to production because it was compared unfairly against the baseline, undermining the actual purpose of the comparison.\n\nFailure 3 — Treating exploratory experiments as permanently disposable, even once one becomes the actual production candidate.\nWhat: an experiment that started as quick, lightweight exploration quietly becomes the model actually being considered for production, without ever being retroactively tracked with full rigor.\nHow it happens: there's no natural trigger that prompts revisiting an experiment's tracking rigor once its status changes from \"just exploring\" to \"this might actually ship.\"\nWhy beginners miss it: the transition from exploration to serious candidate often happens gradually, with no single clear moment that would prompt someone to go back and add proper tracking.\nDetect: before promoting any model toward production, explicitly check whether it has full experiment-tracking rigor, regardless of how it originated.\nPrevent: build a checkpoint into the promotion process itself that requires confirming full tracking exists before a model can be considered for deployment.\nInterview question: \"A promising model started as quick exploratory work with minimal tracking. What do you need to do before trusting it enough to deploy?\"\nReal-world consequence: a production model whose exact provenance and true evaluation methodology are unclear, discovered only when someone later needs to reproduce or audit it.\n\nFailure 4 — Individual, scattered tracking that doesn't scale once more than one person or model is involved.\nWhat: each team member keeps their own informal notes or notebooks, with no shared, queryable record — making it impossible for anyone to get a full picture of what's actually been tried across the team.\nHow it happens: individual informal tracking (a well-organized personal notebook, even) can feel sufficient when a project starts with just one person.\nWhy beginners miss it: it genuinely does work fine for a single person working alone — the gap only appears once a second person or a second model enters the picture and the informal system doesn't extend to them.\nDetect: ask whether a team member could find and understand what a colleague tried last month, using only the tracking system, without asking that colleague directly.\nPrevent: move to a shared, queryable experiment tracking system as soon as a project involves more than one contributor or more than one actively-developed model.\nInterview question: \"Your team is scaling from one to four data scientists working on related models. What changes about how you'd track experiments?\"\nReal-world consequence: duplicated work, conflicting understanding of what's already been tried, and an inability to build a coherent team-level picture of the project's actual progress.",
  },
  {
    section_type: "debugging_playbook",
    title: "We can't trust or reproduce a past result — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"We have a metric from a past experiment but can't reproduce it, or aren't sure what data produced it.\"\nDiagnostic: check what was actually recorded alongside that metric — very likely the exact data version wasn't tracked.\nFix: treat this specific result as untrustworthy until it can be genuinely reproduced, and immediately start tracking data versions rigorously for all future experiments to prevent a recurrence.\n\nSymptom: \"Two models show different scores, but we're not confident the comparison is actually fair.\"\nDiagnostic: diff the actual evaluation code/methodology used for each — check for a different split, a different metric implementation, or different preprocessing.\nFix: re-evaluate both models using one identical, shared evaluation script and split before trusting any conclusion about which is genuinely better.\n\nSymptom: \"A model that started as a quick experiment is now being considered for production, but its tracking is minimal.\"\nDiagnostic: confirm exactly what is and isn't currently recorded for this experiment.\nFix: before proceeding toward deployment, retroactively establish full tracking rigor — confirm the exact code, data, and configuration can be reliably reconstructed, or re-run the experiment properly tracked if they can't.\n\nSymptom: \"A team member can't find or understand what a colleague already tried on a related model.\"\nDiagnostic: check whether a shared, queryable tracking system actually exists, or whether tracking has remained individual and informal despite the team growing.\nFix: consolidate onto one shared experiment tracking tool and migrate what historical information can still be reliably reconstructed.",
  },
  {
    section_type: "checklist",
    title: "Experiment Tracking Discipline Checklist",
    order_index: 6,
    content: "- [ ] Every non-disposable experiment logs code version, exact data version, hyperparameters, and full evaluation metrics together\n- [ ] Comparisons between models use one identical, shared evaluation methodology\n- [ ] Experiments are meaningfully named/tagged, not left as default auto-generated run identifiers\n- [ ] Any experiment that becomes a genuine production candidate has been confirmed to have full tracking rigor, even if it started as quick exploration\n- [ ] The tracking system is shared and queryable by the whole team, not scattered across individual notebooks\n- [ ] Anyone on the team can answer \"what produced this model and how did it perform\" using the tracking system alone, without relying on a specific person's memory\n- [ ] Before promoting a model toward production, its full provenance can be reconstructed from the tracking record",
  },
  {
    section_type: "template",
    title: "Experiment Tracking Log Entry",
    order_index: 7,
    content: "Experiment name/ID: ___\nDate: ___\nCode version (commit hash): ___\nData version/snapshot: ___\n\nHyperparameters: ___\n\nEvaluation methodology (script/split used): ___\nMetrics (full set, not just the headline number): ___\n\nHypothesis being tested: ___\nResult vs. hypothesis: ___\n\nStatus: exploratory / serious candidate / production\nNext step: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [MLflow documentation](https://mlflow.org/docs/latest/index.html) — the most widely-used open-source experiment tracking tool, and the direct hands-on reference for this module's build deliverable.\n- [ZenML documentation](https://docs.zenml.io/) — an alternative/complementary tool emphasizing reproducible pipelines alongside tracking, also directly referenced in this module's build deliverable.\n\nRecommended\n- Google's [Rules of Machine Learning](https://developers.google.com/machine-learning/guides/rules-of-ml) (referenced earlier in the Data Science course track) — several early rules specifically address the reproducibility and simplicity discipline this module builds on.\n\nReference\n- [DVC (Data Version Control) documentation](https://dvc.org/doc) — a widely-used tool specifically for the data-versioning half of the tracking problem, complementary to MLflow/ZenML's experiment-tracking focus.\n\nAdvanced\n- Sculley et al., [\"Hidden Technical Debt in Machine Learning Systems\"](https://papers.nips.cc/paper_files/paper/2015/hash/86df7dcfd896fcaf2674f757a2463eba-Abstract.html) (referenced in the Data Science course track's Module 4) — foundational reading on why ML systems accumulate debt beyond the model code itself, directly motivating the discipline this MLOps course is built around.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Set up MLflow tracking for a simple experiment",
    problem_statement: "Using any small dataset (the UCI Wine Quality dataset from the Data Science course track works well, or any similarly simple dataset), set up MLflow tracking and log at least 3 experiment runs with different hyperparameters, each recording: the code version (or a note of what changed), the data version used, hyperparameters, and full evaluation metrics.\n\nAfter logging all 3 runs, use MLflow's UI or query interface to identify the best-performing run and explain, using only the tracked information, why it's the best.",
    starter_context: "If you don't have MLflow set up locally, their quickstart documentation covers a minimal local setup that's sufficient for this exercise.",
    hints: [
      "Make sure your 3 runs actually differ in a meaningful, recorded way (different hyperparameters) — 3 identical runs won't give you anything real to compare.",
      "Log more than just accuracy or your single headline metric — log the full relevant set, so your comparison in the final step has real substance to draw from.",
    ],
    solution_notes: "A strong submission has 3 genuinely distinct, properly logged runs with full hyperparameter and metric records, and the final comparison explicitly references the tracked data (not memory or assumption) to justify which run is best — demonstrating the core habit this module is built around: decisions grounded in recorded evidence, not recollection.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Audit an untracked notebook and reconstruct what's missing",
    problem_statement: "Write (or find/reuse) a simple, deliberately \"untracked\" ML notebook — one that trains a model and prints a metric, with no experiment tracking at all. Audit it against this module's checklist, listing everything that would need to be added to bring it up to proper tracking discipline.\n\nThen actually add MLflow (or ZenML) tracking to it, and re-run it as a properly tracked experiment.",
    starter_context: "You can write a minimal, deliberately bare-bones notebook for this exercise if you don't already have one on hand.",
    hints: [
      "Before adding tracking, try to answer this module's central question — 'what exactly produced this metric?' — using only what's currently in the untracked notebook, and notice how much you have to guess or assume.",
      "Once you add tracking, verify you can now answer that same question with confidence, using only the tracking record.",
    ],
    solution_notes: "A strong submission's audit correctly identifies the specific missing elements (data version, hyperparameters, a durable metric record) rather than vaguely noting 'no tracking exists,' and the before/after comparison explicitly demonstrates the practical difference — being unable versus being able to confidently answer what produced a given result.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Design a team experiment-tracking standard",
    problem_statement: "You're told: \"Our data science team is growing from one person to four, and everyone currently just keeps their own notebooks with print statements for metrics.\" Design a concrete experiment-tracking standard for this team: what tool, what must be logged for every experiment, what naming/tagging convention, and how the transition from the current informal state would actually happen.",
    starter_context: "This is a process-design exercise — the deliverable is your proposed standard and transition plan, not a specific technical implementation.",
    hints: [
      "Consider how you'd handle the team's existing historical work during the transition — is it worth trying to retroactively migrate, or is a clean cutover point more realistic?",
      "Your standard should be specific enough that a new team member could follow it correctly without having to ask clarifying questions about what 'proper tracking' actually means in practice.",
    ],
    solution_notes: "A strong submission specifies a concrete tool choice with reasoning, a clear list of mandatory fields for every logged experiment (matching this module's checklist), a practical naming/tagging convention, and a realistic transition plan that acknowledges historical untracked work may not be fully reconstructible — proposing a clean go-forward standard rather than an unrealistic promise to retroactively track everything perfectly.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why isn't tracking a model's evaluation metric enough on its own — what else needs to be tracked alongside it?", what_is_tested: "Understanding that a metric is only meaningful in the context of the exact data and configuration that produced it.", strong_answer_structure: "Explain that a metric without the corresponding data version, code version, and hyperparameters can't be reproduced or fairly compared against other results — the metric alone is an unverifiable claim, not a durable, trustworthy record.", weak_answer_example: "\"The metric is the important number, the rest is just bookkeeping\" — misses why the metric is meaningless without its full context.", follow_up_question: "What's the minimum set of things you'd want logged for every experiment your team runs?" },
  { category: "applied", order_index: 1, question: "How would you decide whether a quick exploratory experiment needs full experiment-tracking rigor, or lighter tracking is acceptable?", what_is_tested: "A real decision framework based on how much the result matters, not a blanket policy.", strong_answer_structure: "Explain that tracking rigor should scale with how likely and how consequential it would be for this specific experiment to inform a real decision — genuinely disposable exploration can be lighter, but anything that starts looking promising should be upgraded to full tracking before it's trusted further.", weak_answer_example: "\"Everything should always be fully tracked\" or \"exploration never needs tracking\" — either extreme misses the actual judgment call.", follow_up_question: "What's the risk of being too strict about full tracking on every single exploratory attempt?" },
  { category: "scenario", order_index: 2, question: "A colleague claims their new model is better than the current production model, based on a metric they computed. How do you verify this claim before trusting it?", what_is_tested: "Instinct to check evaluation methodology consistency before accepting a comparison at face value.", strong_answer_structure: "Check whether both models were evaluated using the identical evaluation script, data split, and metric definition — and if not, re-evaluate both under one consistent methodology before trusting the comparison.", weak_answer_example: "\"If the number is higher, it's better\" — accepts the comparison without checking whether it's actually fair.", follow_up_question: "What specific things would you look for to confirm two evaluations were genuinely comparable?" },
  { category: "debugging", order_index: 3, question: "You need to reproduce a model from three months ago, but you're not confident the current pipeline still produces the same result. What's your investigation process?", what_is_tested: "Practical approach to diagnosing a reproducibility gap using tracked (or missing) information.", strong_answer_structure: "Check the experiment tracking record for the exact code version, data version, and configuration originally used, and compare them against the current pipeline to identify what, if anything, has changed since then.", weak_answer_example: "Just re-running the current pipeline and assuming it should match, without checking for tracked differences first.", follow_up_question: "What would you do if the tracking record for that specific experiment turns out to be incomplete?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you (or a team) lost track of what exactly produced a specific model result, and how you handled it.", what_is_tested: "Real or realistic experience with the exact problem this module addresses, and what changed afterward.", strong_answer_structure: "A strong answer describes the specific situation, how the gap was discovered, what (if anything) could be reconstructed, and a concrete change to tracking practices made as a result.", weak_answer_example: "A vague answer with no specific incident or no described change in tracking discipline afterward.", follow_up_question: "What would you do differently from day one on a new project, knowing what you know now?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "MLOps Pipeline Design").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("MLOps Module 1 (MLOps Foundations) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
