import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "2b0ec255-1520-4cd9-aa58-c6ddc51e71ee"; // Anomaly Detection & Correlation

const sections = [
  {
    section_type: "the_field",
    title: "What cutting real alert noise actually looks like",
    order_index: 0,
    content: "An on-call engineer paged 40 times overnight for what turns out to be one real incident isn't experiencing thorough monitoring — they're experiencing a correlation failure. One underlying root cause (a database going slow) cascades into dozens of individual, independently-firing alerts across every service that depends on it, and without correlation, each one pages as if it were a separate, unrelated incident.\n\nML-driven anomaly detection's genuine value here isn't magic — it's pattern recognition applied at a scale a fixed threshold rule can't match. A static threshold (\"alert if latency exceeds 500ms\") is blunt and context-blind; it fires the same way at 3am with low traffic as at peak load, and it can't distinguish \"latency is elevated because of a real problem\" from \"latency is elevated because this is a known, expected daily traffic pattern.\" Anomaly detection that accounts for normal patterns (time of day, day of week, recent trend) produces meaningfully fewer false positives than a fixed threshold.\n\nAlert correlation is the complementary discipline: recognizing that many individually-firing alerts actually stem from one root cause, and collapsing them into a single, coherent incident rather than a flood of noise. The practical skill this module builds is genuinely reducing the noise-to-signal ratio an on-call engineer experiences — not eliminating alerts, but making the alerts that do fire actually represent real, distinct, actionable problems.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about anomaly detection and correlation",
    order_index: 1,
    content: "**1. A static threshold is context-blind; a good anomaly detector accounts for what 'normal' actually looks like for this specific time and pattern.** The same latency value can be perfectly normal at peak traffic and genuinely anomalous at 3am — a fixed threshold can't make this distinction, but a model accounting for time-of-day and trend can.\n\n**2. Alert correlation's core insight is that most alert floods have one root cause, not many independent problems.** Recognizing and grouping alerts that stem from the same underlying issue is what turns 40 pages into one clear, actionable incident.\n\n**3. Reducing noise and missing real signal are opposite failure modes, and tuning always trades off between them.** An overly aggressive noise-reduction approach risks suppressing a genuinely important alert; an overly cautious one doesn't actually reduce noise — the right balance is a real, ongoing tuning decision, not a one-time setting.\n\n**4. The on-call engineer's actual experience is the real measure of success, not a clean-looking demo.** A correlation system that looks impressive in a controlled test but doesn't genuinely reduce what a real on-call engineer experiences during a real incident hasn't actually delivered its intended value.\n\n**5. Anomaly detection needs continuous validation against real incidents, not a one-time training and deployment.** What counts as \"normal\" drifts over time (traffic patterns change, the application itself changes), and a model that isn't periodically re-validated against real outcomes can degrade silently.\n\n**6. False positives and false negatives have asymmetric real costs, and the right tuning depends on which matters more for a given alert category.** A missed critical database failure is generally far more costly than an extra noisy alert; the tuning for different alert categories should reflect this, not be uniform.",
  },
  {
    section_type: "decision_framework",
    title: "How should I design anomaly detection and correlation rules?",
    order_index: 2,
    content: "IF an alert's threshold is based on a fixed value with no accounting for time-of-day, day-of-week, or trend patterns THEN it's a strong candidate for anomaly-detection-based improvement, since context-aware detection will likely produce meaningfully fewer false positives. BECAUSE a fixed threshold applied uniformly across genuinely different traffic patterns is a common, well-understood source of unnecessary noise. WATCH OUT FOR: not every metric has a meaningful cyclical/trend pattern — a genuinely stable metric may not benefit much from this kind of contextual modeling, and a simple threshold may already be appropriate.\n\nIF multiple alerts frequently fire together, tracing back to the same underlying root cause THEN build explicit correlation rules to group them into one incident, rather than letting each page independently. BECAUSE this is the specific, high-value pattern this module targets — collapsing an alert flood into one coherent, actionable incident. WATCH OUT FOR: correlation rules built from a small number of observed incidents may not generalize — validate correlation logic against a genuinely varied set of past incidents, not just the one flood that motivated building the rule.\n\nIF you're tuning the sensitivity of anomaly detection or correlation THEN weigh the asymmetric real cost of false positives versus false negatives for this specific alert category, rather than applying uniform sensitivity across all alerts. BECAUSE a missed critical failure and an extra noisy alert have very different real costs, and the tuning should reflect that difference per category rather than a one-size-fits-all setting. WATCH OUT FOR: tuning to eliminate noise so aggressively that a genuinely important signal gets suppressed is a real, costly risk — validate any sensitivity change against known past real incidents to confirm it wouldn't have missed them.\n\nIF anomaly detection or correlation has been in production for a while THEN periodically re-validate it against real, recent incidents, since what counts as \"normal\" traffic and behavior genuinely drifts over time. BECAUSE a model that isn't re-validated can silently degrade as the application and its traffic patterns evolve, the same drift-monitoring principle established in the MLOps course track applied specifically to operational anomaly detection.",
  },
  {
    section_type: "workflow",
    title: "The real process for reducing alert noise with anomaly detection and correlation",
    order_index: 3,
    content: "1. Audit current alerting for fixed-threshold rules that don't account for time-of-day, day-of-week, or trend context — these are prime candidates for anomaly-detection improvement.\n\n2. Analyze historical alert floods (past incidents where many alerts fired together) to identify recurring root-cause-to-symptom patterns.\n\n3. Build explicit correlation rules for the identified patterns, grouping related alerts into single, coherent incidents.\n\n4. Introduce context-aware anomaly detection for metrics with genuine cyclical or trend patterns, validated against historical data to confirm it reduces false positives without missing known real incidents.\n\n5. Explicitly tune sensitivity per alert category, weighing the real asymmetric cost of false positives versus false negatives for that specific category.\n\n6. Deploy changes incrementally, measuring the actual real-world impact on alert volume and on-call experience, not just a theoretical reduction estimate.\n\n7. Periodically re-validate detection and correlation against recent real incidents, watching for drift in what counts as normal behavior.\n\n8. Gather direct feedback from on-call engineers on whether the noise reduction is genuinely improving their real experience, not just producing a cleaner-looking dashboard.",
  },
  {
    section_type: "failure_modes",
    title: "How anomaly detection and correlation actually breaks",
    order_index: 4,
    content: "Failure 1 — Anomaly detection tuned so aggressively for noise reduction that it misses a genuinely important signal.\nWhat: sensitivity is tuned down enough to eliminate most noise, but in doing so also suppresses an alert that should have fired for a genuine, important issue.\nHow it happens: noise reduction is often the explicitly stated goal, creating pressure to tune aggressively without equally weighing the risk of missing real signal.\nWhy beginners miss it: aggressive tuning genuinely does reduce noise, which feels like success — the cost (a missed real incident) is invisible until that specific scenario actually occurs.\nDetect: validate any sensitivity tuning against a set of known past real incidents, confirming the tuned system would still have caught them, not just that it produces less noise on average.\nPrevent: explicitly weigh the asymmetric cost of false positives versus false negatives per alert category, per this module's decision framework, rather than optimizing purely for noise reduction.\nInterview question: \"You tuned your anomaly detection to cut alert volume by 80%. What's the first thing you'd want to verify before trusting that tuning?\"\nReal-world consequence: a genuinely important incident is missed or significantly delayed because the alert that should have caught it was suppressed by overly aggressive noise-reduction tuning.\n\nFailure 2 — Correlation rules built from a small number of past incidents that don't generalize to real variety.\nWhat: a correlation rule works well for the specific incident pattern it was built from, but fails to correctly group alerts for a differently-shaped incident, either missing the correlation or incorrectly grouping unrelated alerts together.\nHow it happens: correlation rules are often built reactively, from the most recent painful alert flood, without validating against a genuinely varied set of past incidents.\nWhy beginners miss it: the rule works correctly for the motivating incident, creating confidence that doesn't necessarily transfer to differently-shaped future incidents.\nDetect: test correlation rules against multiple, genuinely different past incidents, not just the one that motivated building the rule.\nPrevent: validate correlation logic against real incident variety before trusting it broadly, per this module's workflow.\nInterview question: \"You built a correlation rule based on last week's database-outage alert flood. How would you know if it works for a differently-shaped incident, like a network partition?\"\nReal-world consequence: a future incident with a different alert pattern isn't correctly correlated, either producing the original alert-flood noise problem again or incorrectly grouping genuinely unrelated alerts.\n\nFailure 3 — No periodic re-validation, letting detection silently degrade as traffic patterns drift.\nWhat: anomaly detection that was well-calibrated when first deployed gradually becomes less accurate as the application's real traffic patterns evolve, without anyone noticing until it starts producing noticeably worse results.\nHow it happens: detection is often validated once at deployment and then trusted indefinitely, without a deliberate periodic re-validation practice.\nWhy beginners miss it: the degradation is typically gradual, not a sudden, obvious failure — each individual day's slight drift is easy to miss, and the cumulative effect only becomes clear after enough time has passed.\nDetect: periodically compare detection accuracy against recent real incidents, checking for a decline relative to the original validation.\nPrevent: build periodic re-validation into standard operational practice, per this module's workflow, applying the same drift-awareness principle established in the MLOps course track.\nInterview question: \"Your anomaly detection was well-tuned six months ago. How would you know if it's still accurate today?\"\nReal-world consequence: gradually degrading detection accuracy that goes unnoticed until it's meaningfully worse, at which point real trust in the system (and possibly a real missed incident) has already been damaged.\n\nFailure 4 — Optimizing for a clean-looking dashboard rather than the on-call engineer's actual real experience.\nWhat: metrics like \"alerts reduced by X%\" look good in a report, but the actual on-call engineers still find the correlated incidents confusing, poorly explained, or not genuinely more actionable than the original noisy alerts.\nHow it happens: it's easier to measure and report a clean aggregate statistic (alert volume reduction) than to gather and act on genuine qualitative feedback from the people actually experiencing the on-call rotation.\nWhy beginners miss it: the aggregate metric looks genuinely positive, creating a sense of success that isn't validated against what actually matters — whether the on-call engineer's real experience improved.\nDetect: directly ask on-call engineers whether the correlated incidents are genuinely clearer and more actionable, not just fewer in number.\nPrevent: gather direct on-call feedback as a required part of the workflow, per this module's checklist, not just aggregate volume metrics.\nInterview question: \"Alert volume dropped 60% after your correlation rollout. How do you know this actually made things better for the on-call engineer, not just quieter?\"\nReal-world consequence: a system that looks successful by the numbers but doesn't actually deliver the real value (a genuinely better on-call experience) it was built to provide, discovered only when directly asking the people affected.",
  },
  {
    section_type: "debugging_playbook",
    title: "The anomaly detection or correlation isn't working well — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"We reduced alert volume significantly, but on-call engineers still complain about the experience.\"\nDiagnostic: gather direct qualitative feedback — are the remaining/correlated alerts actually clearer and more actionable, or just fewer in count?\nFix: address the specific complaints found (unclear correlation grouping, missing context in the collapsed incident) rather than assuming volume reduction alone was sufficient.\n\nSymptom: \"A real, important incident was missed or significantly delayed.\"\nDiagnostic: check whether recent sensitivity tuning suppressed the alert that should have caught it, and whether that tuning was validated against known past incidents before being deployed.\nFix: re-tune sensitivity for the relevant alert category with explicit weight on not missing this category of incident, and re-validate against a wider set of known real incidents going forward.\n\nSymptom: \"A correlation rule that worked well for one incident type failed to correctly group alerts for a different incident.\"\nDiagnostic: check whether the correlation rule was validated against varied incident types, or only the one that originally motivated it.\nFix: broaden the correlation logic based on the new incident pattern, and validate against a wider, more varied set of past incidents going forward.\n\nSymptom: \"Anomaly detection accuracy seems to have quietly degraded over time.\"\nDiagnostic: check when the detection was last validated against recent real incidents, and whether traffic patterns have genuinely shifted since then.\nFix: re-validate and re-tune against current, recent data, and establish a periodic re-validation cadence going forward.",
  },
  {
    section_type: "checklist",
    title: "Anomaly Detection & Correlation Checklist",
    order_index: 6,
    content: "- [ ] Fixed-threshold alerts that don't account for time-of-day/trend context are identified as candidates for anomaly-detection improvement\n- [ ] Correlation rules are validated against multiple, genuinely varied past incidents, not just the one that motivated building the rule\n- [ ] Sensitivity tuning explicitly weighs the asymmetric cost of false positives versus false negatives per alert category\n- [ ] Any tuning change is validated against known past real incidents before being trusted in production\n- [ ] Detection and correlation are periodically re-validated against recent incidents, watching for drift\n- [ ] Direct qualitative feedback from on-call engineers is gathered, not just aggregate alert-volume metrics",
  },
  {
    section_type: "template",
    title: "Alert Correlation Rule Design Record",
    order_index: 7,
    content: "Root cause pattern: ___\nAlerts typically fired together for this pattern: ___\n\nCorrelation logic: ___\n\nValidated against past incidents:\n| Incident | Correctly correlated? | Notes |\n|---|---|---|\n| ... | ... | ... |\n\nSensitivity tuning: false-positive cost ___, false-negative cost ___ for this category — chosen sensitivity: ___\n\nOn-call feedback gathered: ___\n\nLast re-validated: ___ — result: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Prometheus: Alerting best practices](https://prometheus.io/docs/practices/alerting/) — practical, established guidance on alert design that anomaly detection and correlation build on top of.\n- Google's [Rules of Machine Learning](https://developers.google.com/machine-learning/guides/rules-of-ml) (referenced across this catalog) — several rules on evaluation and monitoring apply directly to validating anomaly detection accuracy.\n\nRecommended\n- [Facebook/Meta Prophet documentation](https://facebook.github.io/prophet/) — a real, widely-used tool for time-series anomaly detection with built-in seasonality/trend awareness, directly relevant to this module's context-aware detection principle.\n\nReference\n- Google SRE Book chapters on alerting philosophy (part of [sre.google/sre-book](https://sre.google/sre-book/table-of-contents/)) — foundational thinking on what makes an alert worth paging for, relevant to the noise-reduction goal this module addresses.\n\nAdvanced\n- Search for published engineering write-ups on \"alert correlation\" or \"incident grouping\" from major tech companies — useful for seeing this module's principles applied at real production scale with real before/after noise-reduction numbers.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Build a correlation rule set for a mock alert flood",
    problem_statement: "You're given a mock incident: a database connection pool exhaustion causes 12 different alerts to fire within 2 minutes — high latency alerts from 5 dependent services, error rate alerts from those same 5 services, and 2 direct database-health alerts. Design a correlation rule set that would collapse these 12 alerts into 1 coherent incident.\n\nThen design a second, different mock incident (your choice) and test whether your rule set correctly does NOT incorrectly correlate its alerts with the first, unrelated incident.",
    starter_context: "The second, distinct incident is the actual validation test for this exercise — a correlation rule that groups genuinely unrelated alerts together is just as much a failure as one that misses a real grouping.",
    hints: [
      "Consider correlating based on a shared root-cause signal (the database health alerts) as the anchor, with the downstream latency/error alerts across dependent services grouped around it.",
      "Your second incident should be genuinely unrelated (a different root cause affecting different services) to make the non-correlation test meaningful.",
    ],
    solution_notes: "A strong submission designs correlation logic that correctly anchors the 12 related alerts around the shared database root cause, and correctly does NOT group the second, unrelated incident's alerts with the first — demonstrating both the positive correlation case and the equally important negative case (not over-correlating unrelated alerts), directly addressing this module's second failure mode about generalization.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Design context-aware anomaly detection for a metric with real daily patterns",
    problem_statement: "You have request latency data for a service with a clear daily pattern: low traffic overnight (00:00-06:00) with naturally higher latency variance due to low sample size, and high, steady traffic during business hours with tight, consistent latency. Design an anomaly-detection approach (in writing — you don't need to implement actual ML) that would correctly avoid flagging normal overnight latency variance as anomalous, while still catching a genuine anomaly during business hours.",
    starter_context: "This is a design reasoning exercise — describe your approach and why it handles the described pattern correctly, rather than implementing actual code.",
    hints: [
      "Consider how a fixed threshold would behave incorrectly here — would it be too sensitive overnight, too insensitive during the day, or both?",
      "Consider what 'normal' should be modeled as relative to — the same time-of-day historically, rather than a single global threshold.",
    ],
    solution_notes: "A strong submission correctly identifies that a fixed threshold would either be too sensitive overnight (given naturally higher variance) or too insensitive during business hours (given tight, consistent latency, where even a modest increase might be genuinely anomalous), and proposes an approach that models 'normal' relative to the same time-of-day historically (e.g., a rolling baseline per hour-of-day, or a seasonal decomposition approach) rather than one global threshold — directly demonstrating the context-aware detection principle this module's decision framework establishes.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose an over-aggressive noise-reduction rollout",
    problem_statement: "You're told: \"After our AIOps rollout, alert volume dropped 85% and on-call engineers were thrilled — until a real database outage last week went completely unnoticed for 40 minutes because it never generated an alert at all.\" You're given no further detail.\n\nDiagnose what likely went wrong, and propose specific process changes to prevent a repeat, using this module's principles.",
    starter_context: "This is a diagnostic and remediation exercise based on the description alone.",
    hints: [
      "An 85% reduction combined with a genuinely missed real incident is a strong signal about what specifically went wrong in the tuning process — connect this to this module's first failure mode.",
      "Consider what validation step, if it had existed, would likely have caught this risk before it caused a real, damaging incident.",
    ],
    solution_notes: "A strong submission diagnoses that the noise-reduction tuning was likely optimized purely for volume reduction without validating against known past real incidents (or without adequately weighing the asymmetric cost of missing a critical database failure specifically), and proposes requiring explicit validation of any sensitivity tuning against a representative set of past real incidents — especially high-severity ones like database outages — before deploying it, directly closing the gap this module's first failure mode describes.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why is a fixed alert threshold often a source of unnecessary noise, and how does context-aware anomaly detection address this?", what_is_tested: "Understanding of the core mechanism behind context-blind thresholds producing false positives.", strong_answer_structure: "Explain that a fixed threshold applies the same cutoff regardless of time-of-day, traffic pattern, or trend, so it can fire during genuinely normal but naturally elevated periods (like known peak traffic) — a context-aware detector models what's actually normal for that specific time/pattern, reducing this category of false positive.", weak_answer_example: "\"Fixed thresholds are just old-fashioned\" — doesn't explain the actual mechanism causing false positives.", follow_up_question: "What data would you need to build a context-aware model for a given metric?" },
  { category: "applied", order_index: 1, question: "How would you decide how aggressively to tune anomaly detection sensitivity for a given alert category?", what_is_tested: "Understanding that the right sensitivity depends on the asymmetric real cost of false positives versus false negatives for that category.", strong_answer_structure: "Explain weighing the actual cost of missing a real incident in this category against the cost of extra noisy alerts, and tuning accordingly — a critical database-health alert warrants more conservative (less aggressive noise-reduction) tuning than a low-stakes informational alert.", weak_answer_example: "\"I'd just tune everything the same way to reduce noise consistently\" — misses that different alert categories have genuinely different real costs.", follow_up_question: "How would you validate that a specific tuning choice doesn't risk missing a real incident?" },
  { category: "scenario", order_index: 2, question: "A correlation rule built from last month's database-outage alert flood works great for that pattern. This month, a network partition causes a different alert flood. What's your concern?", what_is_tested: "Recognition that correlation rules built from one incident may not generalize.", strong_answer_structure: "Express concern that the correlation rule may not correctly group the differently-shaped network-partition alert flood, since it was built and validated only against the database-outage pattern — recommend testing against this new incident type explicitly.", weak_answer_example: "\"The rule should work fine since it's already proven to work\" — assumes generalization without verifying it.", follow_up_question: "How would you design correlation rules to generalize better across different incident types from the start?" },
  { category: "debugging", order_index: 3, question: "Alert volume dropped significantly after a noise-reduction rollout, but a real incident was later missed entirely. What's your investigation process?", what_is_tested: "Diagnostic instinct toward checking whether the tuning was validated against real incident risk before deployment.", strong_answer_structure: "Investigate whether the sensitivity tuning that reduced volume was validated against known past real incidents (especially high-severity ones) before being deployed, or whether it was tuned purely for volume reduction without that check.", weak_answer_example: "Assuming the missed incident was just bad luck without investigating whether the tuning process itself had a real gap.", follow_up_question: "What specific validation step would you add to the tuning process going forward?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to balance reducing noise/false positives against the risk of missing something real. How did you decide where to draw the line?", what_is_tested: "Real or realistic experience with this specific trade-off, applied thoughtfully rather than defaulting to one extreme.", strong_answer_structure: "A strong answer describes the specific situation, the real cost considerations weighed on each side, and the actual decision made, including any validation done to confirm the choice was reasonable.", weak_answer_example: "A vague answer with no specific trade-off or reasoning process described.", follow_up_question: "Would you make the same trade-off decision again, knowing what happened afterward?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "AIOps & Anomaly Detection").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AIOps Module 2 (Anomaly Detection & Correlation) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
