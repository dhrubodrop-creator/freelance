import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "9ec3cbd9-3b58-4753-9082-8107dd013477"; // Prompt & Model Lifecycle Management

const sections = [
  {
    section_type: "the_field",
    title: "What prompt and model lifecycle management actually looks like",
    order_index: 0,
    content: "The n8n agentic course track already established that a production prompt is production logic, not casual text. LLMOps is the discipline built specifically around that reality at organizational scale: prompts living across multiple environments, multiple model providers, and multiple people editing them, without any one of those dimensions silently breaking the others.\n\nThe specific failure this module prevents is one that happens constantly in real organizations: someone \"just tweaks\" a live prompt to fix an immediate issue, with no record of the change, no testing against known good/bad cases, and no way to know if it broke something else that was working before. Multiply this by several prompts, several model providers (each with slightly different behavior for the \"same\" prompt), and several environments (dev, staging, prod), and you get a system nobody can confidently reason about — exactly the ungoverned mess the n8n course's Module 6 warned against, examined here in full depth as its own discipline.\n\nA prompt registry with real version control, a defined promotion path from dev to staging to production, and an explicit strategy for managing behavior differences across model providers turns this from tribal knowledge and hope into an actual engineering practice — one where a regression can be traced, a change can be reviewed before it ships, and a rollback is a known, fast operation rather than a scramble.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about the prompt and model lifecycle",
    order_index: 1,
    content: "**1. A prompt registry is a database of production logic, not a folder of text files.** Prompts need the same queryability, versioning, and access control as any other production configuration that directly determines system behavior.\n\n**2. Different model providers are not interchangeable behind the same prompt.** The same exact prompt text can produce meaningfully different behavior across providers (and across versions of the same provider's models) — treating a prompt as portable without verification is a real, common source of silent regressions when a provider or model version changes.\n\n**3. Environment promotion exists to catch a bad change before it reaches real users, the same reason it exists for application code.** A prompt change that goes straight from someone's local testing to production, with no staging step, has skipped the exact safety net that would have caught many real problems.\n\n**4. A prompt's version history is what makes 'what changed, and did it cause this' an answerable question.** Without it, every regression investigation starts from zero, relying entirely on someone's memory of what they might have edited.\n\n**5. Managing multiple providers is a real architectural decision, not just an implementation detail.** Whether you standardize on one provider, build an abstraction layer across several, or deliberately use different providers for different tasks based on their strengths — this is a decision with real cost/reliability trade-offs, not something to leave implicit.\n\n**6. A prompt change is a deploy, and should go through the same rigor as any other deploy.** Treating a prompt edit as lower-stakes than a code change, simply because it's \"just text,\" is exactly the gap this module exists to close.",
  },
  {
    section_type: "decision_framework",
    title: "How should I manage prompts across environments and providers?",
    order_index: 2,
    content: "IF a prompt is used anywhere beyond local, disposable experimentation THEN it belongs in a version-controlled registry with a visible change history, not edited ad hoc in a live system. BECAUSE without this, a regression after a prompt change is untraceable and unrollbackable, exactly the failure mode this module exists to prevent. WATCH OUT FOR: a registry that exists but isn't actually the single source of truth (because people still sometimes edit prompts directly elsewhere) provides false confidence — enforce that the registry is genuinely the only path to a production prompt change.\n\nIF you support multiple model providers, or might need to switch providers in the future THEN explicitly test and document how each prompt behaves per provider, rather than assuming a prompt tuned for one provider transfers cleanly to another. BECAUSE providers differ in instruction-following style, output formatting tendencies, and general behavior even for ostensibly similar underlying capability — an untested assumption of portability is a real risk. WATCH OUT FOR: this also applies within one provider across model version upgrades — a provider's own model update can shift behavior for an existing prompt just as much as switching providers can.\n\nIF a prompt change is ready to move from development toward production THEN require it to pass through a defined environment promotion path (dev → staging → prod), with some form of verification at each stage before advancing. BECAUSE this mirrors the same defense-in-depth reasoning as application code promotion — catching a problem in staging is far cheaper than catching it after real users are affected. WATCH OUT FOR: a promotion path that exists on paper but is routinely skipped under time pressure isn't actually providing protection — the discipline needs to hold especially when there's pressure to bypass it.\n\nIF you're deciding how much process to apply to a given prompt change THEN scale the rigor to the prompt's actual production impact — a prompt powering a core, high-traffic feature warrants more testing and review than one used in an internal, low-stakes tool. BECAUSE uniform maximum rigor on every single prompt change everywhere creates unnecessary friction that teams will eventually route around, undermining the whole practice.",
  },
  {
    section_type: "workflow",
    title: "The real prompt and model lifecycle management workflow",
    order_index: 3,
    content: "1. Establish a version-controlled prompt registry as the single, enforced source of truth for any production prompt — no direct live edits outside this system.\n\n2. For any prompt supporting multiple model providers (or anticipating a future provider switch), explicitly test and document behavior differences per provider, rather than assuming portability.\n\n3. Define a real environment promotion path (dev → staging → prod) with a verification step at each stage — connecting directly to the evaluation practices covered in Module 2 of this course.\n\n4. Scale the rigor of the promotion process to the actual production impact of the specific prompt being changed, rather than applying uniform maximum process to everything.\n\n5. Record, for every promoted change, what changed and why — a change log that makes \"what's different since the last known-good version\" an answerable question.\n\n6. When a model provider updates their underlying model, treat this as a potential prompt-behavior change requiring re-verification, not something that happens transparently with no risk.\n\n7. Build a fast, well-understood rollback path for prompts, the same way you would for application code deploys — tested, not just assumed to work.\n\n8. Periodically audit whether the registry is genuinely the only path to production prompt changes, watching for informal workarounds that would undermine the whole system.",
  },
  {
    section_type: "failure_modes",
    title: "How prompt and model lifecycle management actually breaks",
    order_index: 4,
    content: "Failure 1 — A live prompt edited directly in production with no version history.\nWhat: someone fixes an immediate issue by editing a prompt directly where it's currently running, with no record of the change, making a later regression untraceable.\nHow it happens: it's the fastest way to fix an urgent-feeling problem in the moment, and the discipline of going through a registry can feel like unnecessary friction under pressure.\nWhy beginners miss it: the immediate fix works, resolving the pressure of the moment — the cost only shows up later, when a subsequent problem can't be traced back to this unrecorded change.\nDetect: check whether there's any way to see what a production prompt looked like a week ago, or whether the current version is the only one that exists anywhere.\nPrevent: make the registry the only enforced path to a production prompt change, with no legitimate bypass, even under time pressure — build a genuinely fast emergency-fix path within the registry rather than routing around it.\nInterview question: \"Someone needs to fix a broken prompt in production right now. Walk me through what they should actually do.\"\nReal-world consequence: an untraceable, unrollbackable production issue caused by an unrecorded prompt edit, discovered well after the fact with no way to reconstruct what changed.\n\nFailure 2 — Assuming a prompt tuned for one provider transfers cleanly to another without verification.\nWhat: a prompt that was carefully tuned and tested against one model provider is used with a different provider (or a new version of the same provider's model) with no re-verification, and its behavior quietly changes.\nHow it happens: prompts feel like portable, provider-agnostic text, so switching providers can feel like it shouldn't require re-testing.\nWhy beginners miss it: the prompt still \"works\" in the sense of producing some response — the degradation is in quality or format consistency, which isn't always immediately obvious without deliberate comparison.\nDetect: explicitly test the same prompt against both the old and new provider/model version on a consistent set of test cases, comparing outputs directly.\nPrevent: treat any provider or model-version change as requiring re-verification of dependent prompts, connecting to the evaluation practices from Module 2, rather than assuming silent compatibility.\nInterview question: \"Your model provider just released a new version of the model you're using. What do you do before switching your production traffic to it?\"\nReal-world consequence: a silent quality regression across an entire feature, triggered by an upstream provider change nobody explicitly tested against.\n\nFailure 3 — No real environment promotion path, so untested prompt changes reach production directly.\nWhat: a prompt change goes from someone's local experimentation straight into production, with no staging step or verification gate in between.\nHow it happens: setting up a genuine promotion pipeline takes real infrastructure investment, and it's easy to defer while a team is still small or moving fast.\nWhy beginners miss it: most changes made this way do work fine, reinforcing the (risky) habit — the cost only shows up on the specific change that would have been caught by a staging step.\nDetect: check whether there's an actual staging environment and verification gate a prompt change passes through, or whether \"testing\" means one person trying it a few times locally before shipping.\nPrevent: build a real, even if lightweight, promotion path with a defined verification step, matching this module's decision framework for scaling rigor to actual impact.\nInterview question: \"How would you know if a new prompt version is actually safe to promote to production, beyond it looking fine in a few manual tests?\"\nReal-world consequence: a prompt regression reaches real users directly, when a staging verification step would have caught it before any real impact.\n\nFailure 4 — Uniform maximum process applied to every prompt change, causing the team to route around it.\nWhat: every single prompt change, regardless of actual production impact, is required to go through the same heavy review and promotion process, and the team eventually starts finding informal ways to bypass it for \"minor\" changes.\nHow it happens: it feels safer and simpler to apply one uniform standard rather than making a judgment call about relative risk for each change.\nWhy beginners miss it: the policy looks maximally rigorous on paper — the actual behavior (informal bypasses under pressure) only becomes visible by observing what the team does in practice, not what the policy states.\nDetect: check whether low-stakes prompt changes are actually going through the full formal process, or whether informal shortcuts have quietly become the norm for anything perceived as minor.\nPrevent: explicitly scale process rigor to actual production impact, per this module's decision framework, so the formal process remains proportionate enough that people don't feel pressure to route around it.\nInterview question: \"Why might requiring the exact same heavy review process for every single prompt change actually reduce your team's real safety, not increase it?\"\nReal-world consequence: the formal process becomes theater that's regularly bypassed in practice, providing much less real protection than it appears to on paper.",
  },
  {
    section_type: "debugging_playbook",
    title: "A prompt-related regression happened — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"A feature that was working fine suddenly started producing worse output, and we're not sure what changed.\"\nDiagnostic: check the prompt registry's version history for this feature's prompt — if there isn't one, or the most recent change wasn't recorded there, that's very likely part of the problem.\nFix: if a recorded change exists, review and potentially roll it back; if no record exists, this incident is a strong signal to close that registry-enforcement gap immediately, in addition to investigating the current regression.\n\nSymptom: \"Output quality degraded after we updated to a newer model version from our provider.\"\nDiagnostic: compare the current model version's output against the previous version's, using the same prompt and a consistent set of test cases — confirm whether the change is actually attributable to the model version update.\nFix: if confirmed, either roll back to the previous model version while re-tuning the prompt for the new one, or adjust the prompt specifically for the new version's behavior, verified against your test cases before re-deploying.\n\nSymptom: \"A prompt change looked fine in a few manual local tests but broke something once it reached production.\"\nDiagnostic: this points to an inadequate promotion/verification path — manual, ad hoc local testing isn't a substitute for a real staging verification step.\nFix: build a genuine staging verification gate for future changes, sized appropriately to the feature's actual production impact.\n\nSymptom: \"We can't tell which of several recent prompt changes across the team caused a specific regression.\"\nDiagnostic: check whether each change was recorded separately and distinctly in the registry, with clear attribution, or whether multiple changes were bundled together with no way to isolate them.\nFix: for future changes, enforce smaller, individually recorded and reviewable prompt changes rather than bundled edits, so a regression can be isolated to a specific, identifiable change.",
  },
  {
    section_type: "checklist",
    title: "Prompt & Model Lifecycle Management Checklist",
    order_index: 6,
    content: "- [ ] Every production prompt lives in a version-controlled registry that's the genuinely enforced single source of truth\n- [ ] No legitimate path exists to edit a production prompt outside the registry, even under time pressure\n- [ ] Behavior across different model providers (or model versions) is explicitly tested, not assumed to be portable\n- [ ] A real environment promotion path (dev → staging → prod) exists with a verification step at each stage\n- [ ] Process rigor scales to the actual production impact of each specific prompt change\n- [ ] Every promoted change is recorded with what changed and why\n- [ ] A tested, fast rollback path exists for prompt changes, the same as for application code\n- [ ] The registry is periodically audited for informal bypasses that would undermine its role as the single source of truth",
  },
  {
    section_type: "template",
    title: "Prompt Version Change Record",
    order_index: 7,
    content: "Prompt / feature name: ___\nVersion: ___ (previous: ___)\n\nWhat changed: ___\nWhy: ___\n\nModel provider(s) and version(s) tested against: ___\n\nVerification performed: ___ (test cases run, eval results — link to Module 2's practices)\n\nPromotion path status: dev ___ | staging ___ | production ___\n\nRollback plan: ___ (how to revert to the previous version quickly)\n\nApproved by: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [LangSmith: Prompt versioning and hub](https://docs.smith.langchain.com/prompt_engineering) — a widely-used real reference implementation of prompt registry/versioning concepts covered in this module.\n- [n8n: Environments and version control](https://docs.n8n.io/source-control-environments/) (referenced in the n8n agentic course track) — relevant here as a concrete implementation pattern for treating configuration as version-controlled production logic.\n\nRecommended\n- [OpenAI: Prompt engineering guide](https://platform.openai.com/docs/guides/prompt-engineering) and [Anthropic: Prompt engineering overview](https://docs.claude.com/en/docs/build-with-claude/prompt-engineering/overview) — worth comparing directly to see genuine provider-specific behavioral differences firsthand, relevant to this module's multi-provider topic.\n\nReference\n- Standard CI/CD environment-promotion documentation from any major platform (e.g., [GitHub Actions environments](https://docs.github.com/en/actions/deployment/targeting-different-environments/using-environments-for-deployment)) — the underlying dev/staging/prod promotion pattern is not LLM-specific, and understanding it in its general application-deployment context clarifies why it applies here too.\n\nAdvanced\n- Search for published case studies on multi-provider LLM abstraction layers (several companies have written about their internal approaches) — useful for seeing how larger organizations handle the real architectural trade-offs of supporting multiple model providers at scale.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Set up a version-controlled prompt registry for a small prompt library",
    problem_statement: "Take 3-4 prompts (reuse designs from the n8n course track's exercises, or write new simple ones for a hypothetical feature) and set up a version-controlled registry for them — a git-based folder structure with clear versioning is sufficient for this exercise; a dedicated tool isn't required.\n\nMake at least 2 deliberate changes to one of the prompts over time, each recorded with what changed and why, using this module's Prompt Version Change Record template. Confirm you could roll back to the original version using only your recorded history.",
    starter_context: "The specific tooling matters less than the discipline — a well-organized git repository with clear commit messages can satisfy the core requirements of this exercise.",
    hints: [
      "Make your two changes genuinely different and meaningful (not trivial whitespace edits) so your change records actually have something substantive to describe.",
      "Actually perform the rollback, don't just claim you could — confirm your recorded history genuinely lets you reconstruct the original version.",
    ],
    solution_notes: "A strong submission has a genuinely version-controlled set of prompts with clear, substantive change history, complete Prompt Version Change Records for at least two real changes, and a demonstrated (not just claimed) successful rollback using only the recorded version history — directly proving the registry actually functions as a real safety net, not just a nominal system.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Test the same prompt across two different model providers",
    problem_statement: "Take one prompt (from the guided exercise or a new one) and run it against two different model providers (if you have API access to more than one; otherwise, compare two different model tiers from the same provider, e.g. a faster/smaller model versus a more capable one) using at least 5 identical test inputs.\n\nDocument the specific behavioral differences you observe — not just 'they're different' but concrete, specific differences (format, verbosity, instruction-following precision, edge-case handling).",
    starter_context: "If you don't have access to multiple providers, comparing model tiers within one provider still demonstrates the same core lesson about behavioral non-portability.",
    hints: [
      "Choose test inputs that include at least one genuinely ambiguous or edge-case scenario, not just straightforward ones — differences often show up more clearly there.",
      "Document differences specifically enough that someone reading your notes could understand exactly what would need to be adjusted if migrating this prompt from one provider/model to the other.",
    ],
    solution_notes: "A strong submission identifies specific, concrete behavioral differences (not vague impressions) across at least 5 real test cases, with particular attention to how differences show up more clearly on ambiguous/edge-case inputs than straightforward ones — directly demonstrating this module's core lesson that prompt portability across providers or model versions cannot be assumed without explicit testing.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Design a lifecycle process proportionate to different prompt risk levels",
    problem_statement: "You're designing prompt lifecycle management for a company with three distinct prompts: (1) one powering their core, high-traffic customer-facing chat feature, (2) one used in an internal tool by a handful of employees, (3) one still in active experimental development, not yet used by anyone in production.\n\nDesign a distinct lifecycle process (registry rigor, promotion path, review requirements) for each, explicitly justifying why the three differ.",
    starter_context: "The point is genuine differentiation based on actual risk/impact — avoid a design that ends up treating all three essentially the same.",
    hints: [
      "For prompt 3, consider what minimal discipline is still worth applying even during active experimentation, versus what can reasonably wait until it's closer to production use.",
      "For prompt 1, be specific about what verification gate would actually catch a real regression before it reaches the high-traffic feature's real users.",
    ],
    solution_notes: "A strong submission proposes genuinely differentiated processes — likely full registry discipline, staged promotion, and thorough verification for prompt 1; a lighter but still real registry and promotion process for prompt 2; and minimal but not zero discipline (at least basic version tracking) for prompt 3 — with explicit reasoning tied to each prompt's actual production impact, directly applying this module's proportionate-rigor principle rather than defaulting to uniform treatment.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why should a production prompt be treated with the same rigor as production code, rather than as casual configuration?", what_is_tested: "Understanding that prompts directly determine system behavior and deserve corresponding process discipline.", strong_answer_structure: "Explain that an unversioned, untested prompt edit can cause a real, hard-to-diagnose regression exactly like an unversioned code change would, and the fact that it's 'just text' doesn't reduce its actual production impact.", weak_answer_example: "\"Prompts are simpler than code so they don't need the same process\" — misunderstands that simplicity of format doesn't reduce production impact.", follow_up_question: "What specifically would you put in place to enforce this discipline for a team that's currently editing prompts casually?" },
  { category: "applied", order_index: 1, question: "Your team is about to switch from one model provider to another for a production feature. What would you do before making the switch?", what_is_tested: "Practical instinct to verify behavior, not assume portability.", strong_answer_structure: "Describe explicitly testing the existing prompts against the new provider using a consistent set of test cases, comparing output quality and format against the current provider's behavior, before promoting the switch to production.", weak_answer_example: "\"I'd just switch the API endpoint\" — assumes portability without verification.", follow_up_question: "What would you do if you found the new provider handled the same prompt noticeably differently?" },
  { category: "scenario", order_index: 2, question: "A feature that was working well suddenly regressed, and your team suspects a prompt change but isn't sure. How do you investigate?", what_is_tested: "Diagnostic process relying on prompt version history as the first, primary source of truth.", strong_answer_structure: "Check the prompt registry's change history for this feature first — if a recent change is recorded, review it directly; if no record exists, recognize that as itself a significant finding pointing to a process gap, not just an investigation dead end.", weak_answer_example: "Guessing at possible causes without first checking whether a recorded change history actually exists to consult.", follow_up_question: "What would you do differently going forward if you discovered there was no change history to consult?" },
  { category: "debugging", order_index: 3, question: "How would you design an emergency prompt-fix process that's both fast and doesn't bypass your version-control discipline?", what_is_tested: "Ability to reconcile urgency with process rigor, rather than treating them as mutually exclusive.", strong_answer_structure: "Describe a genuinely fast path within the registry itself (a lightweight emergency-change workflow that still records the change and its rationale), rather than allowing emergency situations to become an accepted excuse for bypassing the registry entirely.", weak_answer_example: "\"In an emergency, you just fix it directly and document it later\" — accepts bypassing the registry as normal, which undermines the whole discipline.", follow_up_question: "How would you make sure 'document it later' actually happens, rather than being forgotten once the pressure is off?" },
  { category: "behavioural", order_index: 4, question: "Describe a time a change to a prompt or configuration caused an unexpected regression. How was it diagnosed, and what changed in your process afterward?", what_is_tested: "Real or realistic experience with exactly this failure mode and a genuine process improvement made as a result.", strong_answer_structure: "A strong answer names the specific regression, how it was traced back to its cause (ideally via some form of change history, or the painful discovery that none existed), and a concrete change to prompt management practice made afterward.", weak_answer_example: "A vague answer with no specific incident or no described change in process afterward.", follow_up_question: "How would you verify that your process change actually prevents a repeat of this specific failure mode?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLMOps & Prompt Versioning").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("LLMOps Module 1 (Prompt & Model Lifecycle Management) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
