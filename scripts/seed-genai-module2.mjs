import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "dec9bea6-f0fe-4dc8-9c85-b687651960f3"; // Prompting & Structured Outputs

const sections = [
  {
    section_type: "the_field",
    title: "What getting consistent, parseable output instead of fighting the model every call actually looks like",
    order_index: 0,
    content: "A prompt that works most of the time and occasionally returns malformed output, an extra sentence of preamble, or a subtly different format is a real, recurring engineering tax — every downstream consumer of that output has to defensively handle the variation, and \"just try the prompt again and hope\" is not a real engineering practice. This module builds the actual discipline that eliminates most of this tax: prompt patterns, few-shot design, and structured output enforcement that make the model's output genuinely reliable to build on top of.\n\nThis directly connects to and extends the LCEL structured-output principle from the LangChain course track and the n8n course track's \"JSON mindset\" — this module builds the foundational prompting skill those courses' higher-level frameworks depend on. Understanding why a specific prompt pattern produces more consistent structured output (grounded in this course's Module 1 mechanism-level understanding of how the model actually processes and generates text) is what separates genuine prompt engineering skill from trial-and-error prompt fiddling.\n\nOutput parsing and validation close the loop with the defensive-engineering discipline established throughout this catalog's engineering tracks: even a well-engineered prompt occasionally produces output that doesn't perfectly match the expected structure, and a production system needs explicit validation and a defined fallback, not blind trust that the prompt will always work.",
  },
  {
    section_type: "mental_models",
    title: "How engineers think about prompting and structured outputs",
    order_index: 1,
    content: "**1. Prompt engineering that works reliably is a discipline with real, learnable patterns, not trial-and-error until something seems to work.** Few-shot examples, explicit format specification, and structured output enforcement are established, understood techniques with real mechanisms behind why they improve reliability, not folklore.\n\n**2. A prompt's job is to constrain the model's output space as much as the task allows, not just to describe the task.** A vague instruction leaves room for the model's output to vary in ways a precise, constrained instruction (with explicit format, explicit examples, explicit boundaries) doesn't — this connects directly to the Claude Code AI course track's \"a prompt is a specification, not a wish\" principle, applied specifically to output reliability here.\n\n**3. Few-shot examples work by showing the model the actual pattern you want, which is often more effective than describing it abstractly.** This connects to Module 1's understanding of how the model processes context — concrete examples in context shape the generation distribution more reliably than an abstract instruction alone for many tasks.\n\n**4. Structured output enforcement (JSON schemas, provider-specific structured output features) should be the default for any output a downstream system will parse programmatically**, the same principle established in the LangChain and n8n course tracks, now built from first principles here rather than assumed.\n\n**5. Even well-engineered structured output needs defensive parsing and validation, because a probabilistic system's \"usually reliable\" is never \"always reliable.\"** This is the same defensive-engineering discipline established throughout this catalog's engineering tracks, foundational to any production system built on model output.\n\n**6. Getting consistent output is a genuinely different skill from getting a good single response.** A prompt that produces one excellent response but fails to generalize its format/quality across the real variety of inputs hasn't actually solved the reliability problem this module is built around.",
  },
  {
    section_type: "decision_framework",
    title: "How should I design a prompt for reliable, parseable output?",
    order_index: 2,
    content: "IF the task's output will be consumed by another system programmatically THEN use structured output enforcement (a JSON schema, a provider's structured-output feature) rather than relying on the model to naturally produce well-formatted text. BECAUSE this structurally constrains the output format in a way free-form prose instructions can't guarantee, dramatically reducing the parsing-failure rate downstream systems have to handle. WATCH OUT FOR: even structured output features aren't a perfect guarantee — pair this with defensive validation, per this catalog's established principle, not as a substitute for it.\n\nIF the task has a specific, non-obvious format or reasoning pattern you want the model to follow THEN use few-shot examples showing the actual desired pattern, rather than only describing it abstractly in the instructions. BECAUSE concrete examples in context shape the model's output more reliably for many tasks than an abstract description alone, connecting to Module 1's understanding of in-context learning. WATCH OUT FOR: few-shot examples that are too similar to each other, or don't cover the real variety of cases the task will actually encounter, can lead to overfitting the model's behavior to the specific examples shown rather than the general pattern intended.\n\nIF you're iterating on a prompt to improve reliability THEN test against a genuinely varied set of real or realistic inputs, not just the one example that motivated the current draft. BECAUSE this is the same input-variety testing discipline established throughout this catalog, and a prompt that works well on one example but hasn't been tested against real variety hasn't actually demonstrated reliability.\n\nIF you've built a structured output pipeline THEN add explicit output validation and a defined fallback for malformed responses, regardless of how reliable the prompt has tested. BECAUSE a probabilistic system's demonstrated reliability rate is never a guarantee of 100% correctness, and production systems need to handle the residual failure rate explicitly, not assume it away.",
  },
  {
    section_type: "workflow",
    title: "The real process for building a reliable structured-output pipeline",
    order_index: 3,
    content: "1. Define the exact output structure the downstream system needs — the specific fields, types, and format, as a real schema, not a loose description.\n\n2. Write the prompt with explicit format instructions, using structured output enforcement (a JSON schema or provider feature) as the primary mechanism, not relying on prose instructions alone.\n\n3. If the task has a non-obvious pattern, add few-shot examples showing the actual desired output for realistic input variety, not just one convenient case.\n\n4. Test the prompt against a genuinely varied set of real or realistic inputs, including edge cases and ambiguous ones, checking both format compliance and content quality.\n\n5. Implement output validation that checks the response against the expected schema before it's used downstream, catching format failures explicitly.\n\n6. Define a fallback for validation failures — a retry, a default value, an escalation to human review — rather than letting a malformed response propagate silently.\n\n7. Monitor the actual production failure rate over time, since real-world input variety often surfaces edge cases development testing didn't anticipate.\n\n8. Iterate the prompt based on real observed failures, adding to the few-shot examples or tightening the format instructions as specific gaps are found.",
  },
  {
    section_type: "failure_modes",
    title: "How prompting and structured output pipelines actually break",
    order_index: 4,
    content: "Failure 1 — Relying on prose instructions alone for output format, without structured output enforcement.\nWhat: the prompt describes the desired output format in natural language (\"respond with a JSON object containing...\") but doesn't use the model provider's actual structured output enforcement feature, leaving format compliance dependent on the model correctly following prose instructions every time.\nHow it happens: prose-described format instructions are simpler to write and often work most of the time, making structured output enforcement feel like unnecessary additional configuration.\nWhy beginners miss it: prose-instructed format compliance genuinely does work correctly most of the time — the residual failure rate isn't visible until real production volume surfaces the cases where it doesn't.\nDetect: check whether the pipeline uses actual structured output enforcement, or relies on prose format instructions alone.\nPrevent: use structured output enforcement as the default mechanism for any programmatically-consumed output, per this module's decision framework, not prose instructions alone.\nInterview question: \"Your prompt asks the model to 'respond with valid JSON' but doesn't use a structured output feature. What's the risk?\"\nReal-world consequence: an intermittent parsing failure rate that downstream systems have to defensively handle, when structured output enforcement would have dramatically reduced this failure rate at the source.\n\nFailure 2 — Testing a prompt against only one or a few convenient examples, not real input variety.\nWhat: a prompt appears reliable based on testing against a small, convenient set of examples, but fails or produces inconsistent output once real production input variety is encountered.\nHow it happens: it's simpler and faster to test against a couple of examples that come to mind first than to deliberately construct a genuinely varied test set.\nWhy beginners miss it: the prompt does work correctly for the tested examples, creating confidence that doesn't reflect its actual reliability across real variety.\nDetect: check how many, and how varied, the examples used to validate the prompt actually were, versus the real variety of production input.\nPrevent: test against a genuinely varied set of real or realistic inputs before considering a prompt reliable, per this module's workflow, applying the same input-variety discipline established throughout this catalog.\nInterview question: \"Your prompt worked perfectly on the 3 examples you tested. Why might it still fail in production?\"\nReal-world consequence: a prompt that seemed reliable in development produces unexpected format or content failures once it encounters the real variety of production input it was never actually tested against.\n\nFailure 3 — No output validation, letting malformed responses propagate silently.\nWhat: even a well-engineered, mostly-reliable prompt occasionally produces a malformed or unexpected response, and without explicit validation, this propagates into downstream systems rather than being caught and handled.\nHow it happens: after investing real effort in prompt engineering to achieve high reliability, it can feel like validation is a redundant extra step for a prompt that's already been tested and tuned.\nWhy beginners miss it: the prompt's high reliability rate genuinely does mean most responses are correctly formatted — the residual failure rate, however small, is exactly what validation is meant to catch, and skipping it means that residual rate goes unhandled.\nDetect: check whether the pipeline validates responses against the expected schema before use, or trusts the prompt's reliability implicitly.\nPrevent: add explicit output validation and a defined fallback for validation failures as a standard, non-optional part of any structured-output pipeline, per this module's workflow.\nInterview question: \"Your prompt has a 98% format-compliance rate. Do you still need output validation? Why?\"\nReal-world consequence: the 2% of malformed responses cause downstream errors or silently corrupt data, because nothing was in place to catch and handle them before they propagated.\n\nFailure 4 — Few-shot examples that don't represent the real variety of the task, leading to narrow overfitting.\nWhat: the few-shot examples used in the prompt are too similar to each other or don't cover the real range of cases the task will encounter, causing the model to over-generalize from the specific examples shown rather than the intended general pattern.\nHow it happens: it's simpler to write a few similar, clean examples than to deliberately construct examples spanning the real variety and edge cases of the task.\nWhy beginners miss it: the model does perform well on inputs similar to the few-shot examples — the narrow generalization only becomes visible on inputs that differ meaningfully from what was shown.\nDetect: check whether the few-shot examples span genuinely different cases (including edge cases) or are variations of essentially the same scenario.\nPrevent: deliberately construct few-shot examples spanning real task variety, per this module's decision framework, rather than convenient, similar examples.\nInterview question: \"Your few-shot examples are all variations of a customer asking about billing. How might this affect the prompt's reliability for other question types?\"\nReal-world consequence: a prompt that performs well specifically on inputs resembling its few-shot examples but generalizes poorly to the real variety of production input it's actually meant to handle.",
  },
  {
    section_type: "debugging_playbook",
    title: "Your structured output pipeline isn't reliable — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"We're getting intermittent JSON parsing failures from our model output.\"\nDiagnostic: check whether the pipeline uses actual structured output enforcement, or relies on prose format instructions alone.\nFix: switch to the model provider's structured output feature as the primary mechanism, and add explicit validation as a backup.\n\nSymptom: \"A prompt that worked well in development is producing unexpected output in production.\"\nDiagnostic: compare the real production input variety against what the prompt was actually tested against during development.\nFix: expand your test set to include the real variety now surfacing in production, and iterate the prompt (including few-shot examples) based on the specific gaps found.\n\nSymptom: \"A malformed response caused a downstream error that wasn't caught before it happened.\"\nDiagnostic: check whether output validation exists at all, or whether the pipeline trusted the prompt's reliability implicitly.\nFix: add explicit validation and a defined fallback (retry, default, escalation) for malformed responses going forward.\n\nSymptom: \"The model handles inputs similar to our few-shot examples well, but performs poorly on other legitimate input types.\"\nDiagnostic: check whether the few-shot examples actually span the real variety of the task, or are narrow variations of one scenario.\nFix: broaden the few-shot examples to cover genuinely different real cases, and re-test against the previously poorly-handled input types.",
  },
  {
    section_type: "checklist",
    title: "Reliable Structured-Output Pipeline Checklist",
    order_index: 6,
    content: "- [ ] Output structure is defined as a real schema, not a loose description\n- [ ] Structured output enforcement (a JSON schema or provider feature) is used, not prose format instructions alone\n- [ ] Few-shot examples, if used, span genuinely varied real cases, not narrow variations of one scenario\n- [ ] The prompt has been tested against a genuinely varied set of real or realistic inputs, not just one or two convenient examples\n- [ ] Explicit output validation checks responses against the expected schema before downstream use\n- [ ] A defined fallback exists for validation failures, not silent propagation\n- [ ] Real production failure rate is monitored over time, feeding back into prompt iteration",
  },
  {
    section_type: "template",
    title: "Structured-Output Prompt Design Record",
    order_index: 7,
    content: "Task: ___\n\nOutput schema: { field: type — meaning, ... }\n\nStructured output mechanism used: ___\n\nFew-shot examples (if used) and their coverage of real task variety: ___\n\nTest inputs used (list, including edge cases): ___\nObserved format-compliance rate: ___\n\nOutput validation rules: ___\nFallback for validation failures: ___\n\nProduction failure rate (once live): ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [OpenAI: Structured Outputs guide](https://platform.openai.com/docs/guides/structured-outputs) (referenced across this catalog) — the direct technical reference for structured output enforcement this module centers on.\n- [Anthropic: Prompt engineering overview](https://docs.claude.com/en/docs/build-with-claude/prompt-engineering/overview) — the official, comprehensive reference for prompting patterns including few-shot design.\n\nRecommended\n- [OpenAI: Prompt engineering guide](https://platform.openai.com/docs/guides/prompt-engineering) — practical, provider-specific patterns for reliable prompting.\n\nReference\n- The Claude Code AI course track's Module 1 mental model \"a prompt is a specification, not a wish\" — the direct principle this module's prompt-design discipline applies at the API-integration level.\n\nAdvanced\n- Brown et al., [\"Language Models are Few-Shot Learners\"](https://arxiv.org/abs/2005.14165) (2020) — the foundational research paper on few-shot in-context learning, for a deeper theoretical grounding of the technique this module applies practically.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Build a reliable structured-output pipeline for a real task",
    problem_statement: "Design a task with a real, specific output schema (e.g., extracting structured contact information from unstructured text: name, email, phone, company). Build a prompt using structured output enforcement, test against at least 8 varied real or realistic inputs (including at least 2 deliberately messy/ambiguous ones), and add explicit validation with a defined fallback for any case that fails.",
    starter_context: "Choose a task with a genuinely non-trivial schema — a single-field extraction task won't give you much to practice against.",
    hints: [
      "Include inputs where some expected fields are genuinely missing (e.g., no phone number provided) — your schema and validation need to handle this realistically, not assume every field is always present.",
      "Actually implement the validation logic, not just describe it — check the response against your schema programmatically.",
    ],
    solution_notes: "A strong submission has a genuinely working structured-output pipeline tested against real, varied inputs including deliberately messy cases, real programmatic validation (not just described), and a defined, sensible fallback for validation failures — directly demonstrating the complete reliable-pipeline discipline this module's workflow requires.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Design few-shot examples spanning genuine task variety",
    problem_statement: "For a task with a non-obvious classification or extraction pattern (your choice — e.g., classifying customer message urgency with nuanced criteria), design a set of 4-5 few-shot examples that genuinely span the task's real variety, including at least one deliberately ambiguous or edge case. Test the resulting prompt against 3 new inputs not resembling any single few-shot example, and report whether it generalizes correctly.",
    starter_context: "The genuine variety in your few-shot examples, and the genuinely different test inputs, are the actual point of this exercise.",
    hints: [
      "Deliberately make your few-shot examples different from each other in structure, not just content — this tests whether the model is learning the actual underlying pattern rather than a superficial format.",
      "Your 3 test inputs should be different enough from the few-shot examples that success genuinely demonstrates generalization, not just pattern-matching to a similar example.",
    ],
    solution_notes: "A strong submission's few-shot examples genuinely span real task variety (not narrow variations of one case), and the test on genuinely different new inputs either demonstrates real generalization or honestly reports where it falls short — directly practicing the variety-aware few-shot design this module's fourth failure mode addresses.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose and fix an unreliable production prompt",
    problem_statement: "You're told: \"Our prompt for extracting order details from customer emails worked great in testing (5 examples, all passed) but is failing on about 15% of real production emails, either with malformed JSON or clearly wrong extracted values.\" You're given no further detail.\n\nDiagnose the most likely causes and propose a specific remediation plan.",
    starter_context: "This is a diagnostic exercise based on the description alone, directly testing this module's core failure modes.",
    hints: [
      "The small original test set (5 examples) combined with the real 15% production failure rate is a strong signal pointing toward insufficient test variety, per this module's second failure mode.",
      "Consider whether structured output enforcement was likely used, given the description mentions 'malformed JSON' specifically as a failure symptom.",
    ],
    solution_notes: "A strong submission identifies both likely causes: insufficient test variety during development (5 examples couldn't have surfaced the real 15% failure rate), and likely absence of structured output enforcement (given malformed JSON is an explicit symptom) or absent validation. The remediation plan should propose switching to structured output enforcement if not already used, expanding the test set to include real production failure examples, adding explicit validation with a fallback, and iterating the prompt based on the specific real failures found — directly applying this module's full workflow to a realistic, diagnosable scenario.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why is structured output enforcement generally better than relying on prose instructions asking the model to 'respond in JSON'?", what_is_tested: "Understanding of the reliability difference between structural enforcement and prose-based instruction.", strong_answer_structure: "Explain that structured output enforcement constrains the model's output generation to actually conform to a schema, while prose instructions rely on the model correctly following a natural-language request every time — the former has a meaningfully lower failure rate for format compliance.", weak_answer_example: "\"They're basically equivalent, just different syntax\" — misses the actual reliability difference between structural enforcement and prose-based instruction-following.", follow_up_question: "Is structured output enforcement a perfect guarantee? What else do you need alongside it?" },
  { category: "applied", order_index: 1, question: "Why should few-shot examples span genuinely different cases rather than being similar variations of one scenario?", what_is_tested: "Understanding of the overfitting-to-examples risk in few-shot prompting.", strong_answer_structure: "Explain that narrow, similar few-shot examples risk the model learning the specific pattern of those examples rather than the intended general task, leading to poor generalization on inputs that differ meaningfully from what was shown.", weak_answer_example: "\"More examples are always better regardless of variety\" — misses that variety, not just quantity, is what actually matters for generalization.", follow_up_question: "How many few-shot examples, and how varied, would you consider sufficient for a moderately complex classification task?" },
  { category: "scenario", order_index: 2, question: "A prompt achieved 100% success on your 5 test examples but is failing on 15% of real production traffic. What does this tell you?", what_is_tested: "Recognition that a small test set can't reliably predict real-world reliability.", strong_answer_structure: "Explain that 5 examples is far too small a sample to represent real production input variety, and the 15% failure rate is likely surfacing genuine edge cases and variety the small test set never exercised — the fix is expanding the test set with real production examples, not just re-tuning against the original 5.", weak_answer_example: "\"The prompt must have just gotten worse somehow\" — doesn't recognize the actual root cause (insufficient original test coverage).", follow_up_question: "How would you build an ongoing practice to keep test coverage representative as production traffic evolves?" },
  { category: "debugging", order_index: 3, question: "Your structured-output pipeline has a 98% format-compliance rate. A colleague argues output validation is unnecessary overhead given this high reliability. How do you respond?", what_is_tested: "Whether the candidate insists on defensive validation regardless of measured reliability.", strong_answer_structure: "Explain that a 98% rate still means a real, non-zero failure rate in production, and without validation, that 2% propagates as malformed data into downstream systems — validation and a defined fallback are what actually handle this residual risk, regardless of how high the base reliability is.", weak_answer_example: "\"98% is high enough, validation probably isn't needed\" — accepts residual risk without a plan to handle it.", follow_up_question: "What would a reasonable fallback look like for the 2% of cases that fail validation?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to iterate on a prompt or specification to make its output genuinely reliable across real-world variety, not just for a specific test case.", what_is_tested: "Real or realistic experience with the iteration discipline this module is built around.", strong_answer_structure: "A strong answer describes the specific task, how initial testing revealed a reliability gap, what real variety was incorporated into further testing, and the actual iteration process that improved reliability.", weak_answer_example: "A vague answer with no specific task or iteration process described.", follow_up_question: "How did you know when the prompt was genuinely reliable enough, versus needing further iteration?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "Prompt Engineering").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("GenAI Module 2 (Prompting & Structured Outputs) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
