import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "b4ae4be0-7173-4a31-874e-4b8ccb6f11b0"; // Foundations

const sections = [
  {
    section_type: "the_field",
    title: "What understanding what's actually happening inside the model, not just the API surface, actually looks like",
    order_index: 0,
    content: "Every other course in this catalog treats the model as a capable black box you call through an API — and for most practical engineering work, that's the right level of abstraction. But every one of those courses' debugging playbooks eventually hits a wall a purely API-level understanding can't get past: why does the model handle long context worse near the middle, why do embeddings capture semantic similarity the way they do, why does tokenization occasionally produce a surprising, seemingly arbitrary boundary that breaks a downstream assumption. This module exists to build the layer of understanding beneath the API surface that makes those \"why\" questions answerable.\n\nThe distinction between generative and discriminative models is the conceptual foundation everything else in this course (and much of this catalog) builds on: a discriminative model (classic ML, covered in the Data Science course track) learns a boundary between categories or a mapping to a predicted value; a generative model learns the underlying distribution well enough to produce new, plausible samples from it — this is why an LLM can write coherent new text rather than just classify existing text, and it's the actual technical reason generative and predictive models solve genuinely different problems, the practical distinction established in the Data Science with Generative AI course track.\n\nTransformer architecture and tokenization aren't abstract theory here — they're the direct, practical explanation for real, observable behavior every other course's debugging playbooks reference: why chunking strategy matters (tokenization and context handling), why certain prompts produce inconsistent results (attention patterns and positional effects), and why embeddings power every RAG system in this catalog. Understanding this layer is what turns \"the model just does that sometimes\" into a real, technical explanation you can reason about and predict.",
  },
  {
    section_type: "mental_models",
    title: "How engineers think about what's actually happening inside the model",
    order_index: 1,
    content: "**1. Generative models learn a distribution; discriminative models learn a boundary.** This is the actual technical reason generative models can produce novel, coherent output while classic discriminative models (regression, classification) are better suited to prediction from fixed categories or values — understanding this distinction at the mechanism level makes the Data Science with Generative AI course track's practical positioning guidance make sense as more than just a rule of thumb.\n\n**2. The transformer's attention mechanism is what lets the model weigh the relevance of every other token when processing each token, which is the actual mechanism behind both the model's power and its real, practical limitations** (context window effects, the computational cost that scales with context length) — this isn't abstract; it's the direct explanation for behavior every engineering-focused course in this catalog has to work around.\n\n**3. Tokenization is the model's actual unit of processing, and it doesn't map cleanly onto human concepts like \"words\" or \"characters.\"** This is the real, mechanistic reason certain prompts behave unexpectedly (a word split across an unusual token boundary), and understanding it makes debugging genuinely tokenization-related issues possible instead of mysterious.\n\n**4. Embeddings are a learned geometric representation where semantic similarity corresponds to proximity in a high-dimensional space** — this is the actual mechanism behind every RAG system's retrieval step in this catalog, and understanding it at this level is what makes retrieval quality debugging (from the LangChain course track) a real, reasoned process rather than trial and error.\n\n**5. Understanding the model's internals doesn't mean you need to be able to build a transformer from scratch to work effectively with LLMs day to day — it means having the conceptual grounding to correctly reason about behavior the API surface alone doesn't explain.** This is a genuinely useful, practical level of understanding, distinct from either pure API-usage fluency or deep ML research expertise.\n\n**6. This foundational understanding is what lets you distinguish \"this is a fundamental limitation of how the model works\" from \"this is a fixable prompt/engineering problem\"** — a distinction every debugging playbook throughout this catalog implicitly depends on, made explicit and reasoned here.",
  },
  {
    section_type: "decision_framework",
    title: "When does understanding model internals actually matter for practical engineering work?",
    order_index: 2,
    content: "IF you're debugging behavior that seems inconsistent with how the API surface alone would explain it (unexpected token-boundary effects, degraded performance at specific context positions, embedding similarity results that seem semantically odd) THEN understanding the underlying mechanism (tokenization, attention, embedding geometry) is genuinely necessary to diagnose the actual cause, not just apply a workaround. BECAUSE these are exactly the class of problems where API-level reasoning alone runs out, and the mechanism-level understanding this module builds is what makes real diagnosis possible.\n\nIF you're doing routine application-building work (calling an API, designing a prompt, building a RAG pipeline using established patterns) THEN the API-surface level of understanding from this catalog's other engineering-focused courses is sufficient for day-to-day work, and deep mechanism-level understanding isn't a blocking prerequisite. BECAUSE most practical engineering work genuinely operates well at the abstraction level those courses teach — this module's depth is valuable specifically for the harder diagnostic cases, not a universal requirement for every task.\n\nIF you're evaluating a claim about model behavior or capability that seems to contradict how you understand the underlying mechanism to work THEN use that mechanistic understanding to critically assess the claim, rather than accepting it uncritically. BECAUSE genuine understanding of how transformers, tokenization, and embeddings actually work is what lets you distinguish a plausible claim from a misleading or oversimplified one, in a field with a lot of both.\n\nIF you're building the embedding-based similarity search this module's exercise requires THEN this hands-on implementation is specifically designed to convert the conceptual understanding into genuine, practical fluency — treat it as more than an academic exercise, since it's the direct foundation for every RAG system covered elsewhere in this catalog.",
  },
  {
    section_type: "workflow",
    title: "The real process for building genuine model-internals understanding",
    order_index: 3,
    content: "1. Understand the generative-versus-discriminative distinction at the mechanism level — what each type of model is actually learning (a distribution versus a boundary), not just their different practical use cases.\n\n2. Study the transformer's attention mechanism specifically for what it means practically — why the model can weigh relevance across a full context, and what the real computational and positional trade-offs of that mechanism are.\n\n3. Understand tokenization concretely — experiment with how real text actually gets tokenized, including surprising or unintuitive boundary cases, to build genuine intuition rather than abstract knowledge.\n\n4. Understand embeddings as a geometric representation — build the embedding-based similarity search exercise hands-on, connecting the abstract \"semantic similarity as proximity\" concept to real, observable behavior.\n\n5. Connect each concept explicitly back to a real, practical behavior from elsewhere in this catalog — why chunking strategy matters (tokenization), why RAG retrieval sometimes surfaces surprising results (embedding geometry), why long-context performance can degrade (attention mechanics).\n\n6. Practice distinguishing \"this is a fundamental model limitation\" from \"this is a fixable engineering problem\" using this mechanistic understanding, for real or realistic scenarios.\n\n7. Use this foundational understanding as a genuine diagnostic tool going forward — when application-level debugging (from this catalog's other courses) runs out of explanations, return to this mechanistic layer.\n\n8. Recognize the appropriate depth for different tasks — this level of understanding is valuable for hard diagnostic work, not a universal prerequisite for routine application building.",
  },
  {
    section_type: "failure_modes",
    title: "How model-internals understanding gaps actually cause real problems",
    order_index: 4,
    content: "Failure 1 — Treating a fundamental model limitation as a fixable engineering problem, and iterating indefinitely on a workaround that can't succeed.\nWhat: a real behavior rooted in how the model's architecture actually works (an attention-related context-position effect, a tokenization boundary issue) gets treated as a prompt-engineering or application-code bug, leading to repeated, unsuccessful attempts to \"fix\" something that's actually a genuine model characteristic.\nHow it happens: without mechanism-level understanding, every unexpected behavior looks equally likely to be an application-level fix away from resolution, since there's no way to distinguish the two categories.\nWhy beginners miss it: application-level debugging genuinely does fix many real issues, which can create an assumption that any issue is similarly fixable at that level — the gap only becomes visible once mechanism-level understanding reveals some issues genuinely aren't.\nDetect: for a persistent, unexplained issue that resists repeated application-level fixes, consider whether it might trace back to a genuine model/architecture characteristic rather than a fixable bug.\nPrevent: build the mechanism-level understanding this module provides specifically to make this distinction reliably, rather than treating every issue as equally likely to be application-fixable.\nInterview question: \"You've tried five different prompt variations to fix an inconsistent behavior with no success. What would make you suspect this is actually a fundamental model characteristic rather than a fixable prompt issue?\"\nReal-world consequence: significant wasted engineering time iterating on an unsolvable-at-that-level problem, when recognizing it as a genuine model characteristic would have redirected effort toward an actual workaround (a different model, a different architecture, an application-level compensation) much sooner.\n\nFailure 2 — Debugging tokenization-related issues without any real understanding of how tokenization actually works.\nWhat: an issue caused by an unusual or unexpected token boundary (a specific word or format being split in a way that affects model behavior) gets debugged blindly, without the tokenization understanding that would make the actual cause identifiable.\nHow it happens: tokenization is often treated as an invisible implementation detail, since most application-level work doesn't require thinking about it directly.\nWhy beginners miss it: most of the time, tokenization behaves in ways that don't cause visible problems, so there's no natural prompt to build understanding of it until a genuinely tokenization-related issue actually occurs.\nDetect: for a puzzling, input-format-specific behavior inconsistency, consider whether tokenization of that specific input might be the actual explanation.\nPrevent: build genuine tokenization understanding as part of this module's foundational work, so this diagnostic path is available when needed.\nInterview question: \"A model behaves inconsistently specifically for inputs containing a certain kind of formatting or characters. What would you investigate?\"\nReal-world consequence: a genuinely tokenization-rooted issue goes undiagnosed indefinitely, since the actual explanation requires understanding most engineers never built.\n\nFailure 3 — Treating embedding-based retrieval as a black box, unable to diagnose why semantically-expected results don't appear.\nWhat: a RAG system's retrieval step surfaces results that seem semantically wrong or misses results that seem like they should be found, and without understanding embeddings as a geometric representation, this is undebuggable beyond generic \"try different chunking\" trial and error.\nHow it happens: RAG systems are often built and used at the API/library level (from the LangChain course track) without the underlying embedding-geometry understanding this module provides.\nWhy beginners miss it: library-level RAG tooling does work correctly most of the time without deep embedding understanding — the gap becomes a real problem specifically when retrieval quality issues need genuine diagnosis rather than trial-and-error tuning.\nDetect: for a persistent retrieval-quality issue that doesn't respond to standard chunking/reranking adjustments, consider whether a deeper embedding-geometry understanding would reveal the actual cause.\nPrevent: build genuine embedding understanding through this module's hands-on similarity-search exercise, connecting the abstract mechanism to real, observable retrieval behavior.\nInterview question: \"Your RAG system consistently fails to retrieve a document you'd expect it to find for a semantically related query. What would you investigate, beyond just trying different chunk sizes?\"\nReal-world consequence: retrieval quality issues that could have been diagnosed and fixed with real embedding-geometry understanding instead get worked around indefinitely with trial-and-error tuning, never actually addressing the root cause.\n\nFailure 4 — Uncritically accepting a claim about model capability or behavior that contradicts basic architectural understanding.\nWhat: a claim about what a model can or should do is accepted without the mechanism-level understanding that would let it be critically evaluated, in a field where confident-sounding but inaccurate claims are common.\nHow it happens: without genuine understanding of the underlying mechanism, there's no independent basis for evaluating whether a claim about model behavior is actually plausible.\nWhy beginners miss it: a confident, technically-worded claim can sound authoritative regardless of its actual accuracy, and without mechanism-level grounding, there's no way to distinguish a genuinely informed claim from a plausible-sounding but wrong one.\nDetect: for any surprising or consequential claim about model behavior, check whether it's actually consistent with genuine understanding of how the underlying mechanism works.\nPrevent: build the mechanism-level understanding this module provides specifically to serve as this critical-evaluation foundation.\nInterview question: \"Someone claims a specific prompting trick fundamentally changes how the model's attention mechanism processes context. How would you evaluate whether this claim is actually plausible?\"\nReal-world consequence: a team adopts a practice based on a plausible-sounding but technically inaccurate claim, wasting effort on something that doesn't actually work the way it's claimed to, because nobody had the mechanistic grounding to evaluate the claim critically.",
  },
  {
    section_type: "debugging_playbook",
    title: "When application-level debugging isn't explaining the behavior — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"We've tried many prompt variations and still can't fix an inconsistent behavior.\"\nDiagnostic: consider whether this behavior might trace back to a genuine architectural characteristic (attention/context-position effects, a tokenization boundary issue) rather than a fixable prompt problem.\nFix: if a mechanism-level explanation fits, redirect effort toward a genuine workaround (restructuring input, a different model, application-level compensation) rather than continuing to iterate on prompts alone.\n\nSymptom: \"A model behaves oddly specifically for inputs with unusual formatting or characters.\"\nDiagnostic: investigate how that specific input actually gets tokenized — an unusual token boundary is a strong candidate explanation.\nFix: adjust the input format to avoid the problematic tokenization pattern, or handle the specific case explicitly in application logic.\n\nSymptom: \"A RAG system's retrieval misses results that seem like they should be semantically found.\"\nDiagnostic: consider the actual embedding geometry — is the missed document genuinely semantically close in the embedding space, or does it just seem like it should be from a human's intuitive read?\nFix: if the embedding space genuinely doesn't place them close, this may require different chunking, a different embedding model, or hybrid retrieval approaches — not just more chunking-strategy trial and error.\n\nSymptom: \"A claimed technique or behavior doesn't seem consistent with your understanding of how transformers work.\"\nDiagnostic: check the claim against genuine mechanism-level understanding, rather than accepting it based on confident presentation alone.\nFix: if the claim doesn't hold up mechanistically, don't adopt the practice without independent verification, even if it's presented persuasively.",
  },
  {
    section_type: "checklist",
    title: "Model-Internals Foundational Understanding Checklist",
    order_index: 6,
    content: "- [ ] The generative-versus-discriminative distinction is understood at the mechanism level, not just as a practical rule of thumb\n- [ ] The transformer attention mechanism's practical implications (context effects, computational scaling) are genuinely understood\n- [ ] Tokenization is understood concretely enough to diagnose real, input-specific behavioral issues\n- [ ] Embeddings are understood as a geometric representation, enabling real diagnosis of retrieval quality issues\n- [ ] This understanding is applied specifically where application-level debugging runs out of explanations, not treated as a universal requirement for routine work\n- [ ] Claims about model behavior are evaluated critically against genuine mechanistic understanding, not accepted uncritically",
  },
  {
    section_type: "template",
    title: "Mechanism-Level Diagnostic Worksheet",
    order_index: 7,
    content: "Observed behavior: ___\n\nApplication-level explanations tried and their results: ___\n\nCandidate mechanism-level explanations:\n- Tokenization-related? ___\n- Attention/context-position-related? ___\n- Embedding-geometry-related? ___\n\nEvidence for/against each candidate: ___\n\nConclusion: fundamental model characteristic / fixable application issue — reasoning: ___\n\nRecommended next step: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- Vaswani et al., [\"Attention Is All You Need\"](https://arxiv.org/abs/1706.03762) (2017) — the foundational transformer architecture paper, the actual primary source for the attention mechanism this module is built around.\n- [Jay Alammar: The Illustrated Transformer](https://jalammar.github.io/illustrated-transformer/) — one of the most widely-referenced, accessible visual explanations of transformer architecture, an excellent companion to the original paper.\n\nRecommended\n- [OpenAI: Tokenizer](https://platform.openai.com/tokenizer) — a direct, hands-on tool for exploring real tokenization behavior, useful for building the concrete intuition this module emphasizes.\n- [Jay Alammar: The Illustrated Word2Vec](https://jalammar.github.io/illustrated-word2vec/) — an accessible, visual explanation of embedding geometry directly relevant to this module's similarity-search exercise.\n\nReference\n- [Hugging Face: NLP Course, Chapter 2](https://huggingface.co/learn/nlp-course/chapter2/1) — a practical, code-oriented treatment of tokenization and transformer models.\n\nAdvanced\n- Devlin et al., [\"BERT: Pre-training of Deep Bidirectional Transformers\"](https://arxiv.org/abs/1810.04805) (2018) — for understanding discriminative transformer applications specifically, complementing this module's generative-model focus.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Implement a basic embedding-based similarity search",
    problem_statement: "Using any embedding model you have access to (via an API, or a local open-source model), embed a set of at least 15 short text snippets on a mix of related and unrelated topics. Implement a similarity search: given a new query text, embed it and find the most similar snippets by cosine similarity.\n\nTest with at least 3 queries and verify the results make intuitive semantic sense, and document at least one case where the results are surprising or don't match your intuition.",
    starter_context: "This is a hands-on implementation exercise — the goal is genuine, concrete understanding of embedding geometry, not just calling a library function.",
    hints: [
      "Include at least one deliberately ambiguous or borderline-topic snippet in your set to see how the embedding space handles it.",
      "For the surprising result you document, try to reason about why it might have occurred — is it genuinely a limitation of the embedding model, or does it reveal something about how you were intuitively judging 'similarity' versus how the model actually computes it?",
    ],
    solution_notes: "A strong submission implements a genuinely working similarity search with real, varied text snippets, produces results that mostly match semantic intuition, and — critically — identifies and reasons about at least one genuinely surprising result, connecting it back to actual embedding-geometry understanding rather than dismissing it as an unexplained anomaly.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Investigate a tokenization-related behavioral quirk",
    problem_statement: "Using a real tokenizer (e.g., OpenAI's tokenizer tool, or a Hugging Face tokenizer), investigate how at least 5 different kinds of input (a common word, a rare/technical word, a number, a mix of languages if you have access, a string with unusual punctuation) actually get tokenized. Identify at least one case where the tokenization is genuinely surprising or non-obvious, and reason about what practical implication this might have for a real application.",
    starter_context: "The goal is building genuine, concrete intuition for how tokenization actually works, not an abstract description.",
    hints: [
      "Rare or technical words are often tokenized into multiple sub-word pieces in surprising ways — this is a good candidate for a genuinely non-obvious finding.",
      "Connect your surprising finding to a real, practical implication — e.g., how might this affect prompt engineering, cost estimation, or context-window budgeting for an application actually working with this kind of input.",
    ],
    solution_notes: "A strong submission investigates genuinely varied input types, identifies at least one real, non-obvious tokenization behavior, and connects it explicitly to a practical implication (cost, context budgeting, prompt design) rather than treating it as an isolated curiosity — demonstrating the concrete, applied understanding this module's core lesson requires.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose whether a persistent issue is architectural or application-level",
    problem_statement: "You're told: \"Our summarization feature consistently loses important details from the middle of long documents, even though it captures the beginning and end well, no matter how we adjust the prompt.\" You're given no further detail.\n\nUsing this module's Mechanism-Level Diagnostic Worksheet, reason through whether this is likely a fundamental architectural characteristic or a fixable application-level issue, and propose a specific next step based on your conclusion.",
    starter_context: "This is a diagnostic exercise applying this module's core distinction to a realistic, plausible scenario.",
    hints: [
      "Consider what you know about attention mechanisms and context-position effects — is a 'loses the middle' pattern consistent with a known architectural characteristic?",
      "If you conclude this is architectural, your recommended next step should be a genuine workaround (a different model, restructuring the input, a chunking-and-summarize-separately approach) rather than continued prompt iteration.",
    ],
    solution_notes: "A strong submission correctly identifies this pattern as consistent with known attention/context-position effects in transformer models (sometimes informally called a 'lost in the middle' phenomenon), reasons through why continued prompt-only iteration is unlikely to fully resolve a genuine architectural characteristic, and proposes a specific, genuine workaround (e.g., chunking the document and summarizing sections separately before combining, or restructuring to place critical content near the beginning/end) rather than recommending further prompt tuning alone.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "What's the actual, mechanistic difference between a generative and a discriminative model?", what_is_tested: "Genuine understanding of the underlying mechanism, not just the practical use-case distinction.", strong_answer_structure: "Explain that a discriminative model learns a decision boundary or mapping between inputs and a fixed set of outputs/categories, while a generative model learns the underlying data distribution well enough to produce new, plausible samples from it — this is the actual reason generative models can produce novel coherent output while discriminative models are suited to classification/prediction from fixed categories.", weak_answer_example: "\"Generative models generate things and discriminative models classify things\" — restates the practical difference without explaining the actual underlying mechanism.", follow_up_question: "Why does this distinction explain why an LLM can write new text but a spam classifier can't?" },
  { category: "applied", order_index: 1, question: "When does understanding transformer internals actually matter for practical engineering work, versus being unnecessary depth?", what_is_tested: "Judgment about appropriate depth, not treating deep mechanism understanding as universally required.", strong_answer_structure: "Explain that routine application-building (API calls, standard prompting, established RAG patterns) works well at the API-surface level covered elsewhere in this catalog, while genuinely hard diagnostic problems (unexplained inconsistency, tokenization-specific issues, retrieval quality issues resistant to standard tuning) benefit from mechanism-level understanding.", weak_answer_example: "\"You always need to understand the internals to work with LLMs well\" — overstates the universal necessity of this depth.", follow_up_question: "Give an example of a real engineering task that genuinely doesn't require this depth of understanding." },
  { category: "scenario", order_index: 2, question: "A team has tried five different prompt variations to fix an inconsistent model behavior with no success. What would make you suspect this is actually a fundamental model characteristic rather than a fixable prompt issue?", what_is_tested: "Application of the fundamental-limitation-versus-fixable-issue distinction this module centers on.", strong_answer_structure: "Propose checking whether the behavior pattern is consistent with a known architectural characteristic (context-position effects, tokenization boundaries), and if so, redirecting effort toward a genuine workaround rather than continued prompt iteration that's unlikely to resolve an architectural characteristic.", weak_answer_example: "\"Just keep trying different prompts until one works\" — doesn't consider that some issues may not be prompt-fixable at all.", follow_up_question: "What would a genuine workaround look like if you concluded this was architectural?" },
  { category: "debugging", order_index: 3, question: "Your RAG system's retrieval misses a document you'd expect it to find for a semantically related query. Beyond trying different chunk sizes, what would you investigate?", what_is_tested: "Ability to apply embedding-geometry understanding to a real retrieval diagnostic.", strong_answer_structure: "Propose examining the actual embedding space — is the missed document genuinely close in embedding-similarity terms to the query, or does human intuition about 'similarity' diverge from what the embedding model actually captures — rather than only tuning chunking parameters blindly.", weak_answer_example: "\"Just try different chunk sizes until it works\" — relies purely on trial and error without genuine embedding-geometry diagnosis.", follow_up_question: "What would you conclude, and what would you do differently, if the embedding space genuinely doesn't place them close?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you had to build a deeper, foundational understanding of how something actually worked, beyond the surface-level API or tool you'd been using, in order to solve a real problem.", what_is_tested: "Real or realistic experience with exactly the kind of depth-building this module represents.", strong_answer_structure: "A strong answer describes the specific surface-level tool/API, the problem that required deeper understanding, the actual learning process undertaken, and how that deeper understanding solved the problem the surface-level knowledge couldn't.", weak_answer_example: "A vague answer with no specific tool, problem, or learning process described.", follow_up_question: "How did you know when you'd built enough foundational understanding to actually solve the problem, versus needing to go even deeper?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "RAG Pipeline Design").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("GenAI Module 1 (Foundations) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
