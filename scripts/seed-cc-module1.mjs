import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "4c344d7f-41d3-40ab-a902-21be5a821249"; // Setup & Core Workflow

const sections = [
  {
    section_type: "the_field",
    title: "What using Claude Code as a daily driver actually looks like",
    order_index: 0,
    content: "Most people's first session with Claude Code looks like a slightly better autocomplete: ask for a function, get a function, copy it in. That's not what changes how someone works. The shift happens when you stop treating it like a code-completion tool and start treating it like a capable collaborator that needs the same things a new engineer on your team would need on day one — context about the codebase, a clear description of the actual task, and a way to check its work.\n\nA working daily routine looks like this: the project has a CLAUDE.md file that tells the agent what it needs to know before touching anything (build commands, test commands, architectural conventions, things not to do) — written once, refined over weeks, saving everyone from re-explaining the same context every session. Tasks are described as outcomes with a verification path (\"add pagination to the orders list, tests should cover the boundary at page 1 and the last page\") rather than step-by-step instructions. And every non-trivial change gets reviewed the same way a colleague's pull request would — reading the diff, not just trusting that it compiled.\n\nThe people who get real leverage from this tool aren't the ones who prompt the most cleverly. They're the ones who invested a few hours up front making the tool's context and permissions match how their team actually works, and who never stopped treating \"the AI wrote it\" as a reason to review less carefully, not more.",
  },
  {
    section_type: "mental_models",
    title: "How effective users actually think about the tool",
    order_index: 1,
    content: "**1. Context is not optional politeness — it's the difference between a guess and an informed decision.** An agent without a CLAUDE.md, without having read the relevant files, or without knowing your test command is working blind. Every minute spent building good project context pays back across every future session.\n\n**2. A prompt is a specification, not a wish.** \"Add auth\" is not a task; \"add Clerk-based auth to the admin routes, redirect unauthenticated users to sign-in, and add a test confirming an unauthenticated request gets redirected\" is. The gap between those two is almost entirely where bad results come from.\n\n**3. Autonomy is a dial you set, not a fixed property of the tool.** Claude Code's permission modes (asking before every action, auto-accepting edits, full autonomy in a sandboxed environment) exist because different tasks warrant different levels of trust — a one-line typo fix and a database migration are not the same risk category.\n\n**4. You are still the engineer of record.** The model can write the diff; it doesn't own the consequences of shipping it. Reviewing AI-authored code with the same rigor as human-authored code isn't distrust, it's the actual job.\n\n**5. The codebase is the source of truth, not your mental model of it.** When Claude Code's suggestion contradicts what you assumed the code did, the first move is to re-check the code, not to assume the tool is wrong — and not to assume it's right either.\n\n**6. Small, verifiable steps compound; one giant unreviewable diff does not.** A task broken into \"make the change, run the tests, show me the diff\" steps is recoverable when something goes wrong. A single enormous unsupervised change is much harder to safely undo or even fully understand.",
  },
  {
    section_type: "decision_framework",
    title: "How much autonomy should this task get?",
    order_index: 2,
    content: "**IF** the task is exploratory, read-only, or answering a question about the codebase (\"where is auth handled,\" \"what does this function do\") **THEN** full autonomy is fine — there's nothing to review because nothing is being changed. **BECAUSE** the risk of an exploratory answer is being wrong, not being destructive. **WATCH OUT FOR**: treating a confident-sounding explanation as verified fact — cross-check anything load-bearing against the actual code.\n\n**IF** the task is a small, well-scoped, easily-reviewed change (a typo fix, a well-defined function, a test for existing behavior) **THEN** let it make the edit and review the diff afterward, rather than approving every individual file write. **BECAUSE** reviewing a complete, coherent diff is usually more effective than approving fragments out of context. **WATCH OUT FOR**: \"small\" can be deceptive — a one-line change to a shared config or a migration can have an outsized blast radius even though the diff looks tiny.\n\n**IF** the task touches security-sensitive code (auth, payments, permissions, secrets handling) or could cause data loss (migrations, deletions, force-pushes) **THEN** require explicit approval at each meaningful step, and read every line before it's applied. **BECAUSE** the cost of a mistake here is materially higher than the cost of a slower workflow. **WATCH OUT FOR**: don't let time pressure erode this — this is exactly the category of task where a rushed rubber-stamp review causes real incidents.\n\n**IF** the task is large and multi-step (a new feature spanning several files) **THEN** don't hand it over as one giant instruction — plan first (many workflows have an explicit plan-then-execute mode for exactly this), get the plan reviewed, then execute in verifiable increments. **BECAUSE** catching a wrong approach at the planning stage costs minutes; catching it after 500 lines of generated code costs much more. **WATCH OUT FOR**: skipping the plan-review step because the plan \"looked reasonable\" at a glance — actually read it.",
  },
  {
    section_type: "workflow",
    title: "The real daily setup-to-shipping workflow",
    order_index: 3,
    content: "**1. Set up project context once, properly.** A CLAUDE.md (or equivalent project-memory file) that states build/test/lint commands, architectural conventions, things the agent should never do (e.g. \"never run destructive git commands,\" \"never commit secrets\"), and any project-specific quirks a new hire would need to know.\n\n**2. Start each session by letting the agent orient itself** — read relevant files, check current git status, understand what's already in flight — rather than jumping straight into a large ask cold.\n\n**3. Describe the task as an outcome plus a way to verify it**, not just a list of steps. \"Add X, and here's how I'll know it worked\" produces far better results than a bare instruction.\n\n**4. Match the permission mode to the actual risk of the task**, using the decision framework above — don't default to either extreme (approving everything blind, or micromanaging every keystroke) regardless of what's being changed.\n\n**5. Review the diff like a pull request**, not like a magic trick. Read what changed, not just whether it compiled or the tests went green — tests can pass while still doing the wrong thing.\n\n**6. Run the actual test suite and linter yourself (or have the agent run them) before considering a change done** — \"the AI said it works\" is not verification.\n\n**7. Commit in coherent, reviewable units**, with messages that explain why, matching the same discipline you'd want from a human teammate.\n\n**8. Update project context when you learn something the agent got wrong** — if it made the same mistake twice, that's a signal your CLAUDE.md is missing information, not that the tool is unreliable.",
  },
  {
    section_type: "failure_modes",
    title: "How Claude Code workflows actually break",
    order_index: 4,
    content: "**Failure 1 — No project context, so every session starts from zero.**\n*What*: the agent repeatedly makes the same wrong assumption (wrong test command, wrong framework convention, a pattern the team explicitly avoids) because nothing tells it otherwise.\n*How it happens*: teams skip writing a CLAUDE.md, assuming the agent will \"figure it out\" from the code alone each time.\n*Why beginners miss it*: individual sessions still produce working code, so the inefficiency of re-explaining context every time isn't obviously a problem — it just feels like normal friction.\n*Detect*: notice if you're typing the same clarifications (\"we use Tailwind, not styled-components,\" \"run the test script, not the test runner directly\") more than once across sessions.\n*Prevent*: the moment you correct the same thing twice, add it to the project's context file — treat it as a living document, not a one-time setup task.\n*Interview question*: \"How do you make sure an AI coding tool doesn't repeat the same mistake across sessions?\"\n*Real-world consequence*: wasted time and inconsistent output, and — worse — a team that concludes \"the tool doesn't work well here\" when the actual gap was missing context.\n\n**Failure 2 — Rubber-stamping diffs because \"it's probably fine.\"**\n*What*: a generated change is accepted without actually being read, because it compiled, the tests passed, or it \"looked right\" at a glance.\n*How it happens*: review fatigue sets in once a tool has been reliable for a while, and the review step quietly degrades into a formality.\n*Why beginners miss it*: most changes really are fine, so the habit of not-quite-reading feels harmless — right up until it isn't.\n*Detect*: ask yourself honestly whether you could explain, line by line, what the last diff you accepted actually does.\n*Prevent*: treat every AI-generated diff with the review rigor of a human pull request, permanently — this isn't a phase you graduate out of as trust builds; the cost of a missed bug doesn't go down.\n*Interview question*: \"How is reviewing AI-generated code different from reviewing a colleague's PR, if at all?\"\n*Real-world consequence*: a subtly wrong change (an off-by-one, a missing edge case, an overly broad permission) ships because no human actually looked closely.\n\n**Failure 3 — Giving one enormous, underspecified task and hoping for the best.**\n*What*: \"build the whole checkout flow\" handed over as a single instruction, with no plan reviewed and no intermediate checkpoints.\n*How it happens*: it feels efficient to state the end goal and walk away, especially under deadline pressure.\n*Why beginners miss it*: sometimes it works well enough to reinforce the habit, which makes the failures (when the approach taken diverges from what was actually wanted) more expensive and more surprising when they happen.\n*Detect*: if you can't clearly state, partway through, what's already been decided and why, the task wasn't planned — it was just launched.\n*Prevent*: for anything spanning multiple files or architectural decisions, use a plan-first approach: get the plan, review it, then execute in reviewable increments.\n*Interview question*: \"Walk me through how you'd hand off a multi-file feature versus a single-function fix.\"\n*Real-world consequence*: a large amount of generated code that solves a slightly different problem than the one intended, discovered only after significant time has already been spent.\n\n**Failure 4 — Treating permission mode as a one-time setting instead of a per-task decision.**\n*What*: either everything runs in full-autonomy mode regardless of risk, or everything requires manual approval regardless of triviality — neither matches the actual risk of the work being done.\n*How it happens*: it's simpler to pick one mode and leave it, rather than consciously choosing per task.\n*Why beginners miss it*: both extremes \"work\" most of the time, so the mismatch only shows up on the task where it actually mattered.\n*Detect*: ask whether the current permission setting was a deliberate choice for this specific task, or just whatever was left over from the last one.\n*Prevent*: apply the \"how much autonomy should this task get\" decision framework consciously, every time the task category changes — especially before anything touching security, payments, or destructive operations.\n*Interview question*: \"When would you deliberately reduce an AI coding tool's autonomy, even if it slows you down?\"\n*Real-world consequence*: either unreviewed risky changes ship, or trivial tasks become needlessly slow — both erode trust in the tool for the wrong reasons.",
  },
  {
    section_type: "debugging_playbook",
    title: "The agent keeps getting it wrong — a troubleshooting guide",
    order_index: 5,
    content: "**Symptom: \"It keeps using the wrong testing framework / build command / file structure.\"**\nDiagnostic: check whether this information exists anywhere the agent can read it — a CLAUDE.md, a README, or the package manifest's scripts — or whether it only exists in your head.\nFix: write it down explicitly in the project's context file; don't rely on the agent inferring convention correctly from example files alone, especially in a codebase with mixed or inconsistent patterns.\n\n**Symptom: \"The generated code compiles and passes tests, but does something subtly different from what I actually wanted.\"**\nDiagnostic: re-read your original task description — was the actual intent unambiguous, or did it leave room for a reasonable-but-wrong interpretation?\nFix: add the specific acceptance criteria you had in mind (\"should redirect, not just show an error,\" \"should handle the empty-list case\") — the gap is almost always in the specification, not the execution.\n\n**Symptom: \"A large task went off in a direction I didn't want, and now there's a lot to unwind.\"**\nDiagnostic: this is a sign the task should have gone through a plan-review step before execution, or should have been broken into smaller checkpoints.\nFix: for the next large task, ask for a plan first, actually read it, and only then approve execution — and check in after the first meaningful chunk rather than waiting for the whole thing to finish.\n\n**Symptom: \"Two team members get different-quality results from what feels like the same tool.\"**\nDiagnostic: compare their project context files, their task-description habits, and their permission-mode choices — the tool's underlying capability is the same; the inputs almost never are.\nFix: share and standardize the project's CLAUDE.md and task-description conventions across the team, the same way you'd standardize a linter config.\n\n**Symptom: \"I don't trust what got shipped, but I can't point to what's specifically wrong.\"**\nDiagnostic: this vague unease is usually a sign the diff wasn't actually read closely — trust that isn't backed by verification isn't trust, it's hope.\nFix: go back and read the diff properly, line by line, the way you would for a human's PR — either you'll find the specific issue, or you'll have genuine (not just hopeful) confidence.",
  },
  {
    section_type: "checklist",
    title: "Daily-Driver Setup Checklist",
    order_index: 6,
    content: "- [ ] The project has a CLAUDE.md (or equivalent) documenting build/test/lint commands\n- [ ] Architectural conventions and things to avoid are written down, not just known by the team verbally\n- [ ] The context file is treated as living documentation — updated whenever a mistake repeats\n- [ ] Permission mode is chosen deliberately per task category, not left on a fixed default regardless of risk\n- [ ] Security-sensitive or destructive-operation tasks always get explicit, line-by-line review before being applied\n- [ ] Every non-trivial diff is read the way a human colleague's pull request would be read\n- [ ] Tests and linters are actually run (not assumed) before a change is considered done\n- [ ] Large, multi-file tasks go through a plan-review step before execution begins\n- [ ] Commits are coherent, reviewable units with messages explaining why, not just what",
  },
  {
    section_type: "template",
    title: "Project Context File Starter",
    order_index: 7,
    content: "A minimal but effective project-context file covers, at minimum:\n\n**Build & test commands:** ___ (exact commands — don't make the agent guess between package managers, or between test runners)\n\n**Architectural conventions:** ___ (e.g. \"state management via X, not Y,\" \"API routes follow this response shape\")\n\n**Things to never do:** ___ (e.g. \"never run destructive git commands,\" \"never commit files under .env*,\" \"never modify the payments webhook signature verification without explicit approval\")\n\n**Known project quirks:** ___ (anything a new engineer would be confused by on day one — a legacy pattern, a deliberately unconventional choice and why)\n\n**Where to look before assuming something isn't implemented:** ___ (point to the relevant directories/files so the agent doesn't reinvent something that already exists elsewhere in the codebase)\n\n**Review expectations:** ___ (e.g. \"always show the diff before committing,\" \"run the full test suite, not just the changed file's tests\")",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "**Essential**\n- [Claude Code official documentation](https://docs.claude.com/en/docs/claude-code/overview) — the authoritative, continuously-updated reference for setup, configuration, and every feature covered in this module.\n- [Claude Code: Common workflows](https://docs.claude.com/en/docs/claude-code/common-workflows) — practical, task-oriented walkthroughs that pair well with the decision framework above.\n\n**Recommended**\n- [Claude Code: Manage Claude's memory](https://docs.claude.com/en/docs/claude-code/memory) — the official guidance on project-memory file structure and scope, directly relevant to this module's context-setup workflow.\n- [Claude Code: Security](https://docs.claude.com/en/docs/claude-code/security) — permission modes and the actual risk model behind them, worth reading before deciding your team's default autonomy settings.\n\n**Reference**\n- [Claude Code: CLI reference](https://docs.claude.com/en/docs/claude-code/cli-reference) — the full command and flag reference, useful once you're past the basics and configuring a team-wide setup.\n\n**Advanced**\n- [Claude Code: IDE integrations](https://docs.claude.com/en/docs/claude-code/ide-integrations) — for teams standardizing a shared workflow across VS Code and terminal usage simultaneously.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Write a real CLAUDE.md for an existing project",
    problem_statement: "Pick any real codebase you have access to (a work project, a personal repo, or a fresh clone of any open-source project on GitHub). Write a CLAUDE.md file covering: exact build/test/lint commands, at least two real architectural conventions specific to this codebase (not generic advice), and at least one explicit \"never do this\" rule relevant to this specific project.\n\nThen start a fresh Claude Code session in that project and ask it to summarize what it now knows about how to work in the codebase, based only on the context file — check whether its summary matches what you intended.",
    starter_context: "The goal isn't a generic template — a context file that could apply to any project provides almost no value. It should contain information specific enough that someone could tell it was written for this exact codebase.",
    hints: [
      "Look at the project's actual manifest/build config/CI config for the real build and test commands rather than guessing from convention.",
      "A good 'never do this' rule usually comes from a real mistake — either one you've made, or one you can imagine a new contributor making in this specific codebase.",
      "If the agent's summary of your context file misses or misstates something, that's useful signal about which parts of your file were ambiguous — revise and re-test.",
    ],
    solution_notes: "A strong submission produces a context file specific enough that reading it would tell someone unfamiliar with the project real, actionable information — not generic best practices that could apply to any codebase. The verification step (asking the agent to summarize its understanding) is the actual point of the exercise: if the summary is accurate, the context file is doing its job; if it's wrong or vague, that reveals exactly which part needs to be more explicit.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Practice the permission-mode decision framework across three real tasks",
    problem_statement: "In the same or a different project, identify three real tasks of different risk levels: (1) something read-only/exploratory, (2) a small well-scoped change, (3) something that would touch security-sensitive or destructive-operation code (even hypothetically — you don't have to actually run it).\n\nFor each, state which permission mode/approval approach you'd use and why, using this module's decision framework. Then actually execute tasks 1 and 2, and write a short note on whether your chosen approach felt right in practice, or whether you'd adjust it next time.",
    starter_context: "You choose the three tasks and the codebase — the point is applying the framework to real decisions, not following a prescribed script.",
    hints: [
      "For task 3, it's fine to just describe the task and your reasoning without actually executing it, if it's genuinely destructive or sensitive — the goal is the decision process, not the execution.",
      "Pay attention to whether your instinct before doing the exercise matches the framework's recommendation, or whether you initially would have under- or over-trusted the task.",
    ],
    solution_notes: "There's no single correct set of three tasks, but a strong submission clearly distinguishes the risk profile of each and matches the chosen approval approach to that risk — not a fixed habit applied regardless of the task. The reflection note is the most valuable part: noticing a mismatch between instinct and the framework's recommendation is exactly the kind of self-awareness that prevents both the 'rubber-stamping' and 'over-approving trivial work' failure modes covered in this module's playbook.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose a deliberately vague task handoff",
    problem_statement: "A teammate says: \"Claude Code built me a user settings page but it's not what I wanted, and I don't know why.\" They show you their original instruction: \"add a settings page where users can update their info.\"\n\nWithout more information than that, list every ambiguity in this task description that could plausibly explain a mismatch between what they wanted and what they got. Then rewrite the instruction the way this module's workflow recommends (outcome plus verification path), making reasonable assumptions explicit rather than hidden.",
    starter_context: "This is a diagnostic exercise, not a coding one — the deliverable is the list of ambiguities and the rewritten instruction, not working code.",
    hints: [
      "Consider: which fields count as 'their info'? Is this authenticated-only? What should happen on save — a toast, a redirect, inline validation? Is there a design system/component library that should be used?",
      "A well-rewritten instruction doesn't need to resolve every ambiguity itself — it can also explicitly flag an assumption ('I'm assuming email is not editable here since it's tied to auth — confirm?') rather than silently guessing.",
    ],
    solution_notes: "A strong submission identifies most of the real ambiguities: which specific fields, authentication/authorization scope, expected UI feedback on save, validation requirements, and whether existing design-system components should be reused. The rewritten instruction should be concrete enough that two different people executing it would produce recognizably the same result — which is the actual bar for 'specification,' as opposed to 'wish,' established earlier in this module's mental models.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why does a project context file (like CLAUDE.md) matter more than the individual prompt you type?", what_is_tested: "Understanding that persistent context compounds across sessions, versus one-off prompting.", strong_answer_structure: "Explain that a context file eliminates the need to re-explain the same conventions, commands, and constraints every session, and that its value compounds over time as more real mistakes get captured — a single clever prompt, by contrast, helps exactly once.", weak_answer_example: "\"It's good practice\" with no explanation of the actual compounding-value mechanism.", follow_up_question: "How would you decide what belongs in the context file versus what belongs in an individual task's instructions?" },
  { category: "applied", order_index: 1, question: "How do you decide how much autonomy to give an AI coding tool for a given task?", what_is_tested: "Whether the candidate has an actual framework, versus a fixed habit applied regardless of context.", strong_answer_structure: "Reference risk factors: reversibility of the change, whether it touches security/payments/destructive operations, and how easily the result can be reviewed — matching autonomy level to those factors rather than a single default setting.", weak_answer_example: "\"I always review everything\" or \"I trust it completely\" — either extreme, stated without any task-specific reasoning.", follow_up_question: "Give a specific example of a task where you'd deliberately reduce autonomy, even knowing it slows you down." },
  { category: "scenario", order_index: 2, question: "A teammate says an AI-assisted change 'passed all the tests' and wants to ship it without further review. How do you respond?", what_is_tested: "Whether the candidate understands that passing tests is necessary but not sufficient verification.", strong_answer_structure: "Explain that tests only verify what they were written to check — a change can pass every existing test while still doing something subtly wrong that no test covers; propose an actual diff review before shipping, especially for anything non-trivial.", weak_answer_example: "\"If tests pass, it's fine to ship\" — conflating test coverage with correctness.", follow_up_question: "What kind of bug would pass all existing tests but still be wrong?" },
  { category: "debugging", order_index: 3, question: "You keep having to correct the same mistake across multiple Claude Code sessions on the same project. What's your first move?", what_is_tested: "Instinct to fix the root cause (missing context) rather than repeatedly re-explaining in the moment.", strong_answer_structure: "Recognize this as a signal that the information is missing from the project's persistent context file, not a tool reliability issue, and add it there so it doesn't need to be re-explained again.", weak_answer_example: "\"I'd just keep correcting it each time\" — treating a fixable systemic gap as an unavoidable cost.", follow_up_question: "How would you verify the fix actually worked, beyond just hoping the mistake doesn't recur?" },
  { category: "system_design", order_index: 4, question: "How would you roll out a standardized Claude Code workflow across a team of ten engineers?", what_is_tested: "Thinking about consistency and knowledge-sharing at a team level, not just individual usage.", strong_answer_structure: "Propose a shared, version-controlled project context file (so it's reviewed and improved like any other code artifact), documented conventions for task descriptions and permission-mode choices, and a habit of capturing new corrections back into the shared file rather than letting each engineer accumulate private tribal knowledge.", weak_answer_example: "\"Everyone just uses it however they want\" — no shared context, no consistency mechanism.", follow_up_question: "How would you know whether the rollout was actually working, versus just being used inconsistently?" },
  { category: "behavioural", order_index: 5, question: "Tell me about a time an AI-assisted change went wrong in a way that mattered. What happened and what did you change afterward?", what_is_tested: "Honesty and a real diagnostic/process response, not a story with no consequence or learning attached.", strong_answer_structure: "A strong answer names a specific issue (not necessarily catastrophic), what allowed it to happen (missing context, insufficient review, an overly broad autonomy setting), and a concrete process change made afterward.", weak_answer_example: "A vague answer with no specific incident, or one where nothing changed afterward despite something going wrong.", follow_up_question: "How would you know if that same category of mistake happened again?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "AI Pair Programming (Claude Code)").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("Claude Code Module 1 (Setup & Core Workflow) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
