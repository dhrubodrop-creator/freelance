import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "d9957ed1-ea2d-4c2e-9c93-3d2c97d90ca1"; // Containerizing Agentic Systems

const sections = [
  {
    section_type: "the_field",
    title: "What containerizing an agentic system for real deployment actually looks like",
    order_index: 0,
    content: "A LangChain agent that runs cleanly with \"python main.py\" on a laptop is not the same artifact as one that ships to a client. The laptop version has your local Python environment, your locally-cached model weights or API keys sitting in a .env file, a vector database running as a background process you started once and forgot about, and no real answer to \"what happens if this crashes at 2am.\" Containerizing isn't a packaging formality — it's the process of making all of those implicit, personal-machine assumptions explicit and reproducible.\n\nA real agentic stack usually isn't one container — it's several services working together: the application itself (often a FastAPI service wrapping the agent logic), a vector database (Chroma, Qdrant, Weaviate, or similar) for retrieval, and often a cache (Redis) for session state or rate limiting. Getting these to run together reliably, with the right startup order, the right networking, and secrets handled correctly rather than baked into the image, is the actual skill this module builds.\n\nThe habit that separates a working container from a production-ready one is treating image size, build time, and attack surface as real engineering constraints, not afterthoughts. A 4GB image that takes ten minutes to build and ships with a full build toolchain inside it will work — but it will also be slow to deploy, slow to scale, and carry unnecessary security exposure a client's security review will flag.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about containerizing AI systems",
    order_index: 1,
    content: "**1. A container should contain exactly what's needed to run, not what was needed to build.** A multi-stage Dockerfile exists precisely for this: build with the full toolchain in one stage, then copy only the compiled/installed artifacts into a minimal runtime image — the final image never needs a compiler, a package manager cache, or build-time dependencies.\n\n**2. Secrets never belong in the image.** An API key baked into a Docker image (via an ENV instruction, or copied in as a file) persists in every layer of that image forever, retrievable by anyone who can pull it, regardless of whether you later \"remove\" it in a subsequent instruction. Secrets belong in runtime environment variables or a secrets manager, injected at container start, never at build time.\n\n**3. Multi-service systems need explicit, declared dependencies between services, not implicit timing luck.** \"The app usually starts after the database is ready\" is not a real guarantee — Docker Compose's health checks and depends_on conditions exist because startup order is a real correctness concern, not a minor inconvenience.\n\n**4. Image size is a proxy for a lot of things you actually care about.** A smaller image (using slim/alpine base images, multi-stage builds, minimal dependencies) usually means faster deploys, faster autoscaling, less attack surface, and lower registry storage costs — optimizing it isn't vanity, it has real downstream effects across the rest of the system.\n\n**5. Reproducibility means the exact same image runs the exact same way everywhere.** A build that pulls \"latest\" versions of dependencies at build time is not reproducible — pinned versions (in requirements files, and in a Dockerfile's base image tag) are what makes \"it worked in staging\" actually mean something for production.\n\n**6. Local development convenience and production correctness are often in tension, and production correctness wins.** A convenient local setup that mounts your source code directly into a running container for hot-reloading is genuinely useful for development, but that same pattern in production defeats the entire point of an immutable, reproducible image.",
  },
  {
    section_type: "decision_framework",
    title: "How should I structure the Dockerfile and Compose setup for this agent stack?",
    order_index: 2,
    content: "IF your application has build-time dependencies (compiling native extensions, installing dev-only tooling) that aren't needed at runtime THEN use a multi-stage Dockerfile — build in one stage, copy only the final artifacts into a slim runtime stage. BECAUSE this keeps the final image small and avoids shipping a build toolchain (and its security surface) into production. WATCH OUT FOR: forgetting to copy a runtime dependency that was only implicitly available because the build stage happened to have it installed too — test the final image standalone, not just \"it built successfully.\"\n\nIF your agent stack needs a vector database and/or cache alongside the application THEN define them as separate services in Docker Compose with explicit health checks and depends_on conditions, rather than assuming they'll be ready by the time the app starts. BECAUSE without explicit health checks, a container that starts fast enough on your machine may start too fast in a different environment, causing intermittent connection failures that are maddening to debug. WATCH OUT FOR: depends_on alone (without a healthcheck condition) only waits for the container to start, not for the service inside it to actually be ready to accept connections — this is a common, subtle gap.\n\nIF you need to pass API keys or credentials to the container THEN inject them as runtime environment variables (via Compose's env_file or a secrets manager), never bake them into the image with an ENV or COPY instruction in the Dockerfile. BECAUSE anything baked into an image layer is retrievable from that image indefinitely, even if a later layer appears to remove it. WATCH OUT FOR: accidentally copying a local .env file into the image via a broad COPY . . instruction — always use a .dockerignore file to explicitly exclude secrets and other files that shouldn't ship in the image.\n\nIF you're optimizing image size and security THEN start from a minimal base image (slim or alpine variants where compatible), install only production dependencies, and run the final process as a non-root user. BECAUSE a smaller, non-root image reduces both the attack surface and the practical impact of a container compromise. WATCH OUT FOR: alpine's musl libc occasionally causes subtle compatibility issues with some Python packages that expect glibc — verify compatibility before committing to alpine over a slim glibc-based image.",
  },
  {
    section_type: "workflow",
    title: "The real containerization workflow for an agent stack",
    order_index: 3,
    content: "1. Identify every service the stack actually needs at runtime: the application, vector database, cache, and any other dependencies — and which of these are external managed services versus things you need to containerize yourself.\n\n2. Write a multi-stage Dockerfile for the application: a build stage with full tooling, and a slim runtime stage that copies over only what's needed to actually run.\n\n3. Add a .dockerignore covering anything that should never enter the build context or image — local secrets, .git, local caches, development-only files.\n\n4. Define the full stack in Docker Compose, with explicit health checks for each service and depends_on conditions that wait for real readiness, not just container start.\n\n5. Handle secrets exclusively through runtime environment injection, never through anything that ends up in an image layer.\n\n6. Pin dependency and base-image versions explicitly, so the build is reproducible rather than silently picking up whatever \"latest\" happens to resolve to on a given day.\n\n7. Test the built image standalone (not just via docker-compose up, which can mask missing runtime dependencies that happened to be present during the build stage).\n\n8. Check the final image size and run a basic security scan (many registries and CI tools offer this natively) before considering the containerization done — an unnecessarily large or vulnerable image is a real, avoidable cost.\n\n9. Confirm the non-root user setup and verify the container doesn't require elevated privileges to run correctly.",
  },
  {
    section_type: "failure_modes",
    title: "How containerized agent stacks actually break",
    order_index: 4,
    content: "Failure 1 — Secrets baked into the image via a broad COPY or an ENV instruction.\nWhat: an API key, database credential, or other secret ends up embedded in a layer of the built image, retrievable by anyone who can pull it — regardless of whether a later instruction appears to remove or overwrite it.\nHow it happens: a broad COPY . . instruction picks up a local .env file, or a secret gets set via ENV in the Dockerfile for convenience during early development and never gets moved to runtime injection.\nWhy beginners miss it: the container runs correctly either way, so there's no functional signal that something is wrong — the leak is only visible if someone actually inspects the image's layers.\nDetect: run a layer inspection tool (docker history, or a dedicated image-scanning tool) against the built image and check for any secret-shaped strings in any layer, not just the final one.\nPrevent: maintain a strict .dockerignore, never use ENV or COPY for actual secrets, and inject all credentials via Compose env_file or a secrets manager at runtime.\nInterview question: \"You COPY . . in your Dockerfile, and your .env file has real API keys in it. What's wrong, and does removing the .env file in a later RUN instruction fix it?\"\nReal-world consequence: a credential leak discoverable by anyone with pull access to the image registry, potentially long after the immediate mistake was made and \"fixed\" in a later commit.\n\nFailure 2 — Intermittent startup failures from missing or incomplete health checks.\nWhat: the application container starts before its dependency (vector DB, cache) is actually ready to accept connections, causing connection errors that appear randomly depending on timing.\nHow it happens: depends_on without a healthcheck condition only guarantees the dependency's container has started, not that the service inside it has finished its own startup process.\nWhy beginners miss it: on a fast local machine, the dependency is often ready quickly enough that the race condition rarely manifests — it shows up more reliably in a different, slower, or more loaded environment.\nDetect: deliberately test cold starts under load or on a resource-constrained environment, where startup timing is more likely to expose the race condition.\nPrevent: add real health checks to each service definition and use depends_on with a condition tied to that health check, not just container-start order.\nInterview question: \"Your stack works reliably in local development but intermittently fails to connect to its vector database when deployed. What's your first hypothesis?\"\nReal-world consequence: flaky, hard-to-reproduce production failures that erode confidence in the deployment, often initially misdiagnosed as an application bug rather than a startup-ordering issue.\n\nFailure 3 — An unnecessarily large, slow-to-deploy image from skipping multi-stage builds.\nWhat: the final production image includes a full build toolchain, dev dependencies, and cached build artifacts that are never needed at runtime.\nHow it happens: it's simpler to write a single-stage Dockerfile that installs everything in one pass, especially early in a project when getting something working matters more than optimizing it.\nWhy beginners miss it: the container works correctly — the cost shows up as slower deploys, slower autoscaling, and a larger attack surface, none of which produce an obvious error.\nDetect: check the final image size and compare it against what a properly multi-staged equivalent would produce; anything carrying full compiler toolchains or package manager caches into production is a signal.\nPrevent: adopt multi-stage builds as the default pattern for any application with meaningful build-time dependencies, not an optimization to revisit later.\nInterview question: \"Why would you use a multi-stage Dockerfile instead of a single-stage one, even if both produce a working container?\"\nReal-world consequence: slower deployment and autoscaling cycles, higher registry storage costs, and unnecessary security exposure from unused build tooling shipping into production.\n\nFailure 4 — Non-reproducible builds from unpinned dependencies or base images.\nWhat: a rebuild of \"the same\" Dockerfile weeks later produces a meaningfully different image because dependency versions or the base image tag resolved differently.\nHow it happens: using an unpinned base image tag (like a bare \"latest\") or a requirements file without pinned versions means the build silently picks up whatever is current at build time.\nWhy beginners miss it: most of the time, this doesn't cause a visible problem — until a dependency ships a breaking change and a routine rebuild suddenly fails or behaves differently with no code change on your side.\nDetect: check whether the Dockerfile's base image and the application's dependency manifest both use explicit, pinned versions rather than floating tags.\nPrevent: pin base image versions and dependency versions explicitly, and treat version bumps as a deliberate, tested change rather than something that happens automatically on every rebuild.\nInterview question: \"A rebuild of an unchanged Dockerfile produces a different, broken container a month later. How is that possible, and how would you prevent it?\"\nReal-world consequence: a routine CI rebuild or redeploy breaks production with no corresponding code change, making the root cause much harder to trace.",
  },
  {
    section_type: "debugging_playbook",
    title: "The container stack isn't behaving — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"The app container can't connect to the vector database, but only sometimes.\"\nDiagnostic: check whether the Compose file has a real health check on the database service, or just a plain depends_on with no readiness condition.\nFix: add a proper health check (a command that verifies the database is actually accepting connections, not just that the process started) and configure depends_on to wait on that condition.\n\nSymptom: \"The built image is much larger than expected.\"\nDiagnostic: run a layer-by-layer inspection of the image and check for build-time tooling, package manager caches, or dev dependencies that shouldn't be in the final image.\nFix: restructure as a multi-stage build if it isn't already one, and add a .dockerignore to prevent unnecessary files from entering the build context in the first place.\n\nSymptom: \"An API key or credential accidentally ended up somewhere it shouldn't have.\"\nDiagnostic: check the Dockerfile for any ENV, ARG, or COPY instruction that could have pulled in a secret, and inspect the actual image layers (not just the final running container) since a removed-later secret can still exist in an earlier layer.\nFix: rotate the leaked credential immediately, remove the offending instruction, rebuild from scratch (not incrementally, since old layers may be cached), and add the relevant file/pattern to .dockerignore going forward.\n\nSymptom: \"The exact same Dockerfile produced a working image last month and a broken one today, with no code changes.\"\nDiagnostic: check for unpinned base image tags or dependency versions that could have resolved differently between builds.\nFix: pin explicit versions for the base image and all dependencies, then rebuild and verify the pinned build is reproducible going forward.",
  },
  {
    section_type: "checklist",
    title: "Agent Stack Containerization Checklist",
    order_index: 6,
    content: "- [ ] The Dockerfile uses a multi-stage build if there are meaningful build-time-only dependencies\n- [ ] A .dockerignore file excludes secrets, .git, and unnecessary local files from the build context\n- [ ] No secrets are baked into the image via ENV, ARG, or COPY — all credentials are injected at runtime\n- [ ] Every service in Docker Compose has a real health check, and depends_on conditions wait on actual readiness, not just container start\n- [ ] Base image and dependency versions are explicitly pinned, not left floating on \"latest\"\n- [ ] The final image runs as a non-root user\n- [ ] The built image has been tested standalone, not only via the full Compose stack\n- [ ] Image size has been checked and compared against what an optimized multi-stage build should produce\n- [ ] A basic security scan has been run against the final image before considering it deployment-ready",
  },
  {
    section_type: "template",
    title: "Container Stack Readiness Review",
    order_index: 7,
    content: "Stack / service name: ___\n\nServices included (app, vector DB, cache, other): ___\n\nBuild approach: single-stage / multi-stage — reasoning: ___\n\nSecrets handling: how are credentials injected, and confirmed absent from all image layers? ___\n\nHealth checks and startup ordering: ___ (per service)\n\nPinned versions: base image ___, key dependencies ___\n\nFinal image size: ___ — compared against expectation: ___\n\nNon-root user configured: Yes / No\n\nSecurity scan result: ___\n\nOpen risks or follow-ups before this is production-ready: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [Docker: Multi-stage builds](https://docs.docker.com/build/building/multi-stage/) — the official reference for the pattern this module centers on.\n- [Docker Compose: Health checks and startup order](https://docs.docker.com/compose/how-tos/startup-order/) — directly addresses the health-check and depends_on failure mode covered in this module.\n\nRecommended\n- [Docker: Best practices for writing Dockerfiles](https://docs.docker.com/build/building/best-practices/) — covers layer caching, image size, and secrets handling in more depth than this module's summary.\n- [FastAPI: Deployment documentation](https://fastapi.tiangolo.com/deployment/docker/) — a practical, framework-specific walkthrough directly applicable to the app layer of this module's agent stack.\n\nReference\n- [Chroma documentation](https://docs.trychroma.com/) and [Qdrant documentation](https://qdrant.tech/documentation/) — reference docs for the two most common self-hosted vector databases used in agent stacks like this one.\n\nAdvanced\n- [Docker: Build secrets](https://docs.docker.com/build/building/secrets/) — the proper mechanism for using secrets during the build process itself (as opposed to runtime) when genuinely needed, without leaking them into the final image layers.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Write a multi-stage Dockerfile for a FastAPI + LangChain app",
    problem_statement: "Write a multi-stage Dockerfile for a simple FastAPI application that uses LangChain (assume standard pip dependencies, no compiled extensions needed for this exercise). The build stage should install dependencies; the runtime stage should be minimal, run as a non-root user, and contain nothing from the build toolchain.\n\nInclude a .dockerignore alongside it. Explain, in a short write-up, what you excluded and why.",
    starter_context: "You can use a placeholder application (a minimal main.py with a FastAPI app and a LangChain import) — the focus is the Dockerfile structure and .dockerignore, not the application logic itself.",
    hints: [
      "The build stage can use a full Python image; the runtime stage should use a slim variant and copy only the installed packages and application code.",
      "Remember to create and switch to a non-root user in the runtime stage — this is a common thing to forget even when everything else is correct.",
    ],
    solution_notes: "A strong submission produces a genuinely minimal runtime stage — no compiler toolchain, no pip cache, no dev dependencies — with an explicit non-root USER instruction, and a .dockerignore that excludes at minimum .env files, .git, and local cache directories. The write-up should correctly explain that anything left out of .dockerignore risks being copied into the image via broad COPY instructions, directly connecting to this module's secrets-handling mental model.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Design the Compose health-check strategy for a three-service stack",
    problem_statement: "You have three services: the FastAPI app, a Qdrant vector database, and a Redis cache. Design (in writing, or as an actual docker-compose.yml if you want to go further) the health checks and depends_on relationships needed so the app never starts trying to serve requests before its dependencies are genuinely ready.\n\nState explicitly what happens today (without proper health checks) versus with your design, in a specific failure scenario you construct.",
    starter_context: "You decide the exact health-check commands — the point is getting the dependency logic right, not memorizing exact syntax.",
    hints: [
      "A health check for Redis or Qdrant typically means actually querying them (a ping-style command or a lightweight API call), not just checking that the container process is running.",
      "Construct a concrete failure scenario: e.g., 'on a resource-constrained host, Qdrant takes 8 seconds to become ready, but the app tries to connect after 2 seconds without a health check in place.'",
    ],
    solution_notes: "A strong submission specifies real readiness checks (not just process-liveness checks) for each dependency, correctly uses depends_on with a health-check condition rather than plain ordering, and constructs a genuinely plausible failure scenario (a slow-starting dependency under load) that the design explicitly prevents — demonstrating the difference this module draws between 'container started' and 'service ready.'",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Audit a stack for secret leakage and image bloat",
    problem_statement: "You're handed a description of an existing setup: \"Our Dockerfile does COPY . . near the top, then pip install -r requirements.txt, then sets ENV OPENAI_API_KEY=sk-... directly for convenience. The final image is 3.8GB.\" Nothing else is given.\n\nList every specific problem you can identify from this description alone, explain the real-world consequence of each, and describe exactly how you'd fix each one.",
    starter_context: "This is a diagnostic and remediation exercise based purely on the description given — no actual Dockerfile is provided.",
    hints: [
      "Consider what COPY . . near the top of the Dockerfile does to layer caching, in addition to the secrets question.",
      "The 3.8GB image size is itself a symptom worth diagnosing — connect it back to what's likely missing (multi-stage build, .dockerignore) rather than treating it as a separate, unrelated issue.",
    ],
    solution_notes: "A strong submission identifies at minimum: the hardcoded API key as a real, serious secret leak (retrievable from the image indefinitely), the broad COPY . . as both a secrets risk and a layer-caching inefficiency (invalidating the dependency-install cache on every source change), and the 3.8GB size as a likely symptom of missing multi-stage builds and a missing .dockerignore. The fix for each should be specific: runtime env injection for the key (plus credential rotation, since it was already leaked), reordering COPY instructions and adding a .dockerignore, and restructuring to a multi-stage build.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "Why is baking an API key into a Docker image dangerous even if you remove it in a later instruction in the same Dockerfile?", what_is_tested: "Understanding of how Docker image layers work, not just general secrets-hygiene awareness.", strong_answer_structure: "Explain that each instruction in a Dockerfile creates a new layer, and earlier layers remain part of the image even if a later layer appears to overwrite or delete the file — anyone who can pull the image can inspect earlier layers directly.", weak_answer_example: "\"You shouldn't put secrets in Dockerfiles\" without explaining the actual layer-persistence mechanism.", follow_up_question: "What's the correct way to pass a secret only needed during the build process itself, not at runtime?" },
  { category: "applied", order_index: 1, question: "Walk me through why you'd use a multi-stage Dockerfile instead of a single-stage one for a Python application with compiled dependencies.", what_is_tested: "Practical understanding of the size/security trade-off multi-stage builds address.", strong_answer_structure: "Explain that the build stage needs compilers and build tooling that the runtime doesn't, and multi-stage builds let you discard all of that after compilation, shipping only the final installed packages and application code in a much smaller, more secure runtime image.", weak_answer_example: "\"It makes the image smaller\" without explaining the actual mechanism or the security angle.", follow_up_question: "What's a common mistake people make when copying artifacts from the build stage to the runtime stage?" },
  { category: "scenario", order_index: 2, question: "Your team's agent stack works perfectly in local development but the app container fails to connect to the vector database about 1 in 5 times when deployed. How do you investigate?", what_is_tested: "Diagnostic instinct toward startup-ordering race conditions rather than assuming a code bug.", strong_answer_structure: "Check whether the Compose configuration has real health checks and readiness-based depends_on conditions, and hypothesize that the deployed environment's slower or more variable startup timing is exposing a race condition that rarely manifests locally.", weak_answer_example: "Immediately assuming it's an application-level bug in the connection logic without considering startup ordering.", follow_up_question: "How would you reproduce this intermittent failure reliably enough to verify your fix?" },
  { category: "debugging", order_index: 3, question: "A rebuild of an unchanged Dockerfile a month later produces a broken container. How is that possible?", what_is_tested: "Understanding of reproducibility risks from unpinned base images or dependencies.", strong_answer_structure: "Explain that an unpinned base image tag or dependency version can resolve to a different, newer version at build time than it did previously, potentially introducing a breaking change with no corresponding change to the Dockerfile or application code.", weak_answer_example: "\"That shouldn't be possible if nothing changed\" — doesn't account for floating version tags.", follow_up_question: "How would you make this build fully reproducible going forward?" },
  { category: "system_design", order_index: 4, question: "How would you structure a Docker Compose setup for an agent stack so a new team member can bring up the entire environment reliably with a single command?", what_is_tested: "Ability to synthesize the module's principles (health checks, secrets handling, reproducibility) into a coherent design.", strong_answer_structure: "Describe a Compose file with explicit health checks and readiness-based dependencies between services, secrets injected via an env_file that's gitignored (with a checked-in example/template file), and pinned versions throughout — such that a single 'docker compose up' reliably produces a working stack without manual intervention.", weak_answer_example: "Describing only the application container without addressing the multi-service startup and secrets-handling concerns that make this actually reliable for a new team member.", follow_up_question: "What would you document alongside the Compose file so a new team member understands what they're bringing up and why?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "AI Service Deployment").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("FDE Module 2 (Containerizing Agentic Systems) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
