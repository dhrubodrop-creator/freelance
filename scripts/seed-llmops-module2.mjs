import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "dc0bc662-3f33-41e9-b57c-ef1a326e3a70"; // Evaluation at Scale

const sections = [
  {
    section_type: "the_field",
    title: "What evaluating LLM features at scale actually looks like",
    order_index: 0,
    content: "Checking an LLM feature's quality by manually trying a few prompts and eyeballing the responses works fine for a demo. It completely falls apart the moment you need to know, with confidence, whether a prompt change made things better or worse across the real variety of inputs your feature actually sees — and it falls apart even faster once you need to catch a regression automatically, before a real user does, on every single change.\n\nThis is what evaluation-at-scale actually solves: a golden dataset (a curated set of representative inputs with known-good expected characteristics) that can be run automatically against any new prompt or model version, an LLM-as-judge or rule-based scoring approach that can evaluate response quality without a human manually reading every single output, and automated regression testing wired into the actual deployment pipeline so a quality drop is caught before it ships, not after a user complains.\n\nThe genuinely hard, non-obvious part of this discipline is that LLM output is inherently harder to evaluate than traditional software output — there's rarely one single \"correct\" answer to check against exactly, and \"good\" is often a matter of degree and context, not a boolean pass/fail. Building real evaluation at scale means confronting this directly: designing golden datasets and scoring criteria specific enough to be genuinely useful, while honest about where automated evaluation has real limits and still needs a human in the loop.",
  },
  {
    section_type: "mental_models",
    title: "How professionals think about LLM evaluation at scale",
    order_index: 1,
    content: "**1. A golden dataset is a curated sample of the real variety your feature will actually face, not just a handful of convenient happy-path examples.** If it doesn't include the edge cases, ambiguous inputs, and known-hard cases your feature genuinely encounters, it will pass changes that would actually degrade real-world performance.\n\n**2. LLM-as-judge is a real evaluation technique with real limitations, not a magic oracle.** Using one model to judge another's output can work well for many quality dimensions, but the judge itself can be wrong, biased toward certain response styles, or miscalibrated — its verdicts need to be spot-checked against human judgment, especially when first setting it up.\n\n**3. Automated regression testing exists to catch what manual spot-checking will eventually miss.** A human reviewing a handful of outputs after each change will inevitably miss subtle regressions in cases they didn't happen to check — automated testing against a comprehensive golden dataset covers the cases a human wouldn't think to manually re-verify every time.\n\n**4. Evaluation criteria need to be specific enough to be actionable, not just \"is this good.\"** Faithfulness (is it factually grounded), relevance (does it address the actual question), format compliance, and safety are different dimensions that can each fail independently — a single vague \"quality\" score often hides which specific dimension actually regressed.\n\n**5. A regression caught in an automated eval before deployment is worth far more than the same regression caught by a user complaint after deployment.** This is the entire economic argument for investing in this discipline — the cost asymmetry between catching a problem early versus late is large and real.\n\n**6. Evaluation is never fully \"done\" — golden datasets need to grow as new edge cases and failure modes are discovered in production.** A static golden dataset that never incorporates lessons from real production incidents will eventually stop reflecting what actually matters.",
  },
  {
    section_type: "decision_framework",
    title: "How should I design evaluation criteria and choose between LLM-as-judge and other methods?",
    order_index: 2,
    content: "IF a quality dimension has an objective, checkable answer (does the output match a required JSON schema, does it contain a required piece of information, is it under a length limit) THEN use rule-based/programmatic checks rather than an LLM judge. BECAUSE programmatic checks are faster, cheaper, perfectly consistent, and don't carry the calibration uncertainty an LLM judge introduces — reserve the more expensive, less certain LLM-as-judge approach for dimensions that genuinely require judgment. WATCH OUT FOR: it's tempting to reach for LLM-as-judge as the default sophisticated-sounding tool even for things a simple rule could check reliably and more cheaply.\n\nIF a quality dimension requires genuine judgment (is this response helpful, is it appropriately toned, does it correctly synthesize information from multiple sources) THEN use LLM-as-judge, but calibrate it against real human judgment on a sample before trusting it broadly. BECAUSE this is exactly the class of evaluation programmatic rules can't handle well, but the judge's own reliability needs to be verified, not assumed. WATCH OUT FOR: using the same model as both the system being evaluated and the judge can introduce a subtle bias (a model favoring outputs stylistically similar to its own) — consider using a different, and ideally stronger, model as the judge where practical.\n\nIF you're building a golden dataset THEN include genuine edge cases, ambiguous inputs, and known historical failure cases — not just clean, representative happy-path examples. BECAUSE a golden dataset of only easy cases will pass a change that actually degrades performance on the harder cases that matter most in practice. WATCH OUT FOR: a golden dataset that only grows by adding new happy-path examples, never incorporating real production failures discovered over time, gradually loses its power to catch the regressions that actually matter.\n\nIF an automated eval suite exists and is passing THEN still budget for periodic human spot-checking, especially for the LLM-as-judge-scored dimensions. BECAUSE full automation without any human calibration check risks the judge's own biases or blind spots going undetected indefinitely — human review isn't replaced by automated evaluation, it's redirected to where it adds the most value (calibrating and auditing the automated system, rather than manually checking every single output).",
  },
  {
    section_type: "workflow",
    title: "The real process for building evaluation at scale",
    order_index: 3,
    content: "1. Build a golden dataset genuinely representative of the real input variety your feature faces, deliberately including edge cases and known historical failure modes, not just clean examples.\n\n2. For each quality dimension that matters (faithfulness, relevance, format compliance, safety, tone), decide whether it's checkable by a programmatic rule or genuinely requires an LLM judge, per the decision framework.\n\n3. For LLM-as-judge dimensions, calibrate the judge against real human ratings on a sample before trusting its verdicts broadly, and periodically re-calibrate.\n\n4. Build the full eval suite to run automatically against any candidate prompt or model change, producing a clear pass/fail or scored result per dimension, not just one blended overall number.\n\n5. Wire the eval suite into the actual deployment pipeline as a real gate (this connects directly to Module 3's CI/CD-for-prompts topic) — a regression should block promotion, not just be visible after the fact.\n\n6. When a real production issue is discovered that the eval suite didn't catch, add a representative case to the golden dataset so the same category of regression would be caught next time.\n\n7. Periodically human-spot-check a sample of both passing and failing eval results, especially for LLM-as-judge-scored dimensions, to confirm the automated system remains well-calibrated.\n\n8. Treat the golden dataset and eval criteria as living artifacts that grow and improve over the life of the feature, not a one-time setup task.",
  },
  {
    section_type: "failure_modes",
    title: "How LLM evaluation at scale actually breaks",
    order_index: 4,
    content: "Failure 1 — A golden dataset made only of easy, happy-path examples.\nWhat: the eval suite consistently passes because it's only being tested against clean, representative-of-the-best-case inputs, while real production traffic includes harder cases the golden dataset never captures.\nHow it happens: happy-path examples are the easiest and most obvious to write when first building a golden dataset, and it's easy to stop there without deliberately seeking out harder cases.\nWhy beginners miss it: the eval suite looks like it's working — everything passes — which feels like success rather than a sign the dataset isn't actually testing what matters.\nDetect: check whether the golden dataset includes genuine edge cases and known historical failure modes, or only clean examples that would be easy for almost any reasonable prompt to handle.\nPrevent: deliberately curate hard cases and known failure modes into the golden dataset from the start, and treat this as an ongoing responsibility, not a one-time setup task.\nInterview question: \"Your eval suite has a 100% pass rate. What does that actually tell you, and what might it be hiding?\"\nReal-world consequence: a real regression on a hard or unusual case ships to production because the golden dataset never contained anything that would have caught it.\n\nFailure 2 — Trusting an uncalibrated LLM-as-judge without checking it against human judgment.\nWhat: an LLM judge's quality scores are used as ground truth without ever verifying they actually match what a human evaluator would conclude, and the judge's own biases or blind spots go undetected.\nHow it happens: LLM-as-judge feels objective and automated in a way that invites unquestioning trust, especially once it's set up and producing consistent-looking scores.\nWhy beginners miss it: the judge produces confident, plausible-looking scores every time — there's no obvious signal that it might be systematically miscalibrated on some dimension.\nDetect: sample a set of the judge's verdicts and have a human independently evaluate the same outputs, checking for meaningful disagreement.\nPrevent: calibrate the judge against human judgment before trusting it broadly, and periodically re-check calibration going forward, not just once at initial setup.\nInterview question: \"How would you know if your LLM-as-judge evaluation system was systematically miscalibrated in some way?\"\nReal-world consequence: a real quality regression is missed (or a genuinely good change is falsely flagged as a regression) because the judge's verdicts were trusted without ever being checked against real human judgment.\n\nFailure 3 — Eval results exist but aren't actually wired into the deployment gate.\nWhat: the eval suite runs and produces results, but a failing result doesn't actually block a prompt or model change from being promoted — it's informational rather than an enforced gate.\nHow it happens: building the eval suite itself is a real accomplishment, and wiring it into the actual deployment pipeline as a hard gate is a separate, sometimes deferred, piece of work.\nWhy beginners miss it: the eval suite genuinely works and produces useful results — the gap is specifically in whether those results actually prevent a bad change from shipping, which isn't visible unless someone specifically checks.\nDetect: deliberately introduce a change that should fail the eval suite and confirm it actually blocks promotion, rather than assuming the wiring works.\nPrevent: treat the deployment-gate wiring as a required part of the eval suite's definition of done, not an optional follow-up step.\nInterview question: \"Your eval suite flagged a regression, but the change still made it to production. What does that tell you about your pipeline?\"\nReal-world consequence: a caught regression ships anyway, because the eval suite's results were informational rather than an enforced blocker — providing a false sense of protection.\n\nFailure 4 — A golden dataset that never incorporates lessons from real production failures.\nWhat: the golden dataset is built once and left static, never updated with representative cases from actual production incidents that the original eval suite failed to catch.\nHow it happens: after an incident is resolved, the pressure of the moment passes, and updating the golden dataset can feel like a lower-priority follow-up compared to the immediate fix.\nWhy beginners miss it: the immediate incident does get fixed, which feels like resolution — the gap is specifically in whether the same category of issue would be caught automatically next time, which isn't addressed by fixing just this one instance.\nDetect: for a past production incident, check whether a representative case was ever added to the golden dataset afterward.\nPrevent: make adding a representative case to the golden dataset a standard, required part of the incident postmortem process (connecting to Module 4's incident response focus), not an optional afterthought.\nInterview question: \"You just resolved a production quality incident that your eval suite didn't catch. What's the very next thing you should do, beyond fixing the immediate issue?\"\nReal-world consequence: the exact same category of regression recurs later, because the golden dataset was never updated to reflect the lesson from the first incident.",
  },
  {
    section_type: "debugging_playbook",
    title: "Your eval suite isn't catching real problems — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"A real quality issue reached production, but our eval suite passed the change that caused it.\"\nDiagnostic: check whether the golden dataset includes a case genuinely representative of the failure that occurred, or whether it was made up entirely of easier cases.\nFix: add a representative case to the golden dataset immediately, and audit the rest of the dataset for similar gaps in coverage of hard or unusual cases.\n\nSymptom: \"Our LLM-as-judge scores don't match what a human reviewer concludes when they look at the same outputs.\"\nDiagnostic: this is a calibration failure — sample more judge verdicts and compare against human review to characterize exactly where and how the judge disagrees.\nFix: adjust the judge's prompt/criteria based on the specific pattern of disagreement found, and re-calibrate against human judgment before trusting it again.\n\nSymptom: \"A change that should have failed our eval suite still made it to production.\"\nDiagnostic: check whether the eval suite's results are actually wired as an enforced gate in the deployment pipeline, or just informational.\nFix: wire the eval suite as a genuine, enforced blocking gate, and deliberately test it with a known-bad change to confirm it actually blocks promotion.\n\nSymptom: \"We've had the same category of quality issue happen more than once.\"\nDiagnostic: check whether the golden dataset was updated with a representative case after the first occurrence.\nFix: add the missing case now, and going forward, make golden dataset updates a required part of every incident postmortem, not an optional follow-up.",
  },
  {
    section_type: "checklist",
    title: "LLM Evaluation at Scale Checklist",
    order_index: 6,
    content: "- [ ] The golden dataset includes genuine edge cases and known historical failure modes, not just easy happy-path examples\n- [ ] Each evaluation dimension is scored by the appropriate method — programmatic rules where objectively checkable, LLM-as-judge where genuine judgment is required\n- [ ] LLM-as-judge scores are calibrated against real human evaluation on a sample, and periodically re-checked\n- [ ] The eval suite produces per-dimension results, not just one blended overall score\n- [ ] The eval suite is wired as a genuinely enforced gate in the deployment pipeline, verified by testing with a known-bad change\n- [ ] Every production incident's postmortem includes adding a representative case to the golden dataset\n- [ ] Human spot-checking of eval results happens periodically, even when the automated system is passing",
  },
  {
    section_type: "template",
    title: "Eval Suite Design Record",
    order_index: 7,
    content: "Feature being evaluated: ___\n\nGolden dataset: size ___, composition (happy-path / edge cases / known failure modes) ___\n\nEvaluation dimensions:\n| Dimension | Method (rule-based / LLM-as-judge) | Threshold/criteria |\n|---|---|---|\n| ... | ... | ... |\n\nLLM-as-judge calibration: sample size checked against human review ___, agreement rate ___, last re-calibrated ___\n\nDeployment gate: confirmed enforced (tested with a known-bad change)? ___\n\nGolden dataset update log (from postmortems): ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [OpenAI: Evals framework](https://github.com/openai/evals) — a real, widely-referenced open-source framework for building and running LLM evaluation suites, directly applicable to this module's build deliverable.\n- [Anthropic: Empirical approach to evaluations](https://docs.claude.com/en/docs/build-with-claude/develop-tests) — practical guidance on building test/eval suites for LLM features.\n\nRecommended\n- [RAGAS documentation](https://docs.ragas.io/) — a purpose-built evaluation framework for RAG systems specifically, useful even beyond pure RAG use cases for its faithfulness/relevance scoring methodology.\n- [DeepEval documentation](https://docs.confident-ai.com/) — another widely-used open-source LLM evaluation framework, worth comparing against OpenAI Evals for the specific golden-dataset and LLM-as-judge patterns this module covers.\n\nReference\n- Zheng et al., [\"Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena\"](https://arxiv.org/abs/2306.05685) (2023) — the foundational research paper on LLM-as-judge methodology, including its known biases and limitations, directly relevant to this module's calibration warnings.\n\nAdvanced\n- Search for published case studies on production LLM eval pipelines at scale (several AI companies publish these) — useful for seeing how the golden-dataset-growth and deployment-gate patterns in this module operate at real organizational scale over time.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Build a golden dataset and a rule-based eval for a structured-output feature",
    problem_statement: "Take an agent with structured JSON output (reuse the Lead Qualifier design from the n8n course track, or design a similar one) and build a golden dataset of at least 10 test inputs, including at least 3 deliberately ambiguous or edge-case examples.\n\nBuild a rule-based (programmatic, not LLM-as-judge) evaluation script that checks: output matches the expected JSON schema, required fields are present and non-empty, and any categorical field only contains an allowed value. Run it against your golden dataset and report the pass rate.",
    starter_context: "Rule-based evaluation is the right fit for this exercise specifically because schema/field compliance is objectively checkable — this is deliberately testing the 'when to use programmatic checks' half of this module's decision framework.",
    hints: [
      "Make sure at least one of your golden dataset examples is designed to genuinely be hard for the agent (a very ambiguous input) — if every case passes trivially, your golden dataset isn't testing much.",
      "Your evaluation script should report which specific check failed for any failing case (missing field vs. wrong type vs. invalid categorical value), not just a bare pass/fail.",
    ],
    solution_notes: "A strong submission has a genuinely varied golden dataset (not all easy cases), a rule-based eval script that checks real, specific, objectively-verifiable properties, and a clear per-check failure report rather than a single opaque pass/fail — correctly applying programmatic evaluation to a dimension that doesn't actually require an LLM judge.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Build and calibrate an LLM-as-judge for response helpfulness",
    problem_statement: "Design an LLM-as-judge prompt that scores whether a response to a customer question is genuinely helpful (a judgment call, not objectively checkable by a rule). Run it against 10 example question/response pairs you construct (include some clearly helpful, some clearly unhelpful, and some genuinely borderline).\n\nIndependently score the same 10 pairs yourself as a human reviewer, then compare your scores against the LLM judge's scores and report the agreement rate and any specific pattern of disagreement.",
    starter_context: "Construct your 10 pairs deliberately across the helpful/unhelpful/borderline spectrum — a set that's all clearly one extreme or the other won't actually test calibration meaningfully.",
    hints: [
      "Write your own human scores before looking at the LLM judge's scores, to avoid anchoring your judgment to its output.",
      "For any case where you disagree with the judge, look closely at what specifically drove the difference — this is the most useful part of the exercise for understanding real calibration gaps.",
    ],
    solution_notes: "A strong submission constructs a genuinely varied test set spanning the helpful/unhelpful/borderline spectrum, produces independent human scores before viewing the judge's scores, and reports a real, honest agreement rate along with specific analysis of any disagreement pattern found — directly demonstrating the calibration process this module's decision framework requires before trusting an LLM judge broadly.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose a golden dataset that isn't catching real regressions",
    problem_statement: "You're told: \"Our eval suite has a 98% pass rate and has stayed there for months, but users keep reporting quality issues that somehow don't show up in our evals.\" You're given no further detail about the golden dataset's actual contents.\n\nDiagnose the most likely explanation(s) for this gap, and propose a specific, concrete process for closing it.",
    starter_context: "This is a diagnostic exercise based on the description alone — reason about what a persistently high pass rate combined with real user-reported issues most likely indicates.",
    hints: [
      "A stable, high pass rate over months despite real reported issues is a strong signal about what kind of cases the golden dataset likely does and doesn't contain.",
      "Consider what process gap would explain why real user-reported issues never made their way back into the golden dataset.",
    ],
    solution_notes: "A strong submission correctly diagnoses that the golden dataset most likely consists primarily of easy, happy-path cases that don't reflect the real edge cases users are actually encountering, and that there's likely no process connecting production incidents/user reports back into golden dataset updates. The proposed fix should include both an audit-and-expand pass on the current golden dataset (deliberately adding hard cases) and a standing process change (incident postmortems feeding golden dataset updates), matching this module's failure modes and checklist directly.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "When would you use a rule-based check versus an LLM-as-judge for evaluating an LLM feature's output?", what_is_tested: "Understanding of the trade-off between objective/cheap/reliable rule-based checks and judgment-requiring LLM-as-judge evaluation.", strong_answer_structure: "Explain using rule-based checks for objectively verifiable properties (schema compliance, required fields, format), reserving LLM-as-judge for dimensions that genuinely require judgment (helpfulness, tone, relevance) where a fixed rule can't capture the real criteria.", weak_answer_example: "\"LLM-as-judge is more advanced so I'd usually use it\" — doesn't reflect the actual cost/reliability trade-off consideration.", follow_up_question: "What's the risk of using LLM-as-judge for something that could have been checked with a simple rule?" },
  { category: "applied", order_index: 1, question: "How would you calibrate an LLM-as-judge evaluation system before trusting its scores for real decisions?", what_is_tested: "Practical process for verifying judge reliability, not just assuming it's accurate.", strong_answer_structure: "Describe having the judge score a sample set independently evaluated by a human reviewer, comparing agreement, and investigating any specific pattern of disagreement before trusting the judge's scores broadly.", weak_answer_example: "\"I'd just trust the judge's scores since it's a capable model\" — no actual calibration step.", follow_up_question: "How often would you re-check calibration, and what would prompt an earlier re-check?" },
  { category: "scenario", order_index: 2, question: "Your eval suite has a consistently high pass rate, but users keep reporting quality issues that don't show up in your evals. What's your investigation process?", what_is_tested: "Recognition that a persistently high pass rate combined with real reported issues points to a golden dataset coverage gap.", strong_answer_structure: "Investigate whether the golden dataset actually reflects the hard/edge cases users are encountering in practice, and check whether there's a process connecting real user-reported issues back into golden dataset updates.", weak_answer_example: "Assuming the eval suite must be fine since it's passing, without questioning what it's actually testing.", follow_up_question: "How would you build a process to keep the golden dataset from going stale like this in the future?" },
  { category: "debugging", order_index: 3, question: "A change that your eval suite flagged as a regression still made it to production. How is that possible, and how would you fix it?", what_is_tested: "Recognition that eval results need to be an enforced deployment gate, not just informational.", strong_answer_structure: "Check whether the eval suite's failing result is actually wired to block deployment, or whether it just produces a report that isn't enforced — and fix the pipeline wiring so a failing eval genuinely blocks promotion.", weak_answer_example: "Assuming the eval suite itself must have a bug, without first checking whether it's actually enforced in the deployment pipeline.", follow_up_question: "How would you test that your fix actually works, rather than just assuming the wiring is now correct?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you built or improved an evaluation system for an LLM feature. What was the hardest part, and how did you handle it?", what_is_tested: "Real or realistic experience with the genuine difficulty of LLM evaluation, beyond a superficial description.", strong_answer_structure: "A strong answer names a specific real challenge (building a genuinely representative golden dataset, calibrating a judge, catching a category of regression the initial eval missed) and the concrete steps taken to address it.", weak_answer_example: "A vague, generic answer with no specific difficulty or concrete resolution described.", follow_up_question: "What would you do differently if you were starting this evaluation system from scratch again?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLM Evaluation & Testing").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("LLMOps Module 2 (Evaluation at Scale) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
