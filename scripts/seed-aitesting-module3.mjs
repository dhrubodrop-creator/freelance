import { createClient } from "@supabase/supabase-js";
import { readFileSync } from "node:fs";
import ws from "ws";

const env = Object.fromEntries(
  readFileSync(new URL("../.env.local", import.meta.url), "utf8")
    .split("\n").filter((l) => l.includes("=") && !l.trim().startsWith("#"))
    .map((l) => { const idx = l.indexOf("="); return [l.slice(0, idx).trim(), l.slice(idx + 1).trim().replace(/^"|"$/g, "")]; })
);
const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, { realtime: { transport: ws } });

const MODULE_ID = "3ea7c2de-7382-421e-8e75-76c07981cee9"; // Tooling: DeepEval & RAGAS

const sections = [
  {
    section_type: "the_field",
    title: "What catching AI quality regressions in CI, not from angry users, actually looks like",
    order_index: 0,
    content: "Module 2's golden dataset and metrics have real value only if they're actually run automatically, on every change, before it reaches production — otherwise they're a one-time report that gets stale the moment anyone touches the prompt or the underlying model. This module is about the concrete tooling that makes that automatic: DeepEval and RAGAS are real, widely-used open-source frameworks specifically built for automating LLM evaluation, and wiring one of them into CI is what turns a QA finding into a genuine, ongoing regression gate.\n\nThis connects directly to the LLMOps course track's CI/CD-gating principle, but from the QA side of the same work: a QA professional building this pipeline needs to think about it the way they'd think about any automated regression suite — what triggers it, what counts as a failure, how noisy or flaky it is, and how quickly it gives feedback to whoever made the change. Prompt regression testing specifically means the same discipline QA already applies to code regressions (a change that used to work now doesn't), adapted to prompts and model configuration as the thing being regression-tested.\n\nThe practical skill this module builds is genuinely wiring DeepEval or RAGAS into a real CI pipeline — not just running an eval script manually and reporting results, but making it a real, automatic gate that blocks a regression before a human ever has to notice it in production.",
  },
  {
    section_type: "mental_models",
    title: "How QA professionals think about automated AI eval tooling",
    order_index: 1,
    content: "**1. A manually-run eval script is a report; a CI-integrated one is a regression gate.** The actual value shift this module is about is moving from \"someone remembers to check quality periodically\" to \"quality is checked automatically, on every relevant change, before it ships\" — the same shift traditional QA made decades ago moving from manual to automated regression testing.\n\n**2. Flakiness in an eval suite is just as real a problem as flakiness in a traditional test suite, and needs the same QA discipline to manage.** An eval that intermittently fails for reasons unrelated to a genuine quality regression trains the team to ignore its results, the same way a flaky traditional test suite does — this needs active management, not tolerance.\n\n**3. DeepEval and RAGAS are tools implementing the metric concepts from Module 2, not a replacement for understanding those concepts.** Using the tooling well requires understanding what faithfulness, relevancy, and the other metrics actually measure — the tool automates the computation, not the judgment about what to measure and why.\n\n**4. A prompt regression is the AI-specific analog of a code regression, and deserves the same \"what changed, and did it cause this\" investigation discipline.** Connecting a failing eval result back to the specific prompt or configuration change that caused it is the actual diagnostic value of CI integration.\n\n**5. CI integration needs to give fast enough feedback to be actually useful.** An eval suite that takes hours to run provides much less real value than one giving feedback within a normal PR review cycle — speed is a real design consideration, not an afterthought.\n\n**6. Tooling adoption is itself a QA process change that needs buy-in and clear communication, not just a technical integration.** Getting a team to actually trust and act on CI-gated eval results the way they trust traditional test results takes deliberate communication about what the tool measures and why it matters.",
  },
  {
    section_type: "decision_framework",
    title: "How should I choose and integrate DeepEval or RAGAS into a real CI pipeline?",
    order_index: 2,
    content: "IF your primary evaluation need is RAG-specific metrics (faithfulness, context relevancy, answer relevancy) THEN RAGAS is purpose-built for exactly this and is a strong first choice. BECAUSE RAGAS's metric implementations are specifically designed around the RAG evaluation concepts covered in Module 2. WATCH OUT FOR: RAGAS's metrics assume a RAG-style architecture (a retrieval step feeding into generation) — for non-RAG LLM features, its metrics may not map cleanly onto what you actually need to measure.\n\nIF you need broader, more general-purpose LLM testing capability (custom metrics, a wider range of test types beyond RAG-specific ones) THEN DeepEval offers a more general testing framework, closer in spirit to a traditional test framework adapted for LLM output. BECAUSE DeepEval is designed as a more general-purpose LLM testing tool, useful when your evaluation needs extend beyond the RAG-specific metrics RAGAS focuses on. WATCH OUT FOR: broader flexibility means more configuration decisions fall to you — DeepEval doesn't hand you the same out-of-the-box RAG metric implementations RAGAS provides.\n\nIF you're integrating either tool into CI THEN design the trigger (what changes run the eval suite), the failure criteria (what score constitutes a blocking failure, connecting to Module 2's decision-threshold principle), and the feedback speed (how quickly results reach the person who made the change) deliberately, not just running the tool and hoping it's useful. BECAUSE these design decisions determine whether the CI integration actually functions as a real regression gate or just adds noise to the pipeline. WATCH OUT FOR: running the full eval suite on every trivial change may be too slow to be practical — consider a faster, representative subset on every change with a full run on a slower cadence, if the full suite's runtime genuinely doesn't fit the desired feedback speed.\n\nIF the eval suite shows any signs of flakiness (inconsistent results for the same input across runs) THEN treat this with the same seriousness as flaky traditional tests, investigating and fixing the root cause rather than tolerating or ignoring it. BECAUSE flaky results train the team to distrust and ignore the eval suite's findings, undermining the entire value of the CI integration.",
  },
  {
    section_type: "workflow",
    title: "The real process for wiring automated eval tooling into CI",
    order_index: 3,
    content: "1. Choose DeepEval or RAGAS (or both, for different purposes) based on your feature's actual evaluation needs, per this module's decision framework.\n\n2. Implement the specific metrics identified in Module 2 (faithfulness, relevancy, and any feature-specific criteria) using the chosen tooling.\n\n3. Connect the tooling to your golden dataset from Module 2, confirming the tool correctly evaluates against it and produces the same kind of separately-reported metrics established there.\n\n4. Design the CI trigger: what changes (prompt file changes, model configuration changes) should run the eval suite.\n\n5. Design the failure criteria explicitly, connecting each metric's decision threshold to whether the CI check passes or blocks.\n\n6. Integrate the eval suite as an actual CI step, and deliberately test it by introducing a known-bad prompt change and confirming it correctly blocks.\n\n7. Monitor the eval suite's actual runtime and reliability once live — if it's too slow for practical feedback speed, or shows flakiness, address these before the team starts distrusting or ignoring it.\n\n8. Communicate to the team what the CI-integrated eval suite measures and why, treating this as a real process/culture change, not just a technical integration.",
  },
  {
    section_type: "failure_modes",
    title: "How CI-integrated eval tooling actually breaks",
    order_index: 4,
    content: "Failure 1 — An eval suite that runs and reports results, but isn't actually wired as a blocking CI gate.\nWhat: the same failure mode covered in the LLMOps course track, from the QA integration side — DeepEval or RAGAS results are computed and visible in CI logs, but a failing result doesn't actually prevent the change from merging.\nHow it happens: getting the eval tool to run in CI is a real, distinct technical milestone from actually configuring it as a required, blocking status check.\nWhy beginners miss it: the eval suite genuinely runs and produces visible results — the gap is specifically whether a failure blocks anything, which requires deliberately testing.\nDetect: push a change known to fail the eval criteria and confirm the CI pipeline actually prevents it from merging, not just reports a failing status.\nPrevent: configure the eval suite as a required status check (using your CI platform's branch protection or equivalent mechanism), and verify this explicitly.\nInterview question: \"Your DeepEval suite runs on every pull request and reports pass/fail. How do you know it's actually preventing a regression from merging, not just reporting on it?\"\nReal-world consequence: a real quality regression ships despite the eval suite correctly detecting it, because the CI integration wasn't actually configured to block on failure.\n\nFailure 2 — Tolerating eval-suite flakiness, training the team to ignore its results.\nWhat: the eval suite intermittently fails for the same input across different runs, and rather than being investigated and fixed, the flakiness is worked around (re-running until it passes, or ignoring failures) — the same anti-pattern as tolerating flaky traditional tests.\nHow it happens: some genuine variance is expected for non-deterministic LLM output, making it easy to dismiss a flaky result as \"just AI being AI\" rather than investigating whether it's a real, fixable reliability issue in the eval setup itself.\nWhy beginners miss it: the flakiness genuinely does sometimes stem from real model non-determinism, making it harder to distinguish from an actual eval-suite reliability problem worth fixing.\nDetect: track the eval suite's own pass-rate consistency across repeated runs of identical input — genuine flakiness beyond expected model variance is a real, addressable problem.\nPrevent: investigate flaky results with the same rigor as a flaky traditional test, distinguishing genuine model non-determinism (address by running multiple samples and reporting a rate, per Module 1's distribution principle) from an actual eval-suite bug.\nInterview question: \"Your CI-integrated eval suite sometimes passes and sometimes fails for what looks like the same change. How do you investigate?\"\nReal-world consequence: the team stops trusting and acting on eval suite results, treating a genuinely valuable regression gate as noise to be dismissed, undermining the entire investment in building it.\n\nFailure 3 — An eval suite too slow to provide practical feedback within a normal review cycle.\nWhat: the eval suite takes so long to run that engineers either stop waiting for it before merging, or the slow feedback loop significantly disrupts normal development velocity.\nHow it happens: running a comprehensive golden dataset through an LLM-as-judge evaluation for every metric on every change can genuinely take significant time, especially without deliberate optimization.\nWhy beginners miss it: the eval suite does produce genuinely accurate, thorough results — the practical usability cost of its slow runtime isn't obvious until it's actually integrated into a real, fast-moving development workflow.\nDetect: measure the actual end-to-end time from a change being pushed to eval results being available, and compare against the team's normal review/merge cycle expectations.\nPrevent: consider a faster representative subset run on every change, with a full comprehensive run on a slower cadence, per this module's decision framework, if the full suite's runtime genuinely doesn't fit.\nInterview question: \"Your comprehensive eval suite takes 45 minutes to run. How would you redesign your CI integration to give faster feedback without giving up real coverage?\"\nReal-world consequence: engineers route around the slow eval suite (merging without waiting, or disabling it under deadline pressure), defeating its purpose as a genuine regression gate.\n\nFailure 4 — Rolling out CI-gated evaluation without communicating what it measures and why, undermining team trust and adoption.\nWhat: the eval suite is technically integrated, but the team doesn't understand what it's actually checking or why a failure matters, leading to confusion, resistance, or dismissal of its findings.\nHow it happens: the technical integration work can feel like the complete deliverable, with communication and buy-in treated as a secondary, sometimes skipped, concern.\nWhy beginners miss it: the technical integration genuinely does work correctly — the adoption and trust gap isn't a technical problem, so it's easy to overlook if the focus is entirely on the engineering integration itself.\nDetect: ask team members what they understand the eval suite to actually be checking, and whether they trust and act on its results.\nPrevent: treat rollout communication (what's measured, why it matters, how to interpret a failure) as a required part of the integration process, per this module's workflow, not an afterthought.\nInterview question: \"You've technically integrated an eval suite into CI, but engineers seem confused about what a failing result actually means. What's missing?\"\nReal-world consequence: a technically sound integration that doesn't achieve real adoption or trust, because the team was never given the context needed to understand and act on its results confidently.",
  },
  {
    section_type: "debugging_playbook",
    title: "Your CI-integrated eval tooling isn't working well — a troubleshooting guide",
    order_index: 5,
    content: "Symptom: \"A change that should have failed our eval suite still merged.\"\nDiagnostic: check whether the eval suite is actually configured as a required, blocking status check, or just runs and reports without enforcement.\nFix: configure it as a required status check via your CI platform's branch protection, and re-test with a known-bad change to confirm it now actually blocks.\n\nSymptom: \"Our eval suite gives inconsistent pass/fail results for what looks like the same change.\"\nDiagnostic: distinguish genuine model non-determinism (expected, and better addressed by sampling multiple runs and reporting a rate) from an actual bug or instability in the eval suite's own setup.\nFix: if it's genuine model variance, redesign the CI check to sample multiple runs and use a rate-based threshold rather than a single pass/fail; if it's an eval-suite bug, fix the root cause directly.\n\nSymptom: \"Engineers are merging changes without waiting for our eval suite because it's too slow.\"\nDiagnostic: measure the actual end-to-end runtime and compare against the team's real review-cycle expectations.\nFix: redesign to run a faster representative subset on every change, with the full comprehensive suite on a slower cadence.\n\nSymptom: \"Engineers don't seem to understand or trust what our eval suite is checking.\"\nDiagnostic: directly ask team members what they understand it to measure and whether they act on its results.\nFix: run a clear communication/rollout session explaining what's measured and why it matters, connecting back to real examples of what the eval suite would have caught.",
  },
  {
    section_type: "checklist",
    title: "CI-Integrated Eval Tooling Checklist",
    order_index: 6,
    content: "- [ ] The chosen tool (DeepEval, RAGAS, or both) matches the feature's actual evaluation needs\n- [ ] The tool implements the specific metrics identified in Module 2, connected to the golden dataset\n- [ ] CI triggers are clearly defined — what changes run the eval suite\n- [ ] The eval suite is configured as an actually enforced, blocking status check, verified by testing with a known-bad change\n- [ ] Eval suite runtime fits within the team's practical review-cycle expectations, or uses a faster-subset/full-run split if it doesn't\n- [ ] Any flakiness is investigated and addressed, not tolerated or worked around\n- [ ] The team understands what the eval suite measures and why, via deliberate rollout communication",
  },
  {
    section_type: "template",
    title: "CI Eval Integration Record",
    order_index: 7,
    content: "Feature / prompt suite: ___\n\nTool chosen (DeepEval / RAGAS): ___ — reasoning: ___\n\nMetrics implemented: ___\nGolden dataset connected: ___\n\nCI trigger: ___\nFailure criteria / thresholds: ___\n\nVerified blocking (tested with known-bad change)? ___\n\nRuntime: ___ — fits review cycle? ___ (if not, subset/full-run strategy: ___)\n\nFlakiness observed: ___ — resolution: ___\n\nTeam communication/rollout completed: ___",
  },
  {
    section_type: "resources",
    title: "Go deeper",
    order_index: 8,
    content: "Essential\n- [DeepEval documentation](https://docs.confident-ai.com/) (referenced in the LLMOps course track) — the official reference for the general-purpose LLM testing framework this module covers.\n- [RAGAS documentation](https://docs.ragas.io/) (referenced across this catalog) — the official reference for the RAG-specific evaluation framework this module covers.\n\nRecommended\n- [DeepEval: CI/CD integration guide](https://docs.confident-ai.com/docs/integrations-github-actions) — direct, practical reference for the CI-wiring work this module's build deliverable requires.\n\nReference\n- [GitHub Actions: Required status checks](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches) — the specific mechanism for actually enforcing an eval suite as a blocking gate, referenced throughout this module's failure modes.\n\nAdvanced\n- Search for published engineering blog posts on \"flaky test management\" (a well-established traditional QA topic) — the principles transfer directly to managing eval-suite flakiness, this module's third failure mode.",
  },
];

const exercises = [
  {
    level: "guided",
    order_index: 0,
    title: "Implement a RAGAS or DeepEval evaluation against your Module 2 golden dataset",
    problem_statement: "Using your golden dataset from Module 2, implement automated evaluation using RAGAS (if your feature is RAG-backed) or DeepEval (for a more general LLM feature). Confirm the tool's computed faithfulness and relevancy scores are reasonable by spot-checking a few against your own manual assessment from Module 2's exercises.",
    starter_context: "Reuse your Module 2 golden dataset directly — the point of this exercise is the tooling integration, not rebuilding the dataset.",
    hints: [
      "If the tool's automated scores diverge significantly from your Module 2 manual/human assessment, investigate why before trusting the automated version — this connects to the judge-validation discipline from Module 2.",
      "Start with a small subset of your golden dataset to confirm the tool is working correctly before running the full set.",
    ],
    solution_notes: "A strong submission has a genuinely working RAGAS or DeepEval integration producing separate faithfulness and relevancy scores, with a real spot-check comparison against Module 2's manual assessment confirming (or investigating a discrepancy in) the tool's reliability — directly connecting this module's tooling work to Module 2's evaluation-methodology foundation rather than treating them as disconnected exercises.",
  },
  {
    level: "semi_guided",
    order_index: 1,
    title: "Wire the eval suite into a CI pipeline as an actual blocking gate",
    problem_statement: "Set up a CI pipeline (GitHub Actions or similar) that runs your eval suite from the guided exercise whenever a prompt file changes, and configure it as a required, blocking status check. Test it with both a known-good change (should pass and allow merge) and a deliberately broken prompt change (should fail and block merge).",
    starter_context: "The blocking verification is the actual point of this exercise, per this module's most common failure mode.",
    hints: [
      "Use your CI platform's branch protection or required-status-check feature specifically — a CI step that runs and reports without this configuration doesn't actually block anything.",
      "Test both directions explicitly: confirm the good change is allowed through, not just that the bad change is blocked — a gate that blocks everything isn't useful either.",
    ],
    solution_notes: "A strong submission has a genuinely enforced CI gate, verified by testing both a known-good change (merges successfully) and a deliberately broken one (blocked from merging) — directly closing the gap this module's first failure mode identifies between an eval suite that runs and one that actually enforces.",
  },
  {
    level: "independent",
    order_index: 2,
    title: "Diagnose and fix a flaky and slow eval suite",
    problem_statement: "You're told: \"Our CI-integrated eval suite takes 35 minutes to run, and about 1 in 10 runs shows a different pass/fail result than the previous run on the exact same code, so engineers have started just re-running it until it passes.\" You're given no further detail.\n\nDiagnose both problems (speed and flakiness) and propose specific fixes for each, using this module's principles.",
    starter_context: "This is a diagnostic exercise based on the description alone, addressing both of this module's most impactful failure modes.",
    hints: [
      "For the flakiness, consider whether it's likely genuine model non-determinism (which needs a different fix — sampling and rate-based thresholds) versus a bug in the eval setup itself.",
      "For the speed problem, consider what a faster-subset-on-every-change plus full-run-on-a-slower-cadence strategy would look like specifically for this scenario.",
    ],
    solution_notes: "A strong submission proposes distinguishing genuine model non-determinism from an eval-suite bug for the flakiness (likely recommending a rate-based threshold across multiple sampled runs rather than a single pass/fail, if it's genuine model variance), and proposes a faster representative subset for every-change CI runs with the full 35-minute suite moved to a slower cadence (nightly, or pre-release) for the speed problem — directly addressing both of this module's most consequential failure modes with specific, actionable fixes rather than a vague 'make it better' recommendation.",
  },
];

const interviewQuestions = [
  { category: "fundamentals", order_index: 0, question: "What's the practical difference between RAGAS and DeepEval, and how would you choose between them?", what_is_tested: "Understanding of the two tools' actual, distinct scope and use cases.", strong_answer_structure: "Explain that RAGAS is purpose-built for RAG-specific metrics (faithfulness, context/answer relevancy), while DeepEval is a more general-purpose LLM testing framework — choosing depends on whether your evaluation need is specifically RAG-focused or broader.", weak_answer_example: "\"They're basically the same tool\" — doesn't recognize their genuinely different scope and design focus.", follow_up_question: "Could you use both together in the same project? When would that make sense?" },
  { category: "applied", order_index: 1, question: "Why is it important to verify that a CI-integrated eval suite actually blocks a merge on failure, not just runs and reports?", what_is_tested: "Understanding that CI presence and CI enforcement are different, and only the latter provides real protection.", strong_answer_structure: "Explain that a report-only eval check relies on someone noticing and acting on a failure, which defeats the purpose of automated gating — an actually enforced status check structurally prevents a bad change from merging, regardless of whether anyone notices the failure.", weak_answer_example: "\"If it's running in CI, it's doing its job\" — conflates running with actually enforcing.", follow_up_question: "How would you test that your specific CI platform's enforcement is actually configured correctly?" },
  { category: "scenario", order_index: 2, question: "Your eval suite is flaky — it sometimes passes and sometimes fails for the exact same code change. Engineers have started re-running it until it passes. How do you respond?", what_is_tested: "Recognition that this is a serious problem requiring investigation, not something to tolerate.", strong_answer_structure: "Investigate whether the flakiness is genuine model non-determinism (requiring a redesigned rate-based threshold across multiple runs) or an actual bug in the eval setup, and address the root cause — explicitly discouraging the re-run-until-it-passes workaround, which undermines the entire purpose of the gate.", weak_answer_example: "\"That's just how AI is, it's fine to re-run until it passes\" — normalizes a workaround that defeats the eval suite's purpose.", follow_up_question: "How would you redesign the eval check specifically to handle genuine model non-determinism correctly, rather than treating it as flakiness to work around?" },
  { category: "debugging", order_index: 3, question: "Engineers have stopped waiting for your comprehensive 40-minute eval suite before merging changes. What's your fix?", what_is_tested: "Practical understanding of feedback-speed trade-offs in CI design.", strong_answer_structure: "Propose running a faster, representative subset of the eval suite on every change (fitting the team's normal review cycle) with the full comprehensive suite run on a slower cadence, balancing speed and coverage rather than accepting either extreme.", weak_answer_example: "\"Just tell engineers to wait longer\" — doesn't address the actual practical friction driving the workaround behavior.", follow_up_question: "How would you decide what subset of tests belongs in the fast, every-change check versus the slower comprehensive run?" },
  { category: "behavioural", order_index: 4, question: "Describe a time you rolled out a new automated testing or quality-gating process and had to get a team to actually trust and use it.", what_is_tested: "Real or realistic experience with the adoption/communication challenge, not just the technical integration.", strong_answer_structure: "A strong answer describes the specific process rolled out, the communication or education effort made to build understanding and trust, and evidence of actual adoption (or lessons learned if adoption was initially slow).", weak_answer_example: "A vague answer focused only on the technical work with no mention of the adoption/communication challenge.", follow_up_question: "What would you do differently if the team was initially resistant or skeptical of the new process?" },
];

const { error: delSecErr } = await supabase.from("module_playbook_sections").delete().eq("module_id", MODULE_ID);
const { error: delExErr } = await supabase.from("exercises").delete().eq("module_id", MODULE_ID);
const { error: delIqErr } = await supabase.from("interview_questions").delete().eq("module_id", MODULE_ID);
if (delSecErr || delExErr || delIqErr) { console.error({ delSecErr, delExErr, delIqErr }); process.exit(1); }

const { error: secErr } = await supabase.from("module_playbook_sections").insert(sections.map((s, i) => ({ ...s, module_id: MODULE_ID, order_index: s.order_index ?? i })));
const { error: exErr } = await supabase.from("exercises").insert(exercises.map((e, i) => ({ ...e, module_id: MODULE_ID, order_index: i })));
const { error: iqErr } = await supabase.from("interview_questions").insert(interviewQuestions.map((q) => ({ ...q, module_id: MODULE_ID })));
if (secErr || exErr || iqErr) { console.error({ secErr, exErr, iqErr }); process.exit(1); }

const { data: skill } = await supabase.from("skills").select("id").eq("name", "LLM Evaluation & Testing").single();
const { error: msErr } = await supabase.from("module_skills").delete().eq("module_id", MODULE_ID);
const { error: msErr2 } = await supabase.from("module_skills").insert({ module_id: MODULE_ID, skill_id: skill.id });
if (msErr || msErr2) { console.error({ msErr, msErr2 }); process.exit(1); }

console.log("AI LLM Testing Module 3 (Tooling: DeepEval & RAGAS) seeded:", sections.length, "sections,", exercises.length, "exercises,", interviewQuestions.length, "interview questions, 1 skill mapping.");
